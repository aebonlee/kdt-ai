# 16. 번들 코드 스플리팅 (초기 로딩 최적화)

작업일: 2026-07-01
대상: `src/App.jsx`, `vite.config.js`
배경: 점검보고서(14) 권고 — JS 단일 청크 605kB(>500kB 경고)

---

## 변경
- `App.jsx`: 14개 페이지를 `React.lazy` 동적 import로 전환 + `<Suspense>` 폴백("불러오는 중…")
- `vite.config.js`: `manualChunks` 로 react·react-dom·react-router → `react-vendor`, supabase 분리

## 효과
| | 변경 전 | 변경 후 |
|---|---|---|
| 단일 청크 | 605.9kB (gzip 291) | — (분할) |
| 초기 로딩 | 605kB | 약 196kB (index+react-vendor+Home+curriculum) |
| 강의안 데이터 | 초기 포함 | `/lectures` 방문 시에만(Lectures 청크 339kB) |
| >500kB 경고 | 있음 | **없음** |

- 페이지별 청크 1~22kB로 분리, vendor 별도 청크로 재방문 캐시 효율↑.
- 무거운 강의안(lecture*.js 42 day-key)은 Lectures 청크에만 포함되어 초기 번들에서 제외.

## 검증
- `npm run build` 통과, 청크 크기 경고 해소
