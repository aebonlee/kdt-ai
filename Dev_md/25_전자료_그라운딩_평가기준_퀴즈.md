# 25. 로컬 자료 100% 그라운딩 — 실습 95예제 · 평가기준 · 퀴즈

## 배경
`/Volumes/aebon - 데이터/skala-4/항목을 포함하는 새로운 폴더`에 7/9~7/10자 신규 파일을 포함한 강의 자료 전체(4기 교육생 교재·종합실습 가이드·3기 교재·평가양식)가 수집됨. "폴더 내 모든 자료를 100% 숙지해 사이트에 적용" 지시에 따라 전수 분석·반영했다. (이후 폴더에 새 파일이 오면 같은 흐름으로 반영 — 상시 지시)

## 1) 자료 추출 (총 30여 종)
- **PDF → 텍스트**: `pdftotext -enc UTF-8` — 4기 교육생 교재 8종(Git·Vue·Python·기초통계·sLLM·VectorDB·RAG·Agent), 종합실습 가이드 3종(Python·VectorDB·웹서비스 — 실제 Practice 과제·Checkpoint 감점기준 포함), 3기 교재 13종(모델개발 day1/2·서빙 FastAPI·LLM day1/2·LangChain·Prompt·RAG day1/2/3·VectorDB 2일과정·Agent 등), Quiz 2종
- **zip**: ml-dl 딥러닝 전용 덱 3권(A.ML / B.DL / C.DL-Architecture) 압축해제 후 추출
- **xlsx → 텍스트**: python zipfile+sharedStrings 파싱 — 종합실습 평가기준 13종, 실습 가이드 3종
- **PPTX**: python-pptx — 4기 실라버스 15종 (Dev_md/23에서 대조검증 완료)

## 2) 실습예제 그라운딩 — src/data/lectureexamples3.js (95예제 / 33키)
과목 클러스터별 병렬 분석 에이전트 7개(Python·통계 / Git·Vue·Web / sLLM·Transformer·LangChain / VectorDB·RAG·Agent / ModelDev·Serving / Prompt / ml-dl 아키텍처)가 자료의 **실제 실습·코드**를 근거로 예제를 작성 → `scripts/merge-additions.mjs`가 cluster_*.mjs 를 병합해 `lectureexamples3.js`(examplesExtra3) 생성. `examplesFor` = examples + examplesExtra + examplesExtra3 병합.

원칙: SK 원문 verbatim 금지(우리말 재서술), 왕초보 실습중심, 전 코드라인 한글 주석(사이트 녹색 표기), 코드 내 `<` `>` `&` 는 리터럴(렌더 시 이중이스케이프 방지), 코드 문자열에 백틱·${} 금지.

대표 반영(자료 근거):
- **Python**: Practice1 자료구조 집계(Counter·defaultdict·컴프리헨션, assert 체크포인트)·제너레이터 메모리 비교·dis 바이트코드 / IQR 이상치+named aggregation·DuckDB SQL·카이제곱 독립성
- **통계·Feature·ML/DL**: 로그변환(왜도)·규칙 파생변수·상관 선택 / precision·recall·F1·ROC-AUC·R²·RMSE / autograd·학습루프 4박자·BatchNorm / ML↔DL 통합 비교·과적합 진단 / **CNN(Conv2d·풀링)·RNN/LSTM(시계열)·AutoEncoder(복원·Denoising)** ← ml-dl 전용 덱
- **Git·Vue·Web**: config·브랜치 머지 충돌 해결·.gitignore / 클래스바인딩·폼 v-model·이벤트수식어·emit·computed·watch·동적라우트·storeToRefs·axios 인터셉터·CRUD·Vite base / 액터 정의·DBML(ERD)·OpenAPI 명세·UI-ERD-API 일관성 자가점검
- **LLM 계열**: 멀티헤드·RNN vs Transformer·인과 마스킹·logits→토큰 / KV캐시·FAISS 인덱스·QLoRA·PromptTuning·맥락압축 / LCEL batch·Lambda·async·@chain·FewShot·Partial·LangGraph State/분기
- **VectorDB·RAG·Agent**: FAISS IVF(nlist/nprobe)·Qdrant payload 필터·PDF 청킹·ParentDocument / BM25(bm25s)·RRF(k=60)·BGE 리랭커·HitRate@K·MRR·LLM-as-a-Judge / create_agent(@tool)·Agentic RAG 채점-재작성 루프·Reflection
- **Prompt**: RICE v0→v5 누적·Prompt Chaining·LLM-Judge 4기준·System 템플릿 Placeholder·프롬프트 해부학 4요소

## 3) 평가기준 섹션 신설 — src/data/exams.js (12과목)
종합실습평가 xlsx에서 **실습목적·실습구성(실습명/활동/시간)·평가항목(내용/배점)·제출 필수항목·유의사항**을 구조화(purpose/tasks/criteria/deliverables/notes). 행정 메타문구는 제외하고 우리말 재서술.
- 렌더: 과목 **첫날**에 표시. React `ExamQuiz.jsx`(강의안) + `build-textbook.mjs` renderExam(실습교안·PDF)

## 4) 복습 퀴즈 섹션 신설 — src/data/quizzes.js
원본 Quiz PDF는 "구글폼 링크 안내"뿐이라, 과목 실제 내용에 근거해 문항 작성 — Python·VectorDB 각 10문항(O/X·4지선다·단답) + 문항마다 상세 해설.
- 렌더: 과목 **마지막날**에 `details` 펼치기(질문 클릭 → 정답·해설). PDF 인쇄본은 항상 펼침.

## 5) 검증
- cluster 파일 전부 import 파싱 OK, HTML 엔티티 0건, Vue `<script setup>` 1회 이스케이프(이중 이스케이프 0)
- 녹색 주석 span 2,537 → 3,437개, `npm run build` 통과, Actions 배포 success
- 평가기준 표(배점 포함)·퀴즈 펼치기 실렌더 스크린샷 확인

## 관련 커밋
`c39a7f0`(88예제) · `77b8612`(ml-dl 덱 + 평가기준 + 퀴즈 = 95예제)
