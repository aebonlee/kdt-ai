# 14. SKALA 강의안 사이트 — 점검보고서 (QA)

점검일: 2026-07-01
점검자: Ph.D 이애본 / Claude Code
대상: `skala.dreamitbiz.com` (aebonlee/skala, main `9e7d17e` 기준 + 강의안 보강분)
범위: 배포·빌드·라우팅·데이터 정합성·인증/보안·콘텐츠 무결성

---

## 1. 종합 판정 — ✅ 정상 (서비스 가능)

핵심 결함 0건. 배포·데이터·콘텐츠 모두 건전하며, 경미한 개선 권고 2건(번들 분할, vue 주석 비율).

| 영역 | 결과 |
|---|---|
| 배포/인프라 | ✅ 정상 |
| 빌드 | ✅ 통과(경고 1, 무해) |
| 라우팅 | ✅ 15개 라우트 정상 |
| 데이터 정합성 | ✅ 누락 0 |
| 인증/보안 | ✅ 안전 |
| 콘텐츠 무결성 | ✅ 42 day-key 완비 |

---

## 2. 배포 · 인프라

- 라이브 응답: **HTTP 200** (`https://skala.dreamitbiz.com/`, ~0.5s)
- 커스텀 도메인: `public/CNAME = skala.dreamitbiz.com`, vite `base: '/'` (자산 경로 정상)
- CI/CD: GitHub Actions `Deploy to GitHub Pages` — main push 시 자동 빌드·배포, 최근 실행 **success**
- 액션 버전: checkout v5 · setup-node v6 · configure-pages v6 · upload-pages-artifact v5 · deploy-pages v5, Node 22 (Node20 deprecation 경고 해소 완료)

## 3. 빌드

- `npm run build` **통과** (~1.3s)
- 산출물: `index.html` 2.4kB / `css` 17.4kB(gzip 4.1) / `js` 605.9kB(gzip 291.4)
- ⚠ 경고: 단일 청크 > 500kB (코드 스플리팅 미적용). 동작에는 무해, 초기 로딩 최적화 여지.

## 4. 라우팅 (15)

`/` `· /schedule · /prep · /prep/:id · /lectures · /lectures/:date · /team · /subjects · /reference · /progress · /day/:date · /login · /board(인증) · /board/:id(인증) · /dashboard(인증)` + `*`→Home 폴백.

## 5. 데이터 정합성

- 총 **48 세션(일)** · 과목 18개(일정 배정 15 + 참고 3) · **42 day-key**
- 월별: 7월 8 · 8월 9 · 9월 14 · 10월 17 (합 48)
- 지역/분반: 판교 5층 24 · 판교 4층 6 · 광주 1반 16 · 울산 2
- 강의안 6개 파일 모두 **42 key 완비**(periods·concepts·details·examples·theory·plans)
- **세션 콘텐츠 누락 0건** (모든 세션이 8교시·실습·핵심개념·상세항목 보유)
- git(07/14) 오전3 팀빌딩/오후5 Git 구성 보존 확인

## 6. 인증 · 보안

- `src/lib/supabase.js`: 환경변수(`VITE_SUPABASE_URL/ANON_KEY`)가 있을 때만 클라이언트 생성, 없으면 `null`로 **정적 모드 graceful 동작**
- 배포 시 Actions Secrets로 키 주입 → 라이브에서 로그인/게시판/대시보드 정상
- 소스 내 **하드코딩된 URL/키 0건** (보안 양호)

## 7. 콘텐츠 무결성

- 코드 블록 총 **140개** (실습 예제 88 + 실전 소스 52)
- 코드 주석 밀도 **전체 60%** (1,574 / 2,630 줄) — 거의 모든 의미 줄에 한국어 주석
- 심화이론 본문 **109개 전부 문장별 줄바꿈(100%)** + note·desc는 조립 단계 정규화 적용
- 평균 실습 단계 **9.2단계/일**, 추가 Lab **111개**

---

## 8. 발견 사항 (개선 권고)

| 우선 | 항목 | 내용 | 영향 |
|---|---|---|---|
| 낮음 | 번들 분할 | JS 단일 청크 605kB(>500kB 경고). 라우트 단위 `lazy import`로 분할 권고 | 초기 로딩 |
| 낮음 | vue 주석 비율 | vue 36%(타 과목 50~80%). 단, 주석 줄 수는 124로 최다 — SFC 템플릿 구조 줄(닫는 태그·빈 줄)이 분모를 키운 통계 착시 | 무 |
| 정보 | note 줄바꿈 | 단문 note는 줄바꿈 불필요. 다문장 note는 조립 정규화로 처리됨 | 무 |

**결론**: 배포·데이터·보안·콘텐츠 전 영역 건전. 즉시 운영 가능 상태이며, 번들 분할만 후속 과제로 권고.
