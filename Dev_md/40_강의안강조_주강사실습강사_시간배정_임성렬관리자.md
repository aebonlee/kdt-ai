# 40. 강의안 강조 렌더러 · 주강사/실습강사 분리 · 시간 배정표 · 임성렬 관리자 추가

**작업일**: 2026-07-16
**비고**: 앞 세 건은 재부팅 전 작업분(빌드 검증 후 커밋), 관리자 추가는 당일 요청분

## 1. 강의안 강조 렌더러 (`src/components/Rich.jsx` 신규)
- 명시 마커: `**굵게**` · `==형광펜==` · `__밑줄__` — 강의안 데이터 텍스트에 직접 사용 가능
- 자동 강조: "용어 — 설명" / "용어: 설명" 머리말은 굵게, 신호어(반드시·필수·주의·금지·중요·핵심·권장·절대 등)는 형광펜
- 적용 지점: Lectures(학습목표·핵심개념·심화이론·실습단계·랩·예제노트·과제), PrepDetail(개요·불릿·실습·팁·실수·체크리스트), EtcCourse(학습내용·개념·예제노트·연결팁)
- `index.css`에 `mark.hl`(형광펜, 다크모드 투명도 별도) · `b.rich-b` · `u.rich-u` 스타일 추가

## 2. 주강사/실습강사 분리 표기
- 근거 데이터 `src/data/currihours.js` 신설 — `SUBJECT_AUTHOR`(세션 ID→교안 저자) + `authorOf()`
- `trackschedule.js`: 각 일정 항목에 `leadBy`(주강사=교안 저자, 담당일은 myPairings lead) · `practiceBy`(실습강사=교실 진행 강사) 추가
- **Tracks**: 교과목 카드에 주강사/실습강사 2줄 표기, 시간표 테이블 '강사' 열을 '주강사'·'실습강사' 2열로 분리(동일인은 '주강사 직강')
- **Progress**: 월별 그룹 → **과목 순서 그룹**(분반 수강 순서 기준)으로 변경, 과목별 진도율 바 + 주강사/실습강사 표기

## 3. 캠퍼스별 교과목 시간 배정표 (Tracks 하단 신설)
- 근거: (0602, 수정) 4기 커리큘럼 엑셀 — **총 시수 변경 불가(원본 명기)**, 판교 862h / 광주·울산 818h
- `CURRI_META`(지역 라벨·총시간·기간) + `CURRI_GROUPS`(7그룹, 지역별 시수 차이는 p/k 분리) — 캠퍼스 탭에 따라 해당 열 표시
- 지역 차이 각주: 팀 프로젝트 272↔244h · SK Soft Skills 8↔4h · 도메인 특강 24↔16h · 수료식 10↔6h

## 4. 임성렬 주강사 관리자 추가 (2단 구성 — 둘 다 필요)
- **프론트** `src/config/admin.js` ADMIN_EMAILS에 `humanaiphd@gmail.com` 추가 → 관리자 버튼/메뉴 표시 (배포 필요)
- **DB** `skala_is_admin()` 함수 배열에 동일 이메일 추가 — SQL Editor에서 실행 완료(2026-07-16)
- ⚠️ 인수인계: 이 함수의 이메일 배열에서 **콤마 누락 주의** — PostgreSQL은 줄바꿈으로 나뉜 문자열 리터럴을 오류 없이 이어붙여 두 계정이 통째로 권한을 잃는다(이번에 실제 발생 직전 발견)
- 표기 참고: 강의 데이터(currihours·adminschedule)의 저자명은 "임성열", 관리자 요청 표기는 "임성렬" — 대표 확인 전까지 데이터는 기존 표기 유지
