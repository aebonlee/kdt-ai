# 2026-07-24 액센트 팔레트 · 히어로 타이핑 · 행성 3종 · 호실-반 표기 (+ 전일 Vue A안 그라운딩 기록)

## 한 일
1. **Vue A안 그라운딩** (7/23 배포분 기록 누락 보완)
   - 강병호 전임교수 교재 250p를 pypdf로 텍스트 추출(스크래치 `vue_text_*.txt`) 후 병렬 판독
   - lecturetheory +12편 / lectureconcepts +11종 / 교재 과제(날씨 대시보드 연속) homework 연결 / "실라버스 N단원"→"교재 N장" 정정
2. **실습교안 반 표기 정확화** — `practicelog.js`에 `sessionRooms`(날짜→'판교 502호-7반' 형식, 배정표 정본 47일 전체). Practice.jsx 카드 칩·좌측 메뉴 적용, 미매핑 날짜는 기존 region/klass 폴백
3. **액센트 팔레트** — 다크모드 토글 옆 🎨 버튼(AccentPalette.jsx): 시안(기본)/골드/바이올렛/그린/로즈
   - 구조: `data-accent` 속성이 `--a1/--a2`만 재정의 → `--gold: var(--a1)` 참조화로 전 사이트 연동
   - 하드코딩 액센트(rgba(34,211,238,…)·#3ee0f5)는 `color-mix(in srgb, var(--a1) N%, …)`로 전환
   - 라이트 모드는 대비 확보용 `--gold`만 딥 톤 별도 재정의(§기존 #0e7490 패턴 확장)
   - index.html 부트 스크립트에 `kdt-accent` 복원 추가 (⚠ 키는 AccentPalette 저장 키와 동일 유지)
4. **히어로 타이핑 효과** — 디자인 원본(rest06 `_fx` 로테이터)을 TypeWriter.jsx로 이식, 글자 단위 타이핑/삭제 순환('코드로 배우다' 외 3종). 그라디언트는 부모 `.ht-line:last-child`의 background-clip:text 재사용, 커서는 자식 배경(--a2)이라 클리핑 안 됨
5. **히어로 비주얼 팔레트 연동 + 행성 3종** — 코어 그라디언트·글로우·별·칩핀을 var(--a1/--a2)로, 외곽에 AI/AX/Dev 토성형 행성 3개 부유(hvPlanet 7s, Dev 색은 a1·a2 color-mix)

## 이유·주의
- SVG `<stop>`은 `stopColor="var(--a1)"`이 attr로는 안 먹는 브라우저가 있어 **style={{stopColor}}로 통일**
- prefers-reduced-motion이면 타이핑은 첫 단어 고정, 행성 애니메이션 정지 (화면 정지 사고 예방 원칙)
- 인쇄 시 .accent-palette 숨김 목록에 추가

## 남은 일
- 오늘 2반 팀 발표·실습2 채점, 개인과제(어젯밤 마감) 수합 집계
- 그라운딩 임박순 다음: sLLM(8/28) / Vue B안은 타 전임교수 자료 수령 시
