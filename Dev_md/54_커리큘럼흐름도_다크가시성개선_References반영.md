# 54. 커리큘럼 흐름도 다크 가시성 개선 + References 반영

날짜: 2026-07-22

## 한 일
1. **CurriculumFlow.jsx — 다크 배경 가시성 개선**
   - 상단 단계 구분 화살표: 가이드선 톤 `#2a3040 → #5b6788`, strokeWidth 1.5→1.6, 마커 확대
   - 정규교과 / 본 프로젝트 경계 점선: `#222634 → #63719a`(dasharray 5 4)로 밝힘
   - 계단식 과목 블록: 테두리를 시안 계열 `#2b4a5f → #3a6b8c`(width 1.2)로 밝혀 배경과 분리, 라벨 색 상향
   - Mini-project 블록: 면 `#12141c → #182230`, 테두리 `#2a3040 → #3f7d92`(시안 점선), 배지/글자 톤 상향
   - 하단 상시 트랙: 흐린 구분선 1줄 바 → **4칸 개별 셀 분리**(fillOpacity 0.16 + 테두리 `#4f9c7e` + 선명 글자)로 네 항목이 또렷이 구분

2. **References.jsx — 커리큘럼 흐름도 반영**
   - `CurriculumFlow` import 후 참고자료 섹션 상단에 배치
   - Home과 동일 래퍼(`.card` 다크 + `overflowX:auto`), 제목 "전체 커리큘럼 흐름도"

## 이유
- 다크 IDE 배경(rest06 팔레트)에서 기존 SVG의 가이드선·경계·블록 테두리가 배경과 톤이 붙어 흐릿했음 → 대비를 올려 또렷하게
- 참고자료 페이지에서도 전체 과정 흐름을 한눈에 보이게

## 검증
- `npm run build` 통과 (2.07s)

## 남은 일
- (직전 53편 이월) docs.js `ref` 필드 도입 여부 대표 판단
- 김영희 주강사/실습교수 최종 확인
