// ─────────────────────────────────────────────────────────────
// 프로젝트 팀 편성 — 팀별 학생 명단 (관리자 전용 화면에서 사용)
//
// members 에는 학습자가 로그인할 때 쓰는 "이름"을 넣으세요.
//   (구글/카카오 프로필 이름, 없으면 이메일 — skala_progress.user_name 과 매칭됨)
// 이름이 매칭되면 팀별 명단에 진도율·최근접속이 함께 표시됩니다.
//
// 팀 배정을 아직 안 넣었으면 teams = [] 로 두면 됩니다.
// 그 경우 관리자 팀별 명단 화면은 "전체 학습자 명단"만 보여줍니다.
// ─────────────────────────────────────────────────────────────

export const teams = [
  // 예시 — 실제 편성으로 교체하세요:
  // { no: 1, name: '1팀', topic: '주제(선택)', members: ['홍길동', '김철수'] },
  // { no: 2, name: '2팀', topic: '', members: ['이영희', '박민수'] },
]

// 이름 정규화 — 공백 제거 + 소문자 (매칭 안정화)
export const normName = (s) => (s || '').replace(/\s+/g, '').toLowerCase()
