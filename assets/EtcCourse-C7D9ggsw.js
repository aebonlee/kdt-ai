import{j as e,h as f,i as A}from"./index-BWpq4xAJ.js";import{R as p}from"./Rich-xhUglYvt.js";import{L}from"./react-vendor-DkQmGj46.js";import{b as v,T,o as R}from"./othercontent-CwcvR8bg.js";import{C as x}from"./CodeBlock-B8Ir0sof.js";import{R as P}from"./Rating-CiK99vwR.js";const _={python:[{type:"ox",q:"Python은 코드를 한 줄씩 해석하며 실행하는 인터프리터(interpreter) 언어다.",answer:"O",explain:"맞습니다. Python은 소스코드를 미리 기계어로 통째로 번역(컴파일)해 두는 C/자바(엄밀히는 조금 다름)와 달리, 실행 시점에 한 줄씩 읽어서 바로 실행하는 인터프리터 방식입니다. 그래서 별도의 컴파일 단계 없이 바로 실행할 수 있어 배우기 쉽고, 대화형(REPL)으로 한 줄씩 테스트하기 편합니다. 다만 이 방식은 실행 속도가 상대적으로 느린 단점이 있어, 무거운 계산은 NumPy 같은 C로 만든 라이브러리에 맡깁니다."},{type:"choice",q:"0부터 9까지의 정수 중 짝수만 골라 제곱한 리스트 [0, 4, 16, 36, 64]를 만드는 리스트 컴프리헨션으로 올바른 것은?",choices:["[x*x for x in range(10)]","[x*x for x in range(10) if x % 2 == 0]","[x*x if x % 2 == 0 for x in range(10)]","[x % 2 == 0 for x in range(10)]"],answer:1,explain:'정답은 2번입니다. 리스트 컴프리헨션의 기본 문법은 [표현식 for 변수 in 반복대상 if 조건]입니다. 여기서 for 뒤에 붙는 if는 "걸러내기(필터)" 역할이라 조건이 참인 값만 통과합니다. x % 2 == 0 은 2로 나눈 나머지가 0, 즉 짝수라는 뜻이므로 0,2,4,6,8만 통과하고 각각 제곱해 [0,4,16,36,64]가 됩니다. 3번처럼 if를 for 앞에 쓰는 형태는 필터가 아니라 삼항식(값 선택)이라 else가 없으면 문법 오류가 납니다.'},{type:"ox",q:"Python의 리스트(list)는 값을 바꿀 수 없는(immutable) 자료형이다.",answer:"X",explain:"틀렸습니다. 리스트는 값을 바꿀 수 있는 가변(mutable) 자료형입니다. 예를 들어 a = [1,2,3] 에서 a[0] = 99 처럼 원소를 바꾸거나 a.append(4)로 추가할 수 있습니다. 반대로 값을 바꿀 수 없는(immutable) 자료형은 튜플(tuple), 문자열(str), 정수(int) 등입니다. 이 구분이 중요한 이유는, 가변 객체를 함수 인자로 넘기면 함수 안에서 원본이 바뀔 수 있기 때문입니다."},{type:"short",q:"Python에서 키(key)와 값(value)을 짝지어 저장하며 {'name': 'kim'} 처럼 중괄호로 표현하는 자료형의 이름은?",answer:"딕셔너리(dictionary)",explain:`정답은 딕셔너리(dict)입니다. 딕셔너리는 "이름표(키) → 값" 형태로 데이터를 저장해, 인덱스 번호가 아니라 의미 있는 키로 값을 꺼낼 수 있습니다(예: person['name']). 내부적으로 해시(hash)를 쓰기 때문에 데이터가 많아도 키로 값을 찾는 속도가 매우 빠릅니다. 데이터 분석에서 한 행(레코드)을 표현하거나 JSON 데이터를 다룰 때 아주 자주 쓰입니다.`},{type:"choice",q:"Pandas에서 CSV 파일을 읽어 표(DataFrame) 형태로 불러오는 함수는?",choices:["pd.open_csv()","pd.load_csv()","pd.read_csv()","pd.import_csv()"],answer:2,explain:"정답은 3번 pd.read_csv() 입니다. Pandas에서 파일을 읽어오는 함수는 대부분 read_ 로 시작합니다(read_csv, read_excel, read_json, read_parquet 등). 반환값은 DataFrame이라는 2차원 표 형태 객체로, 엑셀 시트처럼 행과 열로 데이터를 다룰 수 있습니다. 참고로 반대로 저장할 때는 df.to_csv() 처럼 to_ 로 시작하는 메서드를 씁니다."},{type:"ox",q:"Polars는 대용량 데이터를 빠르게 처리하도록 설계된 DataFrame 라이브러리로, 여러 열을 동시에 병렬 처리하고 Lazy(지연) 실행을 지원한다.",answer:"O",explain:'맞습니다. Polars는 Rust로 만들어진 고성능 DataFrame 라이브러리입니다. Pandas가 기본적으로 한 번에 하나씩(단일 스레드) 처리하는 경향이 있는 반면, Polars는 여러 CPU 코어를 활용한 병렬 처리와, 연산을 바로 실행하지 않고 계획만 세워뒀다가 한꺼번에 최적화해서 실행하는 "Lazy 실행"을 지원합니다. 덕분에 대용량 데이터에서 Pandas보다 훨씬 빠른 경우가 많아, 요즘 데이터 분석에서 인기가 높아지고 있습니다.'},{type:"choice",q:"DuckDB에 대한 설명으로 가장 올바른 것은?",choices:["서버를 따로 띄워야만 쓸 수 있는 대규모 분산 데이터베이스다","설치·서버 없이 파일이나 메모리에서 SQL로 분석 쿼리를 실행하는 임베디드 분석용 DB다","이미지 파일만 저장하는 전용 데이터베이스다","Python에서는 사용할 수 없고 C++에서만 동작한다"],answer:1,explain:'정답은 2번입니다. DuckDB는 "분석계의 SQLite"라고 불립니다. 별도 서버 설치나 실행 없이, 프로그램 안에 내장(embedded)되어 동작하며 CSV/Parquet 파일이나 메모리 위의 데이터를 바로 SQL로 조회·집계할 수 있습니다. 특히 대량 데이터를 열(column) 단위로 처리하는 데 최적화되어 있어, Pandas DataFrame을 그대로 SQL로 질의하는 등 데이터 분석 실습에서 매우 편리합니다.'},{type:"short",q:"def add(a: int, b: int) -> int: 처럼 함수의 인자와 반환값에 자료형을 표시해 코드 가독성과 오류 예방을 돕는 Python 문법 요소를 무엇이라 하는가?",answer:"타입 힌트(type hint)",explain:'정답은 타입 힌트(type annotation)입니다. a: int 는 "a는 정수를 기대한다", -> int 는 "이 함수는 정수를 돌려준다"는 의미를 사람과 도구에게 알려줍니다. 중요한 점은 타입 힌트가 실행 시 강제되지는 않는다는 것입니다(문자열을 넣어도 파이썬 자체는 에러를 내지 않음). 대신 mypy 같은 검사 도구나 VS Code 자동완성이 이 정보를 활용해 미리 버그를 잡아주고 코드를 이해하기 쉽게 만듭니다.'},{type:"ox",q:'async / await 문법은 파일 다운로드나 네트워크 요청처럼 "기다리는 시간"이 많은 작업을 효율적으로 처리하기 위한 비동기(asynchronous) 프로그래밍 기능이다.',answer:"O",explain:'맞습니다. async def 로 정의한 함수(코루틴) 안에서 await 를 쓰면, 오래 걸리는 I/O 작업(네트워크 응답, 파일 읽기 등)을 기다리는 동안 CPU를 놀리지 않고 다른 작업을 처리할 수 있습니다. 즉 "응답 기다리는 김에 다른 일 하기"입니다. 이는 여러 API를 동시에 호출하거나 대량 요청을 처리할 때 큰 성능 이점을 줍니다. 다만 순수 계산(CPU 위주) 작업에는 비동기보다 멀티프로세싱이 더 적합합니다.'},{type:"choice",q:"Python에서 파일을 열고 작업이 끝나면 자동으로 닫아 주어(자원 누수 방지) 안전하게 파일을 다루는 권장 방식은?",choices:["f = open('a.txt') 후 close()를 잊지 않고 직접 호출","with open('a.txt') as f: 구문(컨텍스트 매니저) 사용","import 문으로 파일을 불러오기","global 키워드로 파일을 전역 선언"],answer:1,explain:"정답은 2번 with open(...) as f: 구문입니다. 이를 컨텍스트 매니저라고 하며, with 블록이 끝나면(정상 종료든 에러든 상관없이) 파일이 자동으로 닫힙니다. 1번처럼 직접 close()를 부르는 방식은 중간에 에러가 나면 파일이 닫히지 않아 자원이 새는 문제가 생길 수 있습니다. with 구문은 이런 실수를 원천적으로 막아 주므로 파이썬에서 파일·DB 연결·락 등을 다룰 때 표준으로 권장됩니다."}],vectordb:[{type:"ox",q:"임베딩(embedding)이란 텍스트·이미지 같은 데이터를 의미를 담은 숫자 벡터로 바꾸는 것을 말한다.",answer:"O",explain:`맞습니다. 임베딩은 사람의 언어나 이미지처럼 컴퓨터가 직접 비교하기 어려운 데이터를, 여러 개의 숫자로 이루어진 벡터(예: [0.12, -0.98, 0.34, ...])로 변환하는 기술입니다. 핵심은 "의미가 비슷한 것들은 벡터 공간에서 서로 가까이 위치한다"는 점입니다. 예를 들어 '강아지'와 '개'의 벡터는 가깝고, '강아지'와 '자동차'의 벡터는 멀리 떨어집니다. 이 성질 덕분에 의미 기반 검색(semantic search)이 가능해집니다.`},{type:"choice",q:"Vector DB에서 두 벡터가 얼마나 비슷한지(의미가 가까운지) 측정할 때 가장 널리 쓰이는 지표는?",choices:["코사인 유사도(cosine similarity)","파일 크기 비교","알파벳 순 정렬","문자열 길이 차이"],answer:0,explain:'정답은 1번 코사인 유사도입니다. 코사인 유사도는 두 벡터가 이루는 각도를 이용해 방향이 얼마나 비슷한지를 -1~1 사이 값으로 나타냅니다(1에 가까울수록 비슷). 벡터의 크기(길이)가 아니라 "방향"을 보기 때문에, 문장의 길이 같은 요소에 덜 흔들리고 의미의 유사성을 잘 포착합니다. 이 밖에 유클리드 거리(직선 거리), 내적(dot product)도 쓰이지만, 텍스트 임베딩 검색에서는 코사인 유사도가 가장 대표적입니다.'},{type:"ox",q:'HNSW와 IVF는 수많은 벡터 중에서 비슷한 것을 빠르게 찾기 위한 "근사 최근접 이웃(ANN) 인덱스" 알고리즘이다.',answer:"O",explain:"맞습니다. 벡터가 수백만~수억 개가 되면, 질의 벡터와 모든 벡터를 하나하나 비교(정확 검색, brute-force)하는 것은 너무 느립니다. 그래서 정확도를 아주 조금 양보하는 대신 속도를 크게 높이는 ANN(Approximate Nearest Neighbor) 인덱스를 씁니다. HNSW는 계층적 그래프를 만들어 탐색하는 방식이고, IVF(Inverted File)는 벡터를 여러 군집(cluster)으로 나눠 관련 있는 군집만 탐색하는 방식입니다. 둘 다 대규모 벡터 검색을 실용적인 속도로 만들어 주는 핵심 기술입니다."},{type:"choice",q:"HNSW(Hierarchical Navigable Small World) 인덱스의 특징으로 가장 올바른 것은?",choices:["데이터를 알파벳 순으로 정렬해 이진 탐색한다","여러 층으로 이루어진 그래프를 따라가며 가까운 이웃을 빠르게 탐색한다","모든 벡터를 항상 전부 비교하므로 근사가 아니라 완전 정확하다","벡터를 이미지 파일로 저장해 눈으로 비교한다"],answer:1,explain:'정답은 2번입니다. HNSW는 "작은 세상(small world)" 네트워크 아이디어를 이용한 그래프 기반 인덱스입니다. 위층은 노드가 듬성듬성하게 연결돼 멀리 있는 지점으로 빠르게 점프하고, 아래층으로 내려갈수록 촘촘해져 정밀하게 가까운 이웃을 찾습니다. 마치 지도에서 고속도로(위층)로 크게 이동한 뒤 골목길(아래층)로 세밀하게 찾아가는 것과 같습니다. 검색 속도와 정확도의 균형이 좋아 대부분의 Vector DB가 기본 인덱스로 채택합니다.'},{type:"short",q:"Facebook(Meta)이 만든, 대규모 벡터의 유사도 검색과 클러스터링에 특화된 오픈소스 라이브러리의 이름은?",answer:"FAISS",explain:'정답은 FAISS(Facebook AI Similarity Search)입니다. FAISS는 수십억 개 규모의 벡터도 빠르게 유사도 검색할 수 있도록 만든 라이브러리로, IVF·HNSW·PQ(양자화) 등 다양한 인덱스를 지원하고 GPU 가속도 가능합니다. 데이터베이스 서버라기보다 "검색 엔진 부품"에 가까워, Qdrant·Milvus 같은 완성형 Vector DB의 내부 엔진으로 쓰이거나 직접 임베딩 검색을 구현할 때 널리 사용됩니다.'},{type:"ox",q:"Qdrant 같은 Vector DB는 벡터와 함께 저장한 메타데이터(예: 카테고리, 날짜)로 필터링하면서 유사도 검색을 함께 수행할 수 있다.",answer:"O",explain:'맞습니다. 실무에서는 단순히 "비슷한 벡터"만 찾는 게 아니라, "2024년 이후 문서 중에서, 카테고리가 뉴스인 것 중에서 비슷한 것"처럼 조건을 걸어야 할 때가 많습니다. Qdrant, Milvus, Weaviate 같은 Vector DB는 각 벡터에 payload/metadata를 함께 저장해 두고, 필터 조건(payload filtering)과 벡터 유사도 검색을 결합해 실행할 수 있습니다. 이를 통해 훨씬 정확하고 실용적인 검색 결과를 얻을 수 있습니다.'},{type:"choice",q:"하이브리드 검색(hybrid search)에 대한 설명으로 가장 올바른 것은?",choices:["키워드(어휘) 검색과 벡터(의미) 검색을 결합해 두 방식의 장점을 함께 취하는 방법","여러 개의 Vector DB 서버를 물리적으로 합치는 작업","벡터를 절반으로 잘라 저장 공간을 아끼는 압축 기법","GPU와 CPU를 번갈아 쓰는 하드웨어 설정"],answer:0,explain:"정답은 1번입니다. 하이브리드 검색은 전통적인 키워드 기반 검색(BM25 같은 어휘 매칭)과 임베딩 기반 의미 검색을 함께 사용하는 방법입니다. 키워드 검색은 정확한 단어·고유명사·코드 등을 잘 잡아내고, 벡터 검색은 표현이 달라도 의미가 통하는 문서를 잘 찾습니다. 이 둘의 점수를 합치면(예: RRF 방식) 어느 한쪽만 쓸 때보다 더 정확하고 견고한 검색 결과를 얻을 수 있어, RAG 시스템에서 자주 활용됩니다."},{type:"short",q:"벡터 검색으로 뽑은 여러 후보 문서를, 질문과의 관련도를 더 정밀하게 다시 점수 매겨 순서를 바로잡는 과정을 무엇이라 하는가?",answer:"리랭킹(reranking)",explain:'정답은 리랭킹(re-ranking)입니다. 벡터 검색(ANN)은 빠르지만 대략적이라 상위 결과의 순서가 완벽하지 않을 수 있습니다. 그래서 1차로 후보 문서를 넉넉히(예: 상위 50개) 뽑은 뒤, Cross-Encoder 같은 더 정밀하지만 무거운 모델로 "질문과 각 문서"를 함께 보고 관련도를 다시 채점해 최종 순위를 정합니다. 속도가 필요한 넓은 검색은 벡터 DB가, 정확도가 필요한 마지막 정렬은 리랭커가 맡는 2단계 구조로 품질을 크게 높일 수 있습니다.'},{type:"ox",q:"임베딩 벡터의 차원 수(예: 768차원, 1536차원)는 검색 정확도·저장 용량·연산 속도와 아무 관련이 없다.",answer:"X",explain:"틀렸습니다. 차원 수는 매우 중요한 요소입니다. 차원이 높을수록 더 풍부한 의미를 담을 수 있어 정확도가 올라갈 여지가 있지만, 그만큼 벡터 하나당 저장 용량이 커지고 유사도 계산도 느려집니다. 예를 들어 1536차원 벡터는 768차원보다 저장·연산 비용이 대략 두 배입니다. 그래서 실무에서는 정확도와 비용의 균형을 보고 적절한 임베딩 모델·차원을 고르며, 필요하면 양자화(quantization)로 벡터를 압축해 비용을 줄이기도 합니다."},{type:"choice",q:"RAG(검색 증강 생성) 파이프라인에서 Vector DB가 담당하는 역할로 가장 알맞은 것은?",choices:["사용자에게 최종 답변 문장을 직접 생성한다","질문과 의미가 비슷한 문서 조각을 찾아 LLM에게 참고 자료로 전달한다","모델의 가중치를 학습(파인튜닝)한다","웹 브라우저 화면을 렌더링한다"],answer:1,explain:'정답은 2번입니다. RAG는 "검색(Retrieval)으로 찾은 근거를 붙여(Augmented) LLM이 답을 생성(Generation)"하는 구조입니다. 여기서 Vector DB는 미리 문서를 잘게 나눠 임베딩해 저장해 두었다가, 사용자 질문이 들어오면 의미적으로 가장 관련 있는 문서 조각(chunk)을 빠르게 찾아내는 검색 담당입니다. 이렇게 찾은 근거를 프롬프트에 함께 넣어 주면 LLM이 최신·전문 정보를 바탕으로 환각(hallucination)을 줄인 답변을 만들 수 있습니다. 답변 생성 자체는 LLM의 몫입니다.'}],git:[{type:"ox",q:"커밋(commit)은 지금까지의 변경 사항을 한 묶음으로 저장하는 것으로, 게임의 세이브 포인트에 비유할 수 있다.",answer:"O",explain:'맞습니다. 커밋은 "여기까지의 작업을 하나의 저장 시점으로 기록"하는 행동이자 그렇게 만들어진 기록 자체입니다. 게임에서 세이브 포인트를 만들어 두면 실패해도 그 지점으로 돌아갈 수 있듯이, 커밋을 해 두면 코드가 망가져도 언제든 그 시점으로 되돌릴 수 있습니다. 그래서 커밋은 작게, 자주, 의미 있는 메시지와 함께 남기는 것이 좋은 습관입니다.'},{type:"ox",q:"git add 명령을 실행하면 변경 사항이 곧바로 GitHub(원격 저장소)에 업로드된다.",answer:"X",explain:'틀렸습니다. git add는 "다음 커밋에 포함할 변경만 골라 장바구니(스테이징 영역)에 담는" 단계일 뿐, 내 컴퓨터 밖으로는 아무것도 나가지 않습니다. 장바구니에 담은 것을 git commit으로 로컬 저장소에 기록하고, 그 커밋을 git push 해야 비로소 GitHub 같은 원격 저장소에 올라갑니다. 즉 전체 흐름은 수정 → add(담기) → commit(로컬 저장) → push(원격 업로드) 순서입니다.'},{type:"ox",q:"머지 충돌(conflict)은 두 브랜치가 같은 파일의 같은 부분을 서로 다르게 고쳐서 Git이 자동으로 합치지 못할 때 발생한다.",answer:"O",explain:'맞습니다. Git은 서로 다른 파일이나 같은 파일의 다른 줄을 고친 경우에는 똑똑하게 자동으로 합쳐 줍니다. 하지만 두 사람이 같은 줄을 서로 다르게 고쳤다면 "어느 쪽이 맞는지"를 기계가 판단할 수 없어 충돌 표시(<<<<<<<, =======, >>>>>>>)를 남기고 사람에게 결정을 맡깁니다. 충돌은 오류가 아니라 자연스러운 협업 과정이며, 해당 부분을 원하는 내용으로 정리한 뒤 다시 add·commit 하면 해결됩니다.'},{type:"choice",q:"로컬에서 작업한 코드를 GitHub까지 올리는 기본 흐름으로 올바른 순서는?",choices:["push → commit → add → 수정","수정 → add → commit → push","수정 → commit → add → push","add → push → 수정 → commit"],answer:1,explain:"정답은 2번입니다. 먼저 파일을 수정하고, git add로 커밋에 포함할 변경을 스테이징 영역(장바구니)에 담습니다. 그다음 git commit으로 담긴 변경을 하나의 저장 시점으로 로컬 저장소에 기록하고, 마지막으로 git push로 그 커밋을 GitHub 원격 저장소에 업로드합니다. add 없이 commit할 수 없고, commit 없이 push할 것이 없으므로 이 순서는 바뀔 수 없습니다."},{type:"choice",q:"Git의 브랜치(branch)에 대한 설명으로 가장 알맞은 것은?",choices:["저장소를 통째로 삭제하는 기능이다","메인 작업을 건드리지 않고 따로 실험할 수 있는 평행 세계 같은 작업 가지다","파일을 압축해서 용량을 줄이는 기능이다","커밋 메시지를 자동으로 작성해 주는 AI 기능이다"],answer:1,explain:"정답은 2번입니다. 브랜치는 메인(main) 코드에서 갈라져 나온 독립된 작업 공간으로, 여기서 아무리 실험하고 실패해도 본류에는 영향이 없습니다. 새 기능을 만들 때 feature 브랜치를 파서 작업하고, 완성되면 merge로 다시 본류에 합치는 방식이 표준 협업 흐름입니다. 여러 사람이 각자의 브랜치에서 동시에 작업할 수 있어 팀 개발의 핵심 도구가 됩니다."},{type:"choice",q:"GitHub의 Pull Request(PR)에 대한 설명으로 가장 알맞은 것은?",choices:["내 브랜치의 변경을 본류에 합쳐 달라고 요청하고, 팀원이 코드리뷰를 할 수 있게 하는 기능","원격 저장소의 최신 코드를 내 컴퓨터로 내려받는 명령어","커밋을 완전히 삭제해 되돌릴 수 없게 만드는 기능","저장소를 다른 사람 계정으로 복사하는 기능"],answer:0,explain:'정답은 1번입니다. Pull Request는 "제 브랜치의 변경 사항을 main에 당겨(pull) 합쳐 주세요"라고 팀에 공식적으로 요청하는 절차입니다. PR 화면에서 어떤 코드가 어떻게 바뀌었는지 한눈에 비교할 수 있고, 팀원들이 댓글로 코드리뷰를 하며 문제를 미리 잡을 수 있습니다. 리뷰가 끝나 승인되면 머지 버튼으로 본류에 합쳐지므로, 무턱대고 main에 직접 push하는 것보다 훨씬 안전한 협업 방식입니다. 2번은 git pull 명령에 대한 설명입니다.'},{type:"short",q:"node_modules 폴더처럼 Git이 추적하지 않았으면 하는 파일·폴더 목록을 적어 두는 특별한 파일의 이름은?",answer:".gitignore",explain:"정답은 .gitignore 파일입니다. 프로젝트에는 라이브러리 설치 폴더(node_modules), 빌드 결과물, 비밀번호가 담긴 설정 파일처럼 저장소에 올리면 안 되거나 올릴 필요가 없는 것들이 있습니다. 저장소 최상위에 .gitignore 파일을 만들고 그 안에 이름이나 패턴(예: node_modules/, *.log)을 적으면 Git이 해당 파일들을 아예 없는 것처럼 무시합니다. 특히 API 키 같은 민감 정보 파일은 반드시 .gitignore에 넣어 실수로 공개되는 사고를 막아야 합니다."},{type:"short",q:"팀원이 GitHub 원격 저장소에 올린 최신 변경 사항을 내 컴퓨터(로컬 저장소)로 가져와 합치는 Git 명령어는? (git 다음에 오는 한 단어)",answer:"pull",explain:"정답은 pull(git pull)입니다. pull은 원격 저장소의 새 커밋을 내려받아(fetch) 내 로컬 브랜치에 합치는(merge) 것까지 한 번에 해 주는 명령입니다. 반대로 내 커밋을 원격으로 올리는 것은 push입니다. VS Code에서는 Sync Changes 버튼 하나가 pull과 push를 함께 처리해 줍니다. 팀 작업에서는 내 작업을 push하기 전에 먼저 pull로 남의 변경을 받아 충돌을 미리 정리하는 습관이 중요합니다."}],vue:[{type:"ox",q:"Vue에서 ref()로 만든 반응형 값이 바뀌면, 그 값을 사용하는 화면(템플릿)이 자동으로 다시 그려진다.",answer:"O",explain:'맞습니다. 이것이 Vue의 핵심인 반응형(reactivity) 시스템입니다. ref()로 감싼 값은 Vue가 "감시 대상"으로 등록해 두고, 값이 바뀌는 순간 그 값을 쓰고 있는 화면 부분만 골라서 자동으로 갱신합니다. 개발자가 document.getElementById 같은 코드로 화면을 직접 고칠 필요가 없어지는 것이죠. 참고로 스크립트 코드 안에서는 count.value처럼 .value를 붙여 읽고 쓰지만, 템플릿 안에서는 그냥 count로 쓰면 됩니다.'},{type:"ox",q:"v-for는 조건이 참일 때만 요소를 화면에 표시하는 디렉티브다.",answer:"X",explain:'틀렸습니다. 조건에 따라 요소를 보여 주거나 없애는 것은 v-if이고, v-for는 배열 같은 목록 데이터를 반복해서 요소를 여러 개 찍어내는 디렉티브입니다. 예를 들어 v-for="item in items" 라고 쓰면 items 배열의 개수만큼 태그가 반복 생성됩니다. v-for를 쓸 때는 각 항목을 구분하는 :key를 함께 지정해 주어야 Vue가 목록 변화를 효율적으로 추적할 수 있습니다.'},{type:"ox",q:"컴포넌트 통신에서 props는 부모가 자식에게 데이터를 내려 주는 통로이고, emit은 자식이 부모에게 이벤트를 올려 보내는 통로다.",answer:"O",explain:'맞습니다. Vue 컴포넌트는 "데이터는 아래로, 이벤트는 위로"라는 단방향 흐름 원칙을 따릅니다. 부모는 props로 자식에게 필요한 값을 내려 주고, 자식은 그 값을 직접 고치는 대신 emit으로 "이런 일이 일어났어요"라고 부모에게 알립니다. 그러면 부모가 자기 데이터를 직접 수정하죠. 이 규칙 덕분에 데이터가 어디서 바뀌는지 추적하기 쉬워지고, 컴포넌트가 많아져도 코드가 엉키지 않습니다.'},{type:"choice",q:'장바구니 상품 가격들로부터 "합계 금액"처럼 다른 값에서 자동으로 계산되는 파생 값을 만들 때 가장 알맞은 Vue 기능은?',choices:["computed(계산된 속성)","v-for","slot","router-link"],answer:0,explain:'정답은 1번 computed입니다. computed는 "다른 반응형 값들로부터 자동으로 만들어지는 새 값"을 정의하는 기능으로, 원본(상품 가격)이 바뀌면 합계가 알아서 다시 계산됩니다. 결과를 캐싱하기 때문에 원본이 안 바뀌면 다시 계산하지 않아 효율적입니다. 비슷해 보이는 watch는 "값이 바뀌었을 때 어떤 동작(예: API 호출)을 실행"하는 용도라서, 화면에 보여 줄 파생 값을 만들 때는 computed가 정답입니다.'},{type:"choice",q:"입력창(input)에 사용자가 타이핑하면 데이터가 바뀌고, 데이터가 바뀌면 입력창 내용도 함께 바뀌는 양방향 바인딩 디렉티브는?",choices:["v-if","v-bind","v-model","v-on"],answer:2,explain:'정답은 3번 v-model입니다. v-model은 "화면의 입력값 ↔ 자바스크립트 데이터"를 양쪽으로 자동 동기화해 주어, 폼 입력을 다룰 때 가장 많이 쓰입니다. 내부적으로는 값을 내려 주는 v-bind와 입력 이벤트를 받는 v-on을 합쳐 놓은 축약형입니다. 참고로 v-bind는 데이터 → 화면 한 방향 연결, v-on은 클릭 같은 이벤트 처리, v-if는 조건부 표시 용도이므로 양방향 동기화와는 다릅니다.'},{type:"choice",q:"Pinia에 대한 설명으로 가장 알맞은 것은?",choices:["Vue 컴포넌트의 CSS를 자동으로 예쁘게 만들어 주는 디자인 도구다","여러 컴포넌트가 함께 쓰는 데이터를 한곳(스토어)에서 관리하는 Vue 공식 전역 상태 관리 라이브러리다","자바스크립트 코드를 압축해 주는 빌드 도구다","데이터베이스에 SQL을 실행하는 백엔드 라이브러리다"],answer:1,explain:"정답은 2번입니다. 앱이 커지면 로그인한 사용자 정보나 장바구니처럼 여러 화면이 공유해야 하는 데이터가 생기는데, props로 일일이 전달하면 코드가 복잡해집니다. Pinia는 이런 데이터를 스토어(store)라는 한곳에 모아 두고 어떤 컴포넌트든 꺼내 쓰게 해 주는 전역 상태 관리 도구입니다. 스토어는 state(데이터)·getters(파생 값)·actions(데이터를 바꾸는 함수)로 구성되며, Vue 팀이 공식으로 권장하는 표준 라이브러리입니다."},{type:"short",q:"SPA에서 주소(URL)에 따라 알맞은 페이지 컴포넌트를 보여 주고, 동적 파라미터·네비게이션 가드 기능을 제공하는 Vue 공식 라우팅 라이브러리는?",answer:"Vue Router",explain:'정답은 Vue Router입니다. SPA는 화면 전환 시 서버에서 새 HTML을 받아오지 않기 때문에, "주소가 바뀌면 어떤 컴포넌트를 보여 줄지"를 정해 주는 장치가 필요한데 그 역할을 Vue Router가 맡습니다. /users/3 처럼 주소 일부를 변수로 받는 동적 파라미터, 로그인하지 않은 사용자를 막는 네비게이션 가드 같은 기능도 제공합니다. 링크는 a 태그 대신 router-link를 써서 새로고침 없이 부드럽게 이동합니다.'},{type:"short",q:"하나의 .vue 파일 안에 template(화면)·script(로직)·style(디자인)을 함께 담는 Vue의 파일 구조를 무엇이라 하는가? (영문 약자 3글자)",answer:"SFC",explain:"정답은 SFC(Single File Component, 단일 파일 컴포넌트)입니다. 보통 웹 개발은 HTML·JS·CSS 파일을 따로 만들지만, Vue는 한 컴포넌트에 관련된 화면·로직·스타일을 하나의 .vue 파일에 모아 둡니다. 버튼 컴포넌트를 고치고 싶으면 그 파일 하나만 열면 되니 유지보수가 쉽고, 컴포넌트를 부품처럼 재사용하기도 좋습니다. style에 scoped를 붙이면 그 스타일이 해당 컴포넌트 안에서만 적용되어 다른 화면과 충돌하지 않습니다."}],webproject:[{type:"ox",q:"와이어프레임(wireframe)은 색상·디자인을 완성하기 전에, 화면의 배치와 흐름을 간단한 선과 상자로 그려 보는 설계도다.",answer:"O",explain:'맞습니다. 와이어프레임은 "이 화면에 무엇이 어디에 놓이고, 버튼을 누르면 어디로 가는지"를 빠르게 그려 보는 뼈대 설계도입니다. 예쁘게 꾸미는 것이 목적이 아니라, 코드를 짜기 전에 화면 구성과 흐름에 대한 팀의 합의를 만드는 것이 목적입니다. 종이나 간단한 도구로 그려도 충분하며, 이 단계에서 구조를 고치는 것이 코드를 다 짠 뒤에 고치는 것보다 수십 배 저렴합니다.'},{type:"ox",q:"팀원 각자의 코드가 따로따로 잘 동작했다면, 하나로 합친(통합한) 뒤에는 따로 테스트하지 않아도 된다.",answer:"X",explain:"틀렸습니다. 각자 만든 부분이 멀쩡해도 합치는 순간 처음 보는 충돌과 버그가 쏟아지는 것이 개발의 현실입니다. 각자 방에서 만든 가구가 한 집에 모이면 문이 안 맞거나 색이 따로 노는 것과 같습니다. 그래서 통합 후에는 목록 보기부터 글 추가·삭제까지 실제 사용자처럼 처음부터 끝까지 눌러 보는 통합 테스트가 반드시 필요합니다. 미니 프로젝트 3일차에 통합·버그 수정 시간을 따로 두는 이유가 바로 이것입니다."},{type:"ox",q:"이 미니 프로젝트에서는 요약·추천·분류·생성 중 AI 기능 1개를 골라 웹 서비스에 얹는 것이 목표다.",answer:"O",explain:'맞습니다. 이 프로젝트의 핵심은 평범한 웹 서비스에 AI 기능을 딱 하나 얹어 보는 경험입니다. 거창할 필요 없이 서비스 성격에 맞는 것 하나면 충분합니다. 예를 들어 메모 앱이면 "메모 한 줄 요약", 맛집 앱이면 "취향 태그로 추천 문구 생성", 게시판이면 "글 자동 분류" 같은 식이죠. 기획 단계에서 AI 기능 1개와 사용할 AI API(LLM 등)를 정하고, 구현 단계에서 API 연동으로 실제 동작하게 만듭니다.'},{type:"choice",q:"미니 프로젝트의 기획·설계 단계(1일차)에서 가장 먼저 해야 할 일로 알맞은 것은?",choices:["서버를 여러 대 사서 배포 환경부터 구축한다","주제를 정하고 사용자 시나리오(누가, 언제, 왜 쓰는지)를 정의한다","코드를 최대한 많이 작성해 둔다","발표 자료(PPT)부터 완성한다"],answer:1,explain:'정답은 2번입니다. 개발은 "무엇을 왜 만드는가"에서 출발합니다. 주제를 정하고 "직장인 A씨가 점심 메뉴를 못 골라서 이 앱을 켠다"처럼 구체적인 사용자 시나리오를 쓰면, 정말 필요한 기능과 화면이 자연스럽게 드러납니다. 그다음에 와이어프레임·기능 명세·데이터 모델·기술 스택·작업 분담 순서로 설계를 구체화합니다. 기획 없이 코드부터 짜면 방향이 계속 바뀌어 오히려 시간을 크게 잃게 됩니다.'},{type:"choice",q:"AI API를 연동할 때 발급받은 API 키를 다루는 방법으로 가장 알맞은 것은?",choices:["소스코드에 직접 적어 두고 GitHub에 그대로 올린다","팀 단체 채팅방 공지에 올려 모두가 복사해 쓰게 한다","환경변수(.env 파일)로 분리하고, 그 파일은 .gitignore에 넣어 저장소에 올라가지 않게 한다","기억하기 쉽게 1234 같은 값으로 바꾼다"],answer:2,explain:"정답은 3번입니다. API 키는 내 계정으로 유료 API를 쓸 수 있는 비밀번호와 같아서, 코드에 하드코딩해 GitHub에 올리면 누구나 훔쳐 쓸 수 있고 실제 과금 사고로 이어집니다. 그래서 키는 .env 같은 환경변수 파일에 따로 두고 코드에서는 변수로 읽어 오며, .env 파일 자체는 .gitignore에 등록해 저장소에 절대 올라가지 않게 합니다. 이렇게 하면 개발·배포 환경마다 다른 키를 쓰기도 쉬워집니다."},{type:"choice",q:"정적 배포(static deploy)에 대한 설명으로 가장 알맞은 것은?",choices:["빌드로 만든 HTML·CSS·JS 파일을 GitHub Pages 같은 호스팅에 올려 누구나 접속하게 하는 것","데이터베이스 서버를 내 컴퓨터에 설치하는 것","코드를 출력해서 종이로 제출하는 것","프로그램을 USB에 담아 전달하는 것"],answer:0,explain:"정답은 1번입니다. Vite 같은 도구로 빌드하면 프로젝트가 브라우저가 바로 읽을 수 있는 정적 파일 묶음(HTML·CSS·JS)으로 변환되는데, 이 결과물을 GitHub Pages 같은 정적 호스팅 서비스에 올리면 별도 서버 운영 없이도 전 세계 누구나 주소로 접속할 수 있습니다. 서버 프로그램을 계속 돌릴 필요가 없어 무료이거나 매우 저렴하고 관리도 간단해서, SPA로 만든 미니 프로젝트를 공개하는 가장 손쉬운 방법입니다."},{type:"short",q:"프로젝트를 마친 뒤 팀이 모여 잘된 점과 아쉬운 점을 돌아보고 다음에 개선할 점을 정리하는 활동을 무엇이라 하는가?",answer:"회고",explain:'정답은 회고(retrospective)입니다. 회고는 단순한 반성회가 아니라 "무엇이 좋았고(Keep), 무엇이 문제였고(Problem), 다음엔 무엇을 시도할지(Try)"를 팀이 함께 정리하는 성장 도구입니다. 결과물 자체만큼이나 "이번에 배운 것"이 다음 프로젝트의 실력이 되기 때문에, 실무 개발팀들도 스프린트가 끝날 때마다 회고를 정기적으로 진행합니다. 미니 프로젝트 마지막 날 발표 후 회고와 개선점 정리로 마무리하는 이유입니다.'},{type:"short",q:"작성한 소스코드를 브라우저가 실행할 수 있는 최적화된 배포용 파일들로 변환하는 과정을 무엇이라 하는가? (예: npm run ○○)",answer:"빌드",explain:'정답은 빌드(build)입니다. 개발할 때 쓰는 .vue 파일이나 최신 문법의 자바스크립트는 브라우저가 그대로 이해하지 못하거나 비효율적일 수 있습니다. npm run build를 실행하면 Vite가 코드를 변환·압축·최적화해서 dist 폴더에 배포용 정적 파일 묶음을 만들어 줍니다. 이 dist 폴더의 내용물을 GitHub Pages 같은 곳에 올리는 것이 배포입니다. 즉 "빌드 = 배포용 완성품 포장, 배포 = 포장된 상품 진열"이라고 이해하면 쉽습니다.'}],prompt:[{type:"ox",q:"토큰(token)은 LLM이 글을 잘게 쪼개어 세는 단위로, 보통 단어 하나 또는 단어 조각 하나가 토큰 한 개에 가깝다.",answer:"O",explain:"맞습니다. LLM은 글을 사람처럼 문장 단위로 읽는 게 아니라 토큰이라는 작은 조각 단위로 쪼개서 처리합니다. 영어는 대략 단어 하나가 토큰 1~2개, 한국어는 글자 수 대비 토큰이 더 많이 나오는 편입니다. 토큰이 중요한 이유는 두 가지입니다. 첫째, API 요금이 입력·출력 토큰 수에 비례해 부과되고, 둘째, 모델이 한 번에 다룰 수 있는 분량(컨텍스트 윈도우)도 토큰 수로 제한되기 때문입니다."},{type:"ox",q:"LLM의 컨텍스트 윈도우는 무제한이므로, 아무리 긴 문서라도 통째로 넣기만 하면 항상 문제없이 처리된다.",answer:"X",explain:"틀렸습니다. 컨텍스트 윈도우는 모델이 한 번에 기억할 수 있는 글의 최대 분량으로, 책상 위에 한꺼번에 펼칠 수 있는 종이 장수처럼 분명한 한계가 있습니다. 한도를 넘으면 입력이 잘리거나 오류가 나고, 한도 안이라도 너무 길면 비용이 늘고 모델이 중간 내용을 놓치는 품질 저하가 생길 수 있습니다. 그래서 긴 문서는 요약·압축하거나 필요한 부분만 골라 넣는 컨텍스트 최적화 전략이 필요합니다."},{type:"ox",q:"환각(hallucination)이란 LLM이 사실이 아닌 내용을 마치 사실인 것처럼 자신 있게 지어내는 현상을 말한다.",answer:"O",explain:'맞습니다. LLM은 "다음에 올 가장 그럴듯한 말"을 확률적으로 이어 쓰는 방식이라, 모르는 것도 그럴듯한 문장으로 만들어 내곤 합니다. 존재하지 않는 논문이나 틀린 날짜를 확신에 찬 말투로 답하는 것이 대표적인 환각입니다. 이를 줄이려면 프롬프트에 근거 자료를 함께 제공하고, "모르면 모른다고 답해"라는 제약을 걸고, 중요한 답은 반드시 사람이 사실 확인을 하는 습관이 필요합니다.'},{type:"choice",q:"Few-shot 프롬프팅에 대한 설명으로 가장 알맞은 것은?",choices:["아무 예시 없이 지시만 한 문장으로 던지는 기법","정답 예시를 몇 개 보여 준 뒤 같은 방식으로 답하게 하는 기법","모델을 처음부터 다시 학습시키는 기법","질문을 여러 번 반복해서 보내는 기법"],answer:1,explain:'정답은 2번입니다. Few-shot은 "이렇게 푸는 거야"라고 시범을 몇 개 보여 주고 같은 패턴으로 답하게 하는 기법입니다. 예를 들어 "리뷰 → 긍정/부정" 분류 예시를 두세 개 먼저 보여 주면, 모델이 원하는 출력 형식과 판단 기준을 훨씬 정확히 따라합니다. 예시 없이 지시만 주는 것은 Zero-shot이라고 하며(1번 설명), 형식이 중요하거나 애매한 작업일수록 Few-shot 예시를 넣는 것이 효과적입니다.'},{type:"choice",q:"강의에서 배운 Prompt Design Framework의 4가지 구성요소로 올바른 것은?",choices:["속도 · 용량 · 가격 · 디자인","역할(role) · 지시(instruction) · 예시(example) · 제약(constraint)","변수 · 함수 · 클래스 · 모듈","서버 · 클라이언트 · 데이터베이스 · 네트워크"],answer:1,explain:'정답은 2번입니다. 좋은 프롬프트는 네 가지를 챙기면 품질이 크게 올라갑니다. 역할(role)은 "너는 마케팅 전문가야"처럼 모델의 입장을 정해 주고, 지시(instruction)는 해야 할 일을 구체적으로 명시하며, 예시(example)는 원하는 출력의 시범을 보여 주고, 제약(constraint)은 "300자 이내, 존댓말로"처럼 지켜야 할 조건을 겁니다. 막연히 한 줄로 묻는 대신 이 틀에 맞춰 작성하면 원하는 답을 훨씬 안정적으로 얻을 수 있습니다.'},{type:"choice",q:"System · User · Assistant 메시지 중 System 메시지의 역할로 가장 알맞은 것은?",choices:["사용자가 방금 입력한 질문 그 자체다","모델이 생성한 답변이 담기는 자리다","대화 전체에 적용되는 기본 규칙과 역할을 미리 정해 두는 메시지다","컴퓨터 운영체제(OS)를 업데이트하는 명령이다"],answer:2,explain:'정답은 3번입니다. System 메시지는 연극이 시작되기 전에 배역과 무대 설정을 정해 두는 것처럼, 대화 전체에 깔리는 기본 규칙·역할·말투를 지정합니다. 예를 들어 "너는 친절한 고객센터 상담원이고, 항상 존댓말로 답하며, 환불 규정 외의 약속은 하지 마"라고 깔아 두면 이후 모든 답변이 이 틀 안에서 나옵니다. User 메시지는 사용자의 입력(1번), Assistant 메시지는 모델의 답변(2번)이 쌓이는 자리입니다.'},{type:"short",q:'복잡한 문제를 풀 때 "차근차근 단계별로 생각해 봐"처럼 풀이 과정을 적도록 유도해 정답률을 높이는 프롬프트 기법의 이름은? (영문 또는 한글)',answer:"Chain-of-Thought",explain:`정답은 Chain-of-Thought(CoT, 생각의 사슬)입니다. 사람도 어려운 수학 문제를 암산으로 풀면 틀리기 쉽지만 풀이 과정을 적으면 정확해지듯이, LLM도 중간 추론 과정을 글로 쓰게 하면 복잡한 계산·논리 문제의 정답률이 크게 올라갑니다. 가장 간단한 사용법은 프롬프트 끝에 "단계별로 차근차근 생각해 보자(Let's think step by step)"를 붙이는 것입니다. 다만 출력 토큰이 늘어나 비용·시간이 증가하므로 단순한 질문에는 굳이 쓸 필요가 없습니다.`},{type:"short",q:"프롬프트 문장만 다듬는 것을 넘어, 모델에게 어떤 문서·대화·도구 결과를 얼마나, 어떤 순서로 맥락에 넣을지 설계하는 것을 무엇이라 하는가? (영문)",answer:"Context Engineering",explain:'정답은 Context Engineering(컨텍스트 엔지니어링)입니다. 프롬프트 엔지니어링이 "질문 문장을 잘 쓰는 기술"이라면, 컨텍스트 엔지니어링은 모델의 한정된 컨텍스트 윈도우 안에 무엇을(관련 문서, 이전 대화, 도구 실행 결과) 얼마나, 어떤 순서로 담을지를 설계하는 더 넓은 개념입니다. 프롬프트 한 문장의 표현보다 "어떤 재료를 상에 올리느냐"가 답변 품질을 좌우하는 경우가 많기 때문입니다. 강의에서는 여기서 더 나아가 도구 연결·오류 처리까지 설계하는 Harness Engineering, 즉 AI Agent로의 확장까지 소개합니다.'}],transformer:[{type:"ox",q:'GPT 같은 LLM은 기본적으로 "지금까지의 문맥을 보고 다음 토큰(단어 조각)이 무엇일지 확률로 맞히는 것"을 반복하며 문장을 생성한다.',answer:"O",explain:'맞습니다. LLM의 본질은 "다음 단어 맞히기 게임"입니다. 모델은 입력된 문맥을 보고 어휘 전체에 대해 "다음에 올 토큰"의 확률 분포(logits를 softmax로 확률화한 것)를 계산하고, 그중 하나를 골라 붙인 뒤 다시 같은 과정을 반복합니다. 강의 실습에서 GPT-2로 문장을 생성하며 다음 토큰 확률을 직접 들여다본 것이 바로 이 원리를 확인하는 과정이었습니다. 이 단순한 반복이 쌓여서 번역·요약·코딩 같은 복잡한 능력이 나타난다는 점이 LLM의 놀라운 부분입니다.'},{type:"ox",q:"RNN은 문장의 모든 토큰을 한꺼번에 병렬로 처리하기 때문에, 토큰을 하나씩 차례로 읽는 Transformer보다 긴 문장 처리가 빠르다.",answer:"X",explain:'설명이 정반대로 뒤집혀 있습니다. 순차 처리를 하는 쪽은 RNN(LSTM)입니다. RNN은 "한 줄로 서서 차례차례 읽기"처럼 앞 토큰의 계산이 끝나야 다음 토큰을 처리할 수 있어서 느리고, 문장이 길어지면 앞부분 정보가 희미해지는(장기 의존성) 문제도 있습니다. 반면 Transformer는 Self-Attention 덕분에 모든 토큰을 동시에(병렬로) 처리할 수 있어 GPU에서 학습 속도가 훨씬 빠르고, 멀리 떨어진 단어끼리도 한 번에 연결됩니다. 이 병렬성이 Transformer가 RNN을 대체한 핵심 이유입니다.'},{type:"ox",q:"Self-Attention에서 쓰이는 Query, Key, Value 세 가지는 서로 다른 문장에서 따로 가져오는 것이 아니라, 같은 입력 문장(토큰들)에 각각 다른 가중치 행렬(Wq, Wk, Wv)을 곱해 만들어 낸다.",answer:"O",explain:'맞습니다. "Self(자기 자신)" Attention이라는 이름 그대로, 한 문장 안의 토큰들이 서로를 바라보는 구조입니다. 실습에서 했던 것처럼 입력 임베딩 행렬 X 하나에 Wq, Wk, Wv 세 가중치 행렬을 각각 곱해 Q=X@Wq, K=X@Wk, V=X@Wv 를 만듭니다. Query는 "내가 찾고 싶은 것", Key는 "각 토큰이 가진 이름표", Value는 "실제로 가져올 내용"에 비유할 수 있습니다. Q와 K의 내적으로 관련도 점수를 구하고, 그 비율(softmax)만큼 V를 섞어 새로운 표현을 만드는 것이 Self-Attention의 전부입니다.'},{type:"choice",q:"Scaled Dot-Product Attention에서 Q와 K의 내적 점수를 차원 수의 제곱근(sqrt(d_k))으로 나누어 주는 가장 중요한 이유는?",choices:["계산 결과를 무조건 0으로 만들기 위해","점수가 너무 커져 softmax가 한쪽으로 쏠리는(기울기가 사라지는) 것을 막고 학습을 안정시키기 위해","토큰 개수를 줄여 메모리를 아끼기 위해","결과를 정수로 만들기 위해"],answer:1,explain:'정답은 2번입니다. 벡터 차원(d_k)이 커질수록 내적 값의 크기도 함께 커지는 경향이 있는데, 큰 값들이 softmax에 들어가면 확률이 거의 1과 0으로 극단적으로 쏠립니다. 이렇게 되면 기울기(gradient)가 매우 작아져 학습이 잘 진행되지 않습니다. 그래서 점수를 sqrt(d_k)로 나눠 적당한 크기로 "스케일링"해 주는 것이고, 이것이 이름에 붙은 Scaled의 의미입니다. NumPy 실습에서 scores를 sqrt(4)=2로 나눴던 바로 그 단계입니다.'},{type:"choice",q:"문장 중간에 빈칸([MASK])을 뚫어 놓고 그 자리에 들어갈 단어를 맞히는 방식(MLM)으로 사전학습하는, 인코더(Encoder) 기반의 대표 모델은?",choices:["GPT-2","LSTM","BERT","RNN"],answer:2,explain:'정답은 3번 BERT입니다. BERT는 Transformer의 인코더만 쌓아 만든 모델로, 문장 양쪽(앞뒤) 문맥을 모두 보고 [MASK] 자리를 채우는 "빈칸 채우기" 방식으로 학습합니다. 그래서 문장의 의미를 이해하고 임베딩을 뽑는 데(분류, 유사도 비교 등) 강합니다. 반대로 GPT는 디코더 기반으로 앞 문맥만 보고 다음 토큰을 예측하는 "이어 쓰기" 방식(CLM)이라 문장 생성에 강합니다. 실습에서 BERT로는 [MASK] 예측을, GPT-2로는 문장 생성을 해 본 것이 이 차이를 체험한 것입니다.'},{type:"choice",q:"텍스트 생성 시 디코딩 파라미터인 temperature를 아주 낮게(예: 0에 가깝게) 설정하면 어떤 일이 일어나는가?",choices:["확률이 가장 높은 토큰 위주로 골라, 매번 거의 같은 안정적이고 보수적인 답이 나온다","모델이 완전히 무작위 단어만 출력한다","모델의 파라미터 수가 줄어든다","학습 데이터가 삭제된다"],answer:0,explain:'정답은 1번입니다. temperature는 다음 토큰 확률 분포를 얼마나 "평평하게" 만들지 조절하는 값입니다. 낮추면 확률이 높은 토큰이 더욱 압도적으로 뽑혀 출력이 결정적(deterministic)이고 일관돼지며, 높이면 확률이 낮던 토큰들도 뽑힐 기회가 생겨 다양하고 창의적인(대신 산만해질 수 있는) 문장이 나옵니다. 사실 요약이나 코드 생성처럼 정확성이 중요하면 낮게, 아이디어 브레인스토밍처럼 다양성이 중요하면 높게 쓰는 것이 일반적입니다. top-p와 함께 생성 품질을 조절하는 대표 파라미터입니다.'},{type:"short",q:'Transformer는 토큰을 병렬로 처리해서 순서 정보를 스스로 알 수 없다. 그래서 각 토큰의 임베딩에 "몇 번째 위치인지"를 나타내는 값을 더해 순서 정보를 주입하는데, 이 기법의 이름은?',answer:"포지셔널 인코딩(Positional Encoding)",explain:'정답은 포지셔널 인코딩(Positional Encoding)입니다. RNN은 토큰을 순서대로 읽으니 자연히 순서를 알지만, Transformer는 모든 토큰을 한꺼번에 보기 때문에 "나는 학교에 갔다"와 "갔다 학교에 나는"을 구별할 방법이 없습니다. 그래서 각 위치마다 고유한 패턴의 벡터(사인·코사인 함수 기반 등)를 만들어 토큰 임베딩에 더해 줍니다. 이렇게 하면 모델이 단어의 의미와 위치를 동시에 반영해 문장을 이해할 수 있게 됩니다.'},{type:"short",q:"토큰화 이후 각 토큰을 의미를 담은 숫자 벡터로 바꾼 것을 임베딩이라 한다. 이 임베딩 공간에서 두 단어의 의미가 얼마나 비슷한지 벡터 사이 각도로 재는 대표적인 지표는? (실습에서 '강아지'와 '개'의 가까움을 확인할 때 사용)",answer:"코사인 유사도(cosine similarity)",explain:'정답은 코사인 유사도입니다. 임베딩 공간의 핵심 성질은 "의미가 비슷한 단어일수록 벡터가 서로 가까이 있다"는 것인데, 이 가까움을 두 벡터가 이루는 각도로 측정하는 것이 코사인 유사도입니다. 값은 -1에서 1 사이이며 1에 가까울수록 의미가 비슷합니다. 벡터의 길이가 아닌 방향만 보기 때문에 문장 길이 등의 영향에 덜 흔들립니다. 실습에서 BERT로 문장 임베딩을 뽑아 유사도를 비교한 것도 바로 이 지표를 쓴 것입니다.'}],sllm:[{type:"ox",q:"sLLM(작은 LLM)은 파라미터 수가 적어 개인 PC나 사내 서버 같은 로컬 환경에서도 돌릴 수 있어, DBMS·영업비밀·개인정보처럼 외부로 내보내기 어려운 보안 민감 데이터를 다루는 데 유리하다.",answer:"O",explain:"맞습니다. GPT-4 같은 초대형 LLM은 외부 클라우드 API로 호출해야 해서, 회사 기밀이나 개인정보를 프롬프트에 담아 보내는 것 자체가 보안 리스크가 됩니다. 반면 sLLM은 크기가 작아 사내 서버나 내 컴퓨터(온디바이스)에 직접 올려 돌릴 수 있으므로 데이터가 회사 밖으로 나가지 않습니다. 강의에서 다룬 DBMS 질의, 영업비밀 문서, 개인정보 처리 같은 Use Case가 바로 이런 이유로 sLLM이 선택되는 대표 시나리오입니다. 성능은 초대형 모델보다 낮지만, 특정 업무에 파인튜닝하면 충분히 실용적입니다."},{type:"ox",q:"양자화(quantization)는 모델의 파라미터 개수를 늘려서 정확도를 높이는 기법이다.",answer:"X",explain:'틀렸습니다. 양자화는 파라미터의 "개수"가 아니라 각 숫자를 저장하는 "정밀도(비트 수)"를 낮추는 기법입니다. 예를 들어 원래 16비트(FP16)로 저장하던 가중치를 4비트로 줄이면 메모리 사용량이 약 4분의 1로 줄어들어, 큰 모델도 일반 GPU나 노트북에서 돌릴 수 있게 됩니다. 정확도는 오히려 약간 손해를 볼 수 있지만, 잘 설계된 4bit 양자화(bitsandbytes 실습에서 해 본 것)는 품질 저하가 놀랄 만큼 작습니다. 즉 양자화의 목적은 정확도 향상이 아니라 "메모리 절약과 온디바이스 추론"입니다.'},{type:"ox",q:"LoRA는 사전학습된 모델의 원본 가중치 전체를 다시 학습하는 대신, 작은 저랭크(low-rank) 어댑터 행렬만 추가로 학습하는 파인튜닝 기법이다.",answer:"O",explain:'맞습니다. LoRA(Low-Rank Adaptation)의 핵심 아이디어는 "통째로 안 바꾸고 살짝만 고치기"입니다. 수십억 개의 원본 가중치는 얼려 두고(frozen), 그 옆에 아주 작은 행렬 두 개(저랭크 분해)를 붙여 그 부분만 학습합니다. 학습해야 할 파라미터가 전체의 1퍼센트 미만으로 줄어들어 GPU 메모리와 비용이 크게 절감되고, 학습 결과물(어댑터)도 수십 MB 수준으로 작아 관리가 쉽습니다. 실습에서 학습한 LoRA 어댑터를 나중에 본체에 합쳐(merge) 배포용 모델을 만든 것도 이 구조 덕분입니다.'},{type:"choice",q:"강의 실습에서 사용한 Ollama에 대한 설명으로 가장 올바른 것은?",choices:["오픈 모델을 내 컴퓨터에 쉽게 내려받아 로컬에서 채팅·API 서빙까지 해 주는 도구다","GPT-4를 무료로 쓰게 해 주는 클라우드 서비스다","이미지 편집 전용 프로그램이다","데이터베이스 백업 도구다"],answer:0,explain:'정답은 1번입니다. Ollama는 명령어 한 줄(예: ollama run llama3)로 오픈 가중치 모델을 자동으로 내려받아 로컬에서 실행해 주는 도구입니다. 복잡한 환경 설정 없이 바로 채팅을 시작할 수 있고, 동시에 로컬 API 서버(기본 11434 포트)도 열어 주기 때문에 Python 코드에서 HTTP로 호출할 수 있습니다. 실습에서 "로컬 모델을 API로 호출하는 미니 챗봇"을 만들 때 바로 이 API를 사용했습니다. 데이터가 내 컴퓨터를 벗어나지 않으므로 보안 민감 시나리오와도 잘 맞습니다.'},{type:"choice",q:"QLoRA에 대한 설명으로 가장 올바른 것은?",choices:["모델을 처음부터(from scratch) 전부 새로 학습하는 방법이다","4bit로 양자화해 올린 모델 위에 LoRA 어댑터를 붙여, 적은 GPU 메모리로 파인튜닝하는 기법이다","모델의 답변 속도를 측정하는 벤치마크 이름이다","데이터를 암호화해 저장하는 보안 기술이다"],answer:1,explain:'정답은 2번입니다. QLoRA는 이름 그대로 Quantization(양자화) + LoRA의 조합입니다. 본체 모델을 4bit로 양자화해 GPU 메모리에 올려 놓고(메모리 대폭 절약), 그 위에 LoRA 어댑터만 학습합니다. 두 기법의 장점이 합쳐져 무료 Colab의 GPU 한 장으로도 수십억 파라미터 모델을 파인튜닝할 수 있게 됩니다. 실습에서 "내 말투·도메인으로 sLLM 파인튜닝하기"를 Colab에서 할 수 있었던 것이 바로 QLoRA 덕분입니다.'},{type:"choice",q:"70억(7B) 파라미터 모델을 FP16(파라미터당 2바이트) 정밀도로 GPU에 올릴 때 필요한 메모리를 대략 계산하면?",choices:["약 1.4GB","약 7MB","약 14GB","약 700GB"],answer:2,explain:'정답은 3번, 약 14GB입니다. 계산은 아주 단순합니다. 파라미터 1개가 FP16으로 2바이트를 차지하므로 70억 개 곱하기 2바이트 = 약 140억 바이트 = 약 14GB입니다. 실습 예제 "모델 용량 가늠하기"에서 다룬 그대로입니다. 이 어림 계산을 알면 "이 모델이 내 GPU에 올라갈까?"를 바로 판단할 수 있고, 4bit 양자화를 쓰면 약 4분의 1인 3.5GB 수준으로 줄어 일반 노트북에서도 돌릴 수 있다는 것까지 연결해 이해할 수 있습니다.'},{type:"short",q:"LoRA, Adapter, Prefix/Prompt tuning처럼 모델 전체가 아니라 일부 파라미터만 효율적으로 학습하는 파인튜닝 기법들을 통틀어 부르는 약자는? (영문 4글자)",answer:"PEFT",explain:'정답은 PEFT(Parameter-Efficient Fine-Tuning), 즉 파라미터 효율적 파인튜닝입니다. 전체 파라미터를 모두 업데이트하는 풀 파인튜닝은 비용과 GPU 메모리가 엄청나게 들기 때문에, 일부만 학습해도 비슷한 효과를 내는 방법들이 개발되었습니다. 대표 주자가 LoRA와 QLoRA이고, 그 밖에 어댑터 층을 끼워 넣는 Adapter, 입력 앞에 학습 가능한 가상 토큰만 붙이는 Prefix/Prompt tuning이 있습니다. 강의에서 "목적별 PEFT 선택 가이드"로 도메인 지식 주입, 극소 파라미터, 추론 강화 등 상황별 선택 기준을 배운 것이 이 범주입니다.'},{type:"short",q:"instruction 파인튜닝용 학습 데이터를 만들 때 흔히 쓰는, 한 줄에 JSON 객체를 하나씩 적어 나가는 파일 형식의 이름은?",answer:"JSONL",explain:'정답은 JSONL(JSON Lines)입니다. 일반 JSON이 파일 전체를 하나의 큰 덩어리로 감싸는 것과 달리, JSONL은 한 줄이 곧 하나의 독립된 JSON 객체(예: {"instruction": "...", "output": "..."})입니다. 줄 단위로 읽고 쓸 수 있어 대용량 학습 데이터를 스트리밍으로 처리하기 좋고, 한 줄이 깨져도 나머지는 살릴 수 있어 파인튜닝 데이터 표준 포맷으로 자리잡았습니다. 실습에서 "instruction 데이터 JSONL 가공" 후 LoRA 학습에 넣었던 바로 그 형식입니다.'}],langchain:[{type:"ox",q:"LCEL(LangChain Expression Language)에서는 파이프 연산자(|)를 사용해 프롬프트, 모델, 출력 파서 같은 부품을 조립해 하나의 체인으로 만들 수 있다.",answer:"O",explain:'맞습니다. LCEL의 상징이 바로 파이프(|) 문법입니다. chain = prompt | model | parser 처럼 쓰면 "프롬프트에 값을 채운 결과를 모델에 넣고, 모델의 답을 파서로 다듬는다"는 데이터 흐름이 한 줄로 표현됩니다. 부엌의 조리 라인처럼 각 부품(Runnable)이 앞 부품의 출력을 받아 처리하는 구조라, 부품을 갈아 끼우거나 늘리기가 쉽습니다. 첫 실습에서 만든 번역기·요약기 체인이 모두 이 방식이었습니다.'},{type:"ox",q:"LCEL로 만든 체인은 invoke(1건 실행)만 지원하며, 여러 입력을 한 번에 처리하는 batch나 글자가 흐르듯 출력되는 stream은 별도의 프레임워크를 설치해야 쓸 수 있다.",answer:"X",explain:'틀렸습니다. LCEL 체인은 Runnable 인터페이스를 따르기 때문에 별도 설치 없이 invoke, stream, batch를 모두 기본으로 지원합니다. invoke는 입력 1건을 넣어 결과 1건을 받는 기본형, batch는 입력 리스트를 한 번에 병렬 처리하는 형태, stream은 답변이 생성되는 대로 토큰 단위로 흘려 받는 형태입니다. 여기에 비동기 버전인 ainvoke까지 있어 여러 호출을 동시에 띄워 시간을 아낄 수도 있습니다. "인터페이스가 통일되어 있다"는 것이 LCEL의 큰 장점입니다.'},{type:"ox",q:"문서 QA(RAG) 체인에서 리트리버(retriever)는 사용자의 질문과 의미가 가까운 문서 조각들을 벡터스토어에서 찾아오는 역할을 한다.",answer:"O",explain:'맞습니다. 문서 QA의 큰 그림은 "적재 → 검색 → 답변" 3단계입니다. 먼저 PDF나 텍스트를 잘게 쪼개 임베딩(숫자 벡터)으로 바꿔 벡터스토어에 저장해 두고(적재), 질문이 들어오면 리트리버가 질문 임베딩과 가까운 문서 조각들을 찾아옵니다(검색). 마지막으로 찾아온 조각들을 프롬프트에 근거 자료로 붙여 LLM이 답하게 합니다(답변). 리트리버는 이 중 두 번째 단계를 담당하는 "사서" 같은 부품으로, 실습에서 벡터스토어의 as_retriever()로 만들어 체인에 결합했습니다.'},{type:"choice",q:"LangChain으로 가장 기본적인 체인을 만들 때 부품을 연결하는 올바른 순서는?",choices:["출력 파서 | 모델 | 프롬프트","모델 | 프롬프트 | 출력 파서","프롬프트 | 모델 | 출력 파서","프롬프트 | 출력 파서 | 모델"],answer:2,explain:'정답은 3번, 프롬프트 | 모델 | 출력 파서 순서입니다. 데이터가 흐르는 순서를 생각하면 자연스럽습니다. 먼저 프롬프트 템플릿이 사용자 입력을 받아 빈칸을 채운 완성된 지시문을 만들고, 그 지시문이 모델(LLM)에 들어가 답변이 생성되며, 마지막으로 출력 파서가 모델의 답변을 문자열이나 JSON 같은 쓰기 좋은 형태로 다듬습니다. 첫날 실습 과제였던 "한 줄 요약 봇"이 정확히 이 3부품 구성이었습니다.'},{type:"choice",q:"체인이 내부에서 어떤 프롬프트로 모델을 호출했고 각 단계가 얼마나 걸렸는지 추적·디버깅할 수 있게 해 주는, 강의에서 실습한 관측성(observability) 도구는?",choices:["LangSmith","Photoshop","MySQL","Docker"],answer:0,explain:'정답은 1번 LangSmith입니다. 체인이 복잡해지면 "왜 이런 답이 나왔지?"를 코드만 봐서는 알기 어려운데, LangSmith는 체인 실행의 매 단계(어떤 프롬프트가 실제로 들어갔는지, 모델이 뭐라고 답했는지, 시간과 토큰이 얼마나 들었는지)를 웹 대시보드에서 시각적으로 보여 줍니다. 환경변수 몇 개만 설정하면 코드 수정 거의 없이 추적이 시작되어 도입이 쉽습니다. 실서비스 운영 시 비용 관리와 품질 디버깅에 필수적인 도구입니다.'},{type:"choice",q:"LLM 챗봇에 대화 메모리(memory)가 필요한 근본적인 이유는?",choices:["LLM 호출은 기본적으로 이전 대화를 기억하지 못하므로, 앞선 대화 내용을 매번 함께 전달해야 문맥이 이어지기 때문","메모리가 없으면 모델이 아예 실행되지 않기 때문","메모리를 쓰면 API 요금이 무조건 무료가 되기 때문","GPU 온도를 낮추기 위해"],answer:0,explain:'정답은 1번입니다. LLM API 호출은 기본적으로 상태가 없어서(stateless), 매 호출이 서로 독립적입니다. 즉 "제 이름은 김철수예요"라고 말한 다음 "제 이름이 뭐죠?"라고 물으면, 이전 대화를 함께 보내지 않는 한 모델은 알 수 없습니다. 그래서 대화 기록을 저장해 두었다가 다음 호출의 프롬프트에 함께 넣어 주는 장치가 메모리입니다. 실습에서 "앞말을 기억하는 챗봇"을 만들며 대화 기록을 세션별로 관리한 것이 바로 이 문제를 해결한 것입니다.'},{type:"short",q:"LLM이 계산기나 검색처럼 스스로 할 수 없는 일을 외부 함수에 맡길 수 있도록, 함수를 모델에 연결해 주는 LangChain의 부품을 무엇이라 하는가? (bind_tools로 모델에 연결하는 그것)",answer:"도구(Tool)",explain:'정답은 도구(Tool)입니다. LLM은 말은 잘하지만 정확한 계산, 실시간 검색, DB 조회 같은 일은 스스로 할 수 없습니다. 그래서 파이썬 함수에 @tool 데코레이터를 붙여 "이런 기능이 있다"고 설명과 함께 모델에 알려 주면(bind_tools), 모델이 필요할 때 "이 도구를 이 인자로 호출해 달라"고 요청하고 그 결과를 받아 최종 답을 만듭니다. 이 구조가 발전하면 모델이 스스로 도구를 골라 연쇄 호출하는 ReAct 에이전트가 됩니다. 실습에서 나만의 도구를 만들어 모델에 연결해 본 것이 그 첫걸음이었습니다.'},{type:"short",q:"단순한 일직선 체인을 넘어, State(상태)·Node(노드)·Edge(엣지)로 이루어진 그래프 구조로 조건 분기·반복 같은 복잡한 흐름을 만들 수 있는 LangChain의 자매 프레임워크는?",answer:"LangGraph",explain:'정답은 LangGraph입니다. LCEL 체인은 물이 위에서 아래로 흐르듯 한 방향 파이프라인에 적합하지만, "답이 부족하면 다시 검색하기" 같은 반복이나 "질문 유형에 따라 다른 처리로 보내기" 같은 분기는 표현하기 어렵습니다. LangGraph는 공유되는 상태(State)를 두고, 작업 단위인 노드(Node)들을 엣지(Edge)로 연결해 순환·조건 분기가 있는 그래프를 만들 수 있습니다. 강의에서 조건 분기(Conditional Edge)로 질문 유형별 노드 라우팅을 실습했고, "단순하면 체인, 복잡한 제어 흐름이면 LangGraph"가 선택 기준입니다.'}],"spring-ai":[{type:"ox",q:"Spring AI의 ChatClient/ChatModel 추상화를 사용하면, 자바 코드를 거의 바꾸지 않고 application.yml 설정 변경만으로 OpenAI에서 Anthropic 등 다른 모델 프로바이더로 교체할 수 있다.",answer:"O",explain:'맞습니다. Spring AI의 가장 큰 매력이 바로 이 "추상화"입니다. 코드에서는 특정 회사의 SDK가 아니라 공통 인터페이스인 ChatClient/ChatModel만 사용하고, 어떤 회사의 어떤 모델을 쓸지는 application.yml의 설정(API 키, 모델명 등)으로 정합니다. 그래서 OpenAI에서 Anthropic 클로드로 갈아탈 때 비즈니스 로직 코드는 그대로 두고 의존성과 설정만 바꾸면 됩니다. JDBC가 DB 종류를 추상화해 준 것과 같은 원리를 LLM에 적용한 것이라고 이해하면 쉽습니다.'},{type:"ox",q:"Spring AI에서 RAG(문서 기반 답변)를 구현하려면 반드시 파이썬 서버를 따로 만들어 연동해야 하며, 자바만으로는 임베딩·벡터 검색·답변 구성이 불가능하다.",answer:"X",explain:'틀렸습니다. Spring AI는 RAG에 필요한 부품을 자바 생태계 안에 모두 갖추고 있습니다. 문서를 읽는 DocumentReader, 잘게 쪼개는 TextSplitter, 임베딩을 만들어 저장·검색하는 VectorStore(pgvector 연동), 그리고 검색 결과를 프롬프트에 자동으로 결합해 주는 QuestionAnswerAdvisor까지 전부 자바 API로 제공됩니다. 실습에서 만든 "사내 문서 QA API"도 순수 스프링 부트 프로젝트 하나로 완성했습니다. 기존 자바 백엔드 팀이 파이썬 스택을 새로 배우지 않고도 AI 기능을 붙일 수 있다는 것이 Spring AI의 존재 이유입니다.'},{type:"ox",q:"MCP(Model Context Protocol)는 AI 모델이 외부 도구나 데이터 소스에 표준화된 방식으로 접근할 수 있게 해 주는 프로토콜로, 통신 방식으로는 STDIO와 Streamable HTTP 등이 있다.",answer:"O",explain:'맞습니다. MCP는 "AI계의 USB 규격"에 비유할 수 있습니다. 예전에는 모델·프레임워크마다 도구 연결 방식이 제각각이라 도구를 만들 때마다 다시 붙여야 했지만, MCP라는 표준 프로토콜을 따르면 한 번 만든 MCP 서버(도구 묶음)를 다양한 AI 클라이언트에서 재사용할 수 있습니다. 통신 방식은 같은 컴퓨터 안에서 표준 입출력으로 대화하는 STDIO 방식과, 네트워크 너머로 연결하는 Streamable HTTP 방식이 있습니다. 실습에서 자바로 MCP Server를 만들고 Client로 연결해 도구 호출을 확인한 것이 이 구조의 체험이었습니다.'},{type:"choice",q:"프롬프트를 구성할 때 System 메시지의 역할로 가장 올바른 것은?",choices:["사용자가 입력한 질문 그 자체를 담는다","AI의 역할·말투·지켜야 할 규칙 같은 전체 지침을 지정한다","모델의 응답 속도를 물리적으로 빠르게 만든다","데이터베이스 테이블을 자동 생성한다"],answer:1,explain:'정답은 2번입니다. LLM에 보내는 메시지는 역할(role)에 따라 나뉘는데, System 메시지는 대화 전체의 "무대 설정"입니다. 예를 들어 "너는 친절한 사내 IT 헬프데스크 상담원이다. 반드시 한국어 존댓말로 답하라" 같은 역할·톤·규칙을 지정합니다. 반면 User 메시지는 사용자의 실제 질문을 담습니다. 실습에서 System 메시지로 역할을 지정하고 PromptTemplate으로 User 메시지의 빈칸을 채워 보낸 것처럼, 이 둘을 분리해 설계하면 일관된 성격의 챗봇을 만들 수 있습니다.'},{type:"choice",q:"Spring AI로 RAG 파이프라인을 구축하는 단계를 올바른 순서로 나열한 것은?",choices:["벡터 검색 → 문서 쪼개기 → 문서 읽기 → 임베딩 저장","문서 읽기 → 쪼개기(TextSplitter) → 임베딩해 VectorStore에 저장 → 질문 시 유사 문서 검색 → 검색 결과를 프롬프트에 결합해 답변","프롬프트 작성 → 모델 학습 → 가중치 배포 → 문서 삭제","질문 접수 → 문서 전체를 통째로 프롬프트에 넣기 → 끝"],answer:1,explain:'정답은 2번입니다. RAG는 크게 "준비(적재)"와 "질의(검색+답변)" 두 국면으로 나뉩니다. 준비 단계에서는 DocumentReader로 문서를 읽고, TextSplitter로 검색하기 좋은 크기로 쪼갠 뒤, 임베딩 모델로 벡터화해 VectorStore(pgvector)에 저장합니다. 질의 단계에서는 사용자 질문과 의미가 가까운 조각을 벡터 검색으로 찾아, 그 근거를 프롬프트에 붙여 LLM이 답하게 합니다. 4번처럼 문서 전체를 통째로 넣는 방식은 컨텍스트 길이 제한과 비용 때문에 현실적으로 불가능하며, 그래서 "쪼개서 저장하고 필요한 것만 찾아 넣는" RAG가 필요한 것입니다.'},{type:"choice",q:"자바 MCP Server에서 일반 자바 메서드를 AI가 호출할 수 있는 도구로 노출할 때 사용하는 어노테이션은?",choices:["@Autowired","@Entity","@Tool","@Deprecated"],answer:2,explain:'정답은 3번 @Tool입니다. 자바 메서드 위에 @Tool 어노테이션과 설명을 붙이면, Spring AI가 그 메서드를 MCP 도구로 등록해 AI 모델에게 "이런 이름과 기능의 도구가 있고, 이런 인자를 받는다"고 알려 줍니다. 모델은 대화 중 필요하다고 판단하면 그 도구의 호출을 요청하고, 서버가 실제 메서드를 실행해 결과를 돌려줍니다. 참고로 1번 @Autowired는 의존성 주입, 2번 @Entity는 JPA 테이블 매핑용으로 전혀 다른 용도입니다. 실습 마지막 날 "목표를 주면 스스로 도구를 호출하는 에이전트"의 재료가 바로 이 @Tool 메서드들이었습니다.'},{type:"short",q:"PostgreSQL 데이터베이스에 벡터 타입과 유사도 검색 기능을 추가해, Spring AI의 VectorStore 저장소로 쓸 수 있게 해 주는 확장(extension)의 이름은?",answer:"pgvector",explain:"정답은 pgvector입니다. pgvector는 PostgreSQL에 vector라는 컬럼 타입과 벡터 간 거리·유사도 연산을 추가해 주는 오픈소스 확장으로, 익숙한 RDB를 그대로 벡터 DB처럼 쓸 수 있게 해 줍니다. 별도의 전용 벡터 DB를 새로 도입하지 않아도 되고, 기존 업무 데이터와 벡터를 한 DB에서 관리할 수 있어 자바 백엔드 환경과 궁합이 좋습니다. 실습에서는 도커 컨테이너로 pgvector를 띄우고 build.gradle에 의존성을 추가한 뒤, application.yml 설정으로 Spring AI VectorStore와 연동했습니다."},{type:"short",q:"Spring AI에서 벡터 검색으로 찾은 문서 근거를 프롬프트에 자동으로 결합해 RAG 답변을 구성해 주는 Advisor의 이름은? (실습에서 ChatClient에 붙여 사용)",answer:"QuestionAnswerAdvisor",explain:"정답은 QuestionAnswerAdvisor입니다. Advisor는 ChatClient의 요청·응답 과정에 끼어들어 부가 기능을 더해 주는 인터셉터 같은 장치인데, QuestionAnswerAdvisor는 사용자 질문이 들어오면 자동으로 VectorStore에서 관련 문서를 검색해 프롬프트에 근거로 붙여 줍니다. 덕분에 개발자가 검색과 프롬프트 조립 코드를 일일이 짜지 않아도 한두 줄로 RAG가 완성됩니다. 비슷한 방식으로 대화 기록을 유지해 주는 ChatMemory Advisor도 실습에서 함께 사용했습니다."}],"ml-dl":[{type:"ox",q:"모델을 학습시킬 때 데이터를 학습용(train)과 평가용(test)으로 나누지 않고 전부 학습에 쓴 뒤 같은 데이터로 점수를 매기면, 실제 실력보다 점수가 부풀려질 수 있다.",answer:"O",explain:"맞습니다. 공부한 문제집 그대로 시험을 보면 답을 외워서도 100점을 받을 수 있듯이, 학습에 쓴 데이터로 평가하면 모델이 데이터를 통째로 암기했는지 진짜 규칙을 배웠는지 구분할 수 없습니다. 그래서 train_test_split 으로 데이터를 학습용과 평가용으로 미리 나누고, 모델이 한 번도 본 적 없는 평가용 데이터로 점수를 매깁니다. 이렇게 해야 새로운 데이터에서도 잘 맞히는 능력, 즉 일반화(generalization) 성능을 제대로 측정할 수 있습니다."},{type:"choice",q:"학습 데이터에서는 점수가 아주 높은데 처음 보는 평가 데이터에서는 점수가 뚝 떨어지는 현상을 무엇이라 하는가?",choices:["과소적합(underfitting)","과적합(overfitting)","정규화(regularization)","배치 정규화(batch normalization)"],answer:1,explain:"정답은 2번 과적합입니다. 과적합은 모델이 학습 데이터의 세세한 잡음까지 통째로 외워버려서, 새 데이터에는 오히려 약해지는 현상입니다. 반대로 1번 과소적합은 모델이 너무 단순해서 학습 데이터조차 제대로 못 맞히는 상태입니다. 3번 정규화와 4번 배치 정규화는 병명이 아니라 처방에 해당하는 기법으로, 특히 정규화·드롭아웃·조기종료 등은 과적합을 줄이기 위해 사용합니다."},{type:"ox",q:"딥러닝의 학습 루프에서 역전파(backpropagation)는 예측이 얼마나 틀렸는지(손실)를 바탕으로, 각 가중치를 어느 방향으로 고쳐야 할지 계산하는 과정이다.",answer:"O",explain:'맞습니다. 신경망 학습은 "순전파로 예측 → 손실 함수로 틀린 정도 계산 → 역전파로 각 가중치의 책임(기울기) 계산 → 옵티마이저가 가중치 업데이트"를 반복하는 루프입니다. 역전파는 출력 쪽의 오차를 입력 쪽으로 거꾸로 전달하면서, 가중치 하나하나가 오차에 얼마나 기여했는지(기울기)를 미분으로 구합니다. PyTorch에서는 loss.backward() 한 줄이 이 역전파를 수행하고, optimizer.step() 이 그 기울기를 이용해 가중치를 실제로 고칩니다.'},{type:"choice",q:"이미지 데이터를 다룰 때 특히 강한 신경망 구조로, 합성곱(Convolution) 필터로 지역적인 패턴(선·모서리 등)을 뽑아내고 풀링(Pooling)으로 크기를 줄여 가는 구조는?",choices:["MLP(다층 퍼셉트론)","LSTM","CNN(합성곱 신경망)","오토인코더(Autoencoder)"],answer:2,explain:"정답은 3번 CNN입니다. CNN은 작은 필터를 이미지 위에서 슬라이딩하며 선·모서리·질감 같은 지역 패턴을 찾아내고(Conv), 풀링으로 크기를 줄여 중요한 특징만 남깁니다. 전형적인 흐름은 Conv → Pool → Flatten → FC(완전연결) → Softmax 순서로, 마지막에 클래스별 확률을 출력합니다. 참고로 2번 LSTM은 시계열·문장처럼 순서가 있는 데이터에, 4번 오토인코더는 압축·복원을 통한 특징 학습이나 이상 탐지에 주로 쓰입니다."},{type:"ox",q:"기본 RNN은 문장이나 시계열이 아무리 길어져도 먼 과거의 정보를 잊지 않고 완벽하게 기억하므로, LSTM 같은 개선 구조는 필요 없다.",answer:"X",explain:"틀렸습니다. 기본 RNN은 정보를 한 칸씩 다음 단계로 전달하는 구조라, 시퀀스가 길어지면 기울기가 점점 희미해지는 기울기 소실 문제로 먼 과거의 정보를 잊어버립니다(장기 의존성 문제). LSTM은 이를 해결하기 위해 정보가 큰 손실 없이 흘러가는 Cell State라는 별도의 기억 통로와, 무엇을 기억하고 잊고 내보낼지 조절하는 게이트(입력·망각·출력)를 추가한 구조입니다. 실습에서도 LSTM으로 사인파의 다음 값을 예측하며 게이트가 긴 기억을 유지하는 것을 확인했고, 이후 이 한계를 더 근본적으로 푼 것이 Attention·Transformer입니다."},{type:"choice",q:"옵티마이저(optimizer)에 대한 설명으로 가장 올바른 것은?",choices:["모델의 예측이 얼마나 틀렸는지를 숫자 하나로 계산하는 함수다","역전파로 구한 기울기를 이용해 가중치를 실제로 업데이트하는 알고리즘(SGD·Adam 등)이다","데이터를 학습용과 평가용으로 나누는 도구다","이미지를 벡터로 바꾸는 전처리 기법이다"],answer:1,explain:'정답은 2번입니다. 옵티마이저는 "기울기가 가리키는 내리막 방향으로 가중치를 얼마나, 어떻게 움직일지"를 정하는 업데이트 담당입니다. SGD는 기울기 방향으로 일정 보폭(학습률)만큼 이동하는 기본형이고, Adam은 과거 기울기 정보를 활용해 파라미터마다 보폭을 자동 조절해 주어 실무에서 기본값처럼 널리 쓰입니다. 참고로 1번은 손실 함수(loss function)에 대한 설명이며, 손실 함수가 틀린 정도를 재면 옵티마이저가 그걸 줄이는 방향으로 가중치를 고칩니다.'},{type:"short",q:"옵티마이저가 가중치를 한 번에 얼마나 크게 움직일지(보폭)를 정하는 하이퍼파라미터로, 너무 크면 발산하고 너무 작으면 학습이 매우 느려지는 이것은?",answer:"학습률(learning rate)",explain:"정답은 학습률(learning rate)입니다. 산에서 내려올 때의 보폭에 비유되는데, 보폭이 너무 크면 골짜기(최적점)를 건너뛰어 손실이 오히려 튀어 오르고(발산), 너무 작으면 한 걸음씩 기어가느라 학습이 끝없이 오래 걸립니다. 그래서 학습률은 딥러닝에서 가장 먼저, 가장 신경 써서 조정하는 하이퍼파라미터로 꼽힙니다. 실무에서는 0.001 같은 값에서 시작해 학습 곡선을 보며 조절하거나, 스케줄러로 학습 중에 점점 줄여 가기도 합니다."},{type:"short",q:"입력을 저차원으로 압축(인코더)했다가 다시 원래대로 복원(디코더)하도록 학습시키는 신경망으로, 복원 오차가 큰 데이터를 이상치로 잡아내는 이상 탐지에도 쓰이는 이 구조의 이름은?",answer:"오토인코더(Autoencoder)",explain:'정답은 오토인코더(Autoencoder, AE)입니다. 오토인코더는 정답 라벨 없이 "입력을 그대로 다시 만들어내라"는 과제를 주어, 데이터의 핵심 특징만 남긴 압축 표현(잠재 벡터)을 스스로 배우게 하는 비지도 학습 구조입니다. 정상 데이터로만 학습해 두면 정상 패턴은 잘 복원하지만 처음 보는 이상 패턴은 복원 오차가 커지므로, 이 오차를 기준으로 이상 탐지를 할 수 있습니다. 입력에 잡음을 섞어 원본을 복원하게 하는 디노이징 오토인코더는 더 견고한(robust) 특징을 학습합니다.'}],feature:[{type:"ox",q:"피처 엔지니어링은 원본 데이터를 모델이 배우기 좋은 형태의 입력 변수(피처)로 가공하는 작업으로, 좋은 피처는 모델 알고리즘을 바꾸는 것 못지않게 성능을 끌어올릴 수 있다.",answer:"O",explain:"맞습니다. 모델은 결국 입력으로 주어진 피처 안에서만 규칙을 찾을 수 있으므로, 재료(피처)의 품질이 요리(모델 성능)의 상한선을 정합니다. 예를 들어 날짜에서 요일·월을 뽑아내거나, 키와 몸무게로 BMI 같은 파생 피처를 만들어 주면 모델이 훨씬 쉽게 패턴을 학습합니다. 그래서 실무에서는 최신 알고리즘을 찾아 헤매기 전에, EDA로 데이터를 이해하고 피처를 다듬는 데 많은 시간을 씁니다."},{type:"choice",q:"IQR(사분위 범위) 규칙으로 이상치(outlier)를 판단할 때, 일반적으로 이상치로 보는 경계는?",choices:["평균에서 1 이상 떨어진 값","Q1 - 1.5 x IQR 보다 작거나 Q3 + 1.5 x IQR 보다 큰 값","중앙값과 다른 모든 값","0보다 작은 모든 값"],answer:1,explain:"정답은 2번입니다. 데이터를 크기순으로 늘어놓았을 때 하위 25% 지점이 Q1, 상위 25% 지점(하위 75%)이 Q3이고, 그 사이 폭 Q3 - Q1 이 IQR입니다. 여기서 Q1 - 1.5 x IQR 아래, Q3 + 1.5 x IQR 위에 있는 값을 이상치 후보로 보는 것이 IQR 규칙이며, 박스플롯의 수염 바깥 점들이 바로 이 값들입니다. 평균·표준편차 기반의 z-score 방식과 달리 IQR은 극단값 자체에 덜 흔들린다는 장점이 있습니다."},{type:"ox",q:"스케일러(StandardScaler 등)의 fit은 학습 데이터에만 수행하고, 테스트 데이터에는 그 기준으로 transform만 해야 데이터 누수(data leakage)를 막을 수 있다.",answer:"O",explain:'맞습니다. fit은 평균·표준편차 같은 변환 기준을 "계산"하는 단계인데, 이 계산에 테스트 데이터를 섞으면 시험 문제의 정보를 미리 컨닝한 셈이 되어 평가 점수가 실제보다 좋게 나옵니다. 이를 데이터 누수라고 하며, 올바른 순서는 학습 데이터로 scaler.fit_transform(X_train)을 하고 테스트 데이터에는 scaler.transform(X_test)만 적용하는 것입니다. 결측치 대치, 인코딩 등 모든 전처리에 똑같이 적용되는 원칙이며, sklearn의 Pipeline을 쓰면 이 순서를 자동으로 지켜 줍니다.'},{type:"choice",q:"범주형 변수 인코딩에 대한 설명으로 가장 올바른 것은?",choices:["원-핫 인코딩은 범주 종류가 수천 개여도 열이 늘어나지 않아 항상 유리하다","원-핫 인코딩은 범주마다 0/1 열을 만들고, 타깃 인코딩은 그 범주의 타깃 평균값으로 치환한다","타깃 인코딩은 정답(타깃)을 전혀 사용하지 않는다","문자열 범주는 인코딩 없이 그대로 모든 모델에 넣을 수 있다"],answer:1,explain:'정답은 2번입니다. 원-핫 인코딩은 "서울/부산/광주"처럼 범주별로 0 또는 1인 열을 새로 만드는 방식이라 직관적이지만, 범주 종류가 많으면 열이 폭발적으로 늘어나는 단점이 있습니다(그래서 1번은 틀림). 타깃 인코딩은 각 범주를 그 범주에 속한 데이터들의 타깃 평균으로 바꾸는 방식이라 범주가 많을 때 유리한 대신, 정답을 이용하므로 누수 위험이 있어 교차적합(cross-fitting) 같은 안전장치가 필요합니다. sklearn의 TargetEncoder는 내부적으로 이 교차적합을 수행해 누수를 스스로 막아 줍니다.'},{type:"ox",q:'"다음 달 이탈 고객 예측" 모델을 만들 때, 예측 시점에는 알 수 없는 미래 정보(예: 다음 달 결제 여부)를 피처로 넣으면 검증 점수는 높아도 실서비스에서는 쓸모없는 모델이 된다.',answer:"O",explain:'맞습니다. 이는 대표적인 데이터 누수 사례로, 정답과 사실상 같은 정보(미래 정보)를 입력에 넣은 것이라 검증에서는 점수가 비정상적으로 높게 나옵니다. 하지만 실제 서비스 시점에는 그 미래 정보를 알 수 없으므로 모델이 무용지물이 됩니다. 검증 점수가 의심스러울 만큼 좋다면 기뻐하기 전에 누수를 먼저 의심해야 하며, "예측하는 시점에 실제로 확보 가능한 정보인가?"를 피처 하나하나에 대해 점검하는 습관이 중요합니다.'},{type:"choice",q:"값의 범위가 크게 다른 수치 피처들(예: 나이 20~60, 연봉 3000만~2억)을 비슷한 크기로 맞춰 주는 전처리는?",choices:["스케일링(표준화·정규화)","원-핫 인코딩","결측치 대치(imputation)","차원 축소(PCA)"],answer:0,explain:"정답은 1번 스케일링입니다. StandardScaler는 평균 0·표준편차 1로 맞추는 표준화, MinMaxScaler는 0~1 사이로 눌러 담는 정규화, RobustScaler는 중앙값과 IQR을 쓰므로 이상치에 가장 덜 흔들리는 방식입니다. 스케일이 다르면 거리 기반 모델(KNN·SVM)이나 경사하강법 기반 모델(신경망·로지스틱 회귀)이 큰 숫자의 피처에만 휘둘리므로 스케일링이 사실상 필수입니다. 반면 트리 계열 모델(랜덤포레스트 등)은 값의 크고 작음의 순서만 보므로 스케일링 영향이 거의 없습니다."},{type:"short",q:"변수의 유형·분포·상관관계·타깃과의 관계를 그래프와 통계로 살펴보며 데이터를 이해하는, 피처 엔지니어링 이전에 수행하는 탐색 분석 과정을 영문 약자 3글자로 무엇이라 하는가?",answer:"EDA",explain:"정답은 EDA(Exploratory Data Analysis, 탐색적 데이터 분석)입니다. 본격적인 가공에 앞서 히스토그램으로 분포를, 박스플롯으로 이상치를, 상관계수·산점도로 변수 간 관계를 확인하며 데이터와 먼저 친해지는 단계입니다. EDA에서 발견한 사실(치우친 분포, 결측 패턴, 타깃과 상관이 높은 변수 등)이 곧 어떤 피처를 만들고 어떻게 변환할지의 근거가 됩니다. 데이터를 모르는 채 만든 피처는 감으로 지은 집과 같아서, EDA는 피처 엔지니어링의 출발점으로 꼽힙니다."},{type:"short",q:"학습 데이터에만 fit 해야 할 전처리를 전체 데이터에 적용하거나 미래 정보를 피처로 쓰는 등, 평가 시점에 알 수 없는 정보가 학습에 새어 들어가 점수를 부풀리는 문제를 무엇이라 하는가?",answer:"데이터 누수(data leakage)",explain:'정답은 데이터 누수(data leakage)입니다. 누수가 생기면 검증 점수는 훌륭한데 실서비스에서는 성능이 무너지는, 가장 위험한 유형의 실수가 됩니다. 대표 사례는 전체 데이터로 스케일러·인코더를 fit 하는 것, 예측 시점에 알 수 없는 미래 정보를 피처로 쓰는 것, 데이터 분할 전에 타깃 인코딩을 하는 것 등입니다. 예방책은 "분할 먼저, 전처리는 학습 데이터에만 fit"이라는 순서를 지키는 것이고, sklearn Pipeline으로 전처리와 모델을 묶으면 교차검증 중에도 이 원칙이 자동으로 지켜집니다.'}],modeldev:[{type:"ox",q:"K-겹 교차검증(K-fold cross validation)은 데이터를 K개 조각으로 나눠, 매번 다른 조각을 검증용으로 쓰며 K번 학습·평가한 점수의 평균으로 일반화 성능을 측정한다.",answer:"O",explain:'맞습니다. 데이터를 한 번만 나누면 "운 좋게 쉬운 검증 데이터"가 걸려 점수가 우연에 좌우될 수 있습니다. K-겹 교차검증은 예를 들어 5조각으로 나눠 1번 조각을 검증에 쓰고 나머지로 학습, 다음엔 2번 조각을 검증에 쓰는 식으로 5번 반복해 모든 데이터가 한 번씩 검증에 쓰이게 합니다. 5개 점수의 평균과 편차를 보면 훨씬 믿을 만한 성능 추정치를 얻을 수 있어, 모델·하이퍼파라미터 비교의 표준 방법으로 쓰입니다.'},{type:"choice",q:'암 환자를 놓치면 안 되는 의료 진단처럼 "실제 양성을 빠뜨리지 않는 것"이 가장 중요할 때 우선적으로 봐야 할 평가지표는?',choices:["정확도(Accuracy)","정밀도(Precision)","재현율(Recall)","R²(결정계수)"],answer:2,explain:'정답은 3번 재현율입니다. 재현율은 실제 양성(환자) 중에서 모델이 양성이라고 맞게 찾아낸 비율로, 재현율이 낮다는 것은 진짜 환자를 놓치고 있다는 뜻입니다. 반대로 정밀도는 모델이 양성이라고 말한 것 중 진짜 양성의 비율이라, 스팸 필터처럼 "정상 메일을 스팸으로 잘못 분류하면 안 되는" 상황에서 중요합니다. 정확도는 데이터가 불균형하면(예: 환자 1%) 전부 정상이라고 찍어도 99%가 나오는 함정이 있고, R²는 분류가 아닌 회귀용 지표입니다.'},{type:"ox",q:"하이퍼파라미터 튜닝은 테스트 데이터 점수를 보면서 진행해야 최종 성능을 가장 정확하게 알 수 있다.",answer:"X",explain:'틀렸습니다. 테스트 데이터를 보면서 튜닝하면 테스트 데이터에 맞춰 하이퍼파라미터를 고르게 되어, 테스트가 더 이상 "처음 보는 데이터"가 아니게 됩니다(테스트 점수가 낙관적으로 부풀려짐). 올바른 방법은 학습/검증/테스트 3분할 또는 교차검증을 사용해 검증 점수로만 튜닝하고, 테스트 데이터는 모든 선택이 끝난 뒤 최종 성능 확인용으로 딱 한 번 쓰는 것입니다. 시험지는 마지막 순간까지 봉인해 두어야 한다고 기억하면 쉽습니다.'},{type:"choice",q:"하이퍼파라미터 탐색 방법에 대한 설명으로 가장 올바른 것은?",choices:["Grid Search는 지정한 값 조합을 빠짐없이 전부 시도하므로 조합이 많아지면 시간이 폭발적으로 늘어난다","Random Search는 항상 Grid Search보다 나쁜 결과를 낸다","베이지안 최적화(Optuna 등)는 이전 시도 결과를 전혀 참고하지 않고 무작위로만 탐색한다","하이퍼파라미터는 모델이 학습 중에 스스로 배우는 값이다"],answer:0,explain:"정답은 1번입니다. Grid Search는 격자로 정한 모든 조합을 시도하므로 확실하지만, 파라미터 3개 x 값 10개만 돼도 1000번 학습이 필요해 비용이 폭발합니다. Random Search는 무작위 샘플링이라 같은 횟수로 더 넓은 범위를 훑을 수 있어 의외로 효율적이고, 베이지안 최적화(Optuna 등)는 지금까지의 결과를 보고 유망한 구역을 골라 똑똑하게 탐색합니다(그래서 3번은 틀림). 또한 하이퍼파라미터는 학습률·트리 깊이처럼 사람이 학습 전에 정해 주는 값이라 4번도 틀린 설명입니다."},{type:"ox",q:"회귀 모델 평가에서 RMSE는 MAE보다 큰 오차(치명적인 빗나감)에 더 민감하게 반응하는 지표다.",answer:"O",explain:'맞습니다. MAE는 오차의 절댓값 평균이라 모든 오차를 공평하게 취급하지만, RMSE는 오차를 제곱해서 평균 낸 뒤 루트를 씌우므로 큰 오차일수록 제곱 효과로 벌점이 훨씬 커집니다. 예를 들어 오차 10은 제곱하면 100이지만 오차 2는 4에 불과해, 가끔 크게 틀리는 모델은 RMSE가 특히 나빠집니다. 그래서 "한 번의 큰 오차가 치명적인" 문제라면 RMSE를, 이상치의 영향을 덜 받고 싶다면 MAE를 보는 식으로 상황에 맞게 고릅니다. R²는 1에 가까울수록 잘 맞춘 것입니다.'},{type:"choice",q:'혼동행렬(Confusion Matrix)에서 "실제로는 음성인데 모델이 양성이라고 잘못 예측한 경우"를 부르는 이름은?',choices:["TP(True Positive)","TN(True Negative)","FP(False Positive)","FN(False Negative)"],answer:2,explain:'정답은 3번 FP(거짓 양성, 오탐)입니다. 읽는 요령은 뒤 글자(Positive/Negative)가 모델의 예측이고, 앞 글자(True/False)가 그 예측이 맞았는지 여부입니다. 즉 FP는 "양성이라고 예측했는데(P) 틀렸다(F)", 다시 말해 실제는 음성인 것을 양성으로 잘못 잡은 경우입니다. 혼동행렬로 FP가 많은지 FN이 많은지 뜯어보면 모델이 어떤 종류의 실수를 하는지 알 수 있어, 정밀도(FP를 줄이는 방향)와 재현율(FN을 줄이는 방향) 중 무엇을 개선할지 판단하는 근거가 됩니다.'},{type:"short",q:"학습을 반복하는 동안 검증 점수가 더 이상 좋아지지 않으면 학습을 중간에 멈춰 과적합을 막는 기법을 무엇이라 하는가?",answer:"조기종료(early stopping)",explain:"정답은 조기종료(early stopping)입니다. 학습을 오래 할수록 학습 데이터 점수는 계속 오르지만, 어느 시점부터 검증 점수는 오히려 나빠지기 시작하는데 이것이 과적합의 신호입니다. 조기종료는 검증 점수를 매 에폭 지켜보다가 일정 횟수(patience) 동안 개선이 없으면 학습을 멈추고 가장 좋았던 시점의 모델을 사용합니다. 과적합 방지와 학습 시간 절약을 동시에 얻을 수 있어, 신경망은 물론 XGBoost 같은 부스팅 모델에서도 널리 쓰입니다."},{type:"short",q:"여러 모델의 예측을 결합해 단일 모델보다 좋은 성능을 내는 기법의 총칭으로, 배깅·부스팅·스태킹이 대표적인 이것은?",answer:"앙상블(ensemble)",explain:"정답은 앙상블입니다. 한 명의 천재보다 여러 명의 위원회가 더 안정적인 판단을 내리듯, 여러 모델의 예측을 합치면 개별 모델의 실수가 서로 상쇄되어 성능과 안정성이 올라갑니다. 배깅은 데이터를 다르게 샘플링해 병렬로 여러 모델을 학습시키는 방식(대표: 랜덤포레스트), 부스팅은 앞 모델이 틀린 부분을 다음 모델이 보완하도록 순차 학습하는 방식(XGBoost·LightGBM), 스태킹은 여러 모델의 예측값을 다시 입력으로 받아 최종 모델이 조합하는 방식입니다."}],serving:[{type:"ox",q:"모델 서빙(serving)이란 학습을 마친 모델을 API 등으로 외부에 공개해, 다른 서비스나 사용자가 실시간으로 예측 결과를 받아 쓸 수 있게 만드는 것이다.",answer:"O",explain:'맞습니다. 노트북 안에서만 도는 모델은 아직 "제품"이 아니며, 서빙은 모델을 서비스로 배달하는 단계입니다. 대표 패턴은 요청이 올 때마다 즉시 응답하는 온라인 서빙, 쌓아 둔 데이터를 주기적으로 한꺼번에 예측하는 배치 서빙, 흘러들어오는 데이터를 실시간 처리하는 스트림 서빙 세 가지입니다. 실습에서는 FastAPI로 REST API를 만들어, 클라이언트가 JSON으로 입력을 보내면 모델 예측을 JSON으로 돌려주는 온라인 서빙을 구현했습니다.'},{type:"choice",q:"FastAPI에 대한 설명으로 가장 올바른 것은?",choices:["데이터베이스의 한 종류로 벡터를 저장한다","Python 웹 프레임워크로, Pydantic 기반 입력 검증과 자동 API 문서(/docs)를 제공해 추론 API 만들기에 적합하다","모델의 하이퍼파라미터를 자동 튜닝하는 라이브러리다","HTML 화면을 그리는 프론트엔드 프레임워크다"],answer:1,explain:"정답은 2번입니다. FastAPI는 파이썬으로 REST API를 빠르게 만들 수 있는 웹 프레임워크로, 데코레이터 한 줄로 엔드포인트를 정의하고 Pydantic 모델로 요청 데이터의 형식·범위를 자동 검증해 줍니다. 서버를 켜면 /docs 주소에서 Swagger 문서가 자동 생성되어 브라우저에서 바로 API를 테스트할 수 있는 점도 큰 장점입니다. 실행은 uvicorn 이라는 서버 프로그램이 담당하며(uvicorn app:app --port 8000), 모델 서빙 실습에서 joblib으로 저장한 모델을 불러와 예측 엔드포인트를 만들었습니다."},{type:"ox",q:"Docker 컨테이너로 모델 서빙 앱을 포장하면, 내 컴퓨터에서 되던 것이 서버에서는 안 되는 환경 차이 문제를 크게 줄일 수 있다.",answer:"O",explain:'맞습니다. Docker는 애플리케이션 코드뿐 아니라 파이썬 버전, 라이브러리, 설정까지 하나의 이미지로 통째로 포장하는 기술입니다. 이 이미지는 어느 서버에서 실행해도 동일한 환경으로 돌아가므로, "제 컴퓨터에서는 됐는데요" 문제를 근본적으로 줄여 줍니다. Dockerfile에 베이스 이미지 지정 → 작업폴더 설정 → 의존성 설치 → 소스 복사 → 실행 명령 순서로 만드는 법을 적어 두면, docker build로 이미지를 만들고 docker run으로 어디서든 같은 서빙 서비스를 띄울 수 있습니다.'},{type:"choice",q:"운영 중인 모델의 입력 데이터 분포가 학습 때와 달라지는 것(데이터 드리프트)과, 입력과 정답의 관계 자체가 변하는 것(컨셉 드리프트)에 대한 설명으로 가장 올바른 것은?",choices:["드리프트가 생겨도 모델 성능에는 아무 영향이 없다","드리프트는 코드 버그이므로 코드만 고치면 해결된다","드리프트를 감지하면 데이터를 다시 모아 모델을 재학습하는 등의 대응이 필요하다","드리프트는 학습 단계에서만 발생하고 운영 중에는 발생하지 않는다"],answer:2,explain:"정답은 3번입니다. 데이터 드리프트는 고객층·계절·시장 변화 등으로 입력 분포가 변하는 것이고, 컨셉 드리프트는 같은 입력이라도 정답과의 관계가 변하는 것입니다(예: 팬데믹 이후 소비 패턴 변화). 세상이 계속 변하기 때문에 배포 당시 훌륭했던 모델도 시간이 지나면 조용히 성능이 떨어지며, 이는 버그가 아니라 자연스러운 현상이라 코드 수정으로는 해결되지 않습니다. 그래서 MLOps에서는 입력 분포와 예측 성능을 상시 모니터링하고, 드리프트가 감지되면 최신 데이터로 재학습해 다시 배포하는 체계를 갖춥니다."},{type:"ox",q:"서빙 API에 들어오는 입력값 검증은 생략해도 된다. 모델이 어떤 입력이든 알아서 잘 처리해 주기 때문이다.",answer:"X",explain:"틀렸습니다. 모델은 학습 때 본 형식·범위의 입력을 가정하고 있어서, 음수 나이나 빠진 필드, 문자열이 들어오면 엉뚱한 예측을 하거나 서버 에러를 냅니다. 그래서 서빙 API의 첫 관문은 입력 검증이며, FastAPI에서는 Pydantic 스키마로 필드의 자료형과 값 범위를 선언해 두면 잘못된 요청을 모델에 닿기 전에 422 에러로 걸러 줍니다. 검증 다음에는 학습 때와 동일한 전처리를 적용하고, 예측 결과를 사람이 쓰기 좋은 형태로 다듬는 후처리까지가 서빙 파이프라인의 기본 구성입니다."},{type:"choice",q:"운영 중인 서빙 서비스의 상태를 파악하기 위한 관측성(observability)의 3대 요소로 가장 알맞은 조합은?",choices:["메트릭(수치 지표) · 로그(사건 기록) · 트레이싱(요청 추적)","백업 · 압축 · 암호화","HTML · CSS · JavaScript","학습률 · 배치 크기 · 에폭 수"],answer:0,explain:"정답은 1번입니다. 메트릭은 초당 요청 수·응답 시간·에러율처럼 숫자로 집계되는 지표로 대시보드와 알림의 재료가 되고, 로그는 언제 어떤 일이 있었는지 남기는 사건 기록이라 문제의 원인을 파고들 때 봅니다. 트레이싱은 요청 하나가 여러 구성요소(API → 전처리 → 모델 → DB)를 거치는 경로와 각 구간의 소요 시간을 추적해, 어디가 병목인지 찾아 줍니다. ML 서빙에서는 여기에 예측값 분포·드리프트 지표까지 함께 모니터링해야 모델의 조용한 성능 저하를 잡아낼 수 있습니다."},{type:"short",q:'학습된 모델들을 버전별로 등록·관리하며 "지금 운영에 배포된 모델이 어떤 버전인지"를 추적하게 해 주는 MLOps 구성요소를 무엇이라 하는가?',answer:"모델 레지스트리(model registry)",explain:"정답은 모델 레지스트리입니다. 모델 파일을 아무 폴더에나 두면 어떤 데이터·코드로 만든 모델인지, 운영 중인 것이 몇 번째 버전인지 금세 뒤섞입니다. 모델 레지스트리는 모델을 버전과 함께 등록하고 스테이징 → 프로덕션 같은 상태를 관리하며, 학습에 쓴 데이터·파라미터·성능 기록과 연결해 재현성을 보장합니다. MLflow의 Model Registry가 대표적이며, 문제가 생겼을 때 이전 버전으로 빠르게 되돌리는(롤백) 안전장치 역할도 합니다."},{type:"short",q:"코드를 푸시하면 테스트·빌드·배포까지 자동으로 이어지게 하는 개발 자동화 관행을 영문 약자로 무엇이라 하는가? (지속적 통합/지속적 배포)",answer:"CI/CD",explain:'정답은 CI/CD입니다. CI(Continuous Integration, 지속적 통합)는 코드를 저장소에 올릴 때마다 자동으로 테스트와 빌드를 돌려 문제를 조기에 잡는 것이고, CD(Continuous Delivery/Deployment, 지속적 배포)는 검증을 통과한 결과물을 자동으로 서버까지 배포하는 것입니다. GitHub Actions 같은 도구로 "push하면 Docker 이미지 빌드 → 테스트 → 배포"가 자동으로 이어지게 구성합니다. MLOps에서는 여기에 데이터 검증과 모델 재학습·평가 단계까지 파이프라인에 포함시켜, 사람 손을 거치지 않고도 안전하게 모델을 갱신할 수 있게 확장합니다.'}],rag:[{type:"ox",q:"RAG(검색 증강 생성)는 LLM이 학습하지 못한 최신 정보나 사내 문서를 검색해 근거(컨텍스트)로 붙여 준 뒤 답하게 하는 기법이다.",answer:"O",explain:"맞습니다. LLM은 학습 시점 이후의 정보나 회사 내부 문서를 알지 못하고, 모르는 것을 그럴듯하게 지어내는 환각(hallucination) 문제도 있습니다. RAG는 질문과 관련된 문서 조각을 먼저 검색(Retrieval)해 프롬프트에 근거로 넣어 주고, LLM이 그 근거만 보고 답을 생성(Generation)하게 합니다. 덕분에 최신·사내 지식으로 답할 수 있고, 출처를 함께 표기해 신뢰도도 높일 수 있습니다."},{type:"ox",q:"청킹(chunking)에서 chunk_overlap은 옆 조각과 일부 내용을 겹치게 하는 설정으로, 조각 경계에서 문장이 반 토막 나는 것을 막아 주는 안전장치다.",answer:"O",explain:"맞습니다. 긴 문서를 조각으로 자를 때 하필 경계에서 문장이 끊기면, 그 문장의 의미가 어느 조각에도 온전히 담기지 않아 검색에 실패할 수 있습니다. chunk_overlap은 이웃 조각끼리 일부(보통 chunk_size의 10~20%)를 겹치게 해 경계의 문장이 최소 한 조각에는 통째로 들어가도록 합니다. 수업 실습에서도 chunk_size=500, chunk_overlap=50 같은 값으로 시작해 결과를 비교했습니다. RecursiveCharacterTextSplitter처럼 문단→문장→단어 순으로 자연스러운 경계를 우선하는 분할기를 함께 쓰면 더 좋습니다."},{type:"ox",q:"재순위(Re-ranking)는 문서를 벡터 DB에 저장하기 전에 파일 이름을 알파벳순으로 정렬해 두는 작업이다.",answer:"X",explain:'틀렸습니다. 재순위는 저장 단계가 아니라 검색 단계의 품질 향상 기법입니다. 1차 검색(벡터·키워드)으로 후보 조각을 넉넉히 가져온 뒤, CrossEncoder나 Cohere Rerank 같은 더 정밀한 모델로 질문과의 관련도를 다시 채점해 순서를 재정렬하고 상위 몇 개만 남깁니다. 1차 검색은 빠르지만 거칠고, 재순위 모델은 느리지만 정확하므로 "넓게 건져서 정밀하게 고르는" 2단계 구조가 되는 것입니다.'},{type:"choice",q:"RAG 파이프라인의 전체 흐름을 순서대로 올바르게 나열한 것은?",choices:["적재 → 청킹 → 임베딩 → 색인 → 검색 → 생성","생성 → 검색 → 임베딩 → 청킹 → 색인 → 적재","임베딩 → 적재 → 생성 → 검색 → 청킹 → 색인","검색 → 적재 → 색인 → 생성 → 청킹 → 임베딩"],answer:0,explain:'정답은 1번입니다. 먼저 문서를 불러오고(적재), 검색하기 좋은 크기로 자르고(청킹), 각 조각을 의미를 담은 숫자 벡터로 바꾼 뒤(임베딩), 벡터 DB에 저장합니다(색인). 여기까지가 미리 해 두는 준비 단계입니다. 이후 사용자 질문이 들어오면 질문과 비슷한 조각을 찾아(검색) LLM에게 근거로 주고 답을 만들게 합니다(생성). "준비(적재~색인) 따로, 질문 처리(검색~생성) 따로"로 기억하면 순서가 헷갈리지 않습니다.'},{type:"choice",q:"하이브리드 검색(hybrid search)에 대한 설명으로 가장 올바른 것은?",choices:["두 개의 LLM이 동시에 답을 생성해 더 긴 답을 고르는 방식","BM25 같은 키워드 검색과 벡터(의미) 검색을 결합해 서로의 약점을 보완하는 방식","이미지와 텍스트를 동시에 검색하는 방식","검색 결과를 무작위로 섞어 다양성을 높이는 방식"],answer:1,explain:"정답은 2번입니다. 벡터 검색은 의미가 비슷한 문서를 잘 찾지만 제품 코드명·고유명사 같은 정확한 단어 일치에는 약하고, BM25 키워드 검색은 정확한 단어는 잘 찾지만 표현이 달라지면 놓칩니다. 하이브리드 검색은 이 둘을 함께 돌려 결과를 합치므로 서로의 약점을 보완합니다. 실습에서는 BM25Retriever와 벡터 리트리버를 EnsembleRetriever로 묶어(가중치 0.5:0.5) 하이브리드 검색을 구성했습니다."},{type:"choice",q:"RAG 품질을 자동 채점하는 RAGAS의 평가 지표가 아닌 것은?",choices:["충실도(faithfulness)","답변 관련성(answer relevancy)","문맥 정밀도(context precision)","초당 프레임 수(FPS)"],answer:3,explain:"정답은 4번입니다. FPS는 영상·게임의 화면 갱신 속도 지표로 RAG 평가와 무관합니다. RAGAS는 충실도(답이 검색된 문맥에 근거했는가, 지어내지 않았는가), 답변 관련성(질문에 맞는 답인가), 문맥 정밀도(검색해 온 문맥이 실제로 필요한 내용이었는가) 등을 LLM으로 자동 채점합니다. 이런 정량 평가가 있어야 청킹 크기·k값·재순위 같은 튜닝이 추측이 아닌 근거 있는 개선이 됩니다."},{type:"short",q:"검색기 평가에서, 정답 문서가 검색 결과 상위 K개 안에 포함된 비율을 나타내는 지표의 이름은? (영어로)",answer:"Hit Rate",explain:'정답은 Hit Rate(히트레이트, Hit Rate@K)입니다. 예를 들어 질문 4개 중 3개에서 정답 문서가 상위 3개(top-3) 안에 들어 있었다면 Hit Rate@3은 0.75입니다. 함께 배운 MRR(Mean Reciprocal Rank)은 정답이 "몇 번째"에 나왔는지까지 반영해 1위면 1점, 2위면 0.5점, 3위면 0.33점을 주는 지표입니다. 이 두 지표로 검색 실패와 생성 실패를 구분해야 청킹을 고칠지 프롬프트를 고칠지 올바른 개선 포인트를 잡을 수 있습니다.'},{type:"short",q:"리트리버가 유사도 순으로 상위 몇 개의 조각을 가져올지 정하는 검색 파라미터를 무엇이라 부르는가? (알파벳 포함 표기)",answer:"top-k",explain:"정답은 top-k(톱케이)입니다. k=4면 질문과 가장 비슷한 조각 4개를 가져와 LLM의 근거로 사용합니다. k가 너무 작으면 필요한 근거를 놓치고, 너무 크면 관련 없는 조각까지 섞여 답이 흐려지고 토큰 비용도 늘어납니다. 그래서 실습에서도 k를 4에서 6으로 바꿔 가며 RAGAS 점수를 비교했듯이, 한 번에 하나씩 바꿔 측정하며 적정값을 찾는 것이 튜닝의 기본입니다."}],agent:[{type:"ox",q:"ReAct 패턴은 LLM이 생각(Reason)과 행동(Act)을 번갈아 반복하면서, 도구를 호출하고 그 결과를 보고 다음 행동을 정하는 에이전트 패턴이다.",answer:"O",explain:'맞습니다. ReAct는 Reason(추론) + Act(행동)의 합성어로, "지금 무엇이 필요한지 생각 → 도구 호출(검색·계산 등) → 결과 관찰 → 다시 생각"의 루프를 돌립니다. 한 번에 답을 내뱉는 챗봇과 달리, 중간 결과를 보고 계획을 수정할 수 있어 여러 단계가 필요한 문제를 풀 수 있습니다. 수업에서는 검색 도구를 쓰는 단일 ReAct 에이전트를 직접 만들어 보며 이 루프를 확인했습니다.'},{type:"ox",q:"LangGraph에서 State는 노드들이 함께 읽고 갱신하며 돌려쓰는 공유 데이터이고, 노드(Node)는 그 상태를 받아 작업을 수행하는 단위다.",answer:"O",explain:'맞습니다. State는 그래프 전체가 들고 다니는 "보따리"로, TypedDict로 질문·검색결과·재시도 횟수 같은 필드의 모양을 정의합니다. 각 노드는 현재 State를 입력으로 받아 자기 일(검색·채점·생성 등)을 하고 바뀐 값을 돌려주며, 엣지(Edge)가 노드 사이의 실행 순서를 정합니다. 이 세 부품(State·Node·Edge)만 이해하면 StateGraph에 add_node와 엣지 연결로 어떤 에이전트 흐름이든 조립할 수 있습니다.'},{type:"ox",q:"에이전트의 검색·판단 루프는 LLM이 알아서 멈추므로, 최대 반복 횟수 같은 종료 조건을 따로 두지 않아도 무한 루프 걱정이 없다.",answer:"X",explain:"틀렸습니다. 루프를 도는 에이전트는 조건이 계속 불만족이면 영원히 돌 수 있으므로, 종료 조건을 반드시 설계에 넣어야 합니다. 수업 예제에서도 State에 tries(재시도 횟수) 필드를 두고 3회를 넘으면 강제로 생성 단계로 넘어가게 했습니다. 무한 루프는 답이 안 나오는 것뿐 아니라 API 호출이 반복되며 비용이 계속 새는 사고로 이어지므로, END로 가는 길과 반복 상한은 에이전트 설계의 필수 안전장치입니다."},{type:"choice",q:"멀티 에이전트의 Supervisor 패턴에 대한 설명으로 가장 올바른 것은?",choices:["모든 에이전트가 동시에 같은 작업을 중복 수행해 다수결로 답을 정한다","조정자(감독) 에이전트가 상황을 보고 어떤 전문 워커 에이전트에게 일을 맡길지 결정(라우팅)한다","사람이 매 단계 코드를 직접 수정하며 진행한다","에이전트를 한 개만 두고 프롬프트를 길게 쓰는 패턴이다"],answer:1,explain:'정답은 2번입니다. Supervisor 패턴은 회사의 팀장-팀원 구조와 같습니다. 감독 에이전트는 현재 진행 상황(State)을 보고 "다음은 조사 담당에게" 또는 "이제 작성 담당에게"처럼 알맞은 워커에게 일을 넘기고, 워커는 자기 일이 끝나면 다시 감독에게 돌아옵니다. 각 워커가 한 가지 역할만 맡으니 프롬프트가 단순해지고 디버깅이 쉬워지는 것이 장점이며, 실습에서도 조사 담당+작성 담당이 협업하는 워크플로를 만들었습니다.'},{type:"choice",q:'LangGraph에서 "검색 결과가 좋으면 생성 노드로, 나쁘면 질문 재작성 노드로" 처럼 상황에 따라 다음 노드를 다르게 보내려면 무엇을 사용하는가?',choices:["조건부 엣지(add_conditional_edges)","print 문","for 반복문만으로 처리","CSS 미디어 쿼리"],answer:0,explain:'정답은 1번 조건부 엣지입니다. 일반 엣지(add_edge)는 항상 같은 다음 노드로 가지만, 조건부 엣지는 판별 함수가 State를 보고 돌려주는 값에 따라 갈 곳이 달라집니다. 예제에서는 검색 결과를 채점하는 함수가 근거가 충분하면 generate로, 부족하면 rewrite로 보내도록 분기했습니다. 이 조건 분기가 있어야 "결과를 보고 스스로 회복"하는 Agentic한 루프가 만들어집니다.'},{type:"choice",q:"Human-in-the-loop(HITL)를 구현할 때, 그래프를 특정 지점에서 일부러 멈춰 사람의 확인·승인을 기다리게 하는 LangGraph 기능은?",choices:["shutdown","delete","interrupt","format"],answer:2,explain:"정답은 3번 interrupt입니다. 에이전트가 글 발행·결제·삭제처럼 위험하거나 중요한 행동을 하기 직전에 멈추고 사람이 검토하게 하는 것이 HITL이며, LangGraph에서는 interrupt(예: writer 노드 앞에 interrupt_before)로 멈춤 지점을 만듭니다. 멈춘 시점의 상태는 checkpointer가 저장해 두므로, 사람이 승인하면 그 지점부터 이어서 실행됩니다. 완전 자동화보다 느리지만, 실수 비용이 큰 작업에서는 필수적인 안전장치입니다."},{type:"short",q:"에이전트가 초안을 만든 뒤 스스로 비평(critique)하고 그 피드백을 반영해 다시 생성하는 과정을 반복하며 품질을 끌어올리는 패턴의 이름은?",answer:"Reflection",explain:"정답은 Reflection(리플렉션, 성찰) 패턴입니다. Generate(생성)와 Reflect(비평) 두 역할의 LLM 호출을 번갈아 돌려, 초안의 약점을 지적받고 고치기를 반복합니다. Andrew Ng의 실험처럼 같은 모델이라도 이런 반복 워크플로로 감싸면 한 번에 답할 때보다 결과 품질이 크게 오릅니다. 다만 호출이 늘어나 비용이 커지므로, 수업 예제처럼 반복 상한(예: 2회)을 둬 비용과 무한루프를 통제합니다."},{type:"short",q:"Harness Engineering에서 서로 독립적인 여러 작업을 여러 에이전트(노드)에 나눠 동시에 실행시키는 병렬 실행 패턴을 무엇이라 부르는가? (영어로)",answer:"Fan-out",explain:'정답은 Fan-out(팬아웃)입니다. 부챗살처럼 하나의 지점에서 여러 갈래로 작업을 동시에 펼친다는 뜻으로, 예를 들어 "3개 회사 조사"를 에이전트 3개가 동시에 진행하면 순차 실행보다 훨씬 빨라집니다. 서로 결과를 기다릴 필요가 없는 독립 작업일 때만 효과가 있으며, 펼친 결과들은 다시 한 노드로 모아(fan-in) 종합합니다. 멀티 에이전트를 실전 규모로 확장할 때 Supervisor 라우팅과 함께 쓰이는 핵심 패턴입니다.'}],capstone:[{type:"ox",q:"MCP(Model Context Protocol)는 AI 애플리케이션이 외부 도구·데이터에 표준화된 방식으로 연결되도록 해 주는 프로토콜로, MCP Server가 기능을 제공하고 MCP Client가 이를 사용한다.",answer:"O",explain:'맞습니다. MCP가 없으면 도구·데이터마다 제각각의 연동 코드를 짜야 하지만, MCP는 "AI용 USB 규격"처럼 연결 방식을 표준화합니다. 기능(도구·데이터)을 제공하는 쪽이 MCP Server, 그것을 갖다 쓰는 쪽(에이전트·앱)이 MCP Client입니다. 캡스톤에서는 @mcp.tool()로 도구를 정의한 MCP Server를 만들고, FastAPI 백엔드의 에이전트가 MCP Client로 그 도구를 호출하도록 통합했습니다.'},{type:"ox",q:"SSE(Server-Sent Events)는 서버가 클라이언트로 데이터를 단방향으로 계속 흘려보내는 스트리밍 방식이며, 각 이벤트는 data: 로 시작하고 빈 줄로 끝을 표시한다.",answer:"O",explain:'맞습니다. SSE는 한 번의 HTTP 연결을 열어 두고 서버가 준비되는 대로 조각을 계속 보내는 방식으로, ChatGPT처럼 답변이 토큰 단위로 타이핑되듯 나타나는 UI를 만들 때 씁니다. 형식은 아주 단순해서 각 조각을 "data: 내용" 뒤에 빈 줄을 붙여 보내면 브라우저가 이벤트 하나가 끝났음을 압니다. 캡스톤에서는 FastAPI에서 llm.astream()의 토큰을 이 형식으로 yield 하는 /stream 엔드포인트를 구현했습니다.'},{type:"ox",q:"Stateless Session 전환이란 대화 상태를 서버 메모리 안에 쌓아 두는 방식으로 바꾸는 것이어서, 서버를 여러 대로 늘리는 수평 확장에 유리하다.",answer:"X",explain:'틀렸습니다. 방향이 반대입니다. Stateless(무상태) 전환은 대화 상태를 서버 메모리에서 "빼내어" DB·Redis 같은 외부 저장소에 두는 것입니다. 상태가 서버 메모리에 있으면 사용자가 매번 같은 서버에 붙어야 해서 서버를 여러 대로 늘리기 어렵지만, 상태를 밖으로 빼면 어떤 서버가 요청을 받아도 저장소에서 세션(Thread)을 꺼내 이어갈 수 있습니다. 그래서 실서비스의 수평 확장 대비 단계에서 이 전환을 다뤘습니다.'},{type:"choice",q:"MCP Server를 설계할 때 기능을 나누어 담는 세 가지 구성요소로 올바른 것은?",choices:["HTML · CSS · JavaScript","Tool · Resource · Prompt","CPU · RAM · Disk","Model · View · Controller"],answer:1,explain:'정답은 2번입니다. MCP Server는 기능을 세 종류로 나눠 제공합니다. Tool은 에이전트가 호출해 무언가를 실행하는 동작(검색·계산·등록 등), Resource는 읽어 갈 수 있는 데이터(문서·설정 등), Prompt는 재사용 가능한 프롬프트 템플릿입니다. 캡스톤 설계 시간에 "이 기능은 실행이니 Tool로, 이건 조회 데이터니 Resource로"처럼 분리 의사결정을 연습한 이유가 바로 이 구조 때문입니다.'},{type:"choice",q:"SSE 스트리밍 응답을 보낼 때 브라우저가 스트림으로 인식하도록 지정하는 Content-Type은?",choices:["application/json","text/html","text/event-stream","image/png"],answer:2,explain:"정답은 3번 text/event-stream입니다. FastAPI에서는 StreamingResponse(generator, media_type='text/event-stream')처럼 지정하며, 이 헤더를 보고 브라우저가 응답을 한 번에 받는 문서가 아니라 계속 흘러오는 이벤트 스트림으로 처리합니다. 함께 Cache-Control: no-cache를 줘서 중간에서 조각을 캐시하지 않고 즉시 전달되게 합니다. application/json은 일반 API 응답, text/html은 웹 페이지에 쓰는 타입이라 스트리밍 인식이 되지 않습니다."},{type:"choice",q:"Observability(관측성)에 대한 설명으로 가장 올바른 것은?",choices:["서비스 화면을 예쁘게 디자인하는 것","추적(trace)·로그·평가 지표로 AI 서비스가 내부에서 무엇을 했는지 들여다보고 품질을 진단할 수 있게 하는 것","코드를 짧게 줄여 쓰는 것","사용자 수를 늘리기 위한 마케팅 활동"],answer:1,explain:'정답은 2번입니다. 에이전트는 내부에서 여러 번의 LLM 호출과 도구 호출을 거치므로, 답이 이상할 때 어느 단계가 문제였는지 겉만 봐서는 알 수 없습니다. Observability는 실행 과정을 추적·로그·지표로 기록해 "어떤 프롬프트로 어떤 도구를 불렀고 몇 토큰을 썼는지"를 들여다보게 해 줍니다. 캡스톤에서는 LangSmith 환경변수(LANGSMITH_TRACING=true)만 켜면 모든 실행이 자동 추적되는 것을 실습했고, 여기에 응답 품질 평가(Eval)까지 붙여 운영 체계를 만들었습니다.'},{type:"short",q:"직접 만든 MCP Server의 도구 목록을 웹 화면에서 확인하고 직접 호출해 점검할 수 있는 개발용 검사 도구의 이름은? (MCP OOO)",answer:"MCP Inspector",explain:'정답은 MCP Inspector(인스펙터)입니다. 터미널에서 mcp dev server.py 를 실행하면 웹 화면이 열리고, Tools 탭에서 내가 정의한 도구가 제대로 등록됐는지 보고 인자를 넣어 직접 호출해 볼 수 있습니다. 에이전트에 붙이기 전에 서버 단독으로 먼저 점검하면, 문제가 생겼을 때 "서버 문제인지 에이전트 문제인지"를 쉽게 구분할 수 있습니다. 프론트엔드의 브라우저 개발자도구처럼, MCP 개발의 기본 점검 도구라고 생각하면 됩니다.'},{type:"short",q:"일시적 오류로 재시도할 때 대기 시간을 1초 → 2초 → 4초처럼 점점 늘려 가며 서버 부담을 줄이는 재시도 전략을 무엇이라 하는가?",answer:"지수 백오프",explain:"정답은 지수 백오프(exponential backoff)입니다. 실패하자마자 즉시 재시도를 반복하면 가뜩이나 힘든 서버에 요청이 몰려 장애가 더 커질 수 있습니다. 그래서 기다리는 시간을 2배씩(지수적으로) 늘려 가며 재시도해, 일시적 장애(순간 과부하·네트워크 흔들림)가 자연히 회복될 시간을 벌어 줍니다. 캡스톤 Day3의 Error Handling 시간에 예외·타임아웃 처리와 함께 실서비스 안정화의 기본기로 배운 내용입니다."}],miniproject:[{type:"ox",q:"Agentic RAG는 검색 결과를 에이전트가 스스로 평가해, 근거가 부실하면 질문을 재작성해 다시 검색하는 판단 루프를 도는 방식이다.",answer:"O",explain:'맞습니다. 기본(naive) RAG는 한 번 검색해 그 결과로 바로 답을 만들기 때문에 첫 검색이 실패하면 답도 함께 나빠집니다. Agentic RAG는 "검색 → 결과 채점 → 부실하면 질문 재작성(rewrite) → 재검색"의 루프를 넣어 검색 오류에서 스스로 회복합니다. 미니프로젝트 Day1에서 이 루프로 검색 오류의 원인을 찾고 성능을 끌어올렸으며, max_tries 상한을 둬 무한 루프를 막는 것이 함께 가는 안전장치입니다.'},{type:"ox",q:"Supervisor-워커 구조에서 워커 에이전트 하나가 조사·작성·검수 등 가능한 한 많은 역할을 한꺼번에 맡을수록 좋은 설계다.",answer:"X",explain:'틀렸습니다. 워커는 "한 가지 일만" 맡는 것이 좋은 설계입니다. 역할이 좁을수록 각 에이전트의 프롬프트가 단순·명확해지고, 문제가 생겼을 때 어느 에이전트 탓인지 디버깅하기 쉬워집니다. 그래서 미니프로젝트 설계 시간에도 워커 역할을 3개 이내로 나누라고 안내했습니다. 여러 역할을 한 에이전트에 몰아넣는 것은 결국 "프롬프트만 긴 단일 에이전트"로 돌아가는 것과 같습니다.'},{type:"ox",q:"RAG를 개선했는지는 느낌으로 판단하기보다, 같은 평가셋으로 개선 전/후의 정확도·충실도·관련성 점수를 측정해 숫자로 비교해야 한다.",answer:"O",explain:'맞습니다. "답이 좋아진 것 같다"는 인상만으로는 어떤 조치가 효과적이었는지 알 수 없고, 오히려 다른 질문에서 나빠졌을 수도 있습니다. 그래서 미니프로젝트에서는 같은 질문 세트로 개선 전 점수를 먼저 재고(기준선), 청킹 크기·top-k·하이브리드 검색 중 하나만 바꿔 다시 측정해 전/후를 한 표에 나란히 정리했습니다. 이렇게 하면 "어떤 조치가 점수를 얼마나 올렸는지"를 숫자로 증명할 수 있어 평가 결과물 제출로도 이어집니다.'},{type:"choice",q:"Supervisor 멀티에이전트를 설계할 때 반드시 정해 두어야 하는 것으로 가장 올바른 조합은?",choices:["라우팅 규칙(언제 어느 워커에게 넘길지)과 종료 조건(언제 FINISH 할지)","사용할 키보드와 마우스 브랜드","에이전트 이름의 글자 수","발표 자료의 배경 색상"],answer:0,explain:'정답은 1번입니다. Supervisor는 매 턴 "지금 상태에서 다음에 누구를 부를지"를 결정하므로, 그 판단 기준인 라우팅 규칙을 조건표로 명확히 적어 두어야 합니다. 그리고 언제 일을 끝낼지 종료 조건(예: 검수 워커가 통과를 반환하면 FINISH)이 없으면 에이전트들이 일을 영원히 주고받는 무한 루프에 빠집니다. 미니프로젝트 Day2 설계 시간에 이 두 가지와 공유 상태 스키마를 산출물로 제출한 이유가 여기에 있습니다.'},{type:"choice",q:"멀티에이전트가 함께 쓰는 공유 상태(State) 스키마에 담을 항목으로 가장 적절한 조합은?",choices:["강의실 좌석 배치도와 점심 메뉴","messages(대화 기록) · next(다음에 실행할 에이전트) · 중간 산출물 필드","각 팀원의 생년월일","CSS 폰트 크기와 색상 코드"],answer:1,explain:'정답은 2번입니다. 공유 상태는 에이전트들이 주고받는 "공용 작업 보드"이므로, 지금까지의 대화·도구 결과(messages), Supervisor의 라우팅 결정(next), 그리고 조사 결과·초안 같은 중간 산출물을 담습니다. 각 필드는 이름과 타입을 미리 정의(TypedDict 방식)해 두어야 워커들이 서로 무엇을 읽고 써야 하는지 혼동하지 않습니다. 설계 단계에서 이 스키마를 먼저 확정해야 Day3 구현에서 노드·엣지를 바로 배치할 수 있습니다.'},{type:"choice",q:"최종 발표의 라이브 데모를 지키는 안전장치로 적절하지 않은 것은?",choices:["try/except 폴백으로 오류 시 친절한 대체 메시지를 보여준다","핵심 시연 질문의 답을 미리 캐시(DEMO_CACHE)해 네트워크 사고에 대비한다","정상 동작 화면을 미리 녹화한 백업 영상을 준비한다","리허설 없이 발표 직전에 새 기능을 추가해 즉석에서 처음 시연한다"],answer:3,explain:'정답은 4번입니다. 검증 안 된 코드를 발표 직전에 넣는 것은 데모를 지키는 게 아니라 사고를 부르는 행동입니다. 수업에서 강조한 데모 안전장치는 반대 방향으로, 외부 API·모델이 죽어도 화면이 멈추지 않게 하는 try/except 폴백, 핵심 질문의 답을 미리 담아 둔 시연 캐시, 그리고 백업 녹화본과 사전 리허설입니다. 심지어 "일부러 오류를 내고도 대체 응답이 나오는 견고함"을 시연 시나리오에 넣을 정도로, 발표는 준비된 안전망 위에서 하는 것입니다.'},{type:"short",q:'RAG 정량 평가 지표 중 "답변이 검색된 문맥에 없는 내용을 지어내지 않았는가"를 재는 지표의 이름은? (한글 또는 영어)',answer:"충실도(faithfulness)",explain:'정답은 충실도(faithfulness)입니다. 답이 아무리 그럴듯해도 검색해 온 근거에 없는 내용을 섞었다면 환각이며, 충실도 점수가 깎입니다. 미니프로젝트에서는 정확도·충실도·관련성(answer relevancy) 세 지표를 개선 전/후로 측정해 평가표를 만들었습니다. 충실도가 낮으면 "컨텍스트만 근거로 답하고, 근거가 없으면 모른다고 답하라"는 프롬프트 제약을 강화하는 식으로 생성 단계를 개선합니다.'},{type:"short",q:"외부 API 호출이 실패했을 때 서비스가 멈추는 대신 미리 준비한 대체 응답을 보여주는 처리를 무엇이라 하는가? (한글 또는 영어)",answer:"폴백(fallback)",explain:'정답은 폴백(fallback)입니다. safe_run 예제처럼 try/except로 감싸 실패 원인은 로그에 남기고, 사용자 화면에는 "잠시 후 다시 시도해주세요" 같은 친절한 대체 메시지를 돌려줍니다. 오류를 숨기는 게 아니라, 오류가 나도 서비스 전체가 죽지 않게 완충 장치를 두는 것입니다. 특히 발표 데모에서는 폴백에 시연 캐시까지 더해, 네트워크·API 사고가 나도 데모가 끊기지 않도록 준비합니다.'}]},b={git:{challenges:["개인 리포를 만들어 feature 브랜치 → main PR 병합 흐름을 혼자서 한 바퀴 돌려 본다 (커밋 3개 이상).","두 브랜치에서 같은 줄을 일부러 다르게 고쳐 충돌(conflict)을 만들고, 직접 해결해 병합한다.",".gitignore를 작성해 node_modules·.env가 커밋되지 않는 것을 확인한다 — 이미 추적 중인 파일 제외 방법(git rm --cached)까지."],links:[{t:"Learn Git Branching (한국어, 게임처럼 브랜치 연습)",u:"https://learngitbranching.js.org/?locale=ko"},{t:"Pro Git 책 (공식, 한국어판 무료)",u:"https://git-scm.com/book/ko/v2"}]},htmlcss:{challenges:["종합실습(중고차 목록 관리)의 완성본에 localStorage 저장을 직접 추가해 새로고침에도 데이터가 남게 개선해 본다.","TodoList 실습을 Emmet만 사용해 5분 안에 뼈대부터 다시 만들어 본다.","CodePen에서 마음에 드는 카드 UI 하나를 골라 안 보고 재현한 뒤, 원본과 비교해 차이를 분석한다."],links:[{t:"모던 자바스크립트 Deep Dive 소스 (poiemaweb)",u:"https://poiemaweb.com/"},{t:"CodePen — 다른 사람 컴포넌트 뜯어보기",u:"https://codepen.io/"}]},vue:{challenges:["HTML/CSS 과목에서 만든 TodoList를 Vue 컴포넌트(App > TodoInput + TodoList)로 다시 만든다.","props로 데이터를 내리고 emit으로 이벤트를 올리는 부모-자식 통신을 직접 구현해 본다.","vue-router로 목록/상세 2페이지를 만들어 URL 파라미터로 항목을 조회한다."],links:[{t:"Vue.js 공식 문서 (한국어)",u:"https://ko.vuejs.org/"},{t:"수업 공식 소스 — skala-vue (강병호 강사)",u:"https://github.com/kbh73/skala-vue"}]},webproject:{challenges:["본인이 만들고 싶은 서비스 하나를 골라 핵심 화면 3개를 손그림/피그마로 설계해 본다.",'그 서비스에 필요한 REST API 3개를 "메서드 + 경로 + 요청/응답 JSON" 형식으로 명세한다.',"설계한 화면 1개를 HTML/CSS로 실제 코딩해 설계와 구현의 간극을 체험한다."],links:[{t:"Figma (무료 와이어프레임 도구)",u:"https://www.figma.com/"},{t:"부트캠프 학습 사이트 (드림아이티비즈)",u:"https://bootcamp.dreamitbiz.com/"}]},python:{challenges:["Practice 3의 Pandas 풀이 전체를 Polars Lazy API로만 다시 작성하고 timeit으로 몇 배 빠른지 실측한다.",'sales_100k.csv의 region 결측 1만 건을 ① 제거 ② "미상"으로 대체 — 두 전략의 집계 결과 차이를 비교 분석한다.',"종합실습 2 파이프라인에 RandomForest 외 모델(LinearRegression·GradientBoosting)을 추가해 성능표를 만든다."],links:[{t:"파이썬 학습 사이트 (드림아이티비즈)",u:"https://python-study.dreamitbiz.com/"},{t:"통계 학습 사이트 — 기초통계 과목 대비",u:"https://statistics.dreamitbiz.com/"},{t:"Pandas 공식 사용자 가이드",u:"https://pandas.pydata.org/docs/user_guide/"}]},prompt:{challenges:["임원 보고서 압축 프롬프트(결론1+포인트5+리스크3+액션3)를 자신의 전공/경험 문서에 적용해 본다.","같은 질문을 temperature 0.2 / 0.7 / 1.2로 세 번 보내 답변 차이를 표로 정리한다 (API 또는 플레이그라운드).",'[자료 시작]~[자료 끝] + "없으면 확인 필요" 패턴으로 할루시네이션이 실제로 차단되는지 실험한다.'],links:[{t:"OpenAI Cookbook (프롬프트 실전 예제)",u:"https://cookbook.openai.com/"},{t:"Anthropic 프롬프트 엔지니어링 가이드",u:"https://docs.anthropic.com/"}]},transformer:{challenges:["The Illustrated Transformer를 읽고 Self-Attention 계산 흐름을 손으로 한 번 따라 그려 본다.","한국어 문장 하나를 GPT·BERT 계열 토크나이저로 각각 토큰화해 분절 차이를 비교한다.","문장 5개를 임베딩해 코사인 유사도 행렬을 만들고, 직관과 일치하는지 확인한다."],links:[{t:"The Illustrated Transformer (시각 설명의 고전)",u:"https://jalammar.github.io/illustrated-transformer/"},{t:"Hugging Face 무료 코스",u:"https://huggingface.co/learn"}]},sllm:{challenges:["Hugging Face PEFT 문서의 LoRA 튜토리얼을 소형 모델로 끝까지 재현해 본다.","같은 모델의 양자화 전/후(예: 4bit) 답변 품질과 메모리 사용량을 비교표로 만든다.",'"프롬프트로 해결 vs 파인튜닝 필요"의 판단 기준을 자기 언어로 5줄 정리한다.'],links:[{t:"Hugging Face PEFT (LoRA 공식 문서)",u:"https://huggingface.co/docs/peft"},{t:"Hugging Face 무료 코스",u:"https://huggingface.co/learn"}]},vectordb:{challenges:["자기 관심 분야 문서 100개(뉴스·논문 초록 등)를 임베딩해 코사인 유사도 검색기를 만들어 본다.","chunk 크기(200/500/1000자)를 바꿔 같은 질문의 검색 품질이 어떻게 달라지는지 실험한다.","메타데이터 필터(날짜·카테고리)를 걸어 검색 결과가 좁혀지는 것을 확인한다."],links:[{t:"Chroma 공식 문서 (가장 가벼운 벡터 DB)",u:"https://docs.trychroma.com/"},{t:"FAISS (Meta 벡터 검색 라이브러리)",u:"https://github.com/facebookresearch/faiss"}]},langchain:{challenges:["수업 체인에 대화 메모리를 붙여 이전 질문을 기억하는 챗봇으로 확장한다.","계산기·검색 등 도구 2개를 가진 미니 에이전트를 만들어 도구 선택 로그를 관찰한다.","기존 체인 하나를 LCEL(파이프 연산자) 스타일로 재작성해 가독성을 비교한다."],links:[{t:"LangChain 공식 문서",u:"https://python.langchain.com/"}]},"spring-ai":{challenges:["SpringBoot REST API에 Spring AI 챗 엔드포인트(/api/chat)를 추가해 본다.","프롬프트 템플릿을 코드 밖(리소스 파일)으로 분리해 재배포 없이 문구를 바꿔 본다.","스트리밍 응답(SSE)으로 토큰이 실시간 출력되게 개선한다."],links:[{t:"Spring AI 공식 프로젝트",u:"https://spring.io/projects/spring-ai"},{t:"자바 학습 사이트 (드림아이티비즈)",u:"https://java-study.dreamitbiz.com/"}]},"ml-dl":{challenges:["타이타닉 데이터로 전처리→학습→평가 분류 파이프라인을 처음부터 혼자 구축한다.","일부러 과적합을 만든 뒤(깊은 트리) 규제·가지치기로 잡아 학습/검증 곡선 변화를 관찰한다.","단일 홀드아웃 vs 5-fold 교차검증의 성능 추정 차이를 비교한다."],links:[{t:"scikit-learn 공식 튜토리얼",u:"https://scikit-learn.org/stable/tutorial/"},{t:"통계 학습 사이트 (드림아이티비즈)",u:"https://statistics.dreamitbiz.com/"}]},feature:{challenges:["sales_100k의 order_date에서 파생변수 5개(요일·월·분기·주말여부·월초/말)를 만들어 모델 성능 변화를 본다.","범주 인코딩 3종(원핫·라벨·빈도)을 같은 데이터에 적용해 장단점을 표로 정리한다.","데이터 누수(leakage)를 일부러 만들어 성능이 비정상적으로 좋아지는 것을 재현하고 원인을 설명한다."],links:[{t:"Kaggle Learn — Feature Engineering (무료 실습)",u:"https://www.kaggle.com/learn"}]},modeldev:{challenges:["같은 모델을 GridSearchCV vs RandomizedSearchCV로 튜닝해 시간 대비 성능을 비교한다.",'완성 모델의 "모델 카드"(용도·데이터·성능·한계)를 1페이지로 작성해 본다.',"모델 3개의 성능·학습시간·해석가능성을 트레이드오프 표로 만들어 선택 근거를 설명한다."],links:[{t:"scikit-learn 하이퍼파라미터 튜닝 가이드",u:"https://scikit-learn.org/stable/modules/grid_search.html"}]},serving:{challenges:["학습한 모델(joblib)을 FastAPI 엔드포인트로 감싸 curl로 예측을 받아 본다.","그 API를 Dockerfile로 컨테이너화해 로컬에서 이미지 빌드→실행까지 완주한다.","동일 요청을 100번 반복 호출해 평균 응답시간을 재고 병목 지점을 추정해 본다."],links:[{t:"FastAPI 공식 문서 (한국어 지원)",u:"https://fastapi.tiangolo.com/ko/"},{t:"AWS 자격증 학습 사이트 — 클라우드 배포 심화",u:"https://aws.dreamitbiz.com/"}]},rag:{challenges:['자기 문서 10개로 미니 RAG를 만들어 "문서에 있는 질문 vs 없는 질문"의 답변 차이를 확인한다.',"chunk 크기와 top_k를 바꿔가며 답변 품질 실험표를 만든다 (최소 4조합).","답변에 출처(어느 문서 몇 번째 청크)가 표시되도록 인용 기능을 붙인다."],links:[{t:"LangChain RAG 튜토리얼",u:"https://python.langchain.com/"}]},agent:{challenges:['도구 2개(계산·검색)를 가진 에이전트를 만들고 ReAct 로그에서 "생각→행동→관찰" 흐름을 추적한다.',"에이전트가 실패하는 입력을 3개 찾아내고, 가드레일(입력 검증·재시도)을 추가해 방어한다.","멀티 에이전트(작성자+검토자) 구조로 확장해 단일 에이전트와 결과 품질을 비교한다."],links:[{t:"LangGraph 공식 문서",u:"https://langchain-ai.github.io/langgraph/"},{t:"AI 에이전트 완주 학습 사이트 (드림아이티비즈)",u:"https://ai-agents.dreamitbiz.com/"}]},capstone:{challenges:['팀 프로젝트 아이디어를 "문제→대상 사용자→핵심 기능 3개→기술 스택" 1페이지로 기획해 본다.',"후보 기술 스택 2안을 학습곡선·성능·팀 역량 기준으로 비교표를 만들어 결정 근거를 남긴다.","프로젝트 리스크 5개와 각각의 완화 방안을 미리 목록화한다 (일정·기술·데이터·협업·발표)."],links:[{t:"부트캠프 학습 사이트 (드림아이티비즈)",u:"https://bootcamp.dreamitbiz.com/"}]},miniproject:{challenges:['이번 분석을 "질문→데이터→방법→발견→한계" 구조의 1페이지 리포트로 재정리한다.',"만든 차트 중 하나를 골라 제목·축·강조색을 개선해 전달력 전/후를 비교한다.","분석 저장소를 다른 사람이 clone 후 5분 안에 재현할 수 있게 README와 requirements를 정비한다."],links:[{t:"데이터 분석 실습 사이트 (드림아이티비즈)",u:"https://data.dreamitbiz.com/"}]}},g={fontSize:18,fontWeight:800,color:"var(--navy-800)",margin:"28px 0 4px"},C={ox:"O/X",choice:"4지선다",short:"단답"};function S({e:n,title:t="📋 종합실습 평가기준 · 제출물"}){var a,o,l,c,m;if(!n)return null;const i=(a=n.criteria)==null?void 0:a.some(s=>s.points);return e.jsxs(e.Fragment,{children:[e.jsxs("h3",{style:g,children:[t,n.variant&&e.jsx("span",{style:{marginLeft:8,padding:"3px 10px",borderRadius:999,fontSize:12,fontWeight:700,background:"var(--navy-100)",color:"var(--navy-700)",verticalAlign:"middle"},children:n.variant})]}),n.purpose&&e.jsxs("div",{className:"box box-tips",style:{marginTop:12},children:[e.jsx("div",{className:"box-h",children:"🎯 실습 목적"}),e.jsx("p",{style:{margin:0,whiteSpace:"pre-line"},children:e.jsx(p,{text:n.purpose})})]}),((o=n.tasks)==null?void 0:o.length)>0&&e.jsxs(e.Fragment,{children:[e.jsx("div",{className:"exam-sub",children:"실습 구성"}),e.jsx("div",{className:"exam-tablewrap",children:e.jsxs("table",{className:"exam-table",children:[e.jsx("thead",{children:e.jsxs("tr",{children:[e.jsx("th",{children:"실습명"}),e.jsx("th",{children:"주요 활동"}),e.jsx("th",{children:"시간"})]})}),e.jsx("tbody",{children:n.tasks.map((s,r)=>e.jsxs("tr",{children:[e.jsx("td",{children:s.name}),e.jsx("td",{style:{whiteSpace:"pre-line"},children:e.jsx(p,{text:s.activity})}),e.jsx("td",{children:s.time})]},r))})]})})]}),((l=n.criteria)==null?void 0:l.length)>0&&e.jsxs(e.Fragment,{children:[e.jsx("div",{className:"exam-sub",children:"평가 항목"}),e.jsx("div",{className:"exam-tablewrap",children:e.jsxs("table",{className:"exam-table",children:[e.jsx("thead",{children:e.jsxs("tr",{children:[e.jsx("th",{children:"평가 항목"}),e.jsx("th",{children:"평가 내용"}),i&&e.jsx("th",{children:"배점"})]})}),e.jsx("tbody",{children:n.criteria.map((s,r)=>e.jsxs("tr",{children:[e.jsx("td",{children:s.item}),e.jsx("td",{style:{whiteSpace:"pre-line"},children:e.jsx(p,{text:s.desc})}),i&&e.jsx("td",{children:s.points})]},r))})]})})]}),((c=n.deliverables)==null?void 0:c.length)>0&&e.jsxs("div",{className:"box box-practice",style:{marginTop:12},children:[e.jsx("div",{className:"box-h",children:"📦 제출 · 필수 항목"}),e.jsx("ul",{children:n.deliverables.map((s,r)=>e.jsx("li",{children:e.jsx(p,{text:s})},r))})]}),((m=n.notes)==null?void 0:m.length)>0&&e.jsxs("div",{className:"exam-notes",children:[e.jsx("b",{children:"유의사항"}),e.jsx("ul",{children:n.notes.map((s,r)=>e.jsx("li",{children:e.jsx(p,{text:s})},r))})]})]})}function w({qs:n}){return n!=null&&n.length?e.jsxs(e.Fragment,{children:[e.jsxs("h3",{style:g,children:["📝 복습 퀴즈"," ",e.jsx("span",{style:{fontSize:13,fontWeight:600,color:"var(--ink-soft)"},children:"(질문을 눌러 정답·해설 펼치기)"})]}),e.jsx("div",{className:"quiz",children:n.map((t,i)=>{var a;return e.jsxs("details",{className:"quiz-item",children:[e.jsxs("summary",{children:[e.jsx("span",{className:"quiz-tag",children:C[t.type]||"퀴즈"})," Q",i+1,". ",t.q]}),t.type==="choice"&&t.choices&&e.jsx("ol",{className:"quiz-choices",children:t.choices.map((o,l)=>e.jsx("li",{children:o},l))}),e.jsxs("div",{className:"quiz-ans",children:[e.jsxs("b",{children:["정답",t.type==="choice"?` ${t.answer+1}.`:""]})," ",t.type==="choice"?(a=t.choices)==null?void 0:a[t.answer]:String(t.answer)]}),t.explain&&e.jsx("p",{className:"quiz-exp",children:t.explain})]},i)})})]}):null}function I({x:n}){var t,i;return n?e.jsxs(e.Fragment,{children:[e.jsxs("h3",{style:g,children:["🚀 추가 학습 — 더 해보기"," ",e.jsx("span",{style:{fontSize:13,fontWeight:600,color:"var(--ink-soft)"},children:"(과목을 내 것으로 만드는 도전 과제)"})]}),((t=n.challenges)==null?void 0:t.length)>0&&e.jsxs("div",{className:"box box-practice",style:{marginTop:12},children:[e.jsx("div",{className:"box-h",children:"💪 도전 과제"}),e.jsx("ol",{children:n.challenges.map((a,o)=>e.jsx("li",{children:a},o))})]}),((i=n.links)==null?void 0:i.length)>0&&e.jsxs("div",{className:"box box-tips",style:{marginTop:12},children:[e.jsx("div",{className:"box-h",children:"🔗 심화 자료"}),e.jsx("ul",{children:n.links.map((a,o)=>e.jsx("li",{children:e.jsxs("a",{href:a.u,target:"_blank",rel:"noreferrer",style:{color:"var(--gold)",fontWeight:700},children:[a.t," ↗"]})},o))})]})]}):null}function H({subjectId:n,day:t,totalDays:i}){const a=t===1||t===i?A[n]:null,o=t===1||t===i?f[n]:null,l=t===i?_[n]:null,c=t===i?b[n]:null;return!a&&!o&&!l&&!c?null:e.jsxs(e.Fragment,{children:[e.jsx(S,{e:a}),o&&e.jsxs(e.Fragment,{children:[e.jsx(S,{e:o,title:"📑 전임교수 평가안 (참고)"}),e.jsx("p",{style:{marginTop:8,fontSize:12.5,color:"var(--ink-soft)"},children:"※ 같은 과목이라도 담당교수에 따라 평가 체계가 다를 수 있습니다. 위 기본안과 함께 평가 방향을 참고하세요."})]}),e.jsx(w,{qs:l}),e.jsx(I,{x:c})]})}const U={transformer:{title:"종합실습 안내 — LLM과 Transformer 아키텍처",source:"박병선 전임교수 배포본 기준",intro:"아래 코드(첨부 노트북·데이터)를 바탕으로 Transformer 구조를 이해해 봅니다.",files:[{label:"종합실습_LLM과_Transformer_아키텍처_언어모델.ipynb",href:"/materials/transformer_minimodel_practice.ipynb"},{label:"input_kr.txt (학습용 코퍼스)",href:"/materials/input_kr.txt"}],groups:[{h:"■ 조별 Activity — baseline 개선 실험 & 발표 (마감 16:30)",note:"아래 개선 후보 리스트를 참고하여 2가지 이상 조별로 적용해 봅니다.",items:['모델 크기 — n_layer · n_head · n_embd 를 줄이거나 늘려보기 (GPTConfig 셀) · "작은 모델도 문장을 외울까?"',"학습 반복 수 — MAX_ITERS 를 100 / 500 / 2000 등으로 바꾸며 loss 곡선과 생성 문장 비교","문맥 길이 (block_size) — 32 / 128 / 256 등으로 바꿔 짧은/긴 문맥이 생성 품질에 주는 영향 확인","생성 파라미터 (temperature, top_k) — 낮추면 안정적·뻔한 문장, 높이면 다양한 문장. 트레이드오프 확인","데이터 양 — corpus 전체 대신 절반만 잘라 학습, 데이터가 적을 때 특징(암기 vs 일반화) 비교","causal mask 유무 비교 — Block 클래스의 CausalSelfAttention 을 SelfAttention 으로 바꿔 mask 영향 보기(미래를 미리 보면?)"],after:"발표 형식(조당 5~10분): 무엇을 바꿨나(변수+값) / 예상 / 실제 결과(loss 곡선 스크린샷 + 생성 문장 before·after) / 해석(예상과 달랐던 부분과 이유)"},{h:"■ 개인 Activity — 1~2page 개발해석 보고서",note:"하기 내용을 1~2 page 로 작성합니다.",items:["Transformer 구조 설명(자신의 말로) — 토큰화 → 임베딩 → Self-Attention → Causal Mask → Logits/Loss 흐름","조별 실험 과정에서 내가 관찰한 것 — 우리 조가 바꾼 변수는 무엇이고 어떻게 달라졌나, 생성 결과에서 확인한 특징","결과에서 확인한 한계","향후 개선 방향과 그 이유 — 가능하면 다른 조 발표 내용을 최소 1개 인용해 비교"]}],deadline:"개인 제출마감: 7.26(일) 24:00",submit:"제출: 슬랙 7반 종합실습 안내글 스레드에 댓글로 보고서(1~2p) + (선택) 본인이 개선/추가 실험한 코드 파일. 파일명 예: Transformer_7반_홍길동"}},D={htmlcss:{concepts:[{term:"Block vs Inline 요소",desc:"Block 요소(div, p, h1 등)는 한 줄을 통째로 차지하고 새 줄에서 시작하지만, Inline 요소(span, a, img 등)는 글자처럼 흐름 속에 끼어들어 폭·높이 지정이 무시된다. div는 구획을 나누는 대표 Block 컨테이너, span은 텍스트 일부를 감싸는 대표 Inline 컨테이너다."},{term:"CSS Box Model",desc:"모든 HTML 요소는 안쪽부터 Content(실제 내용) → Padding(내용과 테두리 사이 여백) → Border(테두리) → Margin(다른 요소와의 간격) 네 겹의 상자로 감싸져 있다. 브라우저 개발자도구의 Elements > Computed 탭에서 이 상자를 눈으로 확인하며 여백 문제를 잡는다."},{term:"Specificity(명시도)",desc:"같은 요소에 여러 CSS 규칙이 겹칠 때 어떤 스타일이 이기는지 정하는 점수 체계로, 인라인 스타일 > id 선택자 > 클래스 선택자 > 태그 선택자 순으로 강하다. 높은 자릿수가 무조건 승리해서 클래스 1개가 태그 15개보다 세고, !important 남용은 디버깅을 어렵게 하므로 피한다."},{term:"Flexbox",desc:"부모에 display: flex 한 줄만 주면 자식들이 가로(row) 또는 세로(column)로 유연하게 정렬되는 레이아웃 방식이다. justify-content로 주축 정렬, align-items로 교차축 정렬을 조절하며, 보통 페이지 전체 뼈대는 Grid, 내부 요소 정렬은 Flexbox를 쓴다."},{term:"DOM(Document Object Model)",desc:"브라우저가 HTML 문서를 트리 구조의 객체로 바꿔 놓은 것으로, JavaScript가 document.getElementById() 같은 메서드로 요소를 찾아 내용·구조·스타일을 실시간으로 바꿀 수 있게 해 준다. 화면이 동적으로 변하는 모든 웹 앱의 출발점이다."},{term:"이벤트 리스너와 비동기(async/await)",desc:"클릭·입력 같은 사용자 행동은 addEventListener()로 감지하는 방식이 권장되며, HTML 속성(onclick)이나 DOM 프로퍼티 방식보다 여러 핸들러 등록과 유지보수에 유리하다. 서버 통신처럼 오래 걸리는 일은 fetch()와 async/await로 기다리는 동안 다른 작업을 막지 않게 처리한다."},{term:"시맨틱 태그(Semantic Elements)",desc:"div와 span은 아무 의미 없는 상자지만, header·nav·main·section·article·aside·footer는 태그 이름 자체가 그 영역의 역할을 설명하는 시맨틱 태그다. 코드 구조가 한눈에 읽히고(가독성), 스크린 리더 같은 보조 기술이 화면 구조를 정확히 파악하며(접근성), 검색 엔진이 핵심 콘텐츠를 효율적으로 찾는다(SEO). 이미지·도표에는 figure와 figcaption을 짝지어 설명을 붙인다."},{term:"CSS Position 5가지와 z-index",desc:"position은 요소의 배치 유형을 정한다 — static(기본값, top·left 무시), relative(원래 자리 기준으로 이동하되 원래 공간은 유지), absolute(문서 흐름에서 빠져 가장 가까운 position 지정 조상 기준으로 배치), fixed(뷰포트 기준이라 스크롤해도 고정), sticky(스크롤에 따라 relative와 fixed 사이를 전환 — 상단 고정 헤더의 단골). 요소가 겹칠 때 위아래 순서는 z-index 숫자가 큰 쪽이 위로 온다."},{term:"CSS Grid — 2차원 레이아웃",desc:"Flexbox가 한 방향(1차원)만 다룬다면 Grid는 행과 열을 동시에 다루는 2차원 레이아웃이다. 부모에 display: grid를 선언하고 grid-template-columns/rows로 트랙을 나누며, 배치 기준은 칸(셀)의 개수가 아니라 칸을 나누는 선(Grid Line)의 번호다. 실무 관례는 페이지 전체 뼈대는 Grid, 내부 요소 정렬은 Flexbox로 나눠 쓰는 것이다."},{term:"Transition과 @keyframes 애니메이션",desc:"transition은 속성값이 바뀔 때 그 변화를 지정한 시간 동안 부드럽게 이어 주는 기능으로, property·duration·timing-function·delay 네 속성을 transition: width 2s linear 1s처럼 한 줄 축약형으로 쓴다. 보통 :hover와 짝지어 마우스를 올리면 서서히 커지는 효과를 만든다. 더 복잡한 다단계 움직임은 @keyframes로 장면(0%, 50%, 100%)을 정의하고 animation-name·duration·iteration-count 속성으로 재생한다."},{term:"반응형 웹 — Viewport 메타태그와 미디어 쿼리",desc:"반응형 웹은 하나의 HTML 소스로 PC·태블릿·스마트폰 화면 크기에 맞춰 레이아웃이 자동으로 바뀌게 하는 방식으로, 핵심 기술은 Viewport 메타태그·미디어 쿼리·유연한 레이아웃(Grid·Flex) 셋이다. head 안에 meta name=viewport, content=width=device-width, initial-scale=1.0을 넣지 않으면 반응형이 아예 동작하지 않는다. 레이아웃이 바뀌는 기준점을 브레이크포인트라 하며 교재는 767px 이하 모바일, 768~1023px 태블릿, 1024px 이상 데스크톱 구간을 예시로 든다."},{term:"var·let·const와 스코프",desc:"let과 const는 ES6에서 도입된 선언 방식으로 중괄호 블록 안에서만 살아 있는 블록 스코프를 가지며, 같은 스코프에서 재선언할 수 없다. const는 재할당까지 금지라 기본은 const로 선언하고 값이 바뀌는 변수만 let을 쓴다. var는 함수 스코프에 호이스팅 시 초기화까지 되어 선언 전에 접근해도 오류가 안 나는 함정이 있어 현대 JavaScript에서는 권장하지 않는다."},{term:"브라우저 렌더링 4단계",desc:"브라우저는 서버에서 받은 HTML을 네 단계로 화면에 그린다 — ① 파싱: HTML을 읽어 DOM 트리를, CSS를 읽어 CSSOM을 만든다 ② 렌더 트리: DOM과 CSSOM을 합쳐 실제로 화면에 그릴 요소만 추린다 ③ 레이아웃: 각 요소의 위치와 크기(박스)를 계산한다 ④ 페인트: 색·글자·테두리 같은 픽셀을 실제로 칠한다. HTML을 쓴 순서와 구조가 그대로 화면 구조가 되고, script 태그는 파싱을 멈춰 세우므로 보통 body 닫는 태그 직전에 두거나 defer를 붙인다. 2일 과정에서 배우는 HTML(구조)·CSS(표현)·JS(동작)는 이 렌더링 과정에 그대로 대응한다."},{term:"URL의 6가지 구성 요소",desc:"URL은 웹 자원의 위치를 가리키는 주소이며 스킴·호스트·포트·경로·쿼리·프래그먼트로 나뉜다. 스킴은 사용할 프로토콜(http, https, file), 호스트는 서버의 도메인 이름 또는 IP, 포트는 서버 포트 번호(http는 80, https는 443이 기본이라 보통 생략), 경로는 서버 안에서 자원이 놓인 위치, 쿼리는 물음표 뒤에 key=value 형태로 붙는 추가 정보, 프래그먼트는 샵 기호 뒤에 붙어 문서 안의 특정 위치를 가리킨다. 도메인 이름은 DNS를 거쳐 실제 IP 주소로 바뀐 뒤 접속이 이뤄진다."},{term:"Promise 3상태와 async/await",desc:"Promise는 비동기 작업이 성공하거나 실패할 때 결과를 알려주겠다고 약속하는 객체로, 대기(Pending)·이행(Fulfilled)·거부(Rejected) 세 상태를 가진다. async를 함수 앞에 붙이면 그 함수는 항상 Promise를 반환하고, await는 async 함수 안에서만 쓰여 Promise가 처리될 때까지 잠시 멈춘다. 실패는 try/catch로 잡아 화면에 대체 문구를 띄운다. 함정 하나 — fetch는 404 응답도 통신 자체는 성공으로 보기 때문에 반드시 response.ok로 HTTP 상태를 따로 확인해야 한다."},{term:"이벤트 버블링·캡처링과 이벤트 위임",desc:"버블링은 이벤트가 자식에서 부모 방향으로 퍼지는 기본 동작이고, 캡처링은 반대로 부모에서 자식 방향이며 addEventListener의 세 번째 인자를 true로 주면 쓴다. 이 성질을 이용해 자식마다 리스너를 다는 대신 부모에 리스너 하나만 걸고 event.target으로 대상을 판별하는 방식이 이벤트 위임이다. event.target.closest('.item')은 자신을 포함해 가장 가까운 조상 중 일치하는 요소를, event.target.matches('.btn')은 그 요소가 선택자와 맞는지를 알려 준다. 목록 항목이 100개여도 리스너는 목록 하나면 충분하고, 렌더링으로 새로 만들어진 요소도 다시 등록할 필요 없이 그대로 동작한다."},{term:"localStorage와 상태 기반 렌더링",desc:"localStorage는 브라우저에 데이터를 영구 저장하는 빌트인 객체로 setItem(key, value)·getItem(key)·removeItem(key)·clear()를 쓴다. 문자열만 저장되므로 객체나 배열은 JSON.stringify로 넣고 JSON.parse로 꺼낸다. 여기서 더 중요한 원칙은 상태 기반 렌더링이다 — 화면은 항상 상태(예: goals 배열)를 그린 결과여야 하며, DOM을 직접 고쳐서 화면만 바꾸면 상태와 화면이 어긋난다. 상태를 바꾼 직후 save()와 render()를 호출하는 순서를 습관으로 굳히면 새로고침 시 목록이 사라지는 사고가 사라진다."}],examples:[{title:"회원가입 폼 — Form 요소와 Input 타입",lang:"html",code:`<!-- form: 사용자 입력을 모아 서버로 보내는 컨테이너 -->
<form action="/signup" method="post">
  <!-- label의 for와 input의 id를 맞추면 라벨 클릭 시 입력칸에 포커스 -->
  <label for="userId">아이디</label>
  <!-- required: 비우면 제출 불가, placeholder: 흐린 안내 문구 -->
  <input type="text" id="userId" name="userId" placeholder="영문 4자 이상" required>

  <!-- type="password": 입력 글자를 점으로 가려 준다 -->
  <label for="pw">비밀번호</label>
  <input type="password" id="pw" name="pw" required>

  <!-- type="email": 브라우저가 이메일 형식을 자동 검사 -->
  <label for="email">이메일</label>
  <input type="email" id="email" name="email">

  <!-- select: 여러 항목 중 하나를 고르는 드롭다운 -->
  <label for="emailDomain">도메인</label>
  <select id="emailDomain" name="emailDomain">
    <option value="gmail.com">gmail.com</option>
    <option value="naver.com">naver.com</option>
  </select>

  <!-- radio: name이 같은 것끼리 한 개만 선택된다 -->
  <input type="radio" id="male" name="gender" value="m"> <label for="male">남</label>
  <input type="radio" id="female" name="gender" value="f"> <label for="female">여</label>

  <!-- type="submit": 누르면 form의 action 주소로 데이터 전송 -->
  <button type="submit">가입하기</button>
</form>`,note:"교재 3장 Form 단원의 회원가입 화면 실습을 압축한 코드로, name 속성이 서버로 전송되는 데이터의 key가 된다. Vue에서는 이 입력들을 v-model로 데이터에 바로 묶고 @submit.prevent로 새로고침 없이 처리하게 되므로, 여기서 input 타입과 name·required의 역할을 확실히 잡아 두면 폼 컴포넌트 작성이 쉬워진다."},{title:"카드 정렬 — Box Model + Specificity + Flexbox",lang:"css",code:`/* 태그 선택자(점수 1): 모든 div 카드의 기본 상자 설정 */
div.card {
  width: 200px;              /* 콘텐츠 영역 너비 (padding·border는 별도) */
  padding: 16px;             /* 내용과 테두리 사이 안쪽 여백 */
  border: 2px solid navy;    /* 두께·스타일·색을 한 줄로 쓰는 축약형 */
  margin: 10px;              /* 카드와 카드 사이 바깥 간격 */
}

/* 클래스 선택자(점수 10): 위 태그 규칙보다 명시도가 높아 색이 이긴다 */
.highlight {
  border-color: crimson;     /* 강조 카드만 테두리를 붉게 덮어쓴다 */
  background: #fff0f0;       /* 배경도 살짝 붉은 톤으로 변경 */
}

/* id 선택자(점수 100): 클래스보다도 강해 이 카드 하나만 확실히 승리 */
#best {
  border-width: 4px;         /* 최고 카드만 테두리를 더 두껍게 */
}

/* 부모에 flex를 주면 자식 카드들이 가로로 유연하게 배치된다 */
.card-list {
  display: flex;             /* 이 한 줄이 Flexbox 시작점 */
  flex-wrap: wrap;           /* 폭이 모자라면 다음 줄로 자동 줄바꿈 */
  justify-content: center;   /* 주축(가로) 방향 가운데 정렬 */
  align-items: stretch;      /* 교차축(세로)으로 카드 높이를 맞춤 */
  gap: 12px;                 /* 카드 사이 간격을 margin 없이 지정 */
}`,note:"교재 5~6장의 Box Model·Specificity·Flexbox 데모를 하나의 카드 목록으로 합친 예제로, 태그(1점)·클래스(10점)·id(100점) 점수 경쟁을 직접 확인할 수 있다. Vue의 scoped style도 결국 이 명시도 규칙 위에서 동작하고, 컴포넌트 카드 목록 레이아웃은 거의 항상 display: flex로 시작하므로 여기서 감을 잡아 두면 그대로 통한다."},{title:"실시간 날씨 — DOM·이벤트·fetch 비동기",lang:"javascript",code:`// HTML의 <select id="city">와 <div id="weather-box">를 DOM에서 찾아온다
const citySelect = document.getElementById("city");
const box = document.getElementById("weather-box");

// 도시 이름 → 위도/경도 좌표를 담은 JavaScript 객체
const cities = {
  seoul: { name: "서울", lat: 37.57, lon: 126.98 },
  busan: { name: "부산", lat: 35.18, lon: 129.08 },
};

// async 함수: 안에서 await로 서버 응답을 기다릴 수 있다
async function showWeather(key) {
  const c = cities[key];                       // 선택된 도시 정보 꺼내기
  box.innerHTML = "로딩 중...";                 // 기다리는 동안 안내 문구 표시
  // 백틱 대신 문자열 연결(+)로 Open-Meteo 요청 주소를 조립
  const url = "https://api.open-meteo.com/v1/forecast?latitude=" + c.lat +
    "&longitude=" + c.lon + "&current=temperature_2m,relative_humidity_2m";
  const response = await fetch(url);           // 서버 응답이 올 때까지 대기
  const data = await response.json();          // JSON 본문을 객체로 변환
  // DOM 조작(innerHTML)으로 진짜 온도·습도를 화면에 그린다
  box.innerHTML = c.name + " 현재 기온 " + data.current.temperature_2m +
    "도, 습도 " + data.current.relative_humidity_2m + "%";
}

// 권장 방식인 addEventListener로 change 이벤트를 등록
citySelect.addEventListener("change", function () {
  showWeather(citySelect.value);               // 바뀐 도시로 날씨 갱신
});

showWeather("seoul");                          // 첫 화면은 서울 날씨로 시작`,note:"교재 8장 마지막 [과제] 실시간 날씨를 한 파일로 축약한 코드로, select의 change 이벤트 → fetch/await → innerHTML 갱신이라는 교재의 3단계 흐름을 그대로 따른다. Vue에서는 이 패턴이 @change 핸들러와 반응형 데이터로 바뀌지만, 내부에서 하는 일(이벤트 감지·비동기 요청·화면 갱신)은 완전히 동일하다. 교재는 이후 이 파일을 weatherAPI.js와 realtimeInfo.js 모듈로 분리하는데, 그 감각이 Vue 컴포넌트 분리로 이어진다."},{title:"시맨틱 태그로 페이지 뼈대 잡기",lang:"html",code:`<!-- 의미 없는 div 대신, 이름이 곧 역할인 시맨틱 태그로 구조를 잡는다 -->
<header>
  <!-- header: 로고·제목이 들어가는 머리 영역 -->
  <h1>SKALA 기술 블로그</h1>
  <!-- nav: 다른 페이지로 이동하는 링크 모음 영역 -->
  <nav>
    <a href="/">홈</a> <a href="/posts">글 목록</a>
  </nav>
</header>
<!-- main: 이 문서의 핵심 콘텐츠, 페이지에 하나만 둔다 -->
<main>
  <!-- section: 논리적으로 관련 있는 내용의 묶음 -->
  <section>
    <h2>최신 글</h2>
    <!-- article: 떼어 놓아도 독립적으로 성립하는 콘텐츠 단위 -->
    <article>
      <h3>CSS Grid 입문</h3>
      <p>행과 열을 함께 다루는 2차원 레이아웃을 배워 보자.</p>
      <!-- figure + figcaption: 이미지와 그 설명을 한 묶음으로 -->
      <figure>
        <img src="grid.png" alt="그리드 예시 그림">
        <figcaption>그림 1. 3x2 그리드 레이아웃</figcaption>
      </figure>
    </article>
  </section>
  <!-- aside: 본문과 직접 관련이 약한 곁가지 영역(광고·추천 글) -->
  <aside>추천 글: Flexbox 복습하기</aside>
</main>
<!-- footer: 저작권·연락처가 들어가는 꼬리 영역 -->
<footer>ⓒ 2026 SKALA</footer>`,note:"교재 4장 HTML 심화의 시맨틱 레이아웃 태그 표를 한 페이지 뼈대로 조립한 예제다. 태그 이름만 봐도 어디가 머리·본문·곁가지·꼬리인지 드러나므로 가독성·접근성·SEO 세 마리 토끼를 잡는다. Vue 컴포넌트를 만들 때도 최상위 구조를 이 시맨틱 태그로 잡는 습관이 그대로 이어진다."},{title:"Grid + 미디어 쿼리 — 화면 크기별로 변하는 카드 갤러리",lang:"css",code:`/* 상단 헤더: sticky는 스크롤 위치에 따라 relative와 fixed를 오간다 */
.site-header {
  position: sticky;                    /* 조건부 고정 배치 */
  top: 0;                              /* 화면 맨 위에 붙는 기준점 */
  z-index: 10;                         /* 겹칠 때 카드보다 위에 오도록 */
}
/* 카드 갤러리: Grid는 행과 열을 함께 다루는 2차원 레이아웃 */
.gallery {
  display: grid;                       /* 이 한 줄이 Grid의 시작 선언 */
  grid-template-columns: 1fr 1fr 1fr;  /* 남은 공간을 1:1:1로 나눈 3열 */
  gap: 16px;                           /* 셀과 셀 사이 간격 */
}
/* 대표 카드는 그리드 '라인 번호'로 넓게 배치한다 */
.gallery .hero {
  grid-column: 1 / 3;                  /* 1번 선에서 3번 선까지 = 두 칸 차지 */
}

/* 브레이크포인트 1: 767px 이하(모바일)는 1열로 */
@media (max-width: 767px) {
  .gallery {
    grid-template-columns: 1fr;        /* 좁은 화면에선 세로 한 줄 */
  }
}

/* 브레이크포인트 2: 768~1023px(태블릿)는 2열로 */
@media (min-width: 768px) and (max-width: 1023px) {
  .gallery {
    grid-template-columns: 1fr 1fr;    /* 중간 화면은 2열 */
  }
}`,note:"교재 6장 CSS 심화의 Grid와 반응형 웹(미디어 쿼리 브레이크포인트) 단원을 하나로 합친 예제다. 이 CSS가 동작하려면 HTML head에 viewport 메타태그(width=device-width, initial-scale=1.0)가 반드시 있어야 한다는 것이 교재의 강조점. 그리드는 셀 개수가 아니라 선(Grid Line) 번호로 구역을 잡는다는 점을 hero 카드에서 확인하자."},{title:"let·const와 배열 메서드(forEach·map·filter)로 점수 가공",lang:"javascript",code:`// const: 재할당 금지 — 값이 안 바뀌는 변수는 무조건 const로 선언
const scores = [72, 95, 60, 88, 41];
// let: 값이 바뀌는 변수만 let (var는 함수 스코프 함정 때문에 비권장)
let total = 0;

// forEach: 모든 요소를 순회하며 함수를 실행한다 (반환값 없음)
scores.forEach(function (s) {
  total = total + s;                    // 점수를 하나씩 합계에 누적
});
console.log("평균: " + total / scores.length);  // 문자열은 + 로 연결

// map: 모든 요소를 규칙대로 가공한 '새 배열'을 반환한다
const boosted = scores.map(function (s) {
  return s + 5;                         // 전원 5점 가산점
});

// filter: 조건을 통과한 요소만 골라 '새 배열'로 반환한다
const passed = boosted.filter(function (s) {
  return s >= 70;                       // 70점 이상만 합격
});
console.log(passed);                    // [77, 100, 93] 처럼 출력

// 화살표 함수로 쓰면 같은 일을 한 줄로 줄일 수 있다
const evens = scores.filter(s => s % 2 === 0);  // 짝수 점수만 추출

{
  let inner = "블록 안";               // let은 이 중괄호 안에서만 존재
}
// console.log(inner);  // 오류! 블록 밖 접근 불가 — 이것이 블록 스코프`,note:"교재 7장 JavaScript 기초의 변수 선언(let·const·스코프)과 배열 반복 메서드 단원을 한 흐름으로 묶었다. forEach는 반환값이 없고 map·filter는 원본을 건드리지 않고 새 배열을 만든다는 차이가 핵심 — 이 세 메서드는 Vue에서 목록 렌더링용 데이터를 다듬을 때 매일 쓰게 된다."},{title:"종합실습(중고차 목록 관리) — 수업 중 정오표·평가 안내 (7/16 강사 공지)",lang:"text",code:`[종합실습 과제] 제공된 index.html · style.css · app.js의 미구현 부분을 채워
중고차 목록 관리 화면을 완성한다.

[배점] HTML 20점 · CSS 20점 · JavaScript 60점
· HTML: 입력 폼, 검색 영역, 목록 영역 구성
· CSS: 2열 레이아웃, 차량 카드, 반응형 구성
· JS: 차량 등록, 입력값 검증, 수정, 수정 취소, 삭제, 검색, 판매 상태 필터
· 색상·간격·크기는 예시 화면과 완전히 같지 않아도 됨 — 기능 동작이 기준
· 브라우저 저장(localStorage)은 사용하지 않음 — 새로고침 시 예시 데이터로 복귀

[수업 중 정오표 — 권기창 교수 공지 3건]
1. 문제 7페이지 요구사항 7번: alert 창 → confirm 창으로 해결할 것
2. app.js의 const maxYear = new Date().getFullYear() + 1
   → + 1 을 제거하고 new Date().getFullYear() 로 수정
3. 11페이지 "이미지 준비중"은 무시하고 구현

[제출 형식] SKALA번호_반_이름_HTML.zip (예: 123_3반_홍길동_HTML.zip)`,note:"판교 1~5반 종합실습에서 실시간으로 공지된 정오표와 평가 기준을 모았다. 특히 maxYear의 +1 제거는 미래 연식 차량 입력 검증이 틀어지는 원인이었고, alert→confirm은 사용자의 확인/취소 선택이 필요한 삭제 흐름 때문이다 — 요구사항의 '의도'를 읽는 연습이기도 하다."},{title:"Emmet 단축 입력 5규칙 — 실습교수 치트시트 (김영희 교수)",lang:"text",code:`VS Code에서 HTML 뼈대를 몇 글자로 만들어 내는 Emmet 핵심 5가지.

1. !  →  Tab : HTML 기본 뼈대 (압도적 1위)
   <!DOCTYPE html>부터 <body>까지 한 번에. lang="en"은 ko로 바꿀 것.

2. .클래스명 : class 있는 div
   .card → <div class="card"></div>
   .card>img+h1+p → 카드 구조 한 번에 (#todoList처럼 #은 id)

3. ul>li*4 : 자식(>)과 반복(*)
   메뉴·목록·TodoList 등 반복 구조 필수기. ul.menu>li*4>a 로 확장 가능.

4. + 와 {텍스트} : 형제 나열과 내용 채우기
   h1{오늘 할 일}+input+button{추가}
   → <h1>오늘 할 일</h1><input type="text"><button>추가</button>

5. $ : 자동 번호 매기기
   ul>li{항목 $}*3 → 항목 1 / 항목 2 / 항목 3
   갤러리 카드 여러 장은 section>div.photo-card$*4 식으로 응용.

[종합 예시 — 한 줄이 교재 예제 6의 HTML 전체]
div.todo-app>h1{📝 오늘 할 일}+div.input-row>input+button{추가}^ul#todoList>li{할 일 $}*3
(^ 는 한 단계 위로 올라가기)`,note:"판교 4반 김영희 실습교수가 수업 중 정리해 공유한 치트시트다. JSFiddle/CodePen이 숨겨주던 HTML 뼈대를 VS Code에서 직접 만들 때 Emmet이 그 다리 역할을 한다. 다섯 규칙만 손에 붙으면 마크업 작성 속도가 몇 배로 빨라진다.",origin:"practice",source:"김영희 실습교수 — HTML/CSS 수업 배포 자료 (2026-07-21 대표 확인)"},{title:"웹 학습 참고 링크 모음 — 실습교수·수강생 공유 (7/15~16)",lang:"text",code:`[실습교수 공유]
· 모던 자바스크립트 Deep Dive 소스: https://poiemaweb.com/
  (김영희·김범준 교수 추천 — JS 개념을 깊게 파고들 때 표준 레퍼런스)
· JavaScript Numbers: https://www.w3schools.com/js/js_numbers.asp (권기창 교수)
· HTML Color Codes: https://htmlcolorcodes.com/ (권기창 교수 — CSS 색상 고를 때)
· M1-02 HTML 실습 1 TodoList 만들기(노션): 권기창 교수 실습 문서
· HTML+Tailwind CSS 정리(노션): 김범준 교수
· Emmet 정리(노션): 김영희·김범준 교수

[수강생 공유 — 판교 3반 정민교]
· CodePen: https://codepen.io/
  "다른 사람들이 만든 컴포넌트를 뜯어보면서 공부할 수 있고, 웹에서 바로
  코딩해볼 수 있어 리버스 엔지니어링하며 익히기 좋다"

[다음 과목 예고 — 판교 4반 김영희 교수]
· 다음 주 월·화 이은호 교수 강의 — 공유된 교재가 수업 자료이며 Quiz는 교재 오픈북`,note:"여러 반 채널에 흩어져 있던 링크를 한 카드로 모았다. poiemaweb은 JS를 제대로 파려는 사람에게 한국어 자료 중 가장 깊이 있는 레퍼런스이고, CodePen 공유는 수강생이 직접 올린 팁이라 더 값지다 — 다른 사람 코드를 뜯어보는 것이 웹 학습의 지름길이라는 건 현업에서도 통하는 방법이다."},{title:"종합실습 시작 파일 해부 — TODO 24개 지도 (exam_htmlcssjs.zip)",lang:"text",code:`배포된 시작 파일 3개(index.html 82줄 · style.css 198줄 · app.js 197줄)에는
'수강생 구현 #N' TODO가 총 24개 들어 있다. 골격은 이미 제공된다.

[HTML — 10개] 페이지 헤더 / 제조사 목록 / 모델명 / 연식 / 주행거리 / 가격 /
연료 목록 / 판매 상태 목록 / 등록·취소 버튼 / 차량 검색·필터 목록

[CSS — 7개] 입력·목록 영역 2열 배치 / form-row / form-row label /
button-area / search-area / car-list / car-card

[JS — 8개] ① 차량 등록·수정(submit) ② 카드 안 수정·삭제 버튼 처리
③ 검색어 입력 시 목록 재출력 ④ 판매 상태 변경 시 목록 재출력
⑤ 수정 취소 ⑥ 입력 검증 후 차량 객체 반환 ⑦ 차량 정보 수정 설정 ⑧ 차량 삭제

[이미 제공되는 골격 — 먼저 읽을 것]
const cars = [...]        // 예시 데이터 배열
let editingId = null      // 수정 모드 상태
function renderCars()     // 목록 그리기 (완성돼 있음)
function getFilteredCars()// 검색·필터 적용 (완성돼 있음)
function createCarCard()  // 카드 DOM 생성 (완성돼 있음)

[공략 순서 추천] 제공된 renderCars→getFilteredCars→createCarCard를 먼저 읽고,
HTML 폼(#1~10) → CSS 배치(#1~7) → JS는 데이터 흐름 순서(⑥검증→①등록→②버튼→
⑦수정→⑤취소→⑧삭제→③④검색·필터)로 채우면 자연스럽다.`,note:"시작 zip을 직접 열어 TODO를 전수 조사한 지도다. 백지에서 짜는 시험이 아니라 '제공된 골격을 읽고 빈칸을 채우는' 시험이므로, 완성된 render 함수들이 어떤 데이터 구조(cars 배열·editingId)를 기대하는지 먼저 파악하는 것이 최고의 공략법이다. 정오표(alert→confirm, maxYear +1 제거)와 함께 보자."},{title:"CSS 수업 예제 3종 — 종합 레이아웃·Flex 비교·Transition (김영희 교수 배포)",lang:"css",code:`/* 수업 중 배포된 예제 파일 3개의 핵심만 발췌 (skala-4 폴더 보관)
   ① css_complete_example.html — 레이아웃 종합(flex·grid·position 5개 섹션)
   ② css_flex_exe1.html — Flex Box 속성 비교 실험
   ③ Trsnsition.html — 2D/3D 전환·애니메이션 4종 비교 */

/* ③에서 — hover 전환(transition)과 자동 반복(animation)의 차이 */
.t2d { transition: transform 0.35s; }             /* 마우스 올릴 때만 */
.t2d:hover { transform: translateY(-8px) scale(1.07); }

.t3d { transition: transform 0.5s; transform-style: preserve-3d; }
.t3d:hover { transform: rotateY(28deg) rotateX(12deg); }  /* 3D는 perspective 필요 */

.a2d { animation: move2d 1.4s ease-in-out infinite alternate; } /* 항상 반복 */
@keyframes move2d {
  from { transform: translateX(0) rotate(0deg); }
  to   { transform: translateX(16px) rotate(8deg); }
}

/* 부모에 perspective를 줘야 자식의 3D 회전이 원근감 있게 보인다 */
.wrap { display: flex; gap: 14px; flex-wrap: wrap; perspective: 600px; }`,note:"판교 4반 수업에서 배포된 실습 파일 3종이다. ①은 flex-container/grid-container/position(sticky·absolute)·반응형 카드까지 한 파일에 담은 종합 레퍼런스, ②는 justify-content 등 Flex 속성을 값만 바꿔 비교하는 실험 파일, ③은 transition(이벤트 기반)과 animation(자동 반복)의 차이를 2D/3D로 보여준다. 종합실습의 car-card·2열 배치 CSS를 채울 때 ①을 옆에 두면 된다.",origin:"practice",source:"김영희 실습교수 — HTML/CSS 수업 배포 자료 (2026-07-21 대표 확인)"},{title:"스터디 플래너 ① 목표 추가 — submit 검증과 상태 갱신",lang:"javascript",code:`// 폼의 submit 이벤트를 받아 새 목표를 배열에 추가한다
form.addEventListener("submit", (event) => {
  // 폼 기본 동작(페이지 새로고침)을 막는다 — 안 막으면 화면이 깜빡인다
  event.preventDefault();
  // 입력값 앞뒤 공백을 제거해 공백만 입력한 경우를 걸러낸다
  const title = input.value.trim();
  if (title === "") {
    // 에러 문구를 보이게 하고 입력칸으로 커서를 되돌린 뒤 여기서 중단
    errorEl.hidden = false;
    input.focus();
    return;
  }
  // 정상 입력이면 에러 문구를 숨긴다
  errorEl.hidden = true;
  // 상태 배열에 목표 객체를 추가 — id는 현재 시각(ms)으로 고유값을 만든다
  goals.push({
    id: Date.now(),
    title: title,
    category: category.value,
    done: false,
  });
  // 입력칸을 비우고, 저장한 뒤, 화면을 다시 그린다 (이 3단 순서가 핵심)
  input.value = "";
  save();
  render();
});`,note:"2일 완성 교재의 플래너 실습 4단계다. preventDefault·trim 검증·상태 추가·save+render라는 네 동작이 한 핸들러에 모여 있어, 이후 모든 기능(토글·삭제·필터)이 같은 패턴을 반복한다. id에 Date.now()를 쓰는 이유는 삭제·토글 시 배열에서 대상을 찾을 고유 키가 필요하기 때문이다."},{title:"스터디 플래너 ② 이벤트 위임으로 토글·삭제·필터 한 번에",lang:"javascript",code:`// 목록 전체에 리스너를 딱 하나만 건다 — 항목이 몇 개든 이것으로 충분
listEl.addEventListener("click", (event) => {
  // 클릭 지점에서 가장 가까운 항목(li.item)을 거슬러 찾는다
  const li = event.target.closest(".item");
  // 목록 여백을 클릭한 경우엔 li가 없으므로 무시한다
  if (!li) return;
  // data-id 속성에 저장해 둔 목표 id를 숫자로 변환
  const id = Number(li.dataset.id);

  // 눌린 대상이 체크박스면 완료 상태를 토글한다
  if (event.target.matches(".item-check")) {
    const goal = goals.find((g) => g.id === id);
    goal.done = event.target.checked;
    save();
    render();
  }

  // 눌린 대상이 삭제 버튼이면 해당 id만 빼고 새 배열을 만든다
  if (event.target.matches(".item-del")) {
    goals = goals.filter((g) => g.id !== id);
    save();
    render();
  }
});

// 필터 탭도 똑같은 위임 패턴 — 탭 묶음에 리스너 하나
tabsEl.addEventListener("click", (event) => {
  const tab = event.target.closest(".tab");
  if (!tab) return;
  // 상태 변수만 바꾸고 화면 갱신은 render에 맡긴다
  filter = tab.dataset.filter; // "all" 또는 "active" 또는 "done"
  // 눌린 탭에만 활성 클래스를 남긴다
  document.querySelectorAll(".tab").forEach((t) => {
    t.classList.toggle("is-active", t === tab);
  });
  render();
});

// 현재 필터에 맞는 목표만 걸러 돌려주는 함수
function visible() {
  if (filter === "active") return goals.filter((g) => !g.done);
  if (filter === "done") return goals.filter((g) => g.done);
  return goals;
}`,note:"항목마다 리스너를 달면 나중에 추가한 항목만 클릭이 안 되는 전형적인 버그가 난다 — 렌더링으로 DOM이 새로 만들어지기 때문이다. 부모 한 곳에 위임하면 재등록이 필요 없다. closest는 '어느 항목인가', matches는 '무슨 버튼을 눌렀나'를 나눠 판별하는 역할 분담으로 외워 두면 편하다."},{title:"스터디 플래너 ③ 상태를 화면으로 — render와 XSS 방어",lang:"javascript",code:`// 화면 그리기는 오직 이 함수 하나에서만 한다 (상태 기반 렌더링)
function render() {
  // 현재 필터를 통과한 목표만 가져온다
  const items = visible();
  // 목록을 비우고 처음부터 다시 그린다
  listEl.innerHTML = "";
  items.forEach((goal) => {
    const li = document.createElement("li");
    // 완료 여부에 따라 취소선 클래스를 붙인다
    li.className = goal.done ? "item is-done" : "item";
    // 이벤트 위임에서 쓸 id를 data 속성에 심어 둔다
    li.dataset.id = goal.id;
    // 사용자 입력은 escapeHtml을 거쳐서 넣는다 — 그대로 innerHTML에 넣으면 위험
    li.innerHTML =
      "<span class=\\"item-text\\">" + escapeHtml(goal.title) + "</span>" +
      "<span class=\\"badge\\">" + goal.category + "</span>";
    listEl.appendChild(li);
  });
  // 목록이 비었을 때만 안내 문구를 보여 준다
  emptyEl.hidden = items.length > 0;
  // 진행률 표시 갱신
  updateProgress();
}

// 사용자 입력에 섞인 특수문자를 안전한 표기로 바꾼다
function escapeHtml(text) {
  // 빈 요소를 하나 만들어 텍스트로만 넣으면
  const div = document.createElement("div");
  // 브라우저가 태그로 해석하지 않고 문자 그대로 보관해 준다
  div.textContent = text;
  // 안전하게 변환된 문자열을 다시 꺼내 쓴다
  return div.innerHTML;
}`,note:"화면은 언제나 상태(goals)를 그린 결과여야 한다는 원칙이 이 함수에 담겨 있다. 사용자가 입력한 글자를 innerHTML에 그대로 넣으면 script 태그가 실행되는 XSS 취약점이 생기므로, textContent를 쓰거나 escapeHtml을 반드시 거쳐야 한다 — 최종 과제 평가에도 명시된 항목이다."},{title:"스터디 플래너 ④ fetch·async/await·localStorage",lang:"javascript",code:`// 오늘의 팁을 JSON 파일에서 비동기로 불러온다
async function loadTip() {
  const tipEl = document.getElementById("tip");
  try {
    // await는 응답이 올 때까지 이 줄에서 잠시 멈춘다
    const response = await fetch("data/tips.json");
    // 404도 fetch는 성공으로 보기 때문에 상태를 직접 확인해야 한다
    if (!response.ok) throw new Error("HTTP " + response.status);
    // 응답 본문을 JSON 객체로 변환 (이것도 비동기라 await)
    const tips = await response.json();
    // 오늘 날짜를 팁 개수로 나눈 나머지 → 날마다 다른 팁이 나온다
    const today = new Date().getDate() % tips.length;
    tipEl.textContent = tips[today];
  } catch (error) {
    // 실패해도 화면이 비지 않도록 대체 문구를 보여 준다
    tipEl.textContent = "팁을 불러오지 못했습니다.";
    console.error(error);
  }
}

// 저장 키는 한 곳에 상수로 모아 오타를 막는다
const STORAGE_KEY = "skala-planner";
// 시작할 때 저장된 목록을 먼저 읽어 상태를 채운다
let goals = load();
let filter = "all";

function load() {
  // localStorage에는 문자열만 들어가므로 꺼낼 때 JSON.parse로 되돌린다
  const saved = localStorage.getItem(STORAGE_KEY);
  return saved ? JSON.parse(saved) : [];
}

function save() {
  // 배열을 문자열로 바꿔서 저장 — 이 호출을 빠뜨리면 새로고침 시 목록이 사라진다
  localStorage.setItem(STORAGE_KEY, JSON.stringify(goals));
}`,note:"fetch의 response.ok 확인과 try/catch 대체 문구는 실무에서 그대로 쓰이는 최소 방어 코드다. localStorage는 문자열 전용이라는 점, 그리고 상태를 바꾼 직후 save를 부르는 습관 두 가지만 지키면 저장 관련 사고는 거의 나지 않는다."},{title:"최종 제출 과제 — 스터디 플래너 확장(F5~F7) 힌트",lang:"javascript",code:`// [출발점] 기능을 붙이기 전에 goal 객체의 필드부터 늘리는 것이 순서다
goals.push({
  id: Date.now(),
  title: title,
  category: category.value,
  due: dueInput.value, // F5: input type="date"가 주는 "2026-07-31" 형식 문자열
  done: false,
});

// [F6] 검색 — 기존 visible() 결과에 조건을 하나 더 얹는다
const keyword = searchInput.value.trim();
// includes로 제목에 검색어가 들어있는 목표만 남긴다
list = list.filter((g) => g.title.includes(keyword));

// [F7] 분류별 남은 개수 — reduce로 집계한다
// 완료되지 않은 목표만 골라, 카테고리를 key로 개수를 누적
const rest = goals.filter((g) => !g.done).reduce((acc, g) => {
  acc[g.category] = (acc[g.category] || 0) + 1;
  return acc;
}, {});
// 결과 예시: { HTML: 2, CSS: 1, JS: 3 }

// [F5 지난 목표 강조] 오늘보다 마감일이 이르면 상태 클래스를 붙인다
const today = new Date().toISOString().slice(0, 10); // "2026-07-20"
li.classList.toggle("is-overdue", Boolean(goal.due) && goal.due < today && !goal.done);`,note:"과제는 2일간 만든 플래너를 출발점으로 삼되 새 문법을 배울 필요는 없다 — 폼, Flexbox/Grid, 미디어 쿼리, 배열 메서드, 이벤트 위임, localStorage만으로 충분히 해결된다. 막히면 F5는 2·3장(폼 태그·classList), F6은 7·8장(filter·includes·input 이벤트), F7은 7장(reduce)으로 돌아가 그 코드를 먼저 다시 실행해 보는 것이 가장 빠른 길이다."},{title:"최종 제출 과제 — 요구사항·제출 규격·루브릭·자주 하는 실수",lang:"text",code:`[시나리오] 수업에서 만든 스터디 플래너에 운영진 요청을 반영한다
- 목표에 마감일이 있으면 좋겠다
- 목표가 쌓이면 검색이 필요하다
- 분류별로 몇 개 남았는지 요약을 보여 달라
- 휴대폰에서도 볼 수 있어야 한다

[필수 기능] F1~F4는 수업에서 만든 것, F5~F7이 새로 추가할 부분
F1 목표 추가 — 빈 값·공백만 입력은 거부하고 에러 메시지 표시
F2 완료 토글·삭제 — 이벤트 위임으로 리스너는 목록에 하나만
F3 필터·진행률 — 전체 / 진행중 / 완료, 완료 비율 표시
F4 localStorage 저장 — 새로고침해도 목록이 유지될 것
F5 마감일 입력 — input type="date" 추가, 지난 목표는 시각적으로 강조
F6 검색 — 입력한 글자가 포함된 목표만 실시간으로 걸러 보이기
F7 분류별 요약 — HTML / CSS / JS 각각 남은 개수를 집계해 표시

[화면·품질 요구]
구조: 시맨틱 태그로 구성(header/main/section/footer), 모든 입력에 label 연결(for+id 또는 aria-label)
스타일: 색·간격은 :root의 CSS 변수로 정의해 재사용, 클래스명은 하이픈 규칙(goal-item, btn-primary)
레이아웃: Flexbox 또는 Grid로 배치 — 요소를 띄우는 편법 금지
반응형: 375px에서 가로 스크롤이 없고 조작 가능할 것
동작: 콘솔에 빨간 오류가 없을 것
보안: 사용자 입력을 innerHTML에 그대로 넣지 말 것 — textContent 또는 escapeHtml

[제출물] 폴더 이름은 "이름_스터디플래너" 형식 (예: 홍길동_스터디플래너)
홍길동_스터디플래너/
  index.html          구조
  css/style.css       스타일(CSS 변수·레이아웃·반응형)
  js/app.js           동작(이벤트·렌더링·저장)
  data/tips.json      오늘의 팁 데이터
  README.md           ① 구현 기능 목록 ② 실행 방법 ③ 어려웠던 점과 해결 ④ 미완성 항목
zip으로 압축해 제출하며 node_modules 같은 불필요한 폴더는 넣지 않는다.

[평가 루브릭] 완벽한 디자인보다 요구 기능의 완성이 우선
기능 완성도 40 — F1~F7이 실제로 동작하는가 (F5~F7에 가중치)
HTML 구조   15 — 시맨틱 태그·label 연결·유효한 마크업
CSS 품질    15 — CSS 변수 활용·Flex/Grid 레이아웃·일관된 클래스명
반응형      10 — 375px에서 가로 스크롤 없이 사용 가능
JS 품질     15 — 이벤트 위임·상태 기반 렌더링·콘솔 오류 없음
README       5 — 구현 기능·실행 방법·어려웠던 점이 적혀 있는가
※ 배점은 과정 운영에 따라 조정될 수 있으니 각 반 실습교수님 안내를 우선한다.

[자주 하는 실수 5가지]
1. 요소가 null이라 오류 → script가 DOM보다 먼저 실행됨. body 끝에 두거나 defer
2. 새로고침하면 목록이 사라짐 → save() 호출 누락. 상태를 바꾼 직후 반드시 저장
3. 추가 버튼을 누르면 화면이 깜빡임 → submit 기본 동작. event.preventDefault()
4. 새로 추가한 항목만 클릭이 안 됨 → 요소마다 리스너를 달았기 때문. 이벤트 위임으로
5. 모바일에서 가로 스크롤 발생 → 고정 px 폭 사용·min-width 누락. min-width: 0 확인
제출 전 개발자도구 콘솔을 열고 빨간 오류가 없는지 반드시 확인할 것.`,note:"2일 완성 교재(260717)의 최종 과제 장을 요구사항·제출 규격·루브릭·실수 목록으로 정리했다. 배점이 기능 완성도 40점에 몰려 있으므로 디자인을 다듬기 전에 F5~F7을 먼저 동작시키는 순서가 유리하고, 반응형 375px과 콘솔 무오류는 확인만 하면 확보되는 25점이라 마지막에 반드시 점검할 항목이다."}]},smartdata:{concepts:[{term:"정규화(1NF~3NF)와 이상현상",desc:`정규화는 데이터 중복을 최소화해 이상(Anomaly) 현상을 막는 설계 원칙이다. 비정규화 테이블에서는 일부만 저장하려 해도 불필요한 데이터까지 저장되는 삽입 이상, 행 하나를 지우면 다른 중요한 정보까지 사라지는 삭제 이상, 중복된 값 중 일부만 고쳐 불일치가 생기는 갱신 이상이 발생한다.
1NF(원자값) → 2NF(부분종속 제거) → 3NF(이행종속 제거) 순으로 진행하며 보통 3NF나 BCNF까지 적용한다. 다만 과도한 정규화는 JOIN 비용을 키우므로, 성능이 중요한 지점에서는 의도적으로 중복을 허용하는 반정규화(Denormalization) 전략을 함께 검토한다.`},{term:"PK·FK와 참조 무결성, 교차 테이블",desc:`기본키(PK)는 행을 유일하게 식별하는 기준이고, 외래키(FK)는 다른 테이블의 PK를 참조해 테이블 사이의 관계를 DB가 직접 보증하게 만드는 장치다. FK가 걸려 있으면 존재하지 않는 학과 id를 가진 학생을 넣을 수 없다 — 이것이 참조 무결성이다.
학생과 과목처럼 N:M 관계는 두 테이블로 직접 표현할 수 없어서, 수강신청(enrollments) 같은 교차 테이블을 사이에 두고 (student_id, course_id) 복합 PK로 풀어낸다. 교재 종합실습의 학사 도메인 설계가 정확히 이 패턴이다.`},{term:"B-Tree 인덱스와 인덱스 무력화 안티패턴",desc:`인덱스는 책의 찾아보기처럼 WHERE·JOIN·ORDER BY에 자주 쓰는 컬럼의 값과 위치를 정렬해 둔 자료구조로, 전체 스캔 대신 필요한 행으로 바로 접근하게 해 준다. 대신 쓰기 때마다 인덱스도 갱신해야 하므로 무분별하게 만들면 INSERT·UPDATE가 느려진다.
인덱스가 있어도 못 쓰는 대표 안티패턴이 있다 — 조건 컬럼에 함수 적용, 컬럼과 값의 타입 불일치(암묵적 형변환), 앞에 %가 붙은 LIKE 검색, 그리고 N+1 쿼리·NOT IN의 NULL 함정·대용량 OFFSET 페이징이다. 교재는 이를 8가지 안티패턴으로 정리해 암기시킨다.`},{term:"EXPLAIN 실행계획 — 튜닝은 Before/After 비교",desc:`EXPLAIN은 DB가 쿼리를 어떤 순서·방식으로 실행할지 보여 주는 명령이고, EXPLAIN ANALYZE는 실제로 실행해 소요 시간까지 측정한다. 전체 테이블을 훑는 Seq Scan(Full Scan)인지 인덱스를 타는 Index Scan인지, JOIN이 Nested Loop·Hash·Merge 중 어떤 알고리즘으로 처리되는지를 여기서 확인한다.
튜닝의 기본 절차는 느린 쿼리 식별(Slow Query Log) → 실행계획 확인 → 인덱스 추가나 쿼리 재작성 → 같은 쿼리의 실행계획을 다시 떠서 Before/After를 비교하는 것이다. 감이 아니라 실행계획이라는 증거로 판단하는 습관이 핵심이다.`},{term:"트랜잭션 ACID와 WAL — 로그가 먼저다",desc:`트랜잭션은 여러 SQL을 하나의 작업 단위로 묶어, 전부 성공(COMMIT)하거나 전부 취소(ROLLBACK)되게 보장한다. 그 성질이 ACID — 원자성(전부 아니면 전무), 일관성(규칙 위반 상태 금지), 격리성(동시 실행 간섭 차단), 지속성(확정된 결과는 장애에도 보존)이다.
지속성을 구현하는 핵심 기술이 WAL(Write-Ahead Logging)이다. 데이터 파일보다 변경 로그를 먼저 디스크에 기록하기 때문에, 장애가 나도 로그를 순서대로 재실행(Redo)하고 미확정 트랜잭션은 되돌려(Undo) 복구할 수 있다. COMMIT 시점에 WAL을 fsync하는 것이 "로그가 먼저"라는 원칙의 실체다.`},{term:"MVCC·격리 수준과 Lock, Deadlock",desc:`MVCC(다중 버전 동시성 제어)는 데이터를 고치는 대신 새 버전을 만들어, 읽는 쪽은 자기 시점의 스냅샷을 보고 쓰는 쪽은 새 버전을 쓰게 해 읽기와 쓰기가 서로를 막지 않게 한다. PostgreSQL과 MySQL InnoDB가 모두 이 방식이며, 트랜잭션이 서로를 어디까지 볼 수 있는지는 READ COMMITTED 같은 격리 수준으로 조절한다.
그래도 같은 행을 동시에 고치려면 락이 필요하다. SELECT ... FOR UPDATE로 행을 잠그고, 기다리지 않으려면 NOWAIT, 잠긴 행을 건너뛰려면 SKIP LOCKED를 쓴다. 두 트랜잭션이 서로가 잡은 락을 기다리며 멈추는 것이 데드락이며, 테이블 접근 순서를 통일하는 것이 기본 예방책이다.`},{term:"ERD 까마귀발 표기와 개념→논리→물리 3단계 설계",desc:`ERD는 엔티티·속성·관계를 한 장의 그림으로 표현하는 설계 언어다. 실무 표준인 IE(까마귀발) 표기에서 |는 하나, 세 갈래 까마귀발은 여럿, ○는 없거나 하나를 뜻하고, 관계선도 두 종류다 — FK가 자식의 PK 일부가 되는 식별 관계(실선, 부모 없이 존재 불가한 약한 엔티티)와 FK가 일반 컬럼인 비식별 관계(점선).
설계는 3단계로 내려간다. 개념적 모델링은 요구사항 문장에서 엔티티(명사)와 관계를 추출해 이해관계자와 소통하는 단계, 논리적 모델링은 테이블 매핑·PK/FK 설정·정규화를 적용하는 단계(여기까지 DBMS 독립적), 물리적 모델링은 특정 DBMS에 맞춰 타입·인덱스·파티셔닝을 정하는 단계다. 금액은 부동소수점 오류를 피해 NUMERIC(10,2), 글로벌 서비스 시간은 TIMESTAMPTZ를 쓰는 결정이 물리 설계의 예다.`},{term:"NULL의 3값 논리 — 참·거짓, 그리고 UNKNOWN",desc:`SQL의 조건 판정은 참/거짓 2값이 아니라 UNKNOWN이 추가된 3값 논리다. NULL은 '값이 없음/모름'이라 NULL이 낀 비교는 참도 거짓도 아닌 UNKNOWN이 되고, WHERE는 TRUE인 행만 통과시키므로 해당 행은 결과에서 조용히 사라진다. "데이터가 분명히 있는데 안 나와요"라는 신고의 상당수가 이 문제이며, memo = NULL은 항상 0건 — NULL은 반드시 IS NULL / IS NOT NULL로 찾는다.
집계에서도 NULL은 특별 취급이다. COUNT(*)만 NULL 포함 전체 행을 세고, COUNT(컬럼)·SUM·AVG는 NULL을 제외한다 — 특히 AVG는 NULL을 0으로 쳐 주지 않으므로 의도와 다른 평균이 나올 수 있다. 표시용 기본값은 COALESCE(첫 번째 비-NULL 값), 0 나눗셈 방지는 NULLIF(b, 0)가 표준 패턴이다.`},{term:"서브쿼리 4유형 — 스칼라·인라인뷰·WHERE·상관",desc:`서브쿼리는 놓이는 위치에 따라 성격이 갈린다. SELECT 절의 스칼라 서브쿼리는 단일 값 하나를 반환하지만 행마다 실행돼 성능 주의 대상이고, FROM 절의 인라인 뷰는 서브쿼리 결과를 임시 테이블처럼 쓰는 방식으로 페이지네이션에 자주 쓰인다. WHERE 절 서브쿼리는 비교 조건으로 쓰이며, 바깥 쿼리의 컬럼을 참조하는 상관 서브쿼리는 행마다 다시 실행되므로 JOIN이나 CTE로 대체를 검토한다.
다중 행 비교 연산자도 구분해 두자 — IN은 목록 포함 여부, EXISTS는 행이 존재하는지만 확인해(SELECT 1) 대용량에서 IN보다 빠르고 NULL에도 안전하다. ANY는 '하나라도 만족'(>ANY는 최솟값 초과와 동일), ALL은 '전부 만족'(>ALL은 최댓값 초과와 동일)이다. 합집합 UNION(중복 제거)·UNION ALL(중복 허용, 더 빠름), 교집합 INTERSECT, 차집합 EXCEPT까지가 이 단원의 도구 상자다.`},{term:"CTE(WITH)와 재귀 CTE — 쿼리를 이름 붙인 단계로 나눈다",desc:`CTE는 WITH 절로 중간 결과에 이름을 붙여 두고 본 쿼리에서 테이블처럼 쓰는 문법이다. 복잡한 서브쿼리 중첩을 '1단계: 학과별 평균 → 2단계: 평균보다 높은 학생' 같은 읽히는 파이프라인으로 분해할 수 있어 가독성과 재사용성이 좋아진다. 교재의 기준은 명확하다 — 서브쿼리가 복잡해지면 CTE로 분해하라.
WITH RECURSIVE는 CTE만의 강력한 기능으로, 기저 단계(최상위 행)와 재귀 단계(직전 결과에 JOIN해 한 층 아래를 추가)를 UNION ALL로 이어 조직도·카테고리 트리·경로 탐색 같은 계층 데이터를 한 번에 펼친다. 재귀 단계에는 depth 제한 조건을 반드시 넣어 무한 재귀를 방지하는 것이 철칙이다.`},{term:"View와 Materialized View — 저장된 쿼리 vs 저장된 결과",desc:`일반 View는 '저장된 SELECT문'이라 조회할 때마다 원본을 다시 읽어 항상 최신 데이터를 보여준다. 용도는 세 가지 — 민감 컬럼을 숨기는 권한 제어, 복잡한 JOIN·집계 쿼리의 재사용, 그리고 원본 테이블 구조가 바뀌어도 응용에는 같은 모양을 보여주는 인터페이스 안정화(논리적 독립성)다.
Materialized View는 쿼리 '결과'를 디스크에 저장해 조회가 빠르고 인덱스도 걸 수 있지만, 자동 갱신이 없어 REFRESH MATERIALIZED VIEW를 스케줄러로 돌려야 한다(CONCURRENTLY 옵션이면 잠금 없이 갱신). 대용량 집계 화면은 매번 GROUP BY를 도는 대신 MV나 통계 테이블에 미리 계산해 두는 것이 교재가 권하는 성능 전략이다.`},{term:"DB 보안 5층 — 인증·인가·바인딩·암호화·감사",desc:`DB 보안은 다섯 층으로 정리된다. 인증(누가 접속했나), 인가(무엇을 할 수 있나), 입력 검증(악성 입력 차단), 암호화(전송 TLS·저장 TDE·민감 컬럼 암호화), 감사(누가 언제 무슨 쿼리를 실행했나). 인가의 원칙은 최소 권한 — 권한을 역할(ROLE)로 묶어 GRANT하고 사용자에게는 역할만 부여하며, 애플리케이션 계정에 관리자 권한을 주는 것이 교재가 꼽는 대표적 실수다.
SQL Injection의 원인은 단 하나, 사용자 입력을 SQL 문자열에 직접 이어붙이는 것이다. 방어도 한 줄 — 항상 파라미터 바인딩(Prepared Statement)을 쓰고, 바인딩이 불가능한 ORDER BY 컬럼명 같은 곳은 화이트리스트로 검증한다. 여기에 Row-Level Security를 켜면 같은 테이블에서 사용자별로 볼 수 있는 행 자체를 제한할 수 있어 멀티테넌시 구현의 기본기가 된다.`},{term:"OLTP vs OLAP — 쓰기 중심과 읽기 중심은 설계가 다르다",desc:`OLTP(운영계)는 주문·결제처럼 짧은 트랜잭션이 초당 수천 건 쏟아지는 쓰기 중심 부하라 정규화(3NF)를 유지해 갱신 이상을 막고, OLAP(분석계)은 수억 행을 훑는 집계 읽기 중심이라 JOIN을 줄이는 반정규화와 컬럼 기반 저장(Columnstore)이 유리하다. 인덱스 전략도 부하 성격에 따라 달라지므로, 운영 DB에서 무거운 분석 쿼리를 직접 돌리는 대신 데이터 웨어하우스로 분리하는 것이 정석이다.
동시성 제어 방식 선택도 같은 논리다 — 충돌이 드문 읽기 위주 작업은 버전 컬럼으로 커밋 시점에 충돌만 감지하는 낙관적 Lock, 은행 이체·좌석 예매처럼 충돌이 잦은 쓰기 집약 작업은 SELECT ... FOR UPDATE로 미리 잠그는 비관적 Lock이 적합하다. 클라우드 관리형 DB는 백업·복구·확장을 대신해 주지만, OLTP/OLAP 구분과 권한 설계는 여전히 사용자의 몫이다.`},{term:"SQLD 시험 구조와 준비 전략",desc:"SQLD(SQL 개발자)는 60문항을 90분 안에 풀어 100점 만점에 70점 이상이면 합격하는 자격시험이다. 출제 비중은 데이터 모델링 20%, SQL 기본과 활용 40%, SQL 고급 기능과 성능 40%로 나뉘어, 앞의 모델링보다 실제 쿼리 작성·튜닝 쪽 배점이 압도적으로 크다. 자주 틀리는 지점은 세 가지로 압축된다 — COUNT(*)와 COUNT(컬럼)의 차이(후자는 NULL을 세지 않는다), SELECT에 쓴 컬럼을 GROUP BY에 넣지 않아 생기는 오류, 그리고 NULL이 섞인 연산 결과가 통째로 NULL이 되는 문제다. 실습 환경을 PostgreSQL로 잡아 두면 문제를 눈으로 읽는 대신 직접 돌려 확인할 수 있어 이 세 함정이 몸에 남는다."},{term:"PostgreSQL의 고유 기능 — 표준 SQL에서 한 걸음 더",desc:"같은 SQL이라도 PostgreSQL에서만 편해지는 기능들이 있다. SERIAL은 자동 증가 정수 컬럼을 한 단어로 만들어 주고, JSONB는 JSON을 파싱된 이진 형태로 저장해 내부 키까지 인덱싱·검색할 수 있게 한다. ARRAY 타입은 한 칸에 여러 값을 담고 UNNEST로 다시 행으로 펼치며, LATERAL JOIN은 오른쪽 서브쿼리가 왼쪽 행의 값을 참조할 수 있게 해 행마다 다른 조건의 조회를 가능하게 한다. MATERIALIZED VIEW는 뷰의 결과를 실제로 저장해 두어 무거운 집계를 즉시 읽게 해 준다(대신 REFRESH가 필요하다). 인덱스도 B-Tree 하나가 아니라 용도별로 나뉘어 — 일반 비교는 B-Tree, JSONB·배열·전문검색은 GIN, 기하·범위 데이터는 GiST, 아주 큰 시계열 테이블에는 블록 요약만 저장해 가벼운 BRIN을 쓴다."},{term:"SELECT 논리적 처리 순서 — 쓰는 순서와 실행 순서는 다르다 (최진철 교수 교재)",desc:`우리가 SQL을 쓸 때는 SELECT를 맨 앞에 적지만, DBMS가 실제로 처리하는 순서는 FROM으로 대상 테이블을 잡고, WHERE로 행을 거르고, GROUP BY로 묶고, HAVING으로 그룹을 거른 뒤, 그제서야 SELECT로 열과 별칭을 만들고, ORDER BY로 정렬하고, LIMIT으로 개수를 자릅니다.
즉 작성 순서 1번인 SELECT가 실행 순서로는 5번째입니다.
이 순서를 모르면 왜 별칭이 WHERE에서 안 먹히는지 설명할 수 없습니다. SELECT에서 만든 별칭은 실행 5단계에 태어나는데 WHERE는 2단계라 그 이름이 아직 존재하지 않기 때문입니다.
반대로 ORDER BY는 6단계라 별칭을 그대로 쓸 수 있습니다.
교재는 이를 한국어 어순에 빗대 "어디서 - 거르고 - 고른다"로 기억하라고 안내합니다.`},{term:"파일시스템의 한계와 DBMS 3대 기능 — DB를 왜 쓰는가 (최진철 교수 교재)",desc:`데이터를 그냥 파일로 저장하던 시절에는 네 가지 문제가 반복됐습니다.
첫째 데이터 구조가 프로그램에 종속돼 구조를 바꾸면 코드를 같이 고쳐야 했고, 둘째 파일마다 같은 값이 중복 저장돼 서로 값이 어긋났고, 셋째 값이 규칙에 맞는지를 애플리케이션이 매번 직접 검사해야 했고, 넷째 여러 사람이 동시에 고칠 때 통제가 어렵고 장애가 나면 수동 복구뿐이었습니다.
DBMS는 이 넷을 각각 논리·물리 데이터 독립성, 통합 관리를 통한 중복 최소화, 제약조건 기반 무결성 보장, 트랜잭션과 로그 기반 자동 복구로 해결합니다.
뒤에서 배우는 정규화·제약조건·트랜잭션·WAL은 전부 이 네 가지 한계에 대한 답이라는 관점으로 묶어 보면 전체 흐름이 하나로 연결됩니다.`},{term:"DBMS 방언 차이 — 같은 일을 제품마다 다르게 쓴다 (최진철 교수 교재)",desc:`표준 SQL이 있어도 제품마다 표기가 갈리는 지점이 있어서, 실무에서 코드를 옮길 때 이 부분이 먼저 깨집니다.
행 제한은 PostgreSQL과 MySQL이 LIMIT, Oracle은 ROWNUM 또는 FETCH FIRST, SQL Server는 TOP을 씁니다.
문자열 연결은 PostgreSQL과 Oracle이 두 개의 세로줄 연산자, MySQL은 CONCAT 함수, SQL Server는 더하기 기호입니다. 자동 증가 키는 SERIAL 또는 IDENTITY, AUTO_INCREMENT, SEQUENCE로 갈립니다.
LIKE의 대소문자 처리도 PostgreSQL과 Oracle은 항상 구분하는 반면 MySQL과 SQL Server는 콜레이션 설정을 따라가서 기본값이 무시 쪽입니다.
DDL의 되돌리기도 다릅니다. PostgreSQL은 DROP이나 TRUNCATE를 트랜잭션 안에서 되돌릴 수 있지만 Oracle과 MySQL은 실행 즉시 자동 커밋돼 복구가 안 되므로, 실무는 못 되돌린다고 가정하고 다루는 편이 안전합니다.`}],examples:[{title:"JOIN + GROUP BY/HAVING — 학과별 수강 성적 집계",lang:"sql",code:`-- 학과별 수강 성적 집계: 테이블 3개를 JOIN한 뒤 그룹화한다
SELECT m.name AS major_name,                 -- 그룹 키: 학과 이름
       COUNT(DISTINCT s.id) AS student_cnt,  -- 학과별 학생 수(중복 제거)
       ROUND(AVG(e.score), 1) AS avg_score,  -- 학과별 평균 성적(소수 1자리)
       MAX(e.score) AS best_score            -- 학과별 최고 성적
FROM students s                              -- 기준 테이블: 학생
JOIN majors m ON m.id = s.major_id           -- 학과 정보 붙이기(INNER JOIN)
JOIN enrollments e ON e.student_id = s.id    -- 수강 내역 붙이기(교차 테이블)
WHERE e.score IS NOT NULL                    -- 집계 전 행 필터는 WHERE
GROUP BY m.name                              -- SELECT에는 그룹 키·집계함수만 허용
HAVING COUNT(*) >= 3                         -- 집계 후 그룹 필터는 HAVING
ORDER BY avg_score DESC;                     -- 평균 성적 높은 학과부터

-- 미수강 학생 찾기: LEFT JOIN은 짝이 없어도 왼쪽 행을 유지한다
SELECT s.id, s.name                          -- 학생 번호와 이름
FROM students s                              -- 왼쪽(기준) 테이블
LEFT JOIN enrollments e                      -- 수강 내역이 없으면 NULL로 채워짐
       ON e.student_id = s.id                -- 조인 조건
WHERE e.student_id IS NULL;                  -- NULL만 남기면 미수강자 목록`,note:"교재 종합실습 2의 핵심 패턴이다. WHERE는 집계 전 행을 거르고 HAVING은 집계 후 그룹을 거른다는 구분, 그리고 INNER JOIN(교집합)과 LEFT JOIN(왼쪽 유지)의 차이가 SQL 중급의 절반이다. FROM → JOIN → WHERE → GROUP BY → HAVING → SELECT → ORDER BY 순서로 처리된다는 점도 기억해 두자."},{title:"Window Function — 지역별 매출 순위와 전월 대비 증감",lang:"sql",code:`-- 윈도우 함수: GROUP BY와 달리 행을 유지한 채 옆에 분석값을 붙인다
SELECT region, sales_month, revenue,         -- 원본 행은 그대로 유지
       ROW_NUMBER() OVER w AS row_num,       -- 동점 무관 고유 번호: 1,2,3,4
       RANK()       OVER w AS rank_r,        -- 동점 같은 순위, 다음 건너뜀: 1,2,2,4
       DENSE_RANK() OVER w AS dense_r,       -- 동점 같은 순위, 건너뜀 없음: 1,2,2,3
       NTILE(4)     OVER w AS tier           -- 매출을 4개 등급 구간으로 분할
FROM sales                                   -- 매출 테이블
WINDOW w AS (PARTITION BY region             -- 분석 단위: 지역별로 창을 나누고
             ORDER BY revenue DESC);         -- 창 안에서 매출 내림차순 정렬

-- LAG: 한 행 이전(전월) 값을 현재 행 옆으로 끌어온다
SELECT region, sales_month, revenue,         -- 지역·월·당월 매출
       LAG(revenue, 1) OVER (                -- 1행 이전 값 참조
         PARTITION BY region                 -- 지역별로 창 구분
         ORDER BY sales_month                -- 월 순서대로 정렬
       ) AS prev_rev,                        -- 전월 매출 컬럼
       revenue - LAG(revenue, 1) OVER (      -- 당월 - 전월 = 증감액
         PARTITION BY region                 -- 같은 창 정의를 반복 사용
         ORDER BY sales_month                -- 시간 순 정렬이 핵심
       ) AS diff                             -- 증감액 컬럼
FROM sales;                                  -- 행이 줄지 않아 상세+분석을 동시에 본다`,note:"교재 15장(분석 함수)의 두 축이다. PARTITION BY는 GROUP BY처럼 단위를 나누지만 행을 합치지 않고 유지한다는 점이 결정적 차이. ROW_NUMBER/RANK/DENSE_RANK의 동점 처리 차이는 면접 단골이고, LAG/LEAD는 전월 대비·전일 대비 분석의 표준 도구다."},{title:"인덱스 설계·EXPLAIN 튜닝과 트랜잭션 계좌이체",lang:"sql",code:`-- 1) 튜닝 전: 인덱스가 없어 전체 테이블을 훑는다(Seq Scan)
EXPLAIN ANALYZE                              -- 실행계획 + 실제 수행 시간 측정
SELECT id, order_date, amount FROM orders    -- 조회 대상
WHERE customer_id = 42;                      -- 고객 1명 조회인데 Full Scan 발생

-- 2) WHERE 조건 컬럼에 B-Tree 인덱스 생성
CREATE INDEX idx_orders_customer             -- 인덱스 이름
ON orders (customer_id, order_date);         -- 함께 쓰는 컬럼은 복합 인덱스로

-- 3) 튜닝 후: 같은 쿼리를 다시 떠서 Index Scan으로 바뀌었는지 확인
EXPLAIN ANALYZE                              -- Before/After 비교가 튜닝의 기본
SELECT id, order_date, amount FROM orders WHERE customer_id = 42; -- 동일 쿼리

-- 인덱스를 무력화하는 안티패턴 교정
-- 나쁨: WHERE user_id = '12345'   (타입 불일치 → 형변환 → Full Scan)
-- 좋음: WHERE user_id = 12345     (컬럼 타입과 값 타입을 일치)
-- 나쁨: WHERE name LIKE '%길동%'  (앞 와일드카드는 B-Tree 사용 불가)
-- 좋음: WHERE name LIKE '홍%'     (접두어 검색은 인덱스 사용 가능)

-- 4) 트랜잭션: 이체는 두 UPDATE가 전부 되거나 전부 취소돼야 한다(원자성)
BEGIN;                                       -- 트랜잭션 시작(없으면 자동 커밋)
SELECT balance FROM account                  -- 이체 전 잔액 확인
WHERE id = 'A' FOR UPDATE;                   -- 행 잠금: 동시 수정 차단
UPDATE account SET balance = balance - 10000 WHERE id = 'A'; -- 출금
UPDATE account SET balance = balance + 10000 WHERE id = 'B'; -- 입금
COMMIT;                                      -- 확정: WAL 로그를 먼저 쓴 뒤 반영
-- 중간에 오류가 나면 ROLLBACK; 으로 전체를 취소해 일관성을 지킨다`,note:"교재 17~21장을 하나의 흐름으로 압축했다. 인덱스는 만들고 끝이 아니라 EXPLAIN ANALYZE로 실제 효과를 검증해야 하고, 타입 불일치·LIKE 앞 % 같은 안티패턴은 인덱스가 있어도 무력화시킨다. 이체 트랜잭션의 FOR UPDATE는 두 세션이 같은 계좌를 동시에 고치는 사고를 막는 실무 필수 장치다."},{title:"CASE·COALESCE·NULLIF — 요금 등급 분류와 결측 안전 처리",lang:"sql",code:`-- 1) CASE: 월요금 구간별 등급 (위에서부터 평가, 첫 참에서 멈춤)
SELECT name,                                  -- 가입자 이름
       monthly_fee,                           -- 월요금
       CASE                                   -- SQL 안의 if-else
         WHEN monthly_fee >= 80000 THEN '프리미엄'  -- 8만원 이상
         WHEN monthly_fee >= 50000 THEN '스탠다드'  -- 5만~8만 미만
         ELSE '라이트'                        -- 나머지 (ELSE 없으면 NULL)
       END AS grade                           -- 등급이라는 새 컬럼으로 표시
FROM subscription;                            -- 통신 가입 테이블

-- 2) NULL 안전 장치: 표시는 COALESCE, 0 나눗셈은 NULLIF
SELECT name,                                  -- 가입자 이름
       COALESCE(memo, '메모없음') AS memo_show,      -- NULL이면 기본값으로 표시
       used_mb / NULLIF(quota_mb, 0) AS usage_ratio  -- 0이면 NULL로 바꿔 에러 대신 NULL 반환
FROM subscription                             -- 같은 테이블에서
WHERE memo IS NULL;                           -- NULL 찾기는 = NULL(항상 0건)이 아니라 IS NULL

-- 3) 집계와 NULL: COUNT(*)만 NULL을 센다
SELECT COUNT(*) AS all_rows,                  -- 전체 행 수 (NULL 포함)
       COUNT(monthly_fee) AS fee_rows,        -- 값이 있는 행만
       AVG(monthly_fee) AS avg_fee            -- NULL 제외 평균 (0으로 안 쳐 준다!)
FROM subscription;                            -- 두 COUNT의 차이 = NULL 개수`,note:"최진철 교재 Day1 4교시(단일 테이블 조회)의 통신 도메인 실습 패턴이다. CASE가 첫 참에서 멈춘다는 평가 순서, = NULL이 3값 논리 때문에 항상 0건이라는 함정, COUNT(*)와 COUNT(컬럼)의 차이가 핵심 세 가지다. 백정열 교재도 COALESCE는 SQL 표준, NVL(Oracle)·ISNULL(SQL Server)은 방언이라고 정리한다."},{title:"서브쿼리 실전 — 평균보다 비싼 가입, EXISTS, 집합 연산",lang:"sql",code:`-- 1) WHERE 절 스칼라 서브쿼리: 전체 평균 요금보다 비싼 가입 찾기
SELECT name, monthly_fee                      -- 이름과 월요금
FROM subscription                             -- 가입 테이블
WHERE monthly_fee > (SELECT AVG(monthly_fee)  -- 서브쿼리가 평균 하나(단일 값)를 반환
                     FROM subscription);      -- 그 값과 바깥 행을 비교

-- 2) EXISTS: 가입 이력이 하나라도 있는 고객 (행 존재만 확인해 빠르다)
SELECT c.name                                 -- 고객 이름
FROM customer c                               -- 고객 테이블
WHERE EXISTS (SELECT 1                        -- 값은 안 쓰므로 1이면 충분
              FROM subscription s             -- 가입 테이블에서
              WHERE s.cust_id = c.cust_id);   -- 바깥 행을 참조 = 상관 서브쿼리

-- 3) 인라인 뷰: FROM 절의 서브쿼리를 임시 테이블처럼 사용
SELECT p.plan_name, t.avg_fee                 -- 요금제 이름과 평균 요금
FROM (SELECT plan_id,                         -- 요금제 id별로
             AVG(monthly_fee) AS avg_fee      -- 평균 요금을 먼저 계산해 두고
      FROM subscription GROUP BY plan_id) t   -- 별칭 t 필수 (임시 테이블 이름)
JOIN plan p ON p.plan_id = t.plan_id;         -- 요금제 정보를 붙인다

-- 4) 집합 연산: 2025년과 2026년 모두 가입 이력이 있는 고객
SELECT cust_id FROM subscription WHERE start_year = 2025  -- 2025년 가입 고객 집합
INTERSECT                                     -- 교집합 (UNION 합집합, EXCEPT 차집합)
SELECT cust_id FROM subscription WHERE start_year = 2026; -- 2026년 집합과 겹치는 부분만`,note:"백정열 교재 13장(서브쿼리 & 집합 연산자)의 유형 분류를 통신 도메인으로 재구성했다. 스칼라·인라인뷰·상관 서브쿼리의 위치별 구분과, EXISTS가 SELECT 1로 행 존재만 확인해 대용량에서 IN보다 유리하다는 선택 기준이 요점이다. UNION ALL이 UNION보다 빠른 이유(중복 제거 정렬 생략)도 함께 기억해 두자."},{title:"CTE와 재귀 CTE — 단계별 분해와 조직도 계층 탐색",lang:"sql",code:`-- 1) CTE: 복잡한 쿼리를 이름 붙인 단계로 분해한다
WITH plan_avg AS (                            -- 1단계: 요금제별 평균 요금 계산
  SELECT plan_id,                             -- 요금제 id
         AVG(monthly_fee) AS avg_fee          -- 요금제 안의 평균 요금
  FROM subscription                           -- 가입 테이블에서
  GROUP BY plan_id                            -- 요금제 단위로 집계
)
SELECT s.name, s.monthly_fee, a.avg_fee       -- 2단계: 원본 행과 평균을 나란히
FROM subscription s                           -- 가입 테이블
JOIN plan_avg a ON a.plan_id = s.plan_id      -- CTE를 일반 테이블처럼 JOIN
WHERE s.monthly_fee > a.avg_fee;              -- 자기 요금제 평균보다 비싼 가입만

-- 2) 재귀 CTE: 자기참조 테이블(조직도)을 위에서 아래로 전부 펼친다
WITH RECURSIVE org AS (
  SELECT id, emp_name, manager_id,            -- 직원 기본 정보
         1 AS depth                           -- 기저 단계: 깊이 1
  FROM employees                              -- 직원 테이블에서
  WHERE manager_id IS NULL                    -- 상사가 없는 최상위부터 시작
  UNION ALL                                   -- 기저 + 재귀 결과를 이어붙인다
  SELECT e.id, e.emp_name, e.manager_id,      -- 한 단계 아래 직원
         o.depth + 1                          -- 깊이를 1 늘린다
  FROM employees e                            -- 직원 테이블과
  JOIN org o ON o.id = e.manager_id           -- 직전 단계 결과를 연결
  WHERE o.depth < 10                          -- 무한 재귀 방지 (반드시 필요!)
)
SELECT depth, emp_name FROM org ORDER BY depth;  -- 계층 순서대로 출력`,note:"백정열 교재 14장의 두 축이다. CTE는 '요금제 평균 만들기 → 평균과 비교하기'처럼 사고 순서 그대로 쿼리를 쓰게 해 주고, 재귀 CTE는 조직도·카테고리 트리 같은 계층 데이터를 기저+재귀 UNION ALL 패턴으로 푼다. depth 제한이 없으면 데이터 오류(순환 참조) 시 무한 루프가 되므로 교재도 '항상 필요'라고 강조한다."},{title:"VIEW로 가리고 GRANT로 잠그기 — 읽기 전용 계정과 행 단위 보안",lang:"sql",code:`-- 1) 민감 컬럼을 뺀 뷰: 보여줄 것만 골라 담는다
CREATE OR REPLACE VIEW v_customer_public AS   -- 뷰 = 저장된 SELECT문
SELECT cust_id, name, join_date               -- 전화번호·식별번호 컬럼은 제외
FROM customer;                                -- 원본 테이블은 그대로 둔다

-- 2) 읽기 전용 역할(ROLE)을 만들고 필요한 권한만 부여 (최소 권한 원칙)
CREATE ROLE app_reader NOLOGIN;               -- 로그인 불가 — 권한 묶음 용도
GRANT SELECT ON v_customer_public TO app_reader;  -- 이 뷰의 SELECT만 허용

-- 3) 실제 접속 계정을 만들어 역할을 상속
CREATE ROLE app_user LOGIN PASSWORD 'secret'; -- 로그인 가능한 사용자 계정
GRANT app_reader TO app_user;                 -- 역할 부여: 뷰 조회만 가능해진다

-- app_user로 접속해 보면?
-- SELECT * FROM v_customer_public;   → 성공 (허용된 뷰)
-- SELECT * FROM customer;            → 권한 오류 (원본 접근 차단)
-- DELETE FROM v_customer_public;     → 권한 오류 (SELECT만 허용)

-- 4) 행 단위 보안(RLS): 같은 테이블에서 자기 데이터만 보이게
ALTER TABLE orders ENABLE ROW LEVEL SECURITY; -- 테이블에 RLS 켜기
CREATE POLICY my_orders ON orders             -- 정책 이름 지정
  FOR SELECT                                  -- 조회에 적용
  USING (customer_id =                        -- 행의 고객 id가
         current_setting('app.customer_id')::int);  -- 세션에 설정된 내 id와 같을 때만 보임`,note:"백정열 교재 14장(View의 권한 제어 용도)과 30장(사용자·역할·권한, RLS)을 하나의 시나리오로 연결했다. 권한은 사용자에게 직접 주지 않고 역할로 묶어 부여하는 것, 애플리케이션 계정에 관리자 권한을 주지 않는 것이 최소 권한 원칙의 실천이다. RLS는 멀티테넌시(한 테이블에 여러 고객 데이터)에서 행 자체를 격리하는 마지막 방어선이다."},{title:"PostgreSQL 실습 10단계 — 정규화부터 인덱스 튜닝까지 (SQLD 대비)",lang:"sql",code:`-- 1) 정규화(1NF): 한 칸에 여러 전화번호를 넣지 말고 테이블을 분리한다
CREATE TABLE employee (
  emp_id   SERIAL PRIMARY KEY,          -- 자동 증가 정수 (PostgreSQL 고유)
  emp_name VARCHAR(50) NOT NULL,
  dept     VARCHAR(50)
);

CREATE TABLE employee_phone (
  phone_id     SERIAL PRIMARY KEY,
  emp_id       INT REFERENCES employee(emp_id),  -- 외래키로 사원과 연결
  phone_number VARCHAR(20)
);

-- 2) 조회: ILIKE는 대소문자를 구분하지 않는 LIKE (PostgreSQL 고유)
SELECT * FROM employee WHERE emp_name ILIKE '%kim%';

-- 3) JOIN: 분리한 두 테이블을 다시 합쳐 본다
SELECT e.emp_name, p.phone_number
FROM employee e
JOIN employee_phone p ON e.emp_id = p.emp_id;

-- 4) GROUP BY: 부서별 인원수. SELECT에 쓴 dept는 반드시 GROUP BY에도 있어야 한다
SELECT dept, COUNT(*) AS headcount
FROM employee
GROUP BY dept
HAVING COUNT(*) >= 2;                   -- 집계 결과에 대한 조건은 HAVING

-- 5) 서브쿼리: 전화번호를 하나도 등록하지 않은 사원 찾기
SELECT emp_name FROM employee e
WHERE NOT EXISTS (
  SELECT 1 FROM employee_phone p WHERE p.emp_id = e.emp_id
);

-- 6) CTE(WITH): 쿼리를 이름 붙인 단계로 나눠 읽기 쉽게 만든다
WITH dept_count AS (
  SELECT dept, COUNT(*) AS cnt FROM employee GROUP BY dept
)
SELECT * FROM dept_count WHERE cnt > 1;

-- 7) 윈도우 함수: 사원별로 전화번호에 순번을 매긴다
--    PARTITION BY로 사원마다 번호를 1부터 새로 시작한다
SELECT emp_id, phone_number,
       ROW_NUMBER() OVER (PARTITION BY emp_id ORDER BY phone_id) AS seq
FROM employee_phone;

-- 8) CHECK 제약: 정규표현식으로 전화번호 형식을 DB가 직접 검사하게 한다
ALTER TABLE employee_phone
  ADD CONSTRAINT chk_phone CHECK (phone_number ~ '^010-[0-9]{4}-[0-9]{4}$');

-- 9) 트랜잭션: 잘못 지웠을 때 되돌릴 수 있다
BEGIN;
DELETE FROM employee WHERE dept = 'temp';
ROLLBACK;                               -- 취소. 확정하려면 COMMIT

-- 10) 인덱스와 실행계획: 만들기 전후를 반드시 비교한다
CREATE INDEX idx_employee_dept ON employee(dept);
EXPLAIN ANALYZE SELECT * FROM employee WHERE dept = 'AI';
-- Seq Scan(전체 훑기)이 Index Scan으로 바뀌고 실행 시간이 줄었는지 확인`,note:"SQLD 학습자료의 실습 흐름을 PostgreSQL 문법으로 재구성했다. 1NF 분리에서 시작해 조회·조인·집계·서브쿼리·CTE·윈도우 함수로 올라간 뒤 제약·트랜잭션·인덱스로 마무리하는 순서라, 각 단계가 앞 단계의 결과물 위에서 돌아가 개념이 끊기지 않는다. 특히 10단계의 EXPLAIN ANALYZE Before/After 비교는 튜닝을 감이 아니라 측정으로 하는 습관의 출발점이다."},{title:"실습 환경 트러블슈팅 — PostgreSQL 비밀번호와 DBeaver 설정 (7/20)",lang:"text",code:`[0] 설치 여부 먼저 확인 — 파이썬 1일차 환경구성 스크립트로 PostgreSQL 17이 이미 깔렸을 수 있다
  터미널에서:  postgres -V
  버전이 나오면 다시 설치할 필요 없다.

[1] postgres 계정 비밀번호가 없어 접속이 안 될 때
  터미널에서 psql에 먼저 붙는다
    psql postgres
  프롬프트가 바뀌면 비밀번호를 지정한다
    ALTER USER postgres PASSWORD 'your_password';
  빠져나온다
    \\q
  이제 DBeaver 연결 설정에 사용자 postgres / 비밀번호 your_password 를 입력하면 붙는다.

[2] DBeaver에 내가 만든 데이터베이스가 안 보일 때 (가장 많이 막히는 지점)
  증상: CREATE DATABASE telecom; 으로 만들었는데 목록에 postgres만 보인다
  원인: DBeaver 기본 설정이 접속한 DB 하나만 표시하도록 되어 있다
  해결: 연결 우클릭 → Edit Connection → PostgreSQL 탭 → "Show all databases" 체크 → 저장 후 재연결

[3] DBeaver를 한글 화면으로 쓰고 싶을 때
  DBeaver Community → Preferences → User Interface → Language → Korean 선택

[4] 수업 운영 (울산캠퍼스 기준)
  · 교재: tinyurl.com/sk-database → "수강생 배포용 교재 모음" (3일 과정 내내 사용)
  · 1일차 테스트: QUIZ + 코드분석, 17:45~18:00 진행
    - 링크는 17:40~17:45 사이에 채널 공유
    - 접속 시 gmail 계정 정보 입력 필수
  · Day1 퀴즈 제출 마감 18:00 — 시간 초과 시 대폭 감점, 제출 후 수정 불가`,note:'스마트 데이터베이스 첫날 실습에서 실제로 나온 막힘 지점과 해결책을 모았다. DBeaver의 "Show all databases"는 특히 원인을 짐작하기 어려운 항목이라 — 쿼리는 성공했는데 화면에만 안 보이니 DB 생성이 실패한 줄 오해하기 쉽다 — 만든 DB가 안 보이면 무조건 이 설정부터 확인하는 것이 빠르다.'},{title:"처리 순서 체감 — 별칭은 WHERE에서 왜 안 되고 ORDER BY에서는 되는가 (최진철 교수 교재)",lang:"sql",code:`-- 실패하는 쿼리: SELECT에서 만든 별칭을 WHERE에서 바로 쓴다
-- WHERE는 실행 2단계, 별칭은 실행 5단계에 생기므로 이름이 아직 없다
SELECT plan_name, monthly_fee * 12 AS yearly_fee
FROM plan
WHERE yearly_fee > 600000;   -- 오류: column yearly_fee does not exist

-- 해결 1: WHERE에는 별칭이 아니라 계산식을 그대로 다시 적는다
SELECT plan_name, monthly_fee * 12 AS yearly_fee
FROM plan
WHERE monthly_fee * 12 > 600000
ORDER BY yearly_fee DESC;    -- ORDER BY는 실행 6단계라 별칭 사용 가능

-- 해결 2: 인라인 뷰로 별칭을 먼저 확정한 뒤 바깥에서 거른다
SELECT plan_name, yearly_fee
FROM (
  SELECT plan_name, monthly_fee * 12 AS yearly_fee
  FROM plan
) AS t                       -- 안쪽이 먼저 끝나 별칭이 실체가 된다
WHERE yearly_fee > 600000
ORDER BY yearly_fee DESC
LIMIT 5;                     -- LIMIT은 실행 7단계, 정렬 뒤에 잘린다

-- 그룹 필터도 같은 원리: WHERE는 행을, HAVING은 묶인 그룹을 거른다
SELECT data_gb, count(*) AS cnt, avg(monthly_fee) AS avg_fee
FROM plan
WHERE monthly_fee > 0        -- 2단계: 묶기 전에 행을 먼저 거른다
GROUP BY data_gb             -- 3단계: 데이터 용량별로 묶는다
HAVING count(*) >= 2         -- 4단계: 묶은 결과를 거른다
ORDER BY avg_fee DESC;       -- 6단계: 마지막에 정렬한다`,note:`별칭 오류는 초급자가 가장 자주 만나는 에러인데, 원인이 문법이 아니라 처리 순서라는 점이 핵심입니다.
해결 1은 짧지만 계산식이 중복되고, 해결 2는 길지만 식이 한 곳에만 있어 유지보수가 낫습니다.
WHERE와 HAVING을 헷갈릴 때도 같은 순서표를 떠올리면 바로 정리됩니다.`},{title:"제약조건으로 잘못된 값을 입구에서 막기 — CHECK·DEFAULT·UNIQUE (최진철 교수 교재)",lang:"sql",code:`-- 시드 데이터를 보호하려고 연습용 테이블은 별도 스키마에 만든다
CREATE SCHEMA IF NOT EXISTS lab;

-- 요금제 연습 테이블: 타입 + 제약을 함께 정의해 품질을 입구에서 보장
CREATE TABLE lab.plan_practice (
  plan_id     integer PRIMARY KEY,              -- 중복 금지 + 빈값 금지
  plan_name   varchar(40) NOT NULL UNIQUE,      -- 빈값 금지 + 중복 금지
  monthly_fee integer NOT NULL
                CHECK (monthly_fee >= 0),       -- 음수 요금 자체를 차단
  data_gb     integer NOT NULL DEFAULT 0
                CHECK (data_gb >= -1),          -- -1은 무제한 약속값
  status      varchar(10) NOT NULL DEFAULT :active_default
                CHECK (status IN (:s1, :s2, :s3)),  -- 허용값만 통과
  created_at  timestamp NOT NULL DEFAULT now()  -- 누락 시 현재시각 자동
);

-- 정상 입력: DEFAULT가 status와 created_at을 알아서 채운다
INSERT INTO lab.plan_practice (plan_id, plan_name, monthly_fee, data_gb)
VALUES (1, :name_a, 39000, 10);

-- 위반 관찰 1: 음수 요금 -> CHECK 제약 위반으로 거부된다
INSERT INTO lab.plan_practice (plan_id, plan_name, monthly_fee)
VALUES (2, :name_b, -1000);

-- 위반 관찰 2: 요금제명 중복 -> UNIQUE 제약 위반으로 거부된다
INSERT INTO lab.plan_practice (plan_id, plan_name, monthly_fee)
VALUES (3, :name_a, 55000);

-- 실제로 무엇이 막았는지 제약 목록으로 확인한다 (contype: p키 u유일 c검사)
SELECT conname, contype FROM pg_constraint WHERE conrelid = 'lab.plan_practice'::regclass;`,note:`PK는 중복과 빈값을 둘 다 막고, UNIQUE는 중복만, NOT NULL은 빈값만 막는다는 역할 분담을 먼저 잡아야 합니다.
CHECK는 잘못된 값을 막고 DEFAULT는 누락을 합리적 기본값으로 채우는 짝이라, 둘을 함께 쓰면 입력 시점부터 품질이 확보됩니다.
문자열 리터럴은 실습 시 콜론 자리표시자 대신 작은따옴표로 직접 넣어 실행하세요.`},{title:"울산 분반 운영 차이 — 배포 자료 대조 메모 (최진철 교수 교재)",lang:"text",code:`[대조 결과 요약]
통합본 성격의 강의 덱과 울산 배포용 5종을 맞대어 본 결과,
커리큘럼 자체의 차이는 발견되지 않았습니다. 다만 문서 계층이 다릅니다.

1) 커리큘럼 - 동일
   3일 과정, 2026-07-20(월) ~ 07-22(수)
   Day1 설계의 기초 / Day2 관계형 데이터 탐험 / Day3 성능의 예술
   각 Day 4교시 편성(Day3는 3교시 + 마무리) + 종합 실습
   교재는 T1 핵심 / T2 심화 / T3 확장 / T4 실습 네 트랙
   T2 심화와 T3 확장은 3일차 과정 종료 시에 배포

2) 실습 데이터 - 동일
   통신(telecom) 도메인 단일, seed 스크립트 4개
   00_schema.sql   테이블 4종 생성
   01_seed_core.sql 고객 500 / 가입 800 / 사용로그 약 9만
   03_account.sql   계좌 4행, Day2 트랜잭션 동시성 전용
   02_seed_bulk.sql 사용로그 약 300만, Day3 당일에만 적재
   적재 순서 원칙: 00 -> 01 (Day1), +03 (Day2), +02 (Day3)
   대용량을 미리 넣으면 Day1~2 조회가 느려지므로 순서 준수
   조회/적재/ERD는 public, DDL/DML 연습은 lab 스키마로 분리

3) 문서 계층 - 여기서 차이가 난다
   통합본 덱은 T1 본문(강의 이론)만 담고 있고,
   종합실습 과제/평가/제출 규정에 대한 언급이 전혀 없습니다.
   반면 울산 배포용에는 운영 문서가 별도로 붙어 있습니다.
   - 02 과정안내: 3일 일정표, 사전 준비물, 트랙 연계표
   - 04 종합실습가이드: 과제 전문, 평가 배점, 제출 규정
   - 05 실습데이터가이드: 5단계 적재 절차와 트러블슈팅 10종
   또한 통합본 용어집에는 강사 전용 자료에만 나오는 약어가
   포함돼 있어, 통합본은 강사 마스터 계열,
   울산 5종은 수강생 배포본 계열로 보입니다.

4) 평가 체계 - 울산 배포본에서만 확인됨
   총 200점 = 개인 100 + 조별 100
   A 퀴즈와 코드분석 60점: Day별 20점, 매일 17:45~18:00 15분
                          회당 퀴즈 15 + 코드해석 5
   B 개인 과제 40점: 정규화 워크시트 20 + 단일 테이블 조회 20
   C 조별 과제 100점: 실무 SQL 6문항 60 + 종합 튜닝 40
                      Day2 4문항 + Day3 2문항, 팀 공통 100%
   루브릭 4축: 정확성 / 효율성 / 가독성 / 증빙

5) 제출 규정
   제출처: 반별 담당 교수님 Slack DM
   형식: 작성 도구는 자유, 최종 산출물은 PDF로 변환
   파일명 개인: [반][번호][이름][DAYn][과제명].pdf
   파일명 조별: [반][x조][DAYn][과제명].pdf
   마감: Day1~3 종합실습 모두 Day3 종료일까지, 이후는 평가 제외
   감점: 쿼리 없이 결과만 제출, 불필요 컬럼 과다, 문항 누락
   Day3는 EXPLAIN before/after 캡처가 없으면 개선 증명 불가로 감점

[운영 시 주의]
실습 도구는 DBeaver로 통일합니다.
pgAdmin이 PostgreSQL 설치 시 함께 깔리지만 본 과정에서는 쓰지 않습니다.
PostgreSQL 버전은 17로 고정합니다.

포트 표기는 배포 문서마다 다릅니다. 원문 그대로 옮기면 이렇습니다.
  05 실습데이터가이드 : 5432
  13 환경구축과SQL기초 : 연결이 거부되면 5433도 확인

주석) 둘 중 어느 쪽이 맞다기보다, 포트는 설치 환경에 따라 달라지는 값입니다.
5432는 PostgreSQL 기본 포트이고, 이미 다른 인스턴스가 그 포트를 쓰고 있으면
설치 과정에서 5433 같은 다음 번호로 잡히는 일이 흔합니다.
그래서 두 문서가 서로 다르게 적혀 있어도 오류가 아닙니다.
실제 값은 각자 설치할 때 지정한 포트이니, 접속이 안 되면 그것부터 확인하면 됩니다.`,note:`분반별로 배우는 내용이 달라지는 것이 아니라, 배포되는 문서의 층위가 다릅니다.
수강생 질문이 과제 배점이나 제출 형식으로 들어오면 이론 덱이 아니라 종합실습 가이드 쪽을 근거로 답해야 합니다.
제출 마감이 Day별이 아니라 3일차 종료일 일괄이라는 점은 특히 오해가 잦으니 초반에 짚어 주는 편이 좋습니다.`}]},stats:{concepts:[{term:"모집단과 표본 — 기술통계에서 추론통계로",desc:`모집단(Population)은 알고 싶은 대상 전체, 표본(Sample)은 그중 실제로 관측한 일부다. 전체를 다 조사할 수 없으니 표본의 평균·분산 같은 통계량으로 모집단의 모수를 추정하는 것이 추론통계이고, 그 전에 표본 자체를 요약·시각화해 특징을 파악하는 것이 기술통계다.
교재는 순서를 강조한다 — 추정과 검정에 들어가기 전에 반드시 탐색적 데이터 분석(EDA)을 먼저 하라. 데이터의 생김새(분포·결측·이상치)를 모른 채 만든 분석 모형과 결과는 신뢰하기 어렵기 때문이다.`},{term:"평균 vs 중앙값, 그리고 퍼짐(분산·표준편차·IQR)",desc:`평균은 모든 값을 반영하지만 이상치(Outlier)에 민감하고, 중앙값은 크기 순으로 가운데 값이라 이상치에 강건(Robust)하다. 교재 예시처럼 {1,2,2,3,4,7,15}에 79 하나만 추가돼도 평균은 4.9에서 14.1로 튀지만 중앙값은 3에서 3.5로 거의 움직이지 않는다.
중심만으로는 부족하고 퍼짐을 함께 봐야 한다. 분산·표준편차는 평균에서 떨어진 정도를, 사분위수와 IQR(Q3-Q1)은 가운데 50%가 퍼진 폭을 나타내며, Q3 + 1.5×IQR을 넘는 값을 이상치 후보로 잡는 것이 박스플롯의 기준이다. 분포의 비대칭은 왜도, 뾰족함은 첨도로 측정한다.`},{term:"정규분포와 표준화(Z변환)",desc:`정규분포 N(μ, σ²)은 평균을 중심으로 좌우대칭인 종 모양 분포로, 평균=중앙값=최빈값이 일치하고 곡선 아래 전체 면적이 1(확률)이다. 평균에서 표준편차 1배 안에 약 68%, 2배 안에 약 95%의 데이터가 들어온다는 성질 덕분에 "이 값이 얼마나 드문 값인가"를 확률로 말할 수 있다.
단위가 다른 변수끼리 비교하려면 평균 0, 표준편차 1로 맞추는 Z변환(표준화)을 쓴다. 표준화는 집단 간 비교뿐 아니라 분석 모형을 만들 때 변수 간 스케일 차이의 효과를 제거하는 전처리로도 필수다.`},{term:"가설검정과 p-value — 표본으로 하는 통계적 의사결정",desc:`가설검정은 "차이가 없다·효과가 없다"는 귀무가설(H0)을 세워 놓고, 표본 데이터가 그 가정과 얼마나 어긋나는지를 확률로 재는 절차다. p-value는 귀무가설이 맞다고 가정할 때 지금 관측된 정도의 차이가 우연히 나올 확률이며, 유의수준(보통 0.05)보다 작으면 귀무가설을 기각하고 대립가설을 채택한다.
교재의 회귀분석 해석이 좋은 예다 — 쿠폰배포매수의 p값이 0.05보다 작아 "영향을 주지 않는다"는 귀무가설을 기각(유의)하고, 판매가격은 p값 0.81로 귀무가설을 채택해 변수에서 제거한다. p-value는 효과의 크기가 아니라 우연 가능성의 척도라는 점을 혼동하지 말자.`},{term:"상관은 인과를 함축하지 않는다",desc:`상관계수는 두 연속형 변수의 선형 관계 강도를 -1(완전한 음의 관계)부터 +1(완전한 양의 관계) 사이 하나의 수로 요약하며, 공분산을 두 변수의 표준편차로 나눠 표준화한 값이다. 0이면 선형 관계가 없다는 뜻일 뿐, 곡선 관계는 잡아내지 못하므로 반드시 산점도를 함께 봐야 한다.
교재가 원문 그대로 강조하는 문장 — Correlation doesn't imply Causation. X와 Y의 상관이 높다고 X가 Y의 원인인 것은 아니다. 제3의 변수가 둘 다에 영향을 줬을 수도, 우연의 일치일 수도 있다. 상관분석은 예측·분류 모형에 넣을 유의미한 변수를 고르는 탐색 도구로 쓰고, 인과 주장은 별도의 근거가 필요하다.`},{term:"회귀분석 — 기본 가정과 결과 해석 3단계",desc:`상관분석이 관계를 하나의 값으로 요약한다면, 회귀분석은 관계를 y = a·x + b라는 수식으로 표현해 예측과 요인 해석까지 가능하게 한다. 전제는 독립변수와 종속변수의 관계가 직선(선형성)이라는 것 — 산점도로 먼저 확인하고, 만족하지 않으면 로그변환을 하거나 그 변수를 제거한다. 독립변수끼리 상관이 높으면(다중공선성) 중복 변수를 미리 걸러낸다.
결과 해석은 3단계다. ① 적합성: 결정계수(Adj. R²)가 0.735라면 모형이 종속변수 변동의 73.5%를 설명한다. ② 모형 유의성: F검정의 p가 0.05 미만이면 회귀모형 자체가 유의하다. ③ 계수 유의성: 각 계수의 p값을 보고 유의하지 않은 변수는 제거한 뒤 회귀식을 다시 만든다.`},{term:"개념적 정의와 조작적 정의 — 분석은 측정 가능한 문장에서 출발",desc:`개념적 정의는 알고 싶은 것을 추상적으로 서술한 문장이고("정상 범위를 벗어날수록 설비가 멈출 가능성이 높다"), 조작적 정의는 그것을 실제로 측정·관찰할 수 있는 형태로 바꾼 것이다. 교재의 공정 센서 사례가 전형이다 — 정상 기간의 평균과 표준편차로 z값을 구해 '정상 범위 이탈 지수(0~4점 척도)'로 변환하는 순간, 막연한 가설이 계산 가능한 변수가 된다.
조작적 정의가 서야 그다음 작업인 변수 정의, 분석 단위 설정(개인·가구·상품·기간 등), 척도화가 진행된다. 같은 전력 데이터라도 발전 계획에는 1시간 평균을, 실시간 수요 대응에는 순시 데이터를 쓰듯, 분석 단위는 비즈니스 목적이 결정한다.`},{term:"분석 방법론 6단계 — 비즈니스 이해에서 시스템 적용까지",desc:`교재는 분석 프로젝트를 비즈니스 이해 → 가설 수립(데이터 이해) → 데이터 준비 → 분석 모델 정의 → 모델 평가 → 시스템 적용의 6단계로 체계화한다. 각 단계는 절차·방법·도구·산출물(데이터 정의서, 분석 결과서, 모델 평가서 등)로 정리되며, 분석 기법 선택이나 코딩은 이 흐름의 일부일 뿐이다.
단계별 핵심 질문도 정해져 있다 — 데이터 준비에서는 이상치·결측치 처리와 파생변수 로직을, 모델 정의에서는 EDA로 확인한 분포·관계를 근거로 알고리즘 선정을, 모델 평가에서는 비즈니스 관점의 평가와 배포 후 모니터링(성능 저하·데이터 변화 감지) 계획까지 묻는다. 모델을 만들고 끝이 아니라 운영까지가 방법론의 범위다.`},{term:"척도의 4수준 — 명목·순서·구간·비율",desc:`같은 숫자라도 측정 수준이 다르면 할 수 있는 연산이 다르다. 명목척도는 구분만 하는 기호(등번호, 성별=1/2), 순서척도는 순서까지 있는 값(1등·2등·3등, 등급), 구간척도는 간격이 산술적 의미를 갖지만 0이 '없음'이 아닌 값(온도 0도, 서기 0년), 비율척도는 절대영점이 있어 비율 비교까지 가능한 값(몸무게 0kg, 소요시간 0초)이다. 합격=1/불합격=0처럼 두 상태만 구분하면 이진척도다.
척도는 분석 기법 선택의 열쇠다 — 독립·종속변수가 각각 범주형인지 연속형인지에 따라 교차분석, 판별분석, 상관분석, 회귀분석으로 갈라진다. 평균을 낼 수 있는 척도인지부터 확인하는 습관이 잘못된 통계량 계산을 막아 준다.`},{term:"변수의 유형 — 독립·종속·매개·통제, 그리고 파생변수",desc:`원인이 되는 독립변수(X, Feature)와 결과가 되는 종속변수(Y, Target) 외에도 두 유형이 더 있다. 매개변수는 독립변수가 종속변수에 영향을 주는 경로의 중간에 끼는 변수(스마트폰 사용 시간 → 수면시간 → 학업성적)이고, 통제변수는 보려는 효과를 정확히 재기 위해 고정하는 변수(운동 시간과 체중 감소를 볼 때 식이조절 여부)다.
파생변수는 원천 데이터를 조작적 정의에 따라 조합해 새로 만든 변수다 — 거래 데이터에서 '매출액 합계'를 만들거나, '구매 2건 이상이고 합계 500원 이상이면 A등급' 같은 규칙으로 '고객등급'을 만드는 식이다. 비즈니스 로직이 들어가므로 생성 규칙을 주석·문서로 남기는 것이 필수이고, 업무 담당자와의 협의가 품질을 좌우한다.`},{term:"결측치·이상치 처리 — 지우기 전에 비즈니스 관점부터",desc:`결측치(NA) 처리는 크게 두 갈래다 — 분석에서 제외하거나, 0·평균·이동평균 같은 대표값·트렌드값으로 대체한다. 교재가 강조하는 순서는 기술이 아니라 판단이다: 제외해도 되는지 비즈니스 관점에서 반드시 검토하고, 어떤 항목을 어떻게 처리했는지 반드시 공유·기록하라. 어떤 방법을 써도 정보 손실과 왜곡 가능성은 남기 때문이다.
이상치도 박스플롯 기준(IQR 1.5배)을 벗어났다고 기계적으로 지우면 안 된다. 분석 목적에 필요한 데이터인지 먼저 판단해야 하며, 이상거래 탐지(Fraud Detection) 같은 주제에서는 이상치가 삭제 대상이 아니라 바로 분석 대상이다. 이 경우 정상/비정상의 정의와 이상치 구분용 파생변수 설계가 먼저다.`},{term:"데이터 변환 — Min-Max 정규화·로그변환·더미변수",desc:`Z변환 외에도 자주 쓰는 변환이 세 가지 있다. Min-Max 정규화는 (관측값-최소)/(최대-최소)로 모든 값을 0~1 구간에 넣어 스케일 차이를 제거한다. 로그·제곱근 변환은 소득이나 SNS 연결 수처럼 오른쪽 꼬리가 긴 분포를 대칭에 가깝게 펴서, 정규분포를 가정하는 기법(회귀분석, t-검정, ANOVA 등)을 쓸 수 있게 해 준다.
연속형을 구간으로 자르는 이산형화(체중 49kg → '하' 그룹)와, 범주형을 0/1 컬럼으로 펴는 이항변수화(더미변수)도 변환의 일종이다. 이산형화는 구간 경계의 근거(분포, 변곡점)를 남겨야 하고, 더미변수는 범주 수보다 1개 적게 만들어야 한다 — 요일 7개면 더미 6개, 전부 만들면 완전한 중복이 생기는 Dummy Trap에 빠진다.`},{term:"로지스틱 회귀 — 확률로 답하는 회귀",desc:`시험 점수로 합격 '가능성'을 선형회귀로 예측하면 10점에서 -20%, 110점에서 120% 같은 불가능한 값이 나온다. 로지스틱 회귀는 S자 모양의 시그모이드 곡선으로 출력을 0~1 사이에 가둬, 결과를 확률로 해석할 수 있게 만든 회귀다. 종속변수가 연속형이면 선형회귀, 합격/불합격·정상/이상거래처럼 범주형(명목형)이면 로지스틱 회귀 — 이것이 두 기법을 가르는 기준이다.
종속변수 값이 2개면 이항 로지스틱(대출 가능/불가), 3개 이상이면 다항 로지스틱(승인/거절/보류)으로 나뉜다. 교재의 예시처럼 개인 신용도와 사용금액으로 이상거래 여부를 예측하는 문제가 전형적인 활용이며, 담당 과목의 분류(Classification) 모델로 이어지는 다리 역할을 한다.`},{term:"정규성 검정과 등분산 검정은 다르다",desc:"두 개념이 자주 뒤섞이는데 목적이 다르다. 정규성 검정은 데이터가 정규분포를 따르는지 보는 것이고, 등분산 검정(레빈 검정, Levene)은 비교하려는 두 집단의 퍼짐 정도(분산)가 비슷한지를 보는 것이다. 등분산 검정을 먼저 하는 이유는 그 결과에 따라 t-검정의 방식을 고르기 위해서다 — p값이 0.05 이상이면 등분산이 성립한다고 보고 일반 t-검정을, 0.05 미만이면 등분산이 깨진 것으로 보고 웰치(Welch) t-검정(equal_var=False)을 쓴다. 수업 실습에서 레빈 검정만 수행한 것은 검정 방식을 고르기 위한 절차였고 정규성 검정은 생략한 것이다."},{term:"회귀 summary 판독과 평가지표 고르기",desc:"OLS 결과표는 위에서부터 이렇게 읽는다 — R-squared는 모델이 데이터를 설명하는 비율, Adj. R-squared는 독립변수가 2개 이상일 때 변수 개수를 감안해 보정한 값이라 변수를 여럿 쓸 때는 이쪽을 본다. const의 coef는 모든 독립변수가 0일 때의 기준값이고, 각 변수의 coef는 다른 변수를 고정했을 때 그 변수 1단위 변화가 예측값에 주는 영향이다. P>|t|는 그 변수가 의미 있는지를 알려 주며 0.05 미만인 변수만 남기고 나머지는 빼고 다시 돌린다. 예측 성능 지표는 용도가 갈린다 — R2는 예측용으로 쓸 거면 0.8 이상을 권하고, MSE는 오차를 제곱해 단위가 원래 값과 달라지므로 보고서에 쓰지 않고 최적값을 찾는 계산 과정에만 쓴다. 보고용으로는 단위가 그대로 유지되는 MAE를 권하며, RMSE는 큰 오차에 더 민감해 이상치가 있을 때 MAE와 눈에 띄게 다른 값이 나온다 — 두 값의 차이 자체가 이상치 존재의 신호다."},{term:'Baseline과 개선율 — 모델이 "잘한 것"의 기준선',desc:'R2가 0.88이라는 숫자 하나만으로는 모델이 쓸모 있는지 알 수 없다. 그래서 아무 것도 학습하지 않은 가장 단순한 예측기(Baseline)를 먼저 만들어 놓고 그보다 얼마나 나아졌는지를 본다. 회귀에서 흔한 Baseline은 "무조건 학습 데이터의 평균값으로 예측한다"이다. 여기에 MAPE(평균 절대 백분율 오차 — 오차를 실제값으로 나눠 퍼센트로 본 뒤 평균낸 값)를 재고, 개선율은 (Baseline 오차 - 모델 오차) / Baseline 오차로 계산한다. 중고차 가격 예측 실습의 참고 기준값은 Baseline MAPE 약 338%, 모델 MAPE 약 67%, 개선율 약 80%였다 — Baseline MAPE가 세 자리로 큰 이유는 값이 싼 차에서 오차 비율이 폭발하기 때문이며, 이것이 바로 평균 예측의 한계를 보여 준다. 보고서에는 반드시 Baseline과 모델을 나란히 적어야 "그래서 얼마나 좋아졌는가"에 답할 수 있다.'},{term:"로그 변환과 스케일링의 규율 — fit은 train에만, 예측값은 되돌린다",desc:"가격처럼 오른쪽 꼬리가 긴(왜도가 큰) 종속변수는 그대로 회귀하면 비싼 차 몇 대에 모델이 끌려간다. 이때 np.log1p로 로그 세상으로 옮겨 학습하면 분포가 대칭에 가까워져 성능이 크게 오른다. 다만 예측 결과는 로그 세상의 값이므로 짝인 np.expm1로 반드시 원래 단위로 되돌린 뒤에 평가해야 한다 — 이 역변환을 빠뜨리면 오차가 터무니없이 작게 나와 스스로를 속이게 된다. 스케일링에도 규율이 있다. 스케일러는 학습 데이터로만 fit_transform하고 테스트 데이터에는 transform만 적용한다. 테스트 데이터로도 fit하면 평가에 쓸 정보가 학습 과정에 새어 들어가는 데이터 누수가 되어 실제보다 좋은 성능이 찍힌다. 마지막으로 역변환한 예측값에 음수가 섞일 수 있는데, 가격은 음수가 될 수 없으므로 0으로 보정한 뒤 지표를 계산한다."},{term:"비용함수(Cost/MSE)는 왜 U자 곡선이 되는가 (판교 7반 김일한 교수 자료)",desc:"회귀에서 기울기 w를 하나 정하면 예측값이 정해지고, 실제값과의 차이(오차)도 정해집니다. 이 오차를 제곱해서 평균낸 값이 비용(cost)이고 흔히 MSE라고 부릅니다. 제곱하는 이유는 오차가 위로 나든 아래로 나든 똑같이 벌점을 주기 위해서이고, 덤으로 크게 틀린 예측에 더 큰 벌점이 붙습니다. w를 아주 작은 값부터 큰 값까지 조금씩 바꿔가며 cost를 찍어보면 그릇을 엎어놓은 듯한 U자 곡선이 나타나는데, 이 그릇의 가장 밑바닥에 해당하는 w가 바로 우리가 찾는 최적의 기울기입니다. 회귀 학습이란 결국 이 U자 골짜기의 바닥을 찾아가는 일입니다.",origin:"practice",source:"김일한 실습교수(판교 7반) — 회귀 실습 노트북 13종"},{term:"경사하강법 — 미분한 기울기와 학습률로 w와 b를 조금씩 고치기 (판교 7반 김일한 교수 자료)",desc:"비용 곡선의 바닥을 한 번에 찾는 대신, 아무 데서나 출발해 한 걸음씩 내려가는 방법이 경사하강법입니다. 지금 서 있는 자리에서 곡선의 접선 기울기(미분값)를 구하면 어느 쪽이 오르막인지 알 수 있고, 그 반대 방향으로 가면 내리막입니다. 갱신 규칙은 간단히 말해 새로운 w는 지금의 w에서 학습률 곱하기 기울기를 빼는 것이며, 절편 b도 같은 방식으로 함께 고칩니다. 학습률은 한 걸음의 보폭인데, 너무 크면 골짜기를 뛰어넘어 발산하고 너무 작으면 도착하기 전에 반복이 끝나버립니다. 바닥에 가까울수록 기울기가 0에 가까워져 걸음폭이 저절로 줄어드는 것이 이 방법의 묘미입니다.",origin:"practice",source:"김일한 실습교수(판교 7반) — 회귀 실습 노트북 13종"},{term:"공분산과 상관계수 — 최소제곱 회귀계수 공식의 뿌리 (판교 7반 김일한 교수 자료)",desc:"공분산은 두 변수가 각자의 평균에서 얼마나 벗어났는지(편차)를 서로 곱해서 평균낸 값입니다. 두 변수가 함께 커지고 함께 작아지면 곱이 양수로 쌓여 공분산이 양수가 되고, 반대로 움직이면 음수가 되며, 방향이 뒤죽박죽이면 양수와 음수가 상쇄되어 0에 가까워집니다. 다만 공분산은 단위에 휘둘려서 kg으로 재느냐 g으로 재느냐에 따라 값이 통째로 달라지므로, 각 변수의 표준편차로 나누어 -1에서 1 사이로 표준화한 것이 상관계수입니다. 중요한 점은 단순선형회귀의 기울기 추정식이 정확히 x와 y의 편차곱 합을 x의 편차제곱 합으로 나눈 형태, 즉 공분산을 x의 분산으로 나눈 값이라는 것입니다. 상관을 이해하면 회귀계수가 어디서 튀어나온 숫자인지 저절로 풀립니다.",origin:"practice",source:"김일한 실습교수(판교 7반) — 회귀 실습 노트북 13종"},{term:"다중선형회귀에서 변수를 늘리기 전에 점검할 것들 — 다중공선성과 VIF (판교 7반 김일한 교수 자료)",desc:"독립변수를 여러 개 넣으면 설명력은 올라가는 것처럼 보이지만, 변수끼리 서로 너무 닮아 있으면 문제가 생깁니다. 예를 들어 키(cm)와 키(inch)를 함께 넣으면 두 변수 중 누가 얼마나 기여했는지 모델이 판단할 수 없어 회귀계수가 크게 흔들리고 부호까지 뒤집히기도 하는데, 이를 다중공선성이라 합니다. 진단은 상관계수 히트맵으로 눈으로 보고, 숫자로는 VIF(분산팽창계수)를 계산해 보통 10을 넘으면 위험 신호로 봅니다. 조치는 겹치는 변수 중 하나를 빼거나 두 변수를 합쳐 새 지표를 만드는 것입니다. 이 밖에도 잔차의 독립성(더빈-왓슨), 등분산성, 잔차 정규성, 영향점(쿡의 거리)까지 함께 살펴야 안심하고 계수를 해석할 수 있습니다.",origin:"practice",source:"김일한 실습교수(판교 7반) — 회귀 실습 노트북 13종"}],examples:[{title:"기술통계 — 평균 vs 중앙값, IQR 이상치, 분포 모양",lang:"python",code:`import pandas as pd                           # 표 데이터 처리 라이브러리

# 교재 예시: 극단값 하나가 평균을 얼마나 흔드는지 확인
x = pd.Series([1, 2, 2, 3, 4, 7, 15])         # 원래 데이터
y = pd.Series([1, 2, 2, 3, 4, 7, 15, 79])     # 이상치 79가 추가된 데이터
print(x.mean(), x.median())                   # 평균 4.86 / 중앙값 3
print(y.mean(), y.median())                   # 평균 14.13으로 급등 / 중앙값 3.5는 안정
# 평균은 이상치에 민감, 중앙값은 강건(Robust) — 항상 둘을 함께 본다

# 퍼짐: 표준편차와 사분위수·IQR
print(y.std())                                # 표준편차: 평균에서 떨어진 평균적 거리
q1, q3 = y.quantile(0.25), y.quantile(0.75)   # 1사분위수(Q1), 3사분위수(Q3)
iqr = q3 - q1                                 # IQR: 가운데 50%가 퍼진 폭
upper = q3 + 1.5 * iqr                        # 박스플롯의 이상치 상한 기준선
print(y[y > upper])                           # 기준 밖 값(79)을 이상치 후보로 탐지

# 분포 모양: 왜도(비대칭)와 첨도(뾰족함)
print(y.skew(), y.kurt())                     # 왜도 0이면 좌우대칭, 양수면 오른쪽 긴 꼬리`,note:"교재 3-(3) Statistics의 예시 숫자를 그대로 썼다. 이상치 하나로 평균이 3배 가까이 튀는 것을 눈으로 확인하는 것이 포인트 — 그래서 EDA에서는 mean과 median을 나란히 놓고, IQR 기준으로 이상치를 먼저 점검한 뒤에야 모형으로 넘어간다."},{title:"가설검정의 직관 — 두 집단 평균 비교(t-검정)와 p-value",lang:"python",code:`from scipy import stats                       # 통계 검정 함수 모음
import numpy as np                            # 수치 계산

# 질문: 쿠폰을 배포한 매장의 하루 구매고객수가 정말 더 많은가?
coupon = np.array([315, 302, 328, 341, 310, 335, 322])     # 쿠폰 배포 매장 표본
no_coupon = np.array([288, 295, 279, 301, 284, 292, 287])  # 미배포 매장 표본

# 귀무가설 H0: 두 집단의 평균 구매고객수는 같다 (쿠폰 효과 없음)
# 대립가설 H1: 두 집단의 평균이 다르다 (쿠폰 효과 있음)
t_stat, p_value = stats.ttest_ind(coupon, no_coupon)  # 독립 2표본 t-검정
print(t_stat, p_value)                        # 검정통계량과 p-value 출력

# p-value: H0가 맞다고 가정할 때 이 정도 차이가 우연히 나올 확률
if p_value < 0.05:                            # 유의수준 5%와 비교
    print('귀무가설 기각: 쿠폰 효과가 통계적으로 유의하다')   # 차이를 인정
else:                                         # p-value가 0.05 이상이면
    print('귀무가설 채택: 차이가 우연일 가능성을 배제 못 한다') # 효과 단정 금지

# 주의: 표본으로 모집단을 판단하는 것이므로 표본이 작으면 결론의 힘도 약하다`,note:'교재의 "표본 데이터를 활용한 통계적 의사결정"을 코드로 옮겼다. 핵심 어휘는 넷 — 귀무가설(차이 없다), 대립가설(차이 있다), 유의수준(0.05), p-value(우연 확률). p가 작으면 기각, 크면 채택이라는 판정 규칙은 뒤의 회귀계수 유의성 검증에서 그대로 재사용된다.'},{title:"선형회귀 적합과 해석 — 쿠폰·가격이 구매고객수에 주는 영향",lang:"python",code:`import pandas as pd                           # 데이터프레임
import statsmodels.api as sm                  # 회귀분석 라이브러리

# 교재 사례: 쿠폰배포매수(x1)·판매가격(x2) 중 무엇이 구매고객수(y)를 움직이나?
df = pd.DataFrame({                           # 매장별 마케팅 실적 데이터
    'coupon': [500, 700, 900, 1100, 1300, 1500, 1700],    # 쿠폰 배포 매수(장)
    'price':  [3000, 2800, 3200, 2900, 3100, 2700, 3000], # 판매 가격(원)
    'buyers': [330, 355, 372, 400, 419, 442, 460],        # 구매 고객 수(명)
})                                            # 실습에서는 CSV를 읽어 사용
X = sm.add_constant(df[['coupon', 'price']])  # 절편(상수항)을 명시적으로 추가
model = sm.OLS(df['buyers'], X).fit()         # 최소제곱법(OLS)으로 회귀식 적합
print(model.summary())                        # 계수·p값·결정계수 결과표 출력

# 해석 3단계 (교재의 순서 그대로)
# 1) 적합성: Adj. R-squared 0.735라면 모형이 변동의 73.5%를 설명
# 2) 모형 유의성: F검정 p < 0.05 → 회귀모형 자체가 유의
# 3) 계수 유의성: coupon p < 0.05 → 유의 / price p = 0.81 → 유의하지 않음

X2 = sm.add_constant(df[['coupon']])          # 유의하지 않은 판매가격을 제거하고
model2 = sm.OLS(df['buyers'], X2).fit()       # 회귀식을 다시 적합한다
print(model2.params)                          # 예: buyers = 0.108 x coupon + 282.9
# 해석: 쿠폰을 1장 더 배포하면 구매고객이 약 0.1명 늘어난다는 뜻`,note:"교재 3-(6) 회귀분석의 마케팅 사례를 파이썬으로 재현했다. 결정계수로 설명력을, F검정으로 모형 전체를, 계수별 p값으로 개별 변수를 검증하는 3단계가 해석의 정석이며, 유의하지 않은 변수(판매가격 p=0.81)는 빼고 다시 적합하는 것까지가 한 사이클이다. 이 감각은 담당 과목의 피처 선택·모델 평가로 그대로 이어진다."},{title:"정규분포로 상위 몇 %인지 계산하기 — 표준화 Z의 활용",lang:"python",code:`from scipy import stats                       # 정규분포 확률 계산 함수 모음

# 문제: 평균 600점, 표준편차 150점인 시험에서 800점은 상위 몇 %인가?
mu = 600                                      # 전체 평균
sigma = 150                                   # 표준편차(1걸음의 크기)
score = 800                                   # 내 점수

z = (score - mu) / sigma                      # 표준화: 평균에서 몇 걸음 떨어졌나
print(round(z, 2))                            # 1.33 (표준편차 1.33배 위)

below = stats.norm.cdf(z)                     # z 이하일 확률(누적분포) = 0.9082
above = 1 - below                             # 나보다 높은 비율 = 상위 비율
print(round(above * 100, 2))                  # 9.18 → 상위 약 9.2% 수준

# 정규분포의 기본 감각: 1시그마 안에 약 68%, 2시그마 안에 약 95%
p1 = stats.norm.cdf(1) - stats.norm.cdf(-1)   # 평균 좌우 1시그마 구간 확률
p2 = stats.norm.cdf(2) - stats.norm.cdf(-2)   # 평균 좌우 2시그마 구간 확률
print(round(p1, 4), round(p2, 4))             # 0.6827 0.9545

# 응용: N(30, 4의 제곱)에서 값이 22~42 사이일 확률은?
p = stats.norm.cdf(42, 30, 4) - stats.norm.cdf(22, 30, 4)  # 구간 확률 = 큰쪽 누적 - 작은쪽 누적
print(round(p, 4))                            # 0.9759 (z=2 구간 0.4772 + z=3 구간 0.4987)`,note:"교재 확률분포 단원의 연습문제 두 개(TOEIC 800점 상위 %, N(30,4^2) 구간 확률)를 코드로 재현했다. 표준정규분포표를 손으로 찾는 대신 stats.norm.cdf 하나면 되지만, 원리는 같다 — 표준편차를 '걸음 수'로 삼아 내 값이 분포 어디쯤인지 확률로 말하는 것. 이 감각이 뒤의 이상 탐지(정상 범위 이탈 지수)로 그대로 이어진다."},{title:"데이터 변환 3종 비교 — Z변환·Min-Max·로그변환",lang:"python",code:`import numpy as np                            # 수치 계산 라이브러리

# 질문: A집단(평균 10, 표준편차 3)의 19점과 B집단(평균 2, 표준편차 1)의 3점, 누가 더 대단한가?
za = (19 - 10) / 3                            # A집단 Z점수 = 3.0 (평균에서 3시그마 위)
zb = (3 - 2) / 1                              # B집단 Z점수 = 1.0 (1시그마 위)
print(za, zb)                                 # 단위·분포가 달라도 Z로 바꾸면 비교 가능

# Min-Max 정규화: 모든 값을 0~1 구간으로 변환
y1 = (45 - 10) / (50 - 10)                    # min 10, max 50인 집단의 45 → 0.875
y2 = (200 - 100) / (1000 - 100)               # min 100, max 1000인 집단의 200 → 0.111
print(y1, round(y2, 3))                       # 스케일이 달라도 같은 0~1 잣대로 비교

# 로그변환: 오른쪽 꼬리가 긴 분포를 대칭에 가깝게 편다
x = np.array([10, 100, 1000])                 # 10배씩 벌어진 치우친 값들
print(np.log10(x))                            # [1. 2. 3.] — 간격이 고르게 펴짐
print(np.sqrt(x).round(1))                    # [3.2 10. 31.6] — 제곱근 변환도 대안

# 선택 기준 정리
# 집단 간 비교, 모형 입력의 스케일 통일 → Z변환
# 0~1 고정 범위가 필요할 때(신경망 입력 등) → Min-Max
# 소득처럼 오른쪽으로 치우친 분포 교정 → 로그/제곱근 변환`,note:"교재 데이터 변환 단원의 숫자를 그대로 썼다(A집단 19점의 z=3, Min-Max 0.875와 0.111, 로그변환 10/100/1000 → 1/2/3). 변환의 공통 목적은 하나 — 단위와 분포가 다른 데이터를 같은 잣대에 올려 비교·학습 가능하게 만드는 것이다. 특히 로그변환은 정규분포 가정을 쓰는 회귀·t-검정 전에 자주 필요하다."},{title:"결측치·이상치 전처리 — 평균 대체, 이동평균 대체, IQR 필터",lang:"python",code:`import pandas as pd                           # 표 데이터 처리
import numpy as np                            # NaN(결측) 표현용

# 일별 판매량 7일치에 결측 2건이 섞여 있다고 가정
s = pd.Series([120, 130, np.nan, 128, 140, np.nan, 300],          # 판매량 데이터
              index=pd.date_range('2026-07-01', periods=7))        # 날짜 인덱스
print(s.isna().sum())                         # 결측 개수 확인: 2건

dropped = s.dropna()                          # 방법 1: 분석 제외 — 빼도 되는지 비즈니스 검토 먼저
fill_mean = s.fillna(s.mean())                # 방법 2: 대표값(평균)으로 대체
fill_ma = s.fillna(s.rolling(3, min_periods=1).mean())  # 방법 3: 이동평균 — 시계열 흐름을 반영한 대체
print(fill_ma.round(1))                       # 대체 결과 확인

# 이상치: IQR 기준으로 '후보'를 찾고, 삭제는 비즈니스 판단 후에
q1, q3 = s.quantile(0.25), s.quantile(0.75)   # 1사분위수, 3사분위수
iqr = q3 - q1                                 # 가운데 50%가 퍼진 폭
outlier = s[(s < q1 - 1.5 * iqr) | (s > q3 + 1.5 * iqr)]  # 상하한 기준 밖 값
print(outlier)                                # 300이 후보 — 프로모션 날 매출이면 지우면 안 된다

# 마지막 규칙: 무엇을 왜 어떻게 처리했는지 반드시 기록·공유한다
# (어떤 방법을 써도 정보 손실과 왜곡 가능성은 남기 때문)`,note:"교재 Preprocessing 단원의 두 원칙을 코드로 옮겼다 — 결측치는 '제외 vs 대체(0·평균·이동평균)' 중 비즈니스 검토를 거쳐 고르고, 이상치는 IQR로 탐지하되 삭제 여부는 분석 목적으로 판단한다. 이상거래 탐지 주제라면 이상치가 오히려 분석 대상이라는 점, 처리 내역을 문서로 남기라는 당부까지가 교재의 핵심이다."},{title:"로지스틱 회귀 — 시험 점수로 합격 확률 예측하기",lang:"python",code:`import numpy as np                            # 수치 계산
from sklearn.linear_model import LogisticRegression  # 로지스틱 회귀 모델

# 학습 데이터: 시험 점수(x)와 합격 여부(y: 1=합격, 0=불합격)
x = np.array([[35], [42], [50], [55], [62], [70], [78], [85], [90]])  # 점수 9명
y = np.array([0, 0, 0, 0, 1, 1, 1, 1, 1])     # 60점 근처에서 합격이 갈린다

model = LogisticRegression()                  # S자(시그모이드) 곡선을 적합하는 모델
model.fit(x, y)                               # 학습: 점수와 합격의 경계를 찾는다

new = np.array([[10], [58], [65], [110]])     # 새로 예측할 점수 4명
prob = model.predict_proba(new)[:, 1]         # 합격(클래스 1)일 확률만 추출
print(prob.round(3))                          # 항상 0~1 사이 확률로만 나온다

# 만약 선형회귀로 풀었다면?
# 10점 → 합격 가능성 -20%, 110점 → 120% 같은 불가능한 값이 나온다
# 로지스틱 회귀는 시그모이드로 출력을 0~1에 가두므로
# '합격 가능성 몇 %'라는 현실적인 답을 준다

pred = model.predict(new)                     # 확률 0.5를 기준으로 0/1 분류
print(pred)                                   # [0 0 1 1] — 58점 불합격, 65점 합격 예측`,note:"교재 로지스틱 회귀 단원의 도입 예제(시험 점수로 대학 합격 예측, 선형회귀의 -20%·120% 문제)를 sklearn으로 구현했다. 핵심은 종속변수가 범주형일 때는 값이 아니라 확률을 예측한다는 관점 전환이고, 값이 2개면 이항·3개 이상이면 다항 로지스틱으로 확장된다. 신용도·사용금액으로 이상거래를 가르는 교재의 실무 예시도 구조가 같다."},{title:"기초통계 수업 자료 안내 — 교재 2종과 오픈북 Quiz (7/16 배포)",lang:"text",code:`[배포된 교재 2종 — 판교 3·4·6~10반 채널 공유]
· 데이터 분석 개요 및 기초통계 _ Day1 _ 박병선w.pdf (5.7MB)
· 데이터 분석 개요 및 기초통계 _ 이은호.pdf (7.8MB)
  → 다음 주 월·화 이은호 교수 강의의 수업 자료가 이은호판이다 (김영희 교수 안내)

[Quiz 안내] 이 과목 Quiz는 교재 내에서 오픈북으로 진행될 예정 —
교재를 미리 훑어 어디에 무엇이 있는지 '목차 감각'을 만들어 두면 유리하다.

[함께 배포된 참고] SKALA_맥북 네트워크 연결.pdf (김범준 교수, 실습 환경 문제 시)`,note:"판교 캠퍼스 채널들에 배포된 기초통계 수업 자료 현황이다. 같은 과목이라도 캠퍼스·반에 따라 박병선w판과 이은호판이 함께 쓰이니 자기 반 공지의 교재를 기준으로 보되, 두 판 모두 skala-4 자료 폴더와 드라이브 교재 폴더에 보관되어 있다."},{title:"회귀분석 실습 전 과정 — 적재부터 EDA·가설검정·OLS까지 (판교 8반 시퀀스)",lang:"python",code:`# 1) 필요한 라이브러리를 한 번에 불러온다
import pandas as pd
import numpy as np
import matplotlib.pyplot as plt
import seaborn as sns
import statsmodels.api as sm       # OLS 회귀와 결과표(summary)를 제공
from scipy import stats            # 등분산 검정, t-검정

# 2) 구글 드라이브를 연결하고 데이터를 읽는다 (Colab 기준)
from google.colab import drive
drive.mount("/content/drive")
df = pd.read_csv("/content/drive/MyDrive/data/insurance.csv")

# 3) 구조부터 파악한다 — 행·열 개수, 자료형, 결측치 순서로
print(df.shape)          # (행, 열)
df.info()                # 컬럼별 자료형과 non-null 개수
print(df.isna().sum())   # 컬럼별 결측치 개수

# 4) IQR 방식으로 이상치를 찾는다
def detect_outliers_iqr(data, column):
    # 1사분위(25%)와 3사분위(75%)를 구한다
    q1 = data[column].quantile(0.25)
    q3 = data[column].quantile(0.75)
    iqr = q3 - q1                  # 사분위 범위
    lower = q1 - 1.5 * iqr         # 이 아래면 이상치 후보
    upper = q3 + 1.5 * iqr         # 이 위면 이상치 후보
    # 범위를 벗어난 행만 골라 돌려준다
    return data[(data[column] < lower) | (data[column] > upper)]

for col in ["age", "bmi", "children", "expenses"]:
    print(col, len(detect_outliers_iqr(df, col)), "건")

# 5) EDA — 분포는 히스토그램, 이상치는 박스플롯을 나란히 본다
fig, axes = plt.subplots(1, 2, figsize=(12, 4))
sns.histplot(df["expenses"], kde=True, ax=axes[0])   # 분포 모양
sns.boxplot(x=df["expenses"], ax=axes[1])            # 이상치 위치
plt.show()
# 범주형은 개수를 세어 불균형 여부를 확인한다
sns.countplot(x="smoker", data=df)
plt.show()

# 6) 가설검정 — 흡연 여부에 따라 의료비 평균이 다른가
#    귀무가설: 두 집단의 평균은 같다 / 대립가설: 다르다
smoker = df[df["smoker"] == "yes"]["expenses"]
nonsmoker = df[df["smoker"] == "no"]["expenses"]
# 먼저 등분산인지 확인한다 (레빈 검정)
lev = stats.levene(smoker, nonsmoker)
print("levene p =", lev.pvalue)
# p가 0.05 미만이면 등분산이 아니므로 웰치 t-검정을 쓴다
equal_var = lev.pvalue >= 0.05
print(stats.ttest_ind(smoker, nonsmoker, equal_var=equal_var))

# 7) 범주형을 숫자로 바꾼다 — drop_first로 기준 범주 하나를 뺀다(더미 함정 회피)
df_encoded = pd.get_dummies(
    df, columns=["sex", "smoker", "region"], drop_first=True
).astype(float)

# 8) 상관관계 히트맵으로 어떤 변수가 타깃과 관련 있는지 훑는다
corr_matrix = df_encoded.corr()
sns.heatmap(corr_matrix, annot=True, fmt=".2f", cmap="coolwarm")
plt.show()

# 9) OLS 회귀 — statsmodels는 상수항을 직접 넣어 줘야 한다
y = df_encoded["expenses"]
X = df_encoded.drop(columns=["expenses"])
X_with_constant = sm.add_constant(X)
model = sm.OLS(y, X_with_constant).fit()
print(model.summary())

# 10) P>|t|가 0.05를 넘는 변수(예: sex_male, region_northwest)를 빼고 다시 적합한다
X2 = X.drop(columns=["sex_male", "region_northwest"])
model2 = sm.OLS(y, sm.add_constant(X2)).fit()
print(model2.summary())`,note:"판교 8반 회귀분석 실습에서 순서대로 진행된 흐름을 하나로 이어 붙였다. 핵심은 순서 그 자체다 — 구조 파악 → 결측·이상치 → 시각적 EDA → 가설검정 → 인코딩 → 상관 확인 → 회귀 → 변수 정리 재적합. 이 순서를 건너뛰고 바로 모델부터 돌리면 왜 그런 계수가 나왔는지 설명할 근거가 없어져 보고서를 쓸 수 없다. 사용 데이터는 insurance.csv이며, 다중공선성 예시가 필요하면 day.csv의 temp와 atemp(상관 0.99)를 함께 보면 좋다."},{title:"Colab 준비 — 한글 폰트 즉시 적용과 드라이브 마운트",lang:"python",code:`# [Colab] 그래프의 한글이 네모(두부)로 깨지는 문제를 런타임 재시작 없이 해결한다
# 1) 나눔 폰트를 설치한다 (-qq는 설치 로그를 줄여 준다)
!apt-get -qq install -y fonts-nanum

import matplotlib.pyplot as plt
import matplotlib.font_manager as fm

# 2) 방금 설치한 폰트 파일을 matplotlib 폰트 목록에 직접 등록한다
#    이 한 줄 덕분에 런타임을 다시 시작하지 않아도 바로 인식된다
fm.fontManager.addfont("/usr/share/fonts/truetype/nanum/NanumGothic.ttf")

# 3) 기본 글꼴을 나눔고딕으로 지정한다
plt.rc("font", family="NanumGothic")

# 4) 한글 폰트를 쓰면 음수 기호가 깨지므로 유니코드 마이너스를 끈다
plt.rcParams["axes.unicode_minus"] = False

# 확인 — 제목에 한글이 제대로 나오면 성공
plt.title("한글 제목 표시 확인 -1.5")
plt.plot([0, 1], [0, -1.5])
plt.show()

# [로컬 환경] Colab이 아니라 내 컴퓨터에서 돌린다면 OS별로 폰트 이름이 다르다
# 윈도우:  plt.rc("font", family="Malgun Gothic")
# 맥:      plt.rc("font", family="AppleGothic")

# [드라이브 연결] 실습 데이터를 구글 드라이브에 두고 읽는 방식
from google.colab import drive
# 실행하면 권한 승인 창이 뜨고, 승인하면 /content/drive 아래에 내 드라이브가 붙는다
drive.mount("/content/drive")

import pandas as pd
# MyDrive 아래 경로를 그대로 쓰면 된다 (폴더·파일명에 공백이 있으면 따옴표 안에 그대로)
df = pd.read_csv("/content/drive/MyDrive/data/insurance.csv")
print(df.head())`,note:"기초통계 실습에서 가장 많이 막히는 두 지점이 한글 깨짐과 데이터 경로다. 폰트는 addfont로 직접 등록하면 런타임 재시작이 필요 없어 수업 흐름이 끊기지 않고, unicode_minus를 끄지 않으면 음수 축 라벨만 따로 깨져 원인을 찾기 어려워진다. 드라이브를 마운트해 두면 세션이 끊겨도 파일을 다시 업로드할 필요가 없다."},{title:"기초통계 종합실습 — 과제 구성과 제출 규격 (7/20 안내)",lang:"text",code:`[종합실습 과제 — LinearRegression Base code를 출발점으로]
  Task 1. Baseline Model의 회귀분석 결과를 해석한다
  Task 2. Baseline 모델을 개선한다
  Task 3. 통계적으로 유의하지 않은 변수를 제거하고 다시 평가한다

[채점 관점 — 반드시 기억할 것]
  "정답 자체보다 왜 그렇게 판단했는지의 과정을 중심으로 정리한다"
  · 즉 R2가 몇이라는 숫자보다, 어떤 근거로 변수를 뺐고 그 결과 무엇이 좋아졌는지의 서술이 점수를 만든다
  · 수행보고서 양식은 자유

[제출 파일명 규칙]
  · 수행보고서: 기초통계_6반_홍길동
  · 코드(선택): 실습_LinearRegression_홍길동.ipynb
  · 반에 따라 다른 규칙이 안내되기도 한다 —
    예) 고유번호_반_이름_데이터 분석개요 및 기초통계.zip (익일 오후 quiz 제출 시 함께)
  · 최종 파일명·마감은 각 반 실습교수님 안내를 우선한다

[보고서에 넣으면 좋은 문장 뼈대]
  1. 데이터 개요: 행 N개·열 M개, 결측치 처리 방식, 이상치 판단 기준(IQR)
  2. 탐색 결과: 타깃과 상관이 높았던 변수와 그 이유(도메인 해석 한 줄)
  3. Baseline 결과: Adj. R-squared 값과 유의하지 않았던 변수 목록(P>|t| 기준)
  4. 개선 과정: 무엇을 왜 바꿨는가 (변수 제거 · 인코딩 변경 · 이상치 처리)
  5. 개선 결과: 지표 Before/After 비교 — 보고용은 MAE, 이상치 영향 확인은 RMSE 병기
  6. 한계와 다음 단계: 설명하지 못한 부분과 추가로 모으고 싶은 변수`,note:'과제의 배점 무게가 "판단 과정"에 실려 있다는 점이 이 안내의 핵심이다. 지표를 올리는 것보다 왜 그 변수를 뺐는지(P>|t| 근거), 왜 그 지표를 골랐는지(MAE는 보고용·MSE는 계산용)를 문장으로 남기는 쪽이 점수와 실무 역량 둘 다에 유리하다. Before/After를 표로 나란히 놓는 것만으로도 개선 과정이 한눈에 전달된다.'},{title:"종합실습(중고차 가격 예측) 풀이 접근법 — 데이터 검증과 파생변수",lang:"python",code:`# [접근법] 정답 코드를 베끼기 전에 "왜 이 순서인가"를 잡고 가는 것이 핵심이다.
# 데이터: 중고차 거래 301행 x 9컬럼
#   Car_Name / Year / Selling_Price(타깃) / Present_Price / Kms_Driven
#   Fuel_Type / Seller_Type / Transmission / Owner

import pandas as pd
import numpy as np
from datetime import datetime

# 1) 먼저 "내가 기대한 데이터가 맞는지" 확인하고 시작한다
#    필수 컬럼이 없거나 비어 있으면 뒤 작업이 전부 헛수고가 되므로 여기서 끊는다
REQUIRED = ["Car_Name", "Year", "Selling_Price", "Present_Price",
            "Kms_Driven", "Fuel_Type", "Seller_Type", "Transmission", "Owner"]
missing = sorted(set(REQUIRED) - set(df.columns))
if missing:
    raise ValueError("필수 컬럼이 없습니다: " + str(missing))

# 2) 품질 표를 한 번에 만들어 자료형·결측·고유값을 같이 본다
#    세 가지를 따로 출력하면 비교가 안 되지만, 한 표로 묶으면 이상한 컬럼이 바로 눈에 띈다
quality = pd.DataFrame({
    "자료형": df.dtypes.astype(str),
    "결측치 수": df.isna().sum(),
    "고유값 수": df.nunique(dropna=False),
})
print(quality)

# 3) 왜도(skew)로 분포가 얼마나 치우쳤는지 숫자로 확인한다
#    양수가 클수록 오른쪽 꼬리가 길다 = 로그 변환을 검토할 신호
print(df[["Selling_Price", "Present_Price", "Kms_Driven"]].skew())

# 4) [가장 중요한 판단] 연식(Year)을 그대로 쓰지 않고 나이로 바꾼다
#    2015라는 숫자 자체는 의미가 없고, "몇 년 된 차인가"가 가격을 설명한다
current_year = datetime.now().year   # 기준 연도를 고정값으로 박지 않는다
df["Car_Age"] = current_year - df["Year"] + 1

# 5) 단위를 줄이고(43000 -> 43.0), 연간 주행거리라는 새 관점을 만든다
df["Kms_Driven"] = df["Kms_Driven"] / 1000
df["Kms_Per_Year"] = df["Kms_Driven"] / df["Car_Age"]

# 6) 파생변수를 만들었으면 곧바로 정합성을 확인한다
#    나눗셈을 했으니 0으로 나눠 무한대가 생기지 않았는지 반드시 본다
if (df["Car_Age"] <= 0).any():
    raise ValueError("Car_Age에 0 이하 값이 있습니다.")
if not np.isfinite(df["Kms_Per_Year"]).all():
    raise ValueError("Kms_Per_Year에 무한대 값이 있습니다.")

# 7) 모델에 넣지 않을 컬럼을 정리한다
#    Car_Name은 고유값이 너무 많은 이름표라 학습에 방해가 되고,
#    Year는 Car_Age로 이미 표현했으므로 중복이다
model_df = df.drop(columns=["Car_Name", "Year"]).copy()`,note:'이 실습에서 점수가 갈리는 지점은 모델이 아니라 파생변수다. Year를 그대로 넣으면 회귀계수가 "2015년마다 얼마"라는 해석 불가능한 값이 되지만, Car_Age로 바꾸면 "1년 늙을 때마다 얼마 떨어진다"는 문장이 바로 나온다. 파생변수를 만든 직후 무한대·음수를 검사하는 습관도 함께 가져가자 — 나눗셈으로 만든 변수는 조용히 망가지고 한참 뒤 모델 학습에서야 터진다.'},{title:"종합실습 — Baseline부터 로그 회귀·역변환·개선율까지의 흐름",lang:"python",code:`import numpy as np
import statsmodels.api as sm
from sklearn.model_selection import train_test_split
from sklearn.preprocessing import MinMaxScaler
from sklearn.metrics import mean_absolute_percentage_error

# 1) 범주형을 더미로 바꾼다 — drop_first로 기준 범주 하나를 빼 더미 함정을 피한다
model_df = pd.get_dummies(model_df,
    columns=["Fuel_Type", "Seller_Type", "Transmission"],
    drop_first=True, dtype=int)
# 전처리가 끝났으면 문자열 컬럼과 결측이 남지 않았는지 확인하고 넘어간다
assert model_df.isna().sum().sum() == 0, "결측치가 남아 있습니다"
assert not model_df.select_dtypes(include="object").columns.tolist()

# 2) 입력과 타깃을 나누고 학습·평가용으로 쪼갠다
X = model_df.drop(columns=["Selling_Price"])
y = model_df["Selling_Price"]
X_train, X_test, y_train, y_test = train_test_split(
    X, y, test_size=0.3, random_state=42, shuffle=True)

# 3) [기준선] 아무 것도 학습하지 않고 "무조건 평균"으로 예측해 본다
#    이 점수를 이기지 못하면 모델을 만든 의미가 없다
baseline_value = y_train.mean()
baseline_pred = np.repeat(baseline_value, len(y_test))
baseline_mape = mean_absolute_percentage_error(y_test, baseline_pred)
print("Baseline MAPE(%):", round(baseline_mape * 100, 2))

# 4) 수치형만 골라 스케일링한다 — 더미 컬럼(0/1)은 건드릴 필요가 없다
numeric_cols = ["Present_Price", "Kms_Driven", "Owner", "Car_Age", "Kms_Per_Year"]
X_train_scaled, X_test_scaled = X_train.copy(), X_test.copy()
scaler = MinMaxScaler()
# 학습 데이터로만 fit — 여기서 테스트를 섞으면 데이터 누수다
X_train_scaled[numeric_cols] = scaler.fit_transform(X_train[numeric_cols])
# 테스트에는 transform만 적용 (학습에서 배운 최소·최대를 그대로 쓴다)
X_test_scaled[numeric_cols] = scaler.transform(X_test[numeric_cols])

# 5) 상수항을 붙이고, 타깃은 로그 세상으로 옮겨 학습한다
X_train_const = sm.add_constant(X_train_scaled)
X_test_const = sm.add_constant(X_test_scaled, has_constant="add")
model = sm.OLS(np.log1p(y_train), X_train_const).fit()
print(model.summary())

# 6) 예측한 뒤 반드시 원래 단위로 되돌린다 (log1p의 짝은 expm1)
log_pred = model.predict(X_test_const)
test_pred = np.expm1(log_pred)

# 7) 가격은 음수가 될 수 없으므로 0으로 보정한 뒤 평가한다
test_pred = pd.Series(np.where(test_pred < 0, 0, test_pred), index=y_test.index)

# 8) 같은 잣대(MAPE)로 재고 개선율을 계산한다
model_mape = mean_absolute_percentage_error(y_test, test_pred)
improvement = (baseline_mape - model_mape) / baseline_mape
print("Model MAPE(%):", round(model_mape * 100, 2))
print("개선율(%):", round(improvement * 100, 2))

# 9) 유의한 변수만 추려 보고서에 쓴다 (p < 0.05)
p_values = model.pvalues.drop("const", errors="ignore")
print(p_values[p_values < 0.05].sort_values())`,note:"자주 나오는 사고 세 가지가 이 흐름에 다 들어 있다 — ① 테스트 데이터로도 스케일러를 fit해 버리는 데이터 누수, ② expm1 역변환을 빼먹고 로그 값 그대로 평가해 오차가 비현실적으로 작게 나오는 것, ③ 음수 예측을 그대로 두어 MAPE가 왜곡되는 것. 로그 변환을 켜고 끄며 MAPE를 비교해 보면 왜도가 큰 타깃에서 변환이 얼마나 효과적인지 몸으로 느낄 수 있다."},{title:"결측치 처리 4가지 — 무엇을 언제 고르는가 (선행 교안 보완)",lang:"python",code:`import pandas as pd

# 먼저 어디에 얼마나 비어 있는지 센다 — 처리 방법은 이 표를 보고 정한다
print(df.isnull().sum())

# ① 삭제 — 결측이 있는 행을 통째로 버린다
#    쓸 때: 결측이 아주 적고 데이터가 충분히 많을 때
#    주의: 결측이 특정 집단에 몰려 있으면 그 집단이 통째로 사라져 분석이 편향된다
df_dropped = df.dropna()

# ② 평균으로 대체 — 숫자 컬럼의 빈 칸을 그 컬럼 평균으로 채운다
#    쓸 때: 분포가 좌우 대칭에 가깝고 이상치가 별로 없을 때
df["국어"] = df["국어"].fillna(df["국어"].mean())

# ③ 중앙값으로 대체 — 평균 대신 가운데 값으로 채운다
#    쓸 때: 이상치가 있어 평균이 끌려갈 때 (연봉·집값처럼 한쪽 꼬리가 긴 데이터)
#    중앙값은 극단값에 흔들리지 않는다는 점이 핵심 이유다
df["국어"] = df["국어"].fillna(df["국어"].median())

# ④ 최빈값으로 대체 — 가장 자주 나온 값으로 채운다 (범주형 컬럼용)
#    mode()는 결과가 여러 개일 수 있어 Series로 돌아오므로 [0]으로 첫 값을 꺼낸다
df["지역"] = df["지역"].fillna(df["지역"].mode()[0])

# 이름처럼 대표값이라는 개념 자체가 없는 컬럼은 채우지 말고 표시만 남긴다
df["학생"] = df["학생"].fillna("Unknown")

# 처리 후에는 반드시 다시 세어 0이 되었는지 확인한다
print(df.isnull().sum())`,note:"결측치를 어떻게 채우느냐보다 왜 그 방법을 골랐느냐가 보고서에서 평가받는 지점이다. 숫자는 분포가 대칭이면 평균, 이상치가 있으면 중앙값, 범주형은 최빈값이 기본 선택이고, 이름처럼 대표값이 성립하지 않는 컬럼은 억지로 채우지 말고 Unknown 같은 표시를 남기는 편이 정직하다. 채우기 전후로 isnull().sum()을 두 번 찍는 습관이 실수를 막아 준다."},{title:"제출 전 자가 검증 체크리스트 — 채점 전에 내가 먼저 잡는다",lang:"text",code:`[왜 자가 검증인가]
  실습 가이드는 마지막에 assert(단언문) 묶음으로 결과를 스스로 점검하게 되어 있다.
  코드가 오류 없이 끝났다는 것과 결과가 맞다는 것은 전혀 다른 말이기 때문이다.
  아래 항목을 하나씩 통과시켜 놓으면 제출 후에 발견되는 사고가 거의 사라진다.

[데이터 단계]
  □ 원본 행 수가 예상과 같은가 (중고차 데이터 기준 301행)
  □ 원본 컬럼이 9개 전부 있는가 · 필수 컬럼 누락은 없는가
  □ 원본에 결측치가 없는가 (있다면 어떻게 처리했는지 보고서에 적었는가)

[전처리 단계]
  □ 파생변수(Car_Age · Kms_Per_Year)에 무한대나 음수가 없는가
  □ 인코딩 후 문자열(object) 컬럼이 하나도 남지 않았는가
  □ 전처리 후 결측치가 0인가

[분할·학습 단계]
  □ Train 행 수 + Test 행 수가 원본 행 수와 같은가 (210 + 91 = 301)
  □ 스케일러를 학습 데이터로만 fit했는가 (테스트는 transform만)
  □ random_state를 고정해 다시 돌려도 같은 결과가 나오는가

[평가 단계]
  □ 예측값을 원래 단위로 되돌렸는가 (log1p로 학습했다면 expm1)
  □ 예측값이 모두 0 이상인가 (가격에 음수가 있으면 안 된다)
  □ 모델 MAPE가 Baseline MAPE보다 낮은가 ← 이게 아니면 모델이 실패한 것
  □ 유의 변수(p < 0.05)가 1개 이상인가

[참고 기준값 — 버전에 따라 미세한 차이는 정상]
  Baseline MAPE 약 338% / Model MAPE 약 67% / 개선율 약 80% / R-squared 약 0.88
  내 결과가 이와 크게 다르면 전처리나 역변환 어딘가를 놓친 것이니 되짚어 볼 것.

[산출물]
  · 결과 엑셀은 시트를 나눠 저장한다 — 요약 / 유의변수 / 상관 Top2 / 회귀계수 전체 / 예측결과 / 전처리데이터
  · 파일명에 본인 이름을 넣는다 (예: 종합실습_결과물_기초통계_홍길동.xlsx)
  · CSV로 저장할 때 한글이 깨지면 encoding="utf-8-sig"를 쓴다
  · openpyxl이 없다는 오류가 나면 %pip install openpyxl 후 다시 실행한다
  · 최종 파일명·제출처는 각 반 실습교수님 안내를 우선한다`,note:'실습 가이드가 제공하는 자가 검증 항목을 학습자가 스스로 점검할 수 있는 체크리스트로 바꾼 것이다. 특히 "모델 MAPE가 Baseline보다 낮은가"는 통과 못 하면 나머지가 다 맞아도 분석이 실패한 것이라 가장 먼저 확인해야 하고, "예측값이 모두 0 이상인가"는 역변환·보정을 빠뜨렸을 때만 걸리므로 두 실수를 한 번에 잡아 준다.'},{title:"비용함수 곡선 그려보기 — w를 바꿔가며 cost의 U자 확인 (판교 7반 김일한 교수 자료)",lang:"python",code:`import numpy as np
import matplotlib.pyplot as plt

# 아주 단순한 데이터: y가 x와 똑같으므로 정답 기울기는 1이다
x_data = np.array([1, 2, 3])
y_data = np.array([1, 2, 3])

# 기울기 w를 넣으면 그 w의 비용(MSE)을 돌려주는 함수
def mse(x, y, w):
    hx = w * x                      # 예측값: 기울기 w를 곱한 값
    return np.sum((hx - y) ** 2) / len(x)   # 오차를 제곱해서 평균낸다

# w를 -3부터 5까지 50개로 촘촘히 나누어 비용을 계산한다
w_list = np.linspace(-3, 5, 50)
cost_list = [mse(x_data, y_data, w) for w in w_list]

# 몇 개만 숫자로 확인해 본다
for w in [-1, 0, 1, 2, 3]:
    print("w =", w, "일 때 cost =", round(mse(x_data, y_data, w), 3))

# 곡선을 그리면 w=1 지점이 바닥인 U자 모양이 나온다
plt.plot(w_list, cost_list, "ro")
plt.xlabel("w")                     # 가로축은 기울기 후보
plt.ylabel("cost")                  # 세로축은 그 기울기의 오차 크기
plt.show()`,note:"비용함수를 공식으로만 보면 막막하지만, w를 바꿔가며 점을 찍어보면 골짜기가 눈에 바로 보입니다. w가 1일 때 cost가 정확히 0이 되는 것을 확인하면 학습의 목표가 무엇인지 한 번에 이해됩니다.",origin:"practice",source:"김일한 실습교수(판교 7반) — 회귀 실습 노트북 13종"},{title:"경사하강법 직접 구현 — w와 b를 반복해서 갱신하기 (판교 7반 김일한 교수 자료)",lang:"python",code:`import numpy as np

# 공부시간 x와 점수 y, 실제 관계는 y = 2x + 1 이다
x = np.array([1, 2, 3, 4, 5])
y = np.array([3, 5, 7, 9, 11])

w = 10.0            # 기울기 초기값: 일부러 엉뚱한 값에서 출발
b = 10.0            # 절편 초기값
learning_rate = 0.01   # 한 걸음의 보폭(학습률)
n = len(x)

for epoch in range(2000):
    hx = w * x + b                          # 현재 w, b로 예측
    cost = np.sum((hx - y) ** 2) / n        # 오차를 제곱해서 평균낸 비용
    grad_w = np.sum((hx - y) * 2 * x) / n   # w 방향의 접선 기울기
    grad_b = np.sum((hx - y) * 2) / n       # b 방향의 접선 기울기
    w = w - learning_rate * grad_w          # 오르막 반대로 한 걸음
    b = b - learning_rate * grad_b          # 절편도 같은 방식으로 이동
    if epoch % 400 == 0:                    # 400번마다 진행 상황 출력
        print(epoch, "cost:", round(cost, 4), "w:", round(w, 4), "b:", round(b, 4))

print("최종 w =", round(w, 3), " 최종 b =", round(b, 3))
print("공부시간 7시간 예측 점수 =", round(w * 7 + b, 2))`,note:"sklearn이나 statsmodels가 내부에서 하는 일을 열 줄 남짓으로 직접 재현한 코드입니다. cost가 줄어들면서 w가 2, b가 1에 수렴하는 과정을 보면 학습이 마법이 아님을 알 수 있고, 학습률을 0.1로 키우면 발산하는 것도 직접 실험해 볼 수 있습니다.",origin:"practice",source:"김일한 실습교수(판교 7반) — 회귀 실습 노트북 13종"},{title:"최소제곱 공식으로 회귀계수 손계산 — 공분산·상관계수와 이어보기 (판교 7반 김일한 교수 자료)",lang:"python",code:`import numpy as np

X = np.array([1, 2, 3, 4, 5])          # 공부시간
Y = np.array([50, 55, 65, 70, 80])     # 시험점수

X_bar = np.mean(X)                     # X의 평균
Y_bar = np.mean(Y)                     # Y의 평균

dev_x = X - X_bar                      # X의 편차: 평균에서 얼마나 벗어났나
dev_y = Y - Y_bar                      # Y의 편차

Sxx = np.sum(dev_x ** 2)               # X 편차의 제곱합 (분산의 재료)
Sxy = np.sum(dev_x * dev_y)            # 편차끼리의 곱의 합 (공분산의 재료)

beta1 = Sxy / Sxx                      # 기울기 = 공분산 나누기 X의 분산
beta0 = Y_bar - beta1 * X_bar          # 절편 = Y평균 빼기 기울기 곱하기 X평균

print("기울기 beta1 =", round(beta1, 3))
print("절편   beta0 =", round(beta0, 3))

cov = np.mean(dev_x * dev_y)                       # 공분산: 편차곱의 평균
corr = cov / (np.std(X) * np.std(Y))               # 상관계수: 표준편차로 나눠 표준화
print("공분산 =", round(cov, 3), " 상관계수 =", round(corr, 3))

Y_hat = beta0 + beta1 * X                          # 직접 구한 식으로 예측
for a, b, p in zip(X, Y, Y_hat):
    print("X =", a, " 실제 =", b, " 예측 =", round(p, 2))`,note:"OLS가 돌려주는 coef 값이 사실은 편차곱의 합을 편차제곱의 합으로 나눈 나눗셈 한 번이라는 사실을 눈으로 확인하는 실습입니다. 같은 재료(편차의 곱)에서 공분산과 상관계수도 함께 나오므로, 상관과 회귀가 한 뿌리임을 체감할 수 있습니다.",origin:"practice",source:"김일한 실습교수(판교 7반) — 회귀 실습 노트북 13종"},{title:"다중공선성 진단 — VIF로 겹치는 변수 찾아내기 (판교 7반 김일한 교수 자료)",lang:"python",code:`import numpy as np
import pandas as pd
import statsmodels.api as sm
from statsmodels.stats.outliers_influence import variance_inflation_factor

np.random.seed(0)
n = 100

X1 = np.random.normal(50, 10, n)                       # 기본 변수
X2 = X1 * 0.9 + np.random.normal(0, 2, n)              # X1과 거의 같은 변수(일부러 겹치게)
X3 = np.random.normal(100, 20, n)                      # X1과 무관한 독립적 변수
y = 3 * X1 + 2 * X3 + np.random.normal(0, 15, n)       # 정답에는 X2가 들어있지 않다

df = pd.DataFrame({"X1": X1, "X2": X2, "X3": X3})
print("상관계수 행렬")
print(df.corr().round(3))                              # X1과 X2의 상관이 1에 가깝게 나온다

# VIF 계산: 값이 10을 넘으면 다중공선성 위험 신호로 본다
vif = pd.DataFrame()
vif["변수"] = df.columns
vif["VIF"] = [variance_inflation_factor(df.values, i) for i in range(df.shape[1])]
print(vif)

# 겹치는 X2를 그대로 넣고 회귀하면 계수가 불안정해진다
model_all = sm.OLS(y, sm.add_constant(df)).fit()
print("X2 포함 계수:", model_all.params.round(3).to_dict())

# X2를 빼고 다시 적합하면 원래 정답인 3과 2에 가깝게 복원된다
model_drop = sm.OLS(y, sm.add_constant(df[["X1", "X3"]])).fit()
print("X2 제외 계수:", model_drop.params.round(3).to_dict())`,note:"정답을 알고 있는 가짜 데이터를 만들어 다중공선성의 해악을 직접 눈으로 보는 실습입니다. 겹치는 변수를 넣었을 때 계수가 엉뚱하게 흔들리다가 제거하면 참값으로 돌아오는 것을 확인하면, 변수는 많이 넣을수록 좋다는 오해가 깔끔하게 정리됩니다.",origin:"practice",source:"김일한 실습교수(판교 7반) — 회귀 실습 노트북 13종"}]},java:{concepts:[{term:"JVM과 바이트코드 — Write Once, Run Anywhere",desc:".java 소스는 컴파일러(javac)를 거쳐 .class 바이트코드가 되고, 클래스 로더가 이를 JVM에 올리면 JVM이 바이트코드를 그 OS의 기계어로 바꿔 실행한다. 바이트코드는 어떤 OS의 JVM에서도 똑같이 돌기 때문에 플랫폼 독립성이 생긴다. 메모리도 역할이 나뉜다 — 기본형 값과 메서드 호출 정보는 스택에, new로 만든 객체와 참조형은 힙에 저장되며 힙의 객체는 GC(가비지 컬렉터)의 수거 대상이다."},{term:"생성자 주입이 필드 주입(@Autowired)을 이기는 4가지 이유",desc:"① 불변성 — 생성자 주입만 final 선언이 가능해 주입 후 바꿀 수 없다. ② 테스트 — Spring 없이 new PostService(mockRepo)로 바로 만들 수 있다. ③ 순환 의존 — 필드 주입은 서버를 띄운 뒤에야 터지지만 생성자 주입은 컴파일 시점에 걸린다. ④ 가독성 — 생성자 파라미터만 봐도 의존성이 다 보인다. 실무에서는 Lombok의 @RequiredArgsConstructor가 final 필드용 생성자를 자동 생성해 가장 간결한 생성자 주입이 된다."},{term:"단방향 의존 규칙 — Controller → Service → Repository",desc:"계층을 나누는 핵심 이유는 변경의 영향 범위 최소화다. DB가 바뀌면 Repository만, HTTP 스펙이 바뀌면 Controller만 고친다. 의존 방향은 Controller → Service → Repository 한 방향만 허용하고, 역방향과 계층 건너뛰기는 금지다. 흔한 실수는 Controller에 Repository를 직접 주입해 Service를 우회하는 것 — 비즈니스 로직이 여러 Controller에 중복되기 시작한다. 각 계층이 몰라도 되는 것도 분명하다: Controller는 DB 구조를, Service는 HTTP 메서드와 URL을, Repository는 비즈니스 규칙을 몰라도 된다."},{term:"Entity vs DTO — Entity를 그대로 반환하면 안 되는 3가지",desc:"Entity는 JPA가 관리하는 영속 객체(DB 테이블 매핑)이고 DTO는 계층 간 전달용 비영속 객체 — 역할이 다르다. Entity를 Controller에서 그대로 JSON으로 내보내면 ① 테이블 컬럼명·관계가 API 응답에 노출돼 DB 변경이 곧 API 스펙 변경이 되고, ② User의 password·salt 같은 민감 정보가 응답에 실릴 수 있으며, ③ Post→User→Post 양방향 참조가 직렬화 무한 루프(StackOverflow)를 일으킨다. 변환은 Service에서, PostResponse.from(post) 같은 정적 팩토리 패턴으로 노출할 필드만 골라 담는 것이 권장이다."},{term:"@Transactional과 변경 감지(dirty checking)",desc:"@Transactional은 여러 Repository 호출을 하나의 트랜잭션으로 묶어 성공 시 전체 COMMIT, 실패 시 전체 ROLLBACK을 보장한다(부분 성공 없음). 트랜잭션 범위 안에서 findById()로 가져온 Entity는 영속 상태라, 필드만 바꿔도 커밋 시점에 자동으로 UPDATE가 나간다 — update() 메서드에 save()가 없어도 되는 이유다. 조회 메서드에는 readOnly=true를 붙여 변경 감지를 꺼서 SELECT 성능을 올리고 쓰기 실수도 막는다. 트랜잭션 경계 = 비즈니스 단위이므로 Controller가 아닌 Service에 붙인다."},{term:"REST 상태코드를 ResponseEntity로 직접 말하기",desc:"그냥 객체를 반환하면 늘 200 OK지만, ResponseEntity를 쓰면 의미가 정확한 상태코드를 고를 수 있다. 생성 성공은 201 Created(+ 생성된 id 포함), 삭제 성공은 본문 없는 204 No Content가 REST 표준이고, 잘못된 입력은 400 Bad Request, 인증 실패는 401 Unauthorized, ID 중복 같은 충돌은 409 Conflict로 알린다. 참고로 @RestController의 반환 객체가 JSON이 되는 것은 spring-boot-starter-web에 포함된 Jackson이 자동 직렬화해 주기 때문이다."},{term:"== vs equals() — 주소 비교와 내용 비교",desc:"==는 두 변수가 같은 객체(메모리 주소)를 가리키는지 보고, equals()는 담긴 내용이 같은지 본다. 그래서 문자열 비교, 특히 비밀번호 검증은 반드시 equals()를 써야 하며 ==를 쓰면 내용이 같아도 false가 나올 수 있다. 다만 equals()로 평문 비밀번호를 비교하는 것 자체가 타이밍 공격에 취약하다는 것이 Day1의 결론이고, 그래서 Day2에서 BCrypt.checkpw()로 발전시킨다."},{term:"BCrypt — Salt와 Work Factor가 있는 단방향 해시",desc:"BCrypt는 복호화가 불가능한 단방향 해시로 비밀번호를 저장하는 실무 표준이다. SHA-256과 달리 Salt가 자동 포함되어 같은 비밀번호도 매번 다른 해시가 나와 레인보우 테이블 공격을 막고, Work Factor로 해시 속도를 일부러 느리게 만들어(10이면 2의 10제곱 = 1024회 반복) 무차별 대입을 방어한다. 해시 문자열 $2a$10$... 안에 버전·Work Factor·Salt·해시값이 모두 들어 있어 검증은 checkpw() 한 번이면 된다."},{term:"JWT — Header.Payload.Signature 토큰 인증",desc:"JWT는 Header.Payload.Signature 세 파트를 Base64로 이어 붙인 토큰으로, Payload에 사용자 id 같은 정보와 만료 시간(exp)을 담고 Signature 서명으로 위변조를 막는다. 세션 방식은 서버가 로그인 상태를 기억해야 해서 서버 부하와 수평 확장 문제가 생기지만, JWT는 서버에 상태가 없어(Stateless) MSA·분산 환경에 적합하다. 로그인 흐름은 입력 비밀번호를 checkpw()로 검증하고 통과하면 JWT를 발급해 반환하는 것이다."},{term:"SOLID 5원칙 — 인증 서버로 배우는 객체지향 설계",desc:"SRP(단일 책임)는 main() 하나가 입력·암호화·토큰·검증을 다 하던 코드를 User·PasswordEncoder·TokenProvider·AuthService로 나누는 것이고, OCP(개방-폐쇄)는 카카오 로그인을 추가할 때 기존 코드를 고치지 않고 구현체만 새로 만드는 것이다. LSP는 자식 클래스가 부모 자리를 완전히 대체할 수 있어야 한다는 것, ISP는 큰 인터페이스 하나보다 작은 인터페이스 여럿이 낫다는 것이다. DIP(의존 역전)는 구현체가 아닌 인터페이스 타입으로 선언하라는 원칙으로, Spring의 DI가 바로 이 원칙의 실천이다."},{term:"인증 vs 인가, Spring Security 필터 체인",desc:"인증(Authentication)은 당신이 누구인지 확인하는 것(JWT 토큰 검증), 인가(Authorization)는 무엇을 할 수 있는지 결정하는 것(게시글 수정은 작성자만, 관리자 API는 ADMIN만)이다. Spring Security는 필터 체인이 Controller 앞에서 모든 요청을 가로채 JWT를 검증하고, 실패하면 Controller에 들어가 보지도 못하고 401이 자동 반환된다. 커스텀 필터는 요청당 정확히 1회 실행이 보장되는 OncePerRequestFilter를 상속해 만들고, 검증된 사용자 정보는 SecurityContext에 저장한다."},{term:"Spring Profile 환경 분리와 JAR 배포",desc:"application-dev.yml(H2 인메모리, SQL 로그 출력)과 application-prod.yml(MariaDB, 테이블 유지)로 설정을 나누면 실행 시 --spring.profiles.active 옵션만 바꿔 개발·운영 환경을 오간다. H2에서 MariaDB로 DB를 교체해도 JPA가 추상화해 주므로 Controller·Service·Repository 코드는 한 줄도 안 바뀐다. ./gradlew build로 JAR 하나로 패키징하면 내장 톰캣 덕분에 java -jar만으로 단독 실행된다 — bootRun은 개발용, java -jar는 배포·운영용이다."}],examples:[{title:"JPA Entity + record DTO + 정적 팩토리 변환 — 게시글 도메인 3종 세트",lang:"java",code:`// Post.java — JPA가 관리하는 영속 객체 (DB의 posts 테이블과 1:1 매핑)
@Entity @Table(name = "posts")           // 이 클래스를 테이블로 매핑한다는 선언
@Getter @Builder @NoArgsConstructor @AllArgsConstructor  // 반복 코드는 Lombok에 위임
public class Post {
    @Id @GeneratedValue(strategy = GenerationType.IDENTITY)  // PK는 DB가 자동 증가로 발급
    private Long id;                     // 기본형 long이 아닌 래퍼 Long — null을 표현할 수 있다
    @Column(nullable = false)            // DDL 생성 시 NOT NULL 제약이 추가됨
    private String title;                // 제목 컬럼
    @Column(columnDefinition = "TEXT")   // 긴 본문은 TEXT 타입으로 지정
    private String content;              // 내용 컬럼
    private Long authorId;               // 작성자 id — 수정 권한 확인의 기준
}

// PostRequest.java — 입력 DTO: record 한 줄로 불변 객체 완성 (Java 16+)
// getter, equals, hashCode, toString이 전부 자동 생성된다
public record PostRequest(String title, String content, Long authorId) {}

// PostResponse.java — 출력 DTO: Entity를 바깥에 그대로 내보내지 않기 위한 방어막
public record PostResponse(Long id, String title) {
    public static PostResponse from(Post post) {   // 정적 팩토리 변환 패턴 (Day2의 User.of()와 동일)
        return new PostResponse(post.getId(), post.getTitle());  // 노출할 필드만 골라 담는다
    }
}`,note:"교재 Day4의 게시글 실습 구조다. Entity·입력 DTO·출력 DTO를 셋으로 분리하는 것이 핵심 — Entity를 그대로 반환하면 DB 구조 노출·민감 정보 유출·순환 참조 세 가지 문제가 생긴다. DTO는 record로 정의하면 한 줄로 끝난다."},{title:"@Service + @Transactional CRUD — save() 없이 UPDATE되는 변경 감지 체험",lang:"java",code:`// PostService.java — 비즈니스 로직 전담 계층: HTTP를 몰라도 된다
@Service                                  // Bean 등록 + 비즈니스 역할을 이름으로 명시
@RequiredArgsConstructor                  // final 필드용 생성자 자동 생성 → 생성자 주입 완성
public class PostService {
    private final PostRepository repo;    // Repository에만 의존 (계층 건너뛰기 금지)

    @Transactional                        // 쓰기 작업 — 중간 실패 시 전체 롤백(원자성)
    public PostResponse create(PostRequest req) {
        Post saved = repo.save(Post.builder()          // 빌더로 Entity를 조립해 저장
                .title(req.title()).content(req.content())
                .authorId(req.authorId()).build());
        return PostResponse.from(saved);  // Entity → DTO 변환은 Service의 책임
    }

    @Transactional(readOnly = true)       // 읽기 전용 — 변경 감지를 꺼서 조회 성능 향상
    public PostResponse getById(Long id) {
        return PostResponse.from(repo.findById(id)     // findById는 Optional 반환
                .orElseThrow(() -> new RuntimeException("게시글 없음")));  // 없으면 404감 예외
    }

    @Transactional                        // 변경 감지(dirty checking)가 일하는 구간
    public PostResponse update(Long id, PostRequest req) {
        Post post = repo.findById(id)     // 영속 상태의 Entity를 꺼내온다
                .orElseThrow(() -> new RuntimeException("게시글 없음"));
        post.change(req.title(), req.content());       // 필드만 바꿨는데
        return PostResponse.from(post);   // save() 호출 없이 커밋 시점에 자동 UPDATE 발생
    }
}`,note:"교재의 PostService 전체 CRUD 코드 흐름이다. update()에 save()가 없는 것이 포인트 — @Transactional 안에서 findById()로 가져온 Entity는 영속 상태라 필드 변경만으로 커밋 시 UPDATE가 나간다. 서버 실행 후 H2 콘솔(localhost:8080/h2-console)에서 SELECT로 직접 확인해 보자."},{title:"인터페이스 + 커스텀 예외 — HashMap을 JPA로 갈아끼워도 호출 코드 0줄 수정",lang:"java",code:`// AuthService.java — 인터페이스는 계약: 구현이 바뀌어도 호출자는 그대로
public interface AuthService {
    void register(String id, String pw);  // 무엇을 하는지만 선언, 어떻게는 구현체 몫
    String login(String id, String pw);   // 성공 시 토큰 문자열 반환
}

// AuthException.java — RuntimeException을 상속한 커스텀 예외의 뿌리
class AuthException extends RuntimeException {
    public AuthException(String msg) { super(msg); }  // 메시지를 부모 생성자에 전달
}
class DuplicateIdException extends AuthException {    // 상황별로 세분화한 자식 예외
    public DuplicateIdException() { super("이미 존재하는 ID"); }
}

// MemoryAuthService.java — Day2 구현체: HashMap 메모리 저장
public class MemoryAuthService implements AuthService {
    private final Map<String, String> store = new HashMap<>();  // id → BCrypt 해시
    public void register(String id, String pw) {
        if (store.containsKey(id)) throw new DuplicateIdException();  // 중복은 예외로 거절
        store.put(id, BCrypt.hashpw(pw, BCrypt.gensalt()));  // 평문 대신 해시를 저장
    }
    public String login(String id, String pw) {
        if (!store.containsKey(id)) throw new AuthException("없는 ID");   // 존재 확인
        if (!BCrypt.checkpw(pw, store.get(id))) throw new AuthException("비밀번호 불일치");
        return Jwt.issue(id);             // 검증 통과 시 JWT 발급
    }
}
// Day4 — 저장소를 JpaAuthService로 교체해도, AuthService 타입으로 선언한
// 호출 코드는 한 줄도 바뀌지 않는다. 다형성의 실전 가치가 여기서 드러난다.`,note:"교재가 Day2→Day4 내내 반복 강조하는 설계다. HashMap(오늘) ↔ JPA(Day4) 교체가 가능한 이유는 호출부가 구현체가 아닌 AuthService 인터페이스 타입으로 선언됐기 때문(DIP). 커스텀 예외는 RuntimeException 상속으로 만들고, if-return 대신 예외로 흐름을 끊어 오류 처리를 한곳에 모은다."},{title:"PasswordEncoder + TokenProvider — 순수 Java로 BCrypt·JWT 구현",lang:"java",code:`// PasswordEncoder.java — 비밀번호 암호화·검증만 책임지는 클래스(SRP)
public class PasswordEncoder {
    // 평문을 BCrypt 해시로 변환한다 (복호화 불가능한 단방향)
    public String encode(String raw) {
        // gensalt(12): Work Factor 12 — 기본 10보다 반복이 많아 더 안전
        return BCrypt.hashpw(raw, BCrypt.gensalt(12));
    }
    // 입력 평문과 저장된 해시를 비교한다 — equals() 비교 금지
    public boolean matches(String raw, String hash) {
        // Salt가 해시 문자열 안에 있어 checkpw가 알아서 꺼내 검증한다
        return BCrypt.checkpw(raw, hash);
    }
}

// TokenProvider.java — 로그인 성공자에게 JWT를 발급하는 클래스
public class TokenProvider {
    // 서명용 비밀키 — 32자 이상이어야 HMAC 서명이 가능하다
    private final String SECRET = "day2-secret-must-be-at-least-32-chars";
    // 토큰 유효 시간 30분(밀리초 단위)
    private final long EXP = 1800000L;
    // 사용자 id를 담은 토큰을 발급한다
    public String generate(String userId) {
        return Jwts.builder()                    // 빌더 패턴으로 토큰 조립 시작
                .setSubject(userId)              // Payload에 사용자 식별자 저장
                .setExpiration(new Date(System.currentTimeMillis() + EXP)) // 만료 시각(exp)
                .signWith(Keys.hmacShaKeyFor(SECRET.getBytes()))  // 서명 — 위변조 방지
                .compact();                      // Header.Payload.Signature 문자열 완성
    }
}`,note:"교재 Day2 인증 서버 실습의 두 핵심 클래스다. Day1의 평문 저장 + equals() 비교가 왜 위험한지 체감한 뒤, 저장은 hashpw()·검증은 checkpw()·인증 유지 상태는 JWT로 바꾸는 것이 Day2의 전부다. 실무에서는 Spring Security가 제공하는 BCryptPasswordEncoder를 쓰지만 원리는 이 코드와 동일하다."},{title:"SecurityConfig + JwtAuthFilter — 모든 요청을 지키는 문지기 세우기",lang:"java",code:`// SecurityConfig.java — 전체 인증 정책을 한곳에서 선언한다
@Configuration @EnableWebSecurity @RequiredArgsConstructor
public class SecurityConfig {
    private final JwtAuthFilter jwtAuthFilter;   // 아래에서 만드는 JWT 검증 필터

    @Bean  // 필터 체인 규칙을 Bean으로 등록
    public SecurityFilterChain filterChain(HttpSecurity http) throws Exception {
        return http
            // REST API는 JWT가 보호를 맡으므로 CSRF 방어는 끈다
            .csrf(csrf -> csrf.disable())
            // 세션을 아예 만들지 않는다 — 요청마다 JWT로 인증(Stateless)
            .sessionManagement(s -> s.sessionCreationPolicy(SessionCreationPolicy.STATELESS))
            .authorizeHttpRequests(auth -> auth
                // 로그인·회원가입은 토큰이 없어도 접근 허용
                .requestMatchers("/api/auth/**").permitAll()
                // 그 외 모든 요청은 유효한 JWT 필수
                .anyRequest().authenticated())
            // 기본 인증 필터보다 앞에 내 JWT 필터를 배치한다
            .addFilterBefore(jwtAuthFilter, UsernamePasswordAuthenticationFilter.class)
            .build();                            // 설정 완성
    }
}

// JwtAuthFilter.java — 요청당 정확히 1회 실행되는 커스텀 필터
public class JwtAuthFilter extends OncePerRequestFilter {
    // 처리 순서: ① Authorization 헤더에서 "Bearer " 뒤 토큰 추출
    // ② TokenProvider.validate()로 만료·서명 검증
    // ③ Payload의 subject(userId)를 꺼내 SecurityContext에 인증 정보 등록
    // 검증 실패 시 예외 발생 → 401 자동 반환, Controller에는 진입조차 못 한다
}`,note:"교재 Day5의 SecurityConfig 전체 코드와 JWT 필터 처리 흐름 5단계를 압축했다. Day4까지는 JWT 없이도 /api/posts에 접근됐지만, 이 필터를 씌우면 토큰 없이는 401 자동 차단 — 인증 로직을 Controller마다 쓰지 않고 한곳에서 관리하는 것이 Spring Security의 가치다. Controller에서는 @AuthenticationPrincipal로 인증된 사용자 id를 꺼내 쓴다."},{title:"JAR 빌드와 프로파일 배포 — 개발 서버에서 운영 서버로",lang:"bash",code:`# 1) 전체 빌드: 테스트 실행 후 실행 가능한 JAR 하나로 패키징
./gradlew clean build
# 2) 산출물 확인: build/libs 폴더에 JAR가 생겼는지 본다
ls build/libs/
# 3) 개발 프로파일로 실행: H2 인메모리 DB + SQL 로그 출력(dev)
java -jar build/libs/app-0.0.1-SNAPSHOT.jar --spring.profiles.active=dev
# 4) 운영 프로파일로 실행: MariaDB 접속 + 테이블 유지(prod)
#    DB가 바뀌어도 소스코드는 한 줄도 안 바뀐다 (JPA 추상화)
java -jar build/libs/app-0.0.1-SNAPSHOT.jar --spring.profiles.active=prod
# 5) 보안 검증: 토큰 없이 보호된 API 접근 → 401이 나와야 정상
curl -i http://localhost:8080/api/posts
# 6) 로그인으로 받은 JWT를 헤더에 실으면 → 200 성공
curl -i -H "Authorization: Bearer eyJhbG..." http://localhost:8080/api/posts
# 참고: 개발 중에는 ./gradlew bootRun (코드 수정 즉시 반영)
# 배포·운영은 java -jar (내장 톰캣 포함, 의존성 없는 단독 실행)`,note:"교재 Day5 종합 실습의 빌드-배포-검증 흐름 그대로다. Spring Boot는 내장 톰캣을 JAR 안에 품고 있어 서버 설치 없이 java -jar 한 줄로 서비스가 뜬다는 것이 핵심. 401(토큰 없음)과 200(토큰 있음)을 직접 확인하는 것까지가 5일 과정의 완성 조건이다."}]},agile:{concepts:[{term:"스크럼 4이벤트와 타임박스 — 시간을 지키는 것이 규칙의 전부",desc:"2주 Sprint 기준으로 Sprint Planning 2시간(목표 합의·Backlog 선택·Task 분할), Daily Scrum 15분(어제 한 일·오늘 할 일·장애물), Sprint Review 1시간(동작하는 결과물 데모·피드백), Retrospective 45분(Keep/Problem/Try 회고)이 권장 타임박스다. 핵심은 이 시간을 지키는 것 — 논의가 길어지면 별도 회의로 분리한다. Daily Scrum에서 장애물은 발견만 하고, 실제 해결 논의는 종료 후 필요한 사람끼리 따로 한다."},{term:"Product Backlog · Sprint Backlog · Increment — 3대 산출물의 관계",desc:"Product Backlog는 제품에 필요한 모든 항목을 PO가 우선순위대로 관리하는 전체 목록이고, Sprint Backlog는 그중 이번 Sprint에서 완료하기로 팀이 선택한 항목 + 이를 구현하기 위한 Task들이다. Increment는 Sprint 종료 시점까지 완료된 동작하는 결과물의 합. 흐름으로 보면 Epic → User Story(Product Backlog) → Sprint에 들어온 Story가 Task로 쪼개져 Sprint Backlog가 되고, 완료된 것들이 쌓여 Increment가 된다."},{term:"INVEST 원칙과 백로그 그루밍 — 다 쓰지 말고, 꺼낼 것만 다듬는다",desc:"좋은 User Story는 Independent(독립 개발 가능)·Negotiable(세부는 협의 가능)·Valuable(사용자 가치 명확)·Estimable(추정 가능)·Small·Testable을 충족해야 한다. 단 처음부터 전부 상세히 쓰지 않는다 — 백로그 초기 등록 때는 제목+한 줄 가치의 Epic 수준으로 두고, Sprint 진입 후보가 됐을 때 그루밍(정리·구체화·우선순위화)에서 인수기준까지 상세화한다. 우선순위 재정렬 → 상위 항목 상세화 → INVEST 체크 → Story Point 추정 순서다."},{term:"MoSCoW 우선순위와 Story Point — 무엇부터, 얼마나 큰가",desc:"우선순위는 고객 관점에서 Must(반드시)/Should(가능하면)/Could(여유 있으면)/Will not(이번엔 안 함)로 태깅한다(MoSCoW). 규모는 Story Point로 추정하되 절대 시간이 아닌 상대적 난이도를 피보나치 수열(1,2,3,5,8)로 매기며, Planning Poker로 팀원이 동시에 카드를 내 편차가 크면 이유를 토론한다. 추정의 진짜 목적은 정확한 숫자가 아니라 규모에 대한 팀의 공통 이해다."},{term:"모놀리식 vs MSA — 배포 단위와 분리 기준",desc:"모놀리식은 전체가 하나의 배포 단위라 작은 수정에도 전체를 다시 배포하지만, MSA는 서비스마다 독립 배포·독립 확장이 가능하고 기술 스택도 서비스별로 자유롭다. 대신 서비스 간 통신·장애 전파·운영 복잡도가 비용으로 따라온다. 서비스를 나누는 기준은 도메인 경계(Bounded Context)·데이터 소유권·변경 빈도 — 그리고 Day1에서 쓴 User Story가 어느 컴포넌트(인증서버/Eureka/Gateway/Kafka)로 매핑되는지가 분해의 출발점이 된다."},{term:"동기(REST) vs 비동기(Kafka) — 통신 방식 선택 기준",desc:"즉시 응답이 필요한 요청-응답 관계면 REST 동기 호출을, 서비스 간 느슨한 결합과 이벤트 전파가 목적이면 Kafka 비동기를 쓴다. Kafka는 Producer가 Topic에 이벤트를 발행하면 Consumer들이 구독해 가져가는 구조라, 발행자는 누가 받는지 몰라도 된다 — 주문 상태 변경 이벤트를 알림·통계 서비스가 각자 구독하는 식이다. 모의 프로젝트에서는 로그인 검증(즉시 응답 필요)은 REST, 상태 변경 전파는 Kafka로 나눠 체험한다."},{term:"스크럼 3역할 — PO·SM·Dev의 책임 분담",desc:"Product Owner는 제품 백로그를 소유하고 우선순위와 ROI를 책임지는 사람, Scrum Master는 프로세스를 촉진하고 장애물을 제거하며 팀을 보호하고 타임박스를 관리하는 사람, Development Team은 기능을 설계·구현·테스트해 Increment를 완성하는 사람들이다. PO가 다른 업무와 겸직하면 의사결정이 지연되는 병목이 생기므로 전담 배치가 원칙이다. 모의 프로젝트에서는 역할을 고정하지 말고 다음 Sprint에서 다른 역할을 경험해 보는 것을 권장한다."},{term:"Definition of Done vs 인수 기준(Acceptance Criteria)",desc:"Definition of Done(완료의 정의)은 Increment가 완료로 인정받기 위한 팀 공통 기준으로, 실습이든 실무든 Sprint 시작 전에 팀이 합의해야 한다. 인수 기준은 개별 User Story 하나가 끝났는지 검증하는 조건 명세로 Given-When-Then 형태로 적는다. 같은 '로그인 스토리'라도 DoD가 얼마나 구체적이냐에 따라 Task 분할 결과가 완전히 달라진다는 것을 교재는 웹 에디터 예시로 보여 준다."},{term:"Agile 도입 5단계와 실패 패턴",desc:"도입 순서는 현황 진단 → 파일럿팀 선정(자발적 1~2개 소규모 팀) → Sprint 0 준비(백로그 초안·DoD 합의·역할 지정) → 첫 Sprint 실행(1~2주, Retro로 즉시 보정) → 확산·정착이며, 처음부터 전사 적용을 시도하지 않는 것이 핵심 원칙이다. 대표 실패 패턴은 Sprint라는 이름만 쓰는 무늬만 Agile, PO 겸직 병목, 회고 없는 반복, Waterfall식 문서화 병행, Sprint Board 없는 진행상황 미가시화 — 각각 목적에 맞는 이벤트 운영, PO 전담, Retro 액션의 다음 Sprint 반영, 대화·시각화 중심 전환, Board·Burndown 매일 갱신으로 대응한다."},{term:"Release Planning — 백로그에서 출시 로드맵으로",desc:"릴리즈 계획은 백로그 우선순위에 기반해 제품 출시 계획과 Sprint 일정을 세우는 활동으로, 비즈니스 우선순위 정의 → Release별 Epic 배치(Roadmap) → Sprint 일정 수립 순서로 진행한다. 우선순위는 고객 관점의 MoSCoW 기법이나 사용자 행동 순서·빈도를 따지는 User 행동 기법으로 정하고, Sprint 주기는 2~4주 사이에서 전체 기간과 출시 계획을 고려해 결정한다. 화물 운송 서비스라면 차주 등록 기능을 1차 오픈에, 화주 주문·예약을 2차 오픈에 배치하는 식이다."},{term:"SOLID 리팩토링으로 모놀리스를 MSA로 쪼개기",desc:"LoginService 클래스 하나가 입력 검증·비밀번호 확인·세션 생성·로그 기록을 모두 담당하면 인증 로직만 떼어 다른 서비스로 옮길 수 없다. SRP로 CredentialValidator·SessionManager·AuthLogger로 책임을 나누고, OCP로 인증 방식(비밀번호/OAuth) 추가 시 기존 코드 수정 없이 확장하며, DIP로 UserRepository 인터페이스에 의존하게 바꾼다. 이렇게 분리된 컴포넌트가 각각 인증서버 내부 로직·OAuth 모듈·Kafka 이벤트 발행으로 이관된다 — 이 매핑표가 그대로 Sprint 1의 Product Backlog가 된다."},{term:"Circuit Breaker와 Anti-corruption Layer",desc:"MSA는 서비스 간 통신이 많아져 한 서비스의 장애가 연쇄적으로 번질 수 있는데, Circuit Breaker는 오류가 반복되는 서비스로 가는 호출을 차단(사용 불가 전환)해 장애 전파를 막고 fallback 함수로 대체 응답을 주며 대시보드로 상태를 모니터링한다. Anti-corruption Layer는 서비스끼리 데이터 모델을 공유하지 않고 API 결과만 받아 자기 서비스 쪽에서 가공하는 방식이다. 서비스별 중복 코드·중복 데이터가 생기는 것을 감수하는 대신, 각 서비스가 서로 영향 없이 독립적으로 개발할 수 있게 된다."}],examples:[{title:"모호한 한 줄을 Sprint에 넣을 수 있는 User Story로 — Before/After + Task 분할",lang:"text",code:`[Before] 모호한 백로그 항목
  "로그인 기능 개발"
  → 누가 쓰는지, 왜 필요한지, 언제 끝났다고 볼지 몰라 팀마다 다르게 해석한다

[After] INVEST를 적용한 User Story                       [우선순위: Must]
  As a 회원, I want 이메일/비밀번호로 로그인하고 싶다,
  So that 내 계정에 안전하게 접근할 수 있다

  인수 기준(Acceptance Criteria) — Day2 구현의 테스트 기준이 된다
   1. 올바른 이메일/비밀번호 입력 시 메인 화면으로 이동한다
   2. 잘못된 정보 입력 시 오류 메시지를 표시한다
   3. 5회 연속 실패 시 계정이 일시 잠긴다

[Task 분할 + Story Point] — Sprint Backlog로 들어가는 단위
  User Story           Task                              Point
  로그인 API 구현       인증서버 엔드포인트 개발             3
                       JWT 토큰 발급 로직 구현              2
                       단위 테스트 작성                     1
  Kafka 메시지 발행     주문 상태 변경 Producer 구현          3
                       Topic / Consumer 그룹 설정           2

  * Point는 시간이 아니라 상대적 난이도(피보나치 1,2,3,5,8)
  * Planning Poker로 팀이 함께 추정 — 편차가 크면 이유부터 토론`,note:"교재의 Before/After 예시와 Task 분할표를 합친 흐름이다. 팀 실습 순서는 Epic 브레인스토밍 → As a / I want / So that으로 분해 → MoSCoW 태깅 → 상위 10개만 INVEST 체크·인수기준 작성 → Planning Poker 추정. 인수기준이 상세할수록 Day2 MSA 구현이 매끄러워진다."},{title:"MSA 모의 프로젝트 — 요청 흐름 4단계와 Sprint Board·Daily Scrum 운영",lang:"text",code:`[요청 흐름] Client가 보낸 요청 하나가 지나가는 길
  1. Client ---> API Gateway        : 모든 요청의 단일 진입점
  2. Gateway ---> Eureka 조회        : 대상 서비스의 위치를 찾아 라우팅
  3. 인증 필요 시 ---> 인증서버(Auth)  : JWT 토큰 검증
  4. 비즈니스 서비스 처리 후 ---> Kafka : 상태 변경 이벤트 발행,
                                       다른 서비스들이 비동기로 구독

[컴포넌트 x 통신 방식]
  인증서버 Auth : 로그인 처리, JWT 발급·검증        - REST 동기
  Eureka        : 서비스 등록·위치 탐색(Discovery)   - 등록/조회
  API Gateway   : 진입점, 라우팅·인증 필터           - REST 동기
  Kafka         : 이벤트 기반 서비스 간 통신          - Producer/Consumer, Topic

[Sprint Board] 팀 진행 상황을 매일 가시화
  To Do              In Progress        Done
  Kafka Topic 설정    로그인 API 구현     Gateway 라우팅 설정
  단위 테스트 작성    JWT 토큰 로직

[Daily Scrum 한마디 — 15분, 보드 앞에서]
  "어제 Gateway 라우팅 설정을 완료해서 Done으로 옮겼습니다.
   오늘은 로그인 API 구현을 시작하는데, JWT 라이브러리 버전
   확인이 필요해서 In Progress에 유지하겠습니다."
  * 장애물은 여기서 발견만, 해결 논의는 스크럼 종료 후 별도로`,note:'교재 Day2 모의 프로젝트의 아키텍처와 운영 시나리오다. Day1에서 작성한 User Story가 그대로 컴포넌트에 매핑된다 — "로그인하고 싶다"는 인증서버, "다른 서비스를 찾고 싶다"는 Eureka, "주문 상태를 실시간으로 알고 싶다"는 Kafka 구현으로 이어진다. 계획(Story)과 실행(보드)이 한 몸이 되는 것이 이 과목의 핵심 체험이다.'},{title:"우리 팀 Agile 전환 체크리스트 — Sprint 0 전후로 점검",lang:"text",code:`[Sprint 0 이전 — 시작할 준비가 됐는가]
  [ ] PO / SM / Dev 역할을 명확히 확정했는가?
  [ ] Definition of Done 초안을 팀과 합의했는가?
  [ ] Product Backlog에 최소 10~15개 항목을 확보했는가?

[Sprint 0 — 첫 Sprint를 설계했는가]
  [ ] Sprint 목표(Goal)를 한 문장으로 정의했는가?
  [ ] Sprint Backlog를 Task 단위로 구성했는가?
  [ ] Sprint Board(물리 또는 디지털)를 셋업했는가?

[첫 Sprint 종료 후 — 개선 고리가 돌아가는가]
  [ ] Retrospective를 진행하고 액션 아이템을 도출했는가?
  [ ] Velocity(팀 처리량) 기록을 시작했는가?
  [ ] 도출된 개선사항을 다음 Sprint 계획에 반영했는가?

[함께 보는 실패 패턴 -> 대응]
  무늬만 Agile(이름만 Sprint)  -> 이벤트를 형식이 아닌 목적대로 운영
  PO 겸직 병목                 -> PO 전담 배치 또는 권한 팀 위임
  회고 없는 반복               -> Retro 액션을 다음 Sprint에 반드시 반영
  과도한 문서화 병행           -> 꼭 필요한 산출물만, 대화·시각화 중심
  진행상황 미가시화            -> Board·Burndown Chart로 매일 가시화`,note:"교재 Day 1의 'Agile 전환 체크리스트'와 '흔한 문제 상황과 대응' 표를 한 장으로 합쳤다. 처음부터 전사 적용하지 않고 파일럿 팀에서 검증 후 확산한다는 5단계 로드맵의 실행 도구이며, 팀 프로젝트 시작 전에 이 체크리스트를 그대로 돌려 보면 Sprint 0에서 무엇이 빠졌는지 바로 드러난다."},{title:"수강신청 MSA — Sprint 1~3 실행과 Review·갭 분석·KPT",lang:"text",code:`[Sprint별 목표와 대표 Task (Story Point)]
  Sprint 1  인증서버(OAuth) 구축
    기존 로그인 로직 분석(3) / SOLID 리팩토링(5)
    Spring Boot 이관(3) / OAuth 서버 설정(5) / Gateway 라우팅(3)
  Sprint 2  Eureka 서비스 디스커버리 연동
    Eureka Server 구축(3) / 각 서비스 Client 등록(3)
    서비스명 기반 동적 라우팅(3) / 서비스 간 REST 호출(5)
  Sprint 3  Kafka 이벤트 연동
    course-applied Topic 생성(2) / Producer 구현(3)
    알림 서비스 Consumer 구현(3) / End-to-End 통합 테스트(3)

[Sprint Review — 계획 대비 실행 갭 분석]
  계획 항목            실행 결과    차이 원인
  인증/로그인 API      완료         SOLID 리팩토링에 예상보다 시간 소요
  Eureka 연동          완료         계획대로 진행
  Kafka 발행/구독      부분 완료    장애 대응 로직은 다음 Sprint로 이월

[Retrospective — Keep / Problem / Try]
  Keep    : Sprint로 나누니 Kafka 같은 낯선 기술도 부담 없이 접근
  Problem : Story Point 추정이 실제 난이도보다 낮게 잡혔다
  Try     : 낯선 기술은 Spike(사전 조사) Task를 별도 배정,
            난이도 높은 Task는 Point를 보수적으로 추정`,note:"교재 Day 2의 Sprint 1~3 Planning 표, 갭 분석 프레임워크, Retrospective 대본을 하나의 실행 기록으로 재구성했다. Review는 무엇을 완성했는지 보여주는 자리라 미완성 Task도 솔직히 공유하고 다음 계획에 반영하는 것이 규칙. 계획보다 오래 걸린 원인이 추정 오류인지 기술 난이도인지 구분하는 질문이 갭 분석의 핵심이다."},{title:"온라인 교육 플랫폼 — 요구사항에서 서비스 경계 도출까지",lang:"text",code:`[Step 1. 요구사항 분석 — 주체별 기능 나열]
  수강생 : 회원가입, 로그인, 강의 검색, 수강신청, 결제, 추천 강의 조회
  강사   : 회원가입, 로그인, 강의 등록
  시스템 : 결제 완료 후 수강 활성화, 수강 완료 후 추천 갱신

[Step 2. 도메인 분리 — 기능을 묶어 서비스 경계 도출]
  서비스               스택         포트   묶인 기능              핵심 데이터
  User Service         Spring Boot  8081   가입·로그인·JWT 인증   users
  Course Service       Spring Boot  8082   강의 등록·목록·검색    courses
  Enrollment Service   Spring Boot  8083   수강신청·상태 관리     enrollments
  Payment Service      Spring Boot  8084   결제 처리·내역         payments
  Recommend Service    FastAPI      8085   수강 이력 기반 추천    (조회 전용)

[Step 3~4. 인프라·통신 설계 판단 기준]
  즉시 응답이 필요한 요청-응답    -> REST 동기 (예: 로그인, 강의 검색)
  상태 변경을 여럿에게 전파       -> Kafka 비동기 (예: 결제 완료 이벤트)
  장애 전파 차단                  -> Circuit Breaker + fallback
  모델 공유 금지                  -> API 결과만 받아 자기 쪽에서 가공(ACL)`,note:"교재 Day 3(MSA를 이용한 웹 서비스 구축)의 Step 1~4를 압축한 설계 캔버스다. 서비스마다 스택과 포트가 달라도 되는 것(Recommend만 FastAPI)이 MSA의 기술 스택 자유이고, 기능을 묶는 기준은 도메인 경계와 데이터 소유권이다. 팀 프로젝트에서 어떤 기능을 몇 개의 서비스로 나눌지 정할 때 이 표 형식을 그대로 재사용하면 된다."}]},container:{concepts:[{term:"Namespace(네임스페이스)",desc:"같은 컴퓨터 위에서 돌더라도 컨테이너마다 프로세스·네트워크가 서로 안 보이게 칸막이를 쳐 주는 리눅스 기술이다. 독서실 칸막이처럼 시야를 막아 각 컨테이너가 독립된 것처럼 보이게 하며, docker exec 로 컨테이너 안에서 프로세스 목록을 보면 자기 것만 보이는 이유가 바로 이것이다."},{term:"cgroups",desc:"컨테이너마다 CPU·메모리를 얼마나 쓸 수 있는지 상한선을 두는 리눅스 기술이다. 정해진 용돈만 쓰게 하듯 자원 사용량을 제한해 한 컨테이너가 자원을 독차지하지 못하게 막는다. Namespace 가 시야를 나눈다면 cgroups 는 사용량을 나눈다고 기억하면 된다."},{term:"이미지 레이어와 빌드 캐시",desc:"Dockerfile 의 한 줄 한 줄이 케이크처럼 층(레이어)으로 쌓여 이미지가 되고, 코드가 바뀌면 바뀐 층부터만 다시 만든다. 그래서 잘 안 바뀌는 의존성 설치는 앞쪽에, 자주 바뀌는 소스 복사는 뒤쪽에 배치하면 이전 층이 캐시로 재사용되어 빌드가 훨씬 빨라진다."},{term:"볼륨(Volume)",desc:"컨테이너 바깥에 마련된 데이터 창고다. 컨테이너를 지우면 안의 데이터도 함께 사라지지만, 볼륨에 담아 둔 데이터는 컨테이너가 삭제돼도 그대로 남는다. DB 컨테이너처럼 데이터를 지켜야 하는 경우 -v 옵션이나 compose 의 volumes 항목으로 반드시 연결해 둔다."},{term:"브리지 네트워크와 이름 통신",desc:"Docker 가 컨테이너들을 위해 자동으로 깔아 주는 가상 도로망(bridge network)으로, 같은 네트워크 위의 컨테이너끼리는 IP 대신 컨테이너 이름을 주소처럼 써서 통신한다. compose 를 쓰면 이 네트워크가 자동으로 만들어지므로 DB_HOST=db 처럼 서비스 이름만 적어도 연결된다."},{term:"프로그램·프로세스, 이미지·컨테이너 — 같은 관계",desc:"프로그램은 디스크에 가만히 저장된 코드(악보), 프로세스는 CPU와 메모리를 쓰며 실제로 실행 중인 상태(연주)다. 이미지와 컨테이너가 정확히 같은 관계다 — 이미지는 실행되기 전의 저장된 설계도(붕어빵 틀), 컨테이너는 그 이미지로 실제 실행되고 있는 것(붕어빵). 틀 하나로 붕어빵을 여러 개 굽듯, 이미지 하나로 컨테이너를 여러 개 띄울 수 있다."},{term:"Bare-metal → VM → Container 진화",desc:"Bare-metal은 물리 서버에 프로그램을 직접 설치하는 방식이라 앱끼리 라이브러리가 충돌하고 서버 한 대가 죽으면 전부 멈춘다. VM은 Hypervisor 위에 Guest OS를 통째로 얹어 완전히 격리하지만, 원룸마다 부엌·화장실을 새로 짓는 것처럼 무겁고 느리다. 컨테이너는 OS 커널을 공유하면서 앱 실행에 필요한 코드·라이브러리·설정만 포장한다 — 가상 컴퓨터가 아니라 살짝 눈가림된 프로세스라서 VM보다 훨씬 가볍고 순식간에 뜬다."},{term:"포트 매핑(-p 호스트포트:컨테이너포트)",desc:"포트는 한 컴퓨터 안에서 여러 프로그램을 구분하는 번호로, 아파트의 호수(101호, 102호)와 같다. docker run의 -p 6000:5000은 내 컴퓨터 6000번 문을 컨테이너의 5000번 문과 연결하라는 뜻이라, 브라우저에서는 localhost:6000으로 접속해야 열린다. Dockerfile의 EXPOSE는 실제 연결이 아니라 이 컨테이너가 몇 번 문으로 손님을 받는지 적어 둔 안내문이다."},{term:"Bind Mount vs Named Volume",desc:"둘 다 컨테이너 바깥에 데이터를 두는 방법이지만 성격이 다르다. Bind Mount는 내 컴퓨터의 특정 폴더를 컨테이너에 그대로 연결하는 방식이라 코드를 수정하면 즉시 반영되어 개발할 때 편리하다. Named Volume은 Docker가 알아서 관리하는 창고라 DB처럼 계속 남아야 할 데이터에 적합하고, 어느 컴퓨터로 옮겨도 잘 동작한다."},{term:"리눅스 터미널·권한·쉘 스크립트",desc:"리눅스는 Windows의 C드라이브 대신 뿌리(/) 하나에서 시작하는 나무 구조로 파일을 정리하고, 터미널에서 pwd(현재 위치)·ls(목록)·cd(이동)·mkdir·touch·cp·mv·rm으로 다룬다. 모든 파일에는 읽기(r)·쓰기(w)·실행(x) 세 권한이 소유자·그룹·기타 세 그룹별로 걸려 있고, 스크립트를 실행하려면 chmod +x로 실행 권한을 줘야 한다. 명령어를 순서대로 적어 한 번에 실행하는 파일이 쉘 스크립트(.sh)이며 변수·조건문·반복문 세 가지만 알면 충분하다."}],examples:[{title:"레이어 캐시를 살리는 순서로 WAS Dockerfile 쓰기",lang:"dockerfile",code:`# 파이썬 실행 환경이 담긴 공식 이미지에서 출발한다 (첫 번째 레이어)
FROM python:3.11-slim
# 컨테이너 안 작업 폴더를 /app 으로 정한다
WORKDIR /app
# 자주 바뀌지 않는 의존성 목록만 먼저 복사한다 (캐시 활용 포인트)
COPY requirements.txt .
# 의존성 설치 결과가 하나의 층(레이어)으로 저장된다
RUN pip install --no-cache-dir -r requirements.txt
# 자주 바뀌는 소스 코드는 마지막에 복사한다
# 코드만 고치면 위의 설치 레이어는 캐시로 재사용되어 빌드가 빨라진다
COPY . .
# 이 컨테이너가 5000번 포트를 사용한다고 알린다
EXPOSE 5000
# 컨테이너가 시작될 때 서버를 자동으로 실행한다
CMD ["python", "app.py"]`},{title:"이미지 레이어 확인하고 레지스트리에 올리기 (tag → push → pull)",lang:"bash",code:`# 빌드한 이미지가 몇 개의 층으로 쌓였는지 눈으로 확인한다
docker history my-was:1.0          # Dockerfile 한 줄이 한 층씩 기록되어 있다
# Docker Hub 계정으로 로그인한다 (창고 문 열기)
docker login                       # 로그인 없이는 push 가 거부된다
# 창고 주소가 담긴 이름표를 붙인다 (myid 는 본인 계정으로 변경)
docker tag my-was:1.0 myid/my-was:1.0
# 같은 이미지에 태그가 두 개 붙었는지 확인한다
docker images                      # 태그는 어느 창고 어느 칸에 둘지 정하는 이름표
# 창고(레지스트리)에 이미지를 올린다
docker push myid/my-was:1.0        # 레이어 단위로 업로드된다
# 다른 컴퓨터에서는 같은 이미지를 내려받아 그대로 실행할 수 있다
docker pull myid/my-was:1.0        # login, tag, push, pull 순서를 기억하자`},{title:"Web·WAS·DB 3계층 게시판을 compose 파일 하나로 띄우기",lang:"yaml",code:`services:                          # 함께 띄울 컨테이너(서비스) 목록
  web:                             # 1층: 사용자가 보는 화면(웹 서버)
    build: ./web                   # web 폴더의 Dockerfile 로 이미지를 만든다
    ports:                         # 포트 연결 설정
      - "8080:80"                  # 내 컴퓨터 8080 을 컨테이너 80에 연결
    depends_on: [was]              # was 가 먼저 켜진 뒤에 시작한다
  was:                             # 2층: 요청을 처리하는 서버(WAS)
    build: ./was                   # was 폴더의 Dockerfile 로 빌드
    environment:                   # 환경 변수로 설정을 주입한다
      - DB_HOST=db                 # db 는 아래 서비스 이름과 정확히 같아야 한다
    depends_on: [db]               # db 가 먼저 켜진 뒤에 시작한다
  db:                              # 3층: 데이터를 저장하는 DB
    image: postgres:17             # 공식 postgres 이미지를 그대로 사용
    environment:                   # DB 초기 설정값
      - POSTGRES_PASSWORD=1234     # 접속 비밀번호
      - POSTGRES_DB=board          # 게시판용 데이터베이스 이름
    volumes:                       # 데이터를 지킬 볼륨 연결
      - board-data:/var/lib/postgresql/data   # postgres 의 데이터 저장 경로
volumes:                           # 볼륨은 최상위에서 한 번 더 선언해야 한다
  board-data:                      # down 후 다시 up 해도 데이터가 남는 창고`},{title:"터미널 첫걸음 — 실습 폴더 만들기와 쉘 스크립트",lang:"bash",code:`# 지금 내가 어느 폴더에 있는지 확인한다 (print working directory)
pwd
# 실습용 폴더를 새로 만든다
mkdir workspace
# 만든 폴더 안으로 이동한다 (change directory)
cd workspace
# 내용이 빈 파일을 만든다
touch app.py
# 숨김 파일까지 자세히 나열해 결과를 확인한다
ls -al
# 배포 스크립트 파일을 만든다고 하자 — 아래 세 줄이 스크립트의 전부다
cat deploy.sh
#   #!/bin/bash                        <- 이 파일을 bash로 실행하라는 선언
#   APP_NAME="my-app"                  <- 변수: 자주 바뀌는 값에 이름표 붙이기
#   echo "안녕하세요, $APP_NAME 입니다"  <- 변수 값을 넣어 출력
# 스크립트에 '실행' 권한이라는 열쇠를 추가한다
chmod +x deploy.sh
# 이제 직접 실행할 수 있다
./deploy.sh
# 로그 파일의 마지막 20줄만 본다 (최근 상황 확인)
tail -n 20 app.log
# 로그에서 ERROR 글자가 있는 줄만 골라낸다
cat app.log | grep ERROR`,note:"교재 1장 Linux OS 기초의 실습(나만의 폴더 만들기)과 쉘 스크립트 단원을 이어 붙였다. rm은 휴지통 없이 즉시 삭제되니 실행 전 꼭 확인하고, 조건문의 대괄호 [ ] 양옆 띄어쓰기가 가장 흔한 실수라는 것이 교재의 팁. 컨테이너와의 대화는 전부 이 터미널 위에서 이루어지므로 여기서 손을 풀어 두자."},{title:"컨테이너의 정체 확인 — 프로세스·포트·볼륨 3연속 실험",lang:"bash",code:`# nginx 컨테이너를 백그라운드(-d)로 하나 띄운다
docker run -d --name demo nginx
# [실험1] 호스트에서 프로세스 목록을 보면 nginx가 그대로 보인다
ps -ef | grep nginx
# 컨테이너 안에서 보면 자기 혼자만 있는 것처럼 보인다 (Namespace 눈가림)
docker exec demo ps -ef
# 결론: 컨테이너 = 가상 컴퓨터가 아니라 격리된 '프로세스'

# [실험2] 포트 매핑의 앞 숫자만 내가 접속할 문 번호다
docker run -d -p 6000:5000 my-app:1.0
# localhost:6000 은 열리고, localhost:5000 은 안 열린다
curl http://localhost:6000

# [실험3] 볼륨: 컨테이너가 사라져도 데이터가 남는 바깥 창고
docker volume create my-data
# 만들어진 볼륨 목록을 확인한다
docker volume ls
# 볼륨을 /data 경로에 연결해 컨테이너를 실행한다
docker run -d -v my-data:/data alpine sleep 1000
# 볼륨의 실제 저장 위치 등 상세 정보를 확인한다
docker volume inspect my-data`,note:"교재 7장(컨테이너 구조)의 '컨테이너도 결국 프로세스' 실습과 3장(네트워크·스토리지)의 포트 바꿔 실행하기·볼륨 만들기 실습을 하나로 묶었다. 호스트의 ps에는 보이는데 컨테이너 안에서는 자기만 보이는 것이 Namespace의 눈가림이고, 이 가벼움이 VM과의 결정적 차이다."},{title:"Compose 없이 3-tier 게시판 손으로 연결하기 (Day 1 방식)",lang:"bash",code:`# 세 컨테이너가 이름으로 대화할 전용 도로망(네트워크)을 먼저 깐다
docker network create board-net
# [1층 DB] 공식 postgres 이미지를 네트워크에 붙여 실행한다
docker run --name board-db --network board-net \\
  -e POSTGRES_PASSWORD=1234 -e POSTGRES_DB=board -d postgres:17
# [2층 WAS] 서버용 Dockerfile로 이미지를 빌드한다
docker build -t board-was:1.0 ./was
# WAS 실행: DB 주소는 IP가 아니라 컨테이너 '이름'(board-db)을 그대로 쓴다
docker run --name board-was --network board-net \\
  -e DB_HOST=board-db -p 5000:5000 -d board-was:1.0
# [3층 Web] 화면용 nginx 이미지를 빌드한다
docker build -t board-web:1.0 ./web
# Web 실행: 내 컴퓨터 8080 문을 컨테이너 80 문에 연결한다
docker run --name board-web -p 8080:80 -d board-web:1.0
# 세 컨테이너가 모두 Up 상태인지 확인한다
docker ps
# 브라우저에서 localhost:8080 접속 → 글쓰기 → DB 저장까지 확인
# 문제가 있으면 WAS의 DB 연결 로그부터 본다
docker logs board-was`,note:"교재 5장 종합 실습 1의 Step 1~6을 명령어 흐름으로 압축했다. DB_HOST=board-db처럼 컨테이너 이름을 주소로 쓸 수 있는 것이 같은 네트워크의 힘이며, run을 세 번 치는 이 번거로움이 Day 2에서 docker-compose.yml 파일 하나 + up 한 줄로 바뀐다. 기존 실습예제의 compose 3계층 예제와 비교하며 보면 Compose의 고마움이 확실해진다."}]},k8s:{concepts:[{term:"Manifest(매니페스트)",desc:"쿠버네티스에게 무엇을 어떻게 띄워 달라고 적어 내는 요청 명세서로, 보통 YAML 로 작성한다. kubectl 로 전달하면 etcd 라는 내부 저장소에 보관되고, 이후에는 쿠버네티스가 이 문서를 기준으로 알아서 상태를 유지한다. apiVersion·kind·metadata·spec 네 부분이 뼈대다."},{term:"선언적(Declarative) 운영",desc:"어떻게 하라를 하나하나 지시하는 명령형과 달리, 이렇게 되어 있어야 한다는 결과만 선언하는 방식이다. 명세서를 제출한 뒤에는 컨트롤러가 계속 지켜보며 어긋난 부분을 스스로 맞추기 때문에, 운영자가 옆에 붙어 지시하고 조율할 필요가 없어진다."},{term:"SPEC 과 STATUS",desc:"SPEC 은 사용자가 이렇게 되기를 원한다고 선언한 목표 상태(Desired State)이고, STATUS 는 쿠버네티스가 관찰해 기록한 현재 상태(Current State)다. 둘이 어긋나면 쿠버네티스가 스스로 움직여 맞추며, kubectl get -o yaml 로 status 필드를 열어 보면 현재 상태를 확인할 수 있다."},{term:"Control Plane 컴포넌트의 역할 분담",desc:"API Server 는 모든 요청을 받는 접수 창구, etcd 는 명세서를 보관하는 장부, Scheduler 는 파드가 실행될 노드를 고르는 자리 배정 담당, Controller 는 선언된 상태와 어긋나면 복구하는 감독, 각 노드의 kubelet 은 실제로 컨테이너를 실행하는 일꾼, kube-proxy 는 네트워크를 이어 주는 배선 담당이다."},{term:"livenessProbe vs readinessProbe",desc:"liveness 는 앱이 살아 있는지 검사해 실패가 반복되면 컨테이너를 재시작하고, readiness 는 손님 받을 준비가 됐는지 검사해 실패하면 재시작 대신 Service 의 연결 명단(Endpoints)에서 빼 트래픽만 차단한다. 준비 상태가 돌아오면 명단에 다시 올라가므로, 일시적 과부하와 진짜 장애를 구분해 대응할 수 있다."}],examples:[{title:"Deployment 매니페스트로 파드 3개를 선언하기",lang:"yaml",code:`apiVersion: apps/v1                # Deployment 가 속한 API 그룹과 버전
kind: Deployment                   # 리소스 종류: 파드 개수를 지켜 주는 배포 관리자
metadata:                          # 이 리소스의 이름표 영역
  name: nginx-deployment           # 클러스터 안에서 부를 이름
spec:                              # 원하는 상태(desired state)를 적는 곳
  replicas: 3                      # 파드가 항상 3개 있어야 한다는 선언
  selector:                        # 어떤 파드가 내 관리 대상인지 고르는 기준
    matchLabels:                   # 라벨이 일치하는 파드를 관리한다
      app: nginx                   # app=nginx 라벨을 가진 파드가 대상
  template:                        # 새로 만들 파드의 설계도
    metadata:                      # 파드에 붙일 이름표
      labels:                      # selector 와 반드시 짝이 맞아야 한다
        app: nginx                 # 이 라벨 덕분에 관리 대상으로 인식된다
    spec:                          # 파드 내용물 정의
      containers:                  # 파드 안에서 돌릴 컨테이너 목록
        - name: nginx              # 컨테이너 이름
          image: nginx:1.21.6      # 실행할 컨테이너 이미지와 버전
          ports:                   # 컨테이너가 여는 포트
            - containerPort: 80    # 80번 포트로 요청을 받는다`},{title:"liveness·readiness 프로브로 파드 라이프사이클 관리하기",lang:"yaml",code:`spec:                                      # 파드 설계도의 내용물 부분
  containers:                              # 컨테이너 목록
    - name: my-spring-app                  # 스프링부트 앱 컨테이너
      image: myid/my-api-server:1.0.0      # 배포할 내 애플리케이션 이미지
      ports:                               # 앱이 여는 포트
        - containerPort: 8080              # 서비스 요청을 받는 포트
      livenessProbe:                       # 살아 있는지 검사(실패 시 재시작)
        httpGet:                           # HTTP 로 상태를 물어본다
          path: /actuator/health/liveness  # 스프링 액추에이터의 생존 확인 주소
          port: 8081                       # 관리용 포트
        initialDelaySeconds: 5             # 시작 5초 뒤부터 검사 시작
        periodSeconds: 10                  # 10초마다 검사
        failureThreshold: 3                # 3번 연속 실패하면 컨테이너 재시작
      readinessProbe:                      # 손님 받을 준비 검사(실패 시 트래픽 제외)
        httpGet:                           # HTTP 로 상태를 물어본다
          path: /actuator/health/readiness # 준비 상태 확인 주소
          port: 8081                       # 관리용 포트
        initialDelaySeconds: 5             # 시작 5초 뒤부터 검사 시작
        periodSeconds: 10                  # 10초마다 검사
        failureThreshold: 3                # 3번 실패하면 Endpoints 명단에서 빠진다`},{title:"롤링 업데이트로 무중단 배포하고 문제 시 롤백하기",lang:"bash",code:`# 매니페스트를 클러스터에 적용한다 (선언한 대로 맞춰 달라는 요청)
kubectl apply -f deploy.yaml
# 파드 3개가 모두 Running 인지 확인한다
kubectl get pods -l app=nginx
# 이미지 버전을 바꿔 롤링 업데이트를 시작한다 (파드가 하나씩 교체되어 무중단)
kubectl set image deployment/nginx-deployment nginx=nginx:1.25
# 교체가 끝날 때까지 진행 상황을 지켜본다
kubectl rollout status deployment/nginx-deployment
# 지금까지의 배포 이력을 확인한다
kubectl rollout history deployment/nginx-deployment
# 새 버전에 문제가 있으면 직전 버전으로 즉시 되돌린다
kubectl rollout undo deployment/nginx-deployment
# 되돌린 뒤 파드 상태를 다시 확인한다
kubectl get pods -l app=nginx`}]},"k8s-adv":{concepts:[{term:"ServiceAccount 중심 계정 관리",desc:"쿠버네티스는 사람 계정(User·Group)을 직접 관리하지 않아 외부 인증 연동이 필요하고, 실제 운영에서는 ArgoCD·Jenkins 같은 자동화 도구가 파드로 돌면서 API 를 호출한다. 그래서 시스템 계정인 ServiceAccount 를 중심으로 권한을 관리하며, 파드를 만들면 default ServiceAccount 가 자동으로 연결되고 토큰이 파드 안에 마운트된다."},{term:"Role·RoleBinding 의 구조",desc:"권한은 계정에 직접 주지 않고 역할(Role)에 담아 바인딩으로 연결한다. Role 은 어떤 리소스(resources)에 어떤 동작(verbs)을 허용할지 apiGroups 와 함께 적은 권한 묶음이고, RoleBinding 이 그 역할을 ServiceAccount 같은 주체(subject)에 묶어 준다. 네임스페이스를 넘는 전역 권한은 ClusterRole·ClusterRoleBinding 을 쓴다."},{term:"HPA 의 스케일링 계산 공식",desc:"원하는 파드 수는 ceil(현재 파드 수 x 현재 지표값 / 목표 지표값)로 계산된다. 예를 들어 목표 CPU 250m 인데 실제 평균이 400m 이면 파드 4개가 7개로 늘어난다. 기본 동작은 늘릴 때는 대기 없이 빠르게, 줄일 때는 5분 안정화 창을 두고 보수적으로 움직여 출렁임을 막는다."},{term:"QoS 클래스와 축출 순서",desc:"requests 와 limits 를 어떻게 적었느냐에 따라 파드는 Guaranteed(둘이 같음)·Burstable(일부만 지정)·BestEffort(둘 다 없음) 등급을 받는다. 노드 자원이 부족해지면 BestEffort 부터 먼저 쫓겨나므로, 중요한 서비스일수록 requests 와 limits 를 명확히 적어 등급을 높여 두어야 한다."},{term:"Ingress Canary 의 3가지 라우팅 방식",desc:"Ingress Controller 의 canary 어노테이션으로 신버전에 트래픽 일부만 흘려보낼 수 있다. weight 는 정한 비율만큼 무작위로, header 는 특정 HTTP 헤더를 단 요청만, cookie 는 특정 쿠키를 가진 사용자만 신버전으로 보낸다. 내부 테스터에게만 먼저 열어 검증한 뒤 비율을 늘려 가는 안전한 배포가 가능해진다."}],examples:[{title:"ServiceAccount 에 조회 전용 권한 묶기 (Role + RoleBinding)",lang:"yaml",code:`apiVersion: v1                            # 코어 API 그룹
kind: ServiceAccount                      # 파드나 도구가 쓰는 시스템 계정
metadata:                                 # 계정 정보
  name: app-reader                        # 계정 이름
  namespace: team-a                       # 이 네임스페이스 안에서만 존재
---                                       # 다음 리소스와의 구분선
apiVersion: rbac.authorization.k8s.io/v1  # RBAC 전용 API 그룹
kind: Role                                # 네임스페이스 안에서만 통하는 권한 묶음
metadata:                                 # 역할 정보
  name: pod-reader                        # 역할 이름
  namespace: team-a                       # 권한이 적용될 범위
rules:                                    # 허용할 동작 목록
  - apiGroups: [""]                       # 빈 문자열은 코어 리소스(파드 등)를 뜻함
    resources: ["pods", "configmaps"]     # 파드와 컨피그맵만 대상
    verbs: ["get", "list", "watch"]       # 조회만 가능, 생성·수정·삭제는 불가
---                                       # 다음 리소스와의 구분선
apiVersion: rbac.authorization.k8s.io/v1  # RBAC 전용 API 그룹
kind: RoleBinding                         # 역할과 계정을 연결하는 끈
metadata:                                 # 바인딩 정보
  name: read-pods-binding                 # 바인딩 이름
  namespace: team-a                       # 같은 네임스페이스 안에서 연결
subjects:                                 # 권한을 받을 대상 목록
  - kind: ServiceAccount                  # 대상 종류는 서비스 계정
    name: app-reader                      # 위에서 만든 계정
    namespace: team-a                     # 계정이 있는 네임스페이스
roleRef:                                  # 어떤 역할을 부여할지 지정
  apiGroup: rbac.authorization.k8s.io     # 역할이 속한 API 그룹
  kind: Role                              # 네임스페이스 범위 권한
  name: pod-reader                        # 위에서 만든 조회 전용 역할`},{title:"HPA 로 CPU 사용률 기준 자동 확장 규칙 선언하기",lang:"yaml",code:`apiVersion: autoscaling/v2            # HPA 는 autoscaling API 그룹 v2 를 사용
kind: HorizontalPodAutoscaler         # 파드 수를 자동으로 늘리고 줄이는 리소스
metadata:                             # 리소스 정보
  name: my-api-hpa                    # HPA 이름
spec:                                 # 스케일링 규칙 선언
  scaleTargetRef:                     # 어떤 워크로드를 조절할지 지정
    apiVersion: apps/v1               # 대상의 API 그룹
    kind: Deployment                  # Deployment 의 replicas 를 대신 조절한다
    name: my-api-server               # 대상 Deployment 이름
  minReplicas: 2                      # 아무리 한가해도 최소 2개는 유지
  maxReplicas: 10                     # 아무리 바빠도 최대 10개까지만
  metrics:                            # 판단 기준이 되는 지표
    - type: Resource                  # CPU·메모리 같은 기본 자원 지표
      resource:                       # 자원 지표 상세
        name: cpu                     # CPU 사용률 기준
        target:                       # 목표값 정의
          type: Utilization           # 요청량(requests) 대비 사용률 방식
          averageUtilization: 50      # 평균 50 퍼센트를 넘으면 파드를 늘린다
  behavior:                           # 늘리고 줄이는 속도 조절
    scaleUp:                          # 늘릴 때의 규칙
      stabilizationWindowSeconds: 0   # 대기 없이 즉시 증설
    scaleDown:                        # 줄일 때의 규칙
      stabilizationWindowSeconds: 300 # 5분 지켜본 뒤 보수적으로 감축`}]},devops:{concepts:[{term:"클라우드 네이티브 5대 핵심 요소",desc:"애플리케이션을 클라우드에 가장 알맞게 설계·개발·운영하는 접근으로, 컨테이너화·동적 오케스트레이션·마이크로서비스·DevOps(CI/CD)·관측성(로그·지표·추적)의 다섯 기둥으로 이뤄진다. Cloud 트랙에서 배우는 Docker·쿠버네티스·CI/CD 도구가 각각 이 기둥을 하나씩 담당한다."},{term:"지속적 통합·전달·배포의 구분",desc:"CI 는 코드 변경을 자주 합치면서 자동 빌드·테스트로 문제를 조기에 잡는 것이고, 지속적 전달(Continuous Delivery)은 언제든 배포 가능한 상태로 준비하되 마지막 배포 승인은 사람이 하는 것, 지속적 배포(Continuous Deployment)는 그 승인까지 자동화해 통과한 코드가 바로 운영에 나가는 것이다."},{term:"GitHub Actions 의 구성 요소",desc:".github/workflows 폴더의 YAML 파일이 Workflow 이고, 그 안의 Job 은 Step 들로 이뤄진다. push·pull_request·schedule 같은 Event 가 방아쇠가 되고, actions/checkout 처럼 재사용 가능한 부품이 Action, 실제로 Job 을 실행하는 서버가 Runner 다. Job 은 기본적으로 병렬 실행되며 needs 로 앞뒤 순서를 만든다."},{term:"GitOps 와 ArgoCD",desc:"Git 저장소를 단일 진실 원천(Single Source of Truth)으로 삼아, 클러스터의 실제 상태가 Git 에 선언된 매니페스트와 같아지도록 ArgoCD 가 자동으로 동기화하는 배포 방식이다. 배포 이력이 곧 Git 커밋 이력이 되므로, 문제가 생기면 커밋을 되돌리는 것만으로 배포도 되돌아간다."},{term:"Kaniko 와 Tekton",desc:"Kaniko 는 도커 데몬 없이 컨테이너 안에서 Dockerfile 을 읽어 이미지를 빌드하는 도구라, 권한이 제한된 쿠버네티스 클러스터 안에서도 안전하게 빌드할 수 있다. Tekton 은 파이프라인 자체를 Task·Pipeline 같은 쿠버네티스 리소스로 정의해 실행하는 K8s 네이티브 CI/CD 프레임워크로, 두 도구를 조합하면 빌드부터 배포까지 전부 클러스터 안에서 돌아간다."}],examples:[{title:"테스트 → 빌드 → 배포로 이어지는 GitHub Actions 파이프라인 (needs 활용)",lang:"yaml",code:`name: ci-cd                             # 워크플로 이름
on:                                     # 언제 실행할지(이벤트) 정의
  push:                                 # 푸시가 일어나면 실행
    branches: [main]                    # main 브랜치에 푸시될 때만
  pull_request:                         # PR 이 열리거나 갱신될 때도 실행
    branches: [main]                    # main 을 향한 PR 만
  workflow_dispatch:                    # 버튼으로 수동 실행도 허용
jobs:                                   # 실행할 작업(Job) 묶음
  test:                                 # 1단계 Job: 테스트
    runs-on: ubuntu-latest              # 깃허브가 제공하는 우분투 러너에서 실행
    steps:                              # 세부 단계(Step) 목록
      - uses: actions/checkout@v4       # 저장소 코드를 러너로 내려받는다
      - run: npm ci                     # 잠금 파일 그대로 의존성 설치
      - run: npm test                   # 자동 테스트 실행(실패하면 여기서 중단)
  build:                                # 2단계 Job: 빌드
    runs-on: ubuntu-latest              # 별도의 새 러너에서 실행
    needs: test                         # test 가 성공해야만 시작된다
    steps:                              # 빌드 단계 목록
      - uses: actions/checkout@v4       # 러너가 다르므로 코드를 다시 내려받는다
      - run: npm run build              # 배포용 산출물 생성
  deploy:                               # 3단계 Job: 배포
    runs-on: ubuntu-latest              # 배포용 러너
    needs: build                        # 빌드까지 성공해야 실행
    if: github.ref == 'refs/heads/main' # PR 은 제외, main 푸시일 때만 배포
    steps:                              # 배포 단계 목록
      - run: echo deploy                # 실제로는 배포 스크립트가 들어갈 자리`},{title:"ArgoCD 설치하고 GitOps 동기화 준비하기",lang:"bash",code:`# ArgoCD 전용 네임스페이스를 만든다
kubectl create ns argocd
# 공식 설치 매니페스트를 적용해 ArgoCD 를 설치한다
kubectl apply -n argocd -f https://raw.githubusercontent.com/argoproj/argo-cd/stable/manifests/install.yaml
# 설치된 파드들이 모두 Running 인지 확인한다
kubectl get pods -n argocd
# 로컬 8080 포트를 ArgoCD 서버에 연결한다 (브라우저 접속용)
kubectl port-forward svc/argocd-server -n argocd 8080:443
# CLI 도구를 설치한다 (macOS 기준)
brew install argocd
# 관리자 초기 비밀번호를 확인한다
argocd admin initial-password -n argocd
# 확인한 비밀번호로 로그인한다
argocd login localhost:8080
# 초기 비밀번호는 반드시 새 비밀번호로 바꾼다
argocd account update-password
# 이후 Git 저장소를 등록하면 선언된 상태 그대로 클러스터가 자동 동기화된다
kubectl get all -n argocd    # GitOps 를 움직이는 구성 요소 전체 확인`}]},method:{concepts:[{term:"AI 프로젝트의 5대 본질과 PM 대응",desc:`일반 SW와 달리 AI 프로젝트는 데이터 중심성(코드보다 데이터 품질이 성패를 좌우), 불확실성(성능이 항상 개선된다는 보장이 없음), 반복성(학습→검증→피드백→재학습 순환), 융합성(IT·비즈니스·현장 협업 필수), 운영 지속성(개발보다 MLOps 비중이 큼)이라는 다섯 특성을 갖는다.
대응으로는 데이터 수집·전처리를 별도 WBS 트랙으로 운영하고, 성능 KPI를 단일값이 아닌 구간(예: 정확도 85~92%)으로 잡으며, 착수 시점에 MLOps 전환 계획까지 함께 수립한다.`},{term:"발주사 vs 수행사 갈등의 구조적 예방",desc:`발주사는 "왜 약속한 성능이 안 나오나, 왜 일정이 유동적인가"를 묻고, 수행사는 "제공 데이터 품질이 나빠 못 쓴다, 현재 자원으로는 목표가 기술적으로 불가능하다"고 맞선다. 이 갈등은 착수·계약 단계에서 구조적으로 막는 것이 최선이다.
핵심 장치는 성능 KPI를 최소~목표~이상 3단계 밴드로 정의, 데이터 품질 기준서(DQA)를 계약서에 첨부, 변경관리위원회(CCB)와 JIRA 기반 공식 변경 프로세스 운영, PoC와 운영 단계의 전환 기준 명문화, MLOps 전환 비용·책임 범위 명시다.`},{term:"PMBOK 7판 전환과 Hybrid PM",desc:`PMBOK은 6판의 프로세스 중심(49개 프로세스 순차 수행)에서 7판의 원칙 중심(12개 원칙, 테일러링 강조)으로 패러다임이 바뀌었다. 초기 계획 완성·변경 최소화가 아니라 점진적 정교화와 고객 피드백 기반 조정이 기본이다.
Hybrid PM은 고정 영역(데이터 수집 계약, 인프라·예산 확정, 보안·컴플라이언스, 최종 납품 기준)은 Waterfall로, 가변 영역(모델 학습·검증 스프린트, 피처 엔지니어링 실험, MLOps 파이프라인 최적화)은 Agile로 운영하는 이중 트랙 전략이다. 경영진 후원 없이는 전환 실패율이 80%를 넘는다.`},{term:"Agentic AI 개발 방법론과 아키텍처 패턴",desc:`2026년은 Multi-Agent System의 실제 배포 원년으로, Anthropic MCP와 Google A2A 같은 표준 통신 체계가 자리 잡았다. 다만 Gartner는 실질 역량 없이 용어만 쓰는 Agent Washing과 40% 이상 프로젝트 취소 가능성을 경고한다.
표준 아키텍처 패턴은 추론+행동을 반복하는 ReAct, 오케스트레이터가 전문 에이전트를 조율하는 Multi-Agent 오케스트레이션, 외부 도구·DB·API를 표준 연결하는 MCP 기반 통합, 고위험 결정에 인간 승인 단계를 삽입하는 Human-in-the-Loop, 검색 증강과 에이전트를 결합한 RAG+Agent 융합이다.`},{term:"AI 특화 요구사항·WBS·테스트 전략",desc:`AI 요구사항은 기능 + 데이터 + 성능 + 운영 + 윤리/규제 5가지 유형으로 분리 정의하고, 추적 매트릭스에 데이터·모델 요구사항 항목을 추가한다. Agentic AI라면 에이전트 접근 범위, 자율성 수준, 오류 처리 정책, 감사 추적, 환각 허용 수준을 반드시 명시한다.
WBS는 데이터 트랙 + 모델 트랙 + 서비스 트랙 + PM 트랙 4트랙 병렬 구조로 짠다. 테스트는 정해진 입출력이 아닌 확률적 출력을 다루므로 편향·드리프트·환각을 추가 검증하고, 테스트를 먼저 설계한 뒤 AI가 구현을 생성하되 개발자가 반드시 검토하는 AI-Driven TDD를 적용한다.`}],examples:[{title:"AI 프로젝트 착수 전 필수 확인 체크리스트",lang:"text",code:`[ AI 프로젝트 킥오프 게이트 체크리스트 ]

1. 비즈니스 KPI를 측정 가능한 형태로 정의했는가?
   - 나쁜 예: "정확도 향상"  ->  좋은 예: "F1 Score 0.85 이상"
2. 성능 KPI를 최소~목표~이상 3단계 밴드로 설정했는가?
3. 데이터 품질 기준서(DQA)를 계약서에 첨부했는가?
4. PoC와 운영 단계를 분리하고 전환 기준을 명문화했는가?
5. 변경관리위원회(CCB) 운영 규정을 착수 시 합의했는가?
6. MLOps 전환 비용과 운영 책임 범위를 계획에 포함했는가?
7. 경영진 후원(Executive Sponsor)을 확보했는가?
8. AI 윤리/규제(EU AI Act 등) 요건을 요구사항에 반영했는가?
9. Agentic AI라면 자율성 수준과 Human-in-the-Loop 위치를 정했는가?
10. 운영 단계 모델 드리프트 모니터링과 재학습 트리거를 설계했는가?`,note:'교재의 "AI 프로젝트 성공 요인 체크리스트"를 킥오프 게이트로 재구성했다. 항목을 팀 프로젝트 계획서 앞머리에 그대로 넣으면 착수 단계에서 갈등 요인을 미리 걸러낼 수 있다.'},{title:"AI 프로젝트 문제정의 캔버스",lang:"text",code:`[ AI 프로젝트 문제정의 캔버스 ]

(1) 비즈니스 문제   : 어떤 의사결정/업무를 개선하려는가?
(2) AI 과제 유형    : 분류 / 회귀 / 생성 / 추천 / 에이전트 등
(3) 성공 지표(KPI)  : 지표명 + 목표 밴드(최소~목표~이상)
(4) 데이터 가용성   : 보유 데이터 / 필요 데이터 / 품질 리스크
(5) 요구사항 5유형  : 기능 | 데이터 | 성능 | 운영 | 윤리/규제
(6) WBS 4트랙       : 데이터 | 모델 | 서비스 | PM
(7) 불확실성/가정   : 검증이 필요한 핵심 가설은?
(8) PoC->운영 기준  : 어떤 조건을 넘겨야 운영 전환인가?
(9) 리스크/거버넌스 : 편향/드리프트/환각 대응, 승인 게이트

작성자: ____   승인자(PM): ____   버전: 0.1`,note:"교재의 요구사항 5유형 분리, WBS 4트랙, PoC->운영 전환 기준, 산출물 버전 관리(0.1 시작)를 한 장 캔버스로 묶었다. 기능과 데이터·성능 요구사항을 반드시 분리해 적는 것이 일반 SW 프로젝트와의 결정적 차이다."}]},datamini:{concepts:[{term:"미니프로젝트 전체 흐름(2일 16시간, 실습 90%)",desc:`Day 1은 DS 과정 정리(DS 목표·분석 방법론·통계·ML/DL)와 프로젝트 가이드로 시작해, EDA와 피처 엔지니어링을 거쳐 모델 전략 수립 보고서를 작성·제출하는 데까지 이어진다.
Day 2는 세운 전략에 따라 모델을 개발·최적화하고, 목표 성능과 비교한 뒤 Git 링크를 제출하며, 마지막 Wrap-up과 퀴즈로 마무리한다. 이론 10%, 실습 90% 구성이라 배운 흐름을 실전으로 완주하는 자리다.`},{term:"실습 과제 데이터: 분자 독성 예측",desc:`학습용 train.csv(약 51.2MB, 8,349건)로 모델을 만들고, 문제용 predict_input.csv(약 5.7MB, 927건)의 독성 여부를 예측한다. predict_input.csv는 train.csv와 구조가 같지만 정답 라벨만 없다.
각 분자는 SMILES 코드로 표현되고 MolWt(분자량), clogp(지용성), sa_score(합성 접근성), qed(약물성 지표), label(독성 여부) 같은 속성을 갖는다. 라벨은 이진값(1 = 독성 없음/Positive, 0 = 독성 있음/Negative)으로 정의된다.`},{term:"분자 지문(Fingerprint) 기반 피처 엔지니어링",desc:`SMILES 문자열만으로는 모델 입력이 되지 않으므로, 분자를 고정 길이 비트/카운트 벡터로 바꾸는 분자 지문을 만든다. 교재는 대표적으로 세 가지를 든다.
확장 연결성 지문(ECFP)은 원자 주변 환경을 반경 단위로 인코딩하고, 기능성 클래스 지문(FCFP)은 원자를 기능기 관점으로 일반화하며, 패턴 지문(Pattern Fingerprint)은 부분 구조 패턴의 존재 여부를 표현한다. MolWt·clogp·qed 같은 물리화학 기술자와 함께 쓰면 입력 표현이 풍부해진다.`},{term:"분류 모델 평가 지표",desc:`이진 분류 성능은 여러 지표를 함께 본다. Accuracy는 전체 중 맞게 예측한 비율, Precision은 Positive로 예측한 것 중 실제 Positive 비율, Recall(민감도)은 실제 Positive 중 Positive로 맞힌 비율, Specificity는 실제 Negative를 Negative로 맞힌 비율이다.
Precision과 Recall은 서로 상충(trade-off)하므로 이 둘을 통합한 F1 Score를 함께 쓰고, 임계값 변화에 따른 성능은 ROC 곡선과 Precision-Recall Plot으로 확인한다. 독성 데이터처럼 클래스가 치우칠 때는 Accuracy만 보지 말고 F1과 PR 곡선을 중심으로 판단한다.`},{term:"모델 전략 수립과 하이퍼파라미터 최적화",desc:`바로 모델을 돌리기 전에, 도메인과 데이터 EDA 결과를 근거로 어떤 피처·모델·검증 방식을 쓸지 정리한 모델 전략 수립 보고서를 먼저 작성·제출한다. 이것이 Day 1의 핵심 산출물이다.
Day 2에는 전략에 따라 베이스라인 모델을 만들고 하이퍼파라미터를 최적화해 성능을 끌어올린 뒤, 사전에 정한 목표 성능과 비교한다. 전략 없이 파라미터만 바꾸는 것이 아니라, 세운 가설을 성능으로 검증하는 순서가 중요하다.`}],examples:[{title:"EDA에서 베이스라인 모델까지 골격",lang:"python",code:`import pandas as pd
from sklearn.model_selection import train_test_split
from sklearn.ensemble import RandomForestClassifier
from sklearn.metrics import f1_score, classification_report

# 1) 학습 데이터 적재 (분자 독성 예측 과제)
df = pd.read_csv("train.csv")

# 2) 간단 EDA: 형태/결측/라벨 분포 확인
print("행/열:", df.shape)
print("결측 합계:\\n", df.isna().sum())
print("라벨 분포:\\n", df["label"].value_counts())  # 1=독성無, 0=독성有

# 3) 입력 피처(X)와 정답(y) 분리 - SMILES 문자열은 제외
feature_cols = ["MolWt", "clogp", "sa_score", "qed"]
X = df[feature_cols].fillna(0)   # 결측은 0으로 임시 대체
y = df["label"]

# 4) 학습/검증 분할 (라벨 비율 유지: stratify)
X_tr, X_va, y_tr, y_va = train_test_split(
    X, y, test_size=0.2, stratify=y, random_state=42)

# 5) 베이스라인 모델 학습 및 F1 평가
model = RandomForestClassifier(n_estimators=200, random_state=42)
model.fit(X_tr, y_tr)
pred = model.predict(X_va)
print("검증 F1:", round(f1_score(y_va, pred), 4))
print(classification_report(y_va, pred))`,note:"적재 -> EDA(형태·결측·라벨 분포) -> 피처/정답 분리 -> stratify 분할 -> 베이스라인 학습 -> F1 평가로 이어지는 최소 골격이다. 여기서 라벨이 치우쳐 있으면 Accuracy 대신 F1과 PR 곡선을 기준으로 개선하고, SMILES를 ECFP 등 분자 지문으로 바꿔 피처를 늘리는 것이 다음 단계다."},{title:"제출물·평가 체크리스트",lang:"text",code:`[ 미니프로젝트 제출물 체크리스트 ]

(Day 1) 모델 전략 수립 보고서
  [ ] 도메인/데이터 EDA 요약(형태·결측·라벨 분포)
  [ ] 피처 엔지니어링 계획(분자 지문 ECFP/FCFP/Pattern + 물리화학 기술자)
  [ ] 모델 후보와 검증 전략(분할/교차검증) 명시
  [ ] 목표 성능 지표와 목표치 선언(예: F1 0.85 이상)

(Day 2) 모델 개발/평가 산출물
  [ ] predict_input.csv(927건)로 예측 수행
  [ ] predict_output_작성자.csv 제출(SMILES + 예측 label)
  [ ] 하이퍼파라미터 최적화 내역 기록
  [ ] 목표 성능 대비 실제 성능 비교표
  [ ] 소스코드 Git Link 제출

[ 평가 관점 ]  Accuracy / Precision / Recall / Specificity / F1 / ROC / PR`,note:"교재의 산출물 안내(모델 전략 보고서 제출, predict_output_작성자.csv 제출, Git Link 제출)와 분류 평가 지표를 한 장으로 정리했다. 제출용 답안 파일에는 SMILES 코드와 예측 label만 담고, 파일명에 작성자를 넣는 규칙을 지킨다."}]}},y={container:{purpose:"Docker 설치와 컨테이너 동작 확인부터 시작해, 리눅스·컨테이너 기본·네트워크·스토리지 개념을 실습으로 익힌다. Compose 없이 네트워크 연결만으로 3-Tier를 구성해 본 뒤 Compose 기반으로 확장하며, 동료 코드 리뷰로 서로의 컨테이너 구성을 점검한다. 간단한 3-Tier 구성을 통해 향후 개발 환경을 스스로 구성하는 역량을 연습한다.",tasks:[{name:"종합실습 1 : Web-WAS-DB 3-Tier 미니 게시판",activity:"Web, WAS, DB 세 개의 컨테이너를 직접 구성해 미니 게시판을 동작시키고, 동료와 코드 리뷰를 진행한다.",time:""},{name:"종합실습 2 : 실습 1의 Compose 전환",activity:"종합실습 1에서 만든 구성을 Docker Compose 기반으로 확장·전환하고, 동료와 코드 리뷰를 진행한다.",time:""}],criteria:[{item:"환경 구성 (실습 1)",desc:"Web과 WAS의 Dockerfile 연결, DB 컨테이너 실행과 네트워크 연결, 세 컨테이너 간 통신, 브라우저에서 글쓰기·저장 기능까지 동작하는지 확인한다.",points:"45점"},{item:"코드 리뷰 (실습 1)",desc:"동료와 진행한 코드 리뷰 결과를 정리해 제출했는지 평가한다.",points:"45점"},{item:"제출 기한 (실습 1)",desc:"기한 내 제출 여부를 평가한다.",points:"10점"},{item:"환경 구성 (실습 2)",desc:"종합실습 1의 구성을 Compose로 정상 전환했는지 확인한다.",points:"45점"},{item:"코드 리뷰 (실습 2)",desc:"동료와 진행한 코드 리뷰 결과를 정리해 제출했는지 평가한다.",points:"45점"},{item:"제출 기한 (실습 2)",desc:"기한 내 제출 여부를 평가한다.",points:"10점"}],deliverables:["개인별 점수, 점수 판단 근거, 보완사항","3-Tier 컨테이너 구성 결과(게시판 동작 화면 포함)","Compose 전환 구성 결과","동료 코드 리뷰 정리 문서"],notes:["두 실습은 각각 100점 만점(환경 구성 45 + 코드 리뷰 45 + 제출 기한 10)으로 평가한다.","종합실습 1은 Compose 없이 네트워크 연결만으로 구성하고, 종합실습 2에서 Compose로 확장한다."]},agile:{purpose:"Agile 방법론에 따라 요구사항을 백로그로 설계하고 Sprint를 분할해 Scrum 프로세스를 적용할 수 있는지 평가한다. 이어서 백로그 기반 요구사항을 MSA로 설계하고 인증서버, Eureka, Kafka 연동 등 컨테이너 기반으로 구현하는 역량을 평가한다.",tasks:[{name:"백로그 설계 및 Scrum 프로세스 적용",activity:"요구사항을 백로그로 구체화하고 우선순위를 정한 뒤, Sprint 단위로 분할해 계획을 수립하고 스탠드업·리뷰·회고 등 Scrum 프로세스를 적용한다.",time:""},{name:"백로그 기반 MSA 설계 및 구현",activity:"백로그로 도출한 요구사항을 서비스 단위로 분리해 MSA로 설계하고, 인증서버 구축, Eureka 서비스 디스커버리 구성, Kafka 기반 서비스 간 비동기 통신까지 컨테이너 기반으로 구현한다.",time:""}],criteria:[{item:"백로그 설계 및 우선순위화 역량",desc:"요구사항을 백로그 항목으로 구체화하고, 비즈니스 가치와 긴급도에 따라 우선순위를 설정할 수 있는지 평가한다.",points:"40점"},{item:"Sprint 분할 및 Scrum 프로세스 운영 역량",desc:"백로그를 Sprint 단위로 적절히 분할하고, 계획-실행-리뷰-회고로 이어지는 Scrum 프로세스에 따라 진행할 수 있는지 평가한다.",points:"60점"},{item:"MSA 아키텍처 설계 및 인증서버 구현 역량",desc:"요구사항을 서비스 단위로 적절히 분리·설계하고, 인증서버를 독립된 마이크로서비스로 구현할 수 있는지 평가한다.",points:"40점"},{item:"서비스 디스커버리 및 비동기 연동 구현 역량",desc:"Eureka로 서비스 디스커버리를 구성하고, Kafka를 활용해 서비스 간 비동기 통신을 구현할 수 있는지 평가한다.",points:"60점"}],deliverables:["개인별 점수, 점수 판단 근거, 보완사항","백로그·Sprint 계획 및 Scrum 프로세스 적용 결과","MSA 설계 및 구현 결과(인증서버, Eureka, Kafka 연동)"],notes:["실습 1(백로그·Scrum)과 실습 2(MSA 설계·구현)는 각각 100점 만점으로 평가한다."]},java:{purpose:"순수 Java로 로그인 기능을 구현하고 서비스로 빌드·배포할 수 있는지 평가한다. 이어서 Java로 구현한 로그인 기능을 SOLID 원칙에 따라 재설계하고 SpringBoot 기반 인증 서버로 전환할 수 있는지 평가한다.",tasks:[{name:"Java 로그인 기능 구현 및 빌드·배포",activity:"Java 기본 원칙과 문법을 바탕으로 쿠키 기반 로그인 인증 기능을 구현하고, Gradle로 빌드해 배포까지 완료한다.",time:""},{name:"SOLID 원칙 기반 SpringBoot 인증 서버 구축",activity:"순수 Java로 구현한 로그인 기능을 SOLID 원칙에 따라 리팩토링하고, 이를 SpringBoot 기반 인증 서버로 구현해 빌드·배포한다.",time:""}],criteria:[{item:"Java 로그인 인증 기능 구현 역량",desc:"순수 Java와 쿠키 기반 인증 원리를 이해하고 로그인 기능을 정상적으로 구현할 수 있는지 평가한다.",points:"40점"},{item:"애플리케이션 빌드 및 배포 수행 역량",desc:"Gradle을 활용해 구현한 애플리케이션을 정상적으로 빌드하고 배포까지 완료할 수 있는지 평가한다.",points:"60점"},{item:"SOLID 원칙 기반 리팩토링 역량",desc:"기존 순수 Java 로그인 기능을 SOLID 원칙(단일 책임, 개방-폐쇄, 리스코프 치환, 인터페이스 분리, 의존 역전)에 따라 재설계·리팩토링할 수 있는지 평가한다.",points:"40점"},{item:"SpringBoot 기반 인증 서버 구현 및 빌드·배포 역량",desc:"리팩토링한 구조를 SpringBoot 기반 인증 서버로 구현하고, 정상적으로 빌드·배포까지 수행할 수 있는지 평가한다.",points:"60점"}],deliverables:["개인별 점수, 점수 판단 근거, 보완사항","Java 로그인 기능 구현 코드 및 Gradle 빌드·배포 결과","SOLID 리팩토링 및 SpringBoot 인증 서버 구현·배포 결과"],notes:["실습 1(Java 로그인·빌드 배포)과 실습 2(SpringBoot 인증 서버)는 각각 100점 만점으로 평가한다."]},stats:{purpose:"주어진 데이터를 기초통계량으로 탐색·해석하고, 변수 간 관계를 상관분석으로 확인한 뒤, 회귀분석 모델을 구축·해석하는 데이터 분석 기초 역량을 평가한다. 세 강사안(배기주·이은호·박병선) 모두 EDA에서 출발해 회귀모델 구축과 결과 해석으로 이어지는 공통 흐름을 갖는다.",tasks:[{name:"EDA 및 기초통계 해석",activity:"중심화 경향, 퍼짐·분포·대칭 등 기초통계량을 확인하고 도메인 관점에서 해석한다. 필요 시 결측치·이상치 처리, 인코딩·스케일링, 파생변수 생성 등 전처리를 수행한다.",time:""},{name:"상관분석",activity:"종속변수와 설명변수의 상관관계, 설명변수 간 상관관계(다중공선성 가능성)를 분석해 핵심 변수와 위험 변수쌍을 식별한다.",time:""},{name:"회귀분석 모델 구축 및 해석",activity:"변수를 선별해 회귀모델을 구축하고, 성능 지표와 변수 유의성·회귀계수를 해석해 인사이트를 정리한다.",time:""}],criteria:[{item:"기초통계 기반 EDA 해석",desc:"중심화 경향·퍼짐·분포·대칭 등 기초통계량을 목적에 맞게 적용해 데이터 특성을 해석했는지 평가한다.",points:"배기주 40점(25+15) / 이은호 20점(파생변수 10+전처리 10) / 박병선 30점(문제 정의·변수 설계 15+데이터 이해 15)"},{item:"변수 간 관계·상관 분석",desc:"두 개 이상 변수의 관계를 숫자-현상-도메인으로 연결해 해석하고, 종속변수와의 상관 및 설명변수 간 다중공선성을 확인했는지 평가한다.",points:"배기주 35점(25+10) / 이은호 20점(종속변수 상관 10+설명변수 간 상관 10)"},{item:"회귀모델 구축",desc:"회귀 가정사항을 고려해 변수를 선별하고 모델을 구축했는지, 성능 지표를 적절히 산출했는지 평가한다.",points:"배기주 25점(성능 미고려) / 이은호 50점(Test MAPE 40+Baseline MAPE 10) / 박병선 40점(모델 설계·평가 전략 20+구축·성능 평가 20)"},{item:"결과 해석 및 보고",desc:"변수 유의성과 회귀계수를 올바르게 해석하고, 분석 결과를 보고서로 논리 있게 전달했는지 평가한다.",points:"이은호 10점(변수 유의성) / 박병선 30점(해석·인사이트 20+보고서 구성 10)"}],deliverables:["개인별 점수, 점수 판단 근거, 보완사항","분석·개발 코드(Jupyter Notebook 또는 Python)","기초통계·상관분석 해석 및 회귀분석 결과 보고서"],notes:["세 강사안 모두 총 100점 만점이다.","배기주안(울산): 실습 1 EDA-중심화 경향(2H, 50점), 실습 2 퍼짐/분포/대칭·상관관계·회귀분석(2H, 50점). 전공·비전공 혼합을 고려해 난이도가 높지 않은 제조 도메인 오픈 데이터셋을 사용한다.","이은호안(판교): 실습 3개 구성 - EDA·전처리(20점), 상관분석(20점), 회귀분석(60점, Test/Baseline MAPE와 변수 유의성 평가).","박병선안: 개인 과제형 회귀분석 실습으로 설계역량 50점 + 구현역량 50점. UCI 등 공개 데이터셋과 Google Colab 사용을 권장하며, 절대평가에 점수 구간별 권장 분포를 참고한다.","마감 시간을 지키지 못하면 강사 판단에 따라 감점될 수 있다."]},smartdata:{purpose:"데이터 모델링과 SQL 활용 역량을 평가한다. 업무 요구사항을 분석해 데이터 구조를 설계(ERD·정규화)하고, SQL로 필요한 정보를 조회·분석하며, 실행계획 기반 쿼리 튜닝까지 실무 사례로 연습한다. 이론시험만으로 확인하기 어려운 문제 해결 능력을 실습으로 평가하고, 이후 데이터베이스와 Vector DB 활용의 기초 역량을 확보한다.",tasks:[{name:"종합실습 1 : 학사관리시스템 DB 설계-구축-조회",activity:"ERD를 작성하고 DB를 구축한 뒤 데이터를 입력하고 조회 작업을 진행한다.",time:""},{name:"종합실습 2 : CampusHub 복합 쿼리(JOIN+서브쿼리+윈도우함수)",activity:"실습 1의 DB 환경을 확장하고 스키마를 생성해 복합 쿼리 심화 활동을 수행한다.",time:""},{name:"종합실습 3 : HR DB 느린 쿼리 최적화",activity:"EXPLAIN 실행계획을 활용해 슬로 쿼리를 Before/After로 비교하며 튜닝한다(DBA 기초).",time:""},{name:"종합실습 4 : ecommerce 매출 분석",activity:"월간 매출 리포트·AOV, 카테고리별 성과, 재구매·RFM 분석, 재고 이슈 탐지를 수행하고 실행계획(성능)까지 개선한다.",time:""}],criteria:[{item:"종합실습 1 : 학사관리시스템",desc:"ERD 기준 PK·FK 연결관계 설정(30점), PostgreSQL 설치·환경 구성 및 접속(40점), 문항별 SQL문·결과 화면 제출(20점), 제출 기한 준수(10점)를 평가한다.",points:"100점"},{item:"종합실습 2 : 복합 쿼리",desc:"25개 실습 과제 전체 제출(40점), 과제에 맞는 쿼리 작성(30점), PDF 문서화(20점), 제출 기한 준수(10점)를 평가한다.",points:"100점"},{item:"종합실습 3 : 쿼리 최적화",desc:"쿼리별 차이점 분석·개선 방법 제안(40점), 5개 실습 과제별 제출(30점), 불필요한 컬럼 선택이 없는 쿼리 정합성(20점), 제출 기한 준수(10점)를 평가한다.",points:"100점"},{item:"종합실습 4 : 매출 분석",desc:"실습 문항별 쿼리·결과 제출(쿼리 튜닝, JOIN 전략 비교, 엔진별 특징 정리, 40점), 과제에 맞는 쿼리 작성(30점), 주석 처리 등 개발 표준 준수(20점), 제출 기한 준수(10점)를 평가한다.",points:"100점"},{item:"[울산·최진철] Day1 : 데이터 모델링·정규화",desc:"함수종속·분해 근거·요구사항 반영을 담은 3NF 정규화 워크시트(20점)와 DB 개요·모델링·SQL 기초 QUIZ·코드분석 5문항(10점)을 평가한다.",points:"30점"},{item:"[울산·최진철] Day2 : JOIN·집계·트랜잭션",desc:"다중/OUTER JOIN, 서브쿼리·뷰, 집계·ROLLUP, 윈도우함수, 트랜잭션·동시성 과제 7개(30점)와 해당 범위 QUIZ·코드분석 5문항(10점)을 평가한다.",points:"40점"},{item:"[울산·최진철] Day3 : 인덱스·실행계획·튜닝",desc:"인덱스 Before/After, EXPLAIN 병목 진단, SARGable 리라이팅 과제 3개(20점)와 해당 범위 QUIZ·코드분석 5문항(10점)을 평가한다.",points:"30점"},{item:"[울산·최진철] 심화(선택)",desc:"Day2 심화 5개(50점)와 Day3 커버링 인덱스(10점)·종합 튜닝(40점)으로 구성하며, 기본과제 11개 중 80%(9개) 이상 제출 시에만 기본 100점과 별도로 가산한다.",points:"별도 100점"}],deliverables:["개인별 점수, 점수 판단 근거, 보완사항","ERD·정규화 워크시트 및 DB 구축 결과","과제별 SQL문과 결과 화면(PDF 문서화 포함)","실행계획 Before/After 비교 및 튜닝 증빙(산출물 PPT)"],notes:["백정열안(판교): 종합실습 4개를 각 100점 만점으로 평가한다.","최진철안(울산): 통신(이동통신) 도메인 telecom 실습 데이터로 3일간 진행하며, 총점 100점 = QUIZ·코드분석 30점 + 과제 70점. 심화(선택)는 별도 100점 가산으로 최대 200점이다.","최진철안 세부 채점은 4축 루브릭(정확성 40%, 효율성 25%, 가독성 15%, 증빙 20%)을 따르고, 석차는 합계-과제-QUIZ 순으로 정한다.","과제 공통 마감은 Day3 종료일이며, 마감 미준수 시 감점될 수 있다."]},datamini:{purpose:"데이터 분석 전 과정을 정리하고 내재화하기 위한 실습이다. 데이터를 기반으로 모델링 전략을 수립하고 이를 실제 개발·평가로 연결하는 능력을 확인하며, 도메인 특성을 충분히 고려했는지를 실습 결과물로 평가한다.",tasks:[{name:"모델 전략 수립",activity:"데이터를 이해하고 EDA와 Feature Engineering을 수행한 뒤, 그 결과를 토대로 모델링 전략을 수립해 보고서를 작성·제출한다.",time:"2H"},{name:"모델 개발 및 평가",activity:"수립한 전략을 토대로 파이프라인을 구성해 모델을 개발하고, 성능을 정리·해석한다.",time:"2H"}],criteria:[{item:"EDA",desc:"핵심 변수의 분포를 탐색하고 통계량을 확인·해석하며, Feature Selection과 Engineering을 수행했는지 평가한다.",points:"50점"},{item:"EDA-전략 연결성",desc:"EDA 결과를 기반으로 시사점을 도출하고, 연결 논리가 일관성 있게 이어지는지 평가한다.",points:"30점"},{item:"모델링 전략 수립",desc:"Feature 설계 논리와 모델 선택 논리가 타당한지 평가한다.",points:"20점"},{item:"전략-개발 연결성",desc:"수립한 전략대로 Feature와 모델이 구현되었는지 평가한다.",points:"20점"},{item:"Pipeline 개발",desc:"개발 파이프라인 구성, 데이터 분할의 적절성, 핵심 변수 구현을 평가한다.",points:"40점"},{item:"성능 리포팅 및 해석",desc:"성능을 정리하고 목표 성능 대비 Gap을 해석하는 능력을 평가한다.",points:"20점"},{item:"분석 결과의 도메인 해석",desc:"분석 결과를 도메인과 연결해 해석하고 개발 한계점을 도출했는지 평가한다.",points:"20점"}],deliverables:["개인별 점수, 점수 판단 근거, 보완사항","모델링 전략 보고서(EDA·Feature Engineering 결과 포함)","모델 개발 파이프라인 및 성능 리포트"],notes:["실습 1(전략 수립) 100점, 실습 2(개발·평가) 100점으로 총 200점 만점이다.","기존 활용 데이터는 Steal Plates Faults, MIT-Stanford Dataset 등 오픈 데이터셋이다.","1인 또는 2인 구성으로 진행하되 LeaderBoard 없이 진행한다.","마감 시간을 지키지 못하면 실습교수 재량으로 감점될 수 있다."]}};function O({concepts:n=[],examples:t=[]}){if(!n.length&&!t.length)return null;const i=[...new Set([...n,...t].map(a=>a.source).filter(Boolean))];return e.jsxs("div",{style:{marginTop:28},children:[e.jsx("h3",{style:{fontSize:18,fontWeight:800,color:"var(--navy-800)",margin:"0 0 4px",paddingTop:14,borderTop:"1px solid var(--line)"},children:"🧩 실습교수 보충자료"}),i.length>0&&e.jsxs("p",{style:{margin:"2px 0 14px",fontSize:12.5,color:"var(--ink-soft)",lineHeight:1.6,wordBreak:"keep-all",overflowWrap:"break-word"},children:["출처 — ",i.join(" / ")]}),n.length>0&&e.jsx("div",{className:"grid grid-2",style:{marginTop:4,marginBottom:t.length?18:0},children:n.map((a,o)=>e.jsxs("dl",{className:"concept",children:[e.jsx("dt",{children:a.term}),e.jsx("dd",{style:{whiteSpace:"pre-line"},children:e.jsx(p,{text:a.desc})})]},o))}),t.length>0&&e.jsx("div",{style:{display:"flex",flexDirection:"column",gap:18},children:t.map((a,o)=>{var l;return e.jsxs("div",{children:[e.jsxs("div",{className:"box-h",style:{marginBottom:8},children:[a.title," ",e.jsxs("span",{style:{fontWeight:600,color:"var(--ink-soft)",fontSize:12},children:["(",a.lang,")"]})]}),((l=a.files)==null?void 0:l.length)>0&&e.jsx("div",{style:{display:"flex",gap:8,flexWrap:"wrap",margin:"2px 0 8px"},children:a.files.map(c=>e.jsxs("a",{href:c.href,download:!0,className:"btn btn-ghost",style:{padding:"6px 14px",fontSize:12.5},children:["⬇ ",c.label]},c.href))}),e.jsx(x,{code:a.code,lang:a.lang}),a.note&&e.jsxs("p",{style:{marginTop:8,fontSize:13,color:"var(--ink-soft)",lineHeight:1.7,whiteSpace:"pre-line"},children:["💡 ",e.jsx(p,{text:a.note})]})]},o)})})]})}const M=n=>`${n.slice(5,7)}-${n.slice(8,10)}`,h=n=>(n==null?void 0:n.origin)==="practice",E=n=>!h(n);function G({courseId:n}){var s;const t=R[n],i=D[n]||{},a=(i.concepts||[]).filter(E),o=(i.examples||[]).filter(E),l=(i.concepts||[]).filter(h),c=(i.examples||[]).filter(h);if(!t)return e.jsxs("div",{children:[e.jsx("p",{style:{color:"var(--ink-soft)"},children:"과목 정보를 찾을 수 없습니다."}),e.jsx(L,{to:"/lectures",className:"btn btn-ghost",style:{marginTop:12},children:"← 강의안으로"})]});const m=[];for(const r of v)for(const d of T){const u=r[d.key];(u==null?void 0:u.c)===n&&m.push({date:r.date,track:d.label,by:u.by})}return e.jsxs("div",{className:"etc-detail",children:[e.jsxs("div",{className:"detail-meta",children:[e.jsx("span",{className:"chip chip-code",children:"기타"}),e.jsx("span",{className:"chip chip-cat",children:t.category}),e.jsxs("span",{className:"chip chip-day",children:[t.hours,"시간"]}),e.jsx("span",{className:"chip chip-region gwangju",children:"타 강사 진행"}),e.jsx(P,{level:t.level,weight:t.weight})]}),e.jsx("h2",{style:{fontSize:26,fontWeight:800,color:"var(--navy-800)",marginTop:8},children:t.name}),e.jsx("p",{style:{color:"var(--ink-soft)",marginTop:4},children:t.summary}),e.jsx("div",{className:"mode-note",style:{marginTop:14},children:e.jsxs("span",{className:"mode-desc",children:["이 과목은 ",e.jsx("b",{children:"별도 강사"}),"가 진행합니다. 이애본 강사 담당 강의의 앞뒤에 배우는 내용이므로, 아래 학습내용을 참고해 예습·복습하면 과정 흐름이 이어집니다."]})}),((s=t.topics)==null?void 0:s.length)>0&&e.jsxs(e.Fragment,{children:[e.jsx("h3",{style:{fontSize:18,fontWeight:800,color:"var(--navy-800)",margin:"28px 0 4px"},children:"📖 주요 학습내용"}),e.jsx("div",{className:"card",style:{marginTop:12},children:e.jsx("ul",{className:"dot-list",children:t.topics.map((r,d)=>e.jsx("li",{children:e.jsx(p,{text:r})},d))})})]}),a.length>0&&e.jsxs(e.Fragment,{children:[e.jsx("h3",{style:{fontSize:18,fontWeight:800,color:"var(--navy-800)",margin:"28px 0 4px"},children:"📚 핵심 개념"}),e.jsx("div",{className:"grid grid-2",style:{marginTop:12},children:a.map((r,d)=>e.jsxs("dl",{className:"concept",children:[e.jsx("dt",{children:r.term}),e.jsx("dd",{style:{whiteSpace:"pre-line"},children:e.jsx(p,{text:r.desc})})]},d))})]}),o.length>0&&e.jsxs(e.Fragment,{children:[e.jsx("h3",{style:{fontSize:18,fontWeight:800,color:"var(--navy-800)",margin:"28px 0 4px"},children:"💻 따라하기 실습"}),e.jsx("div",{style:{display:"flex",flexDirection:"column",gap:18,marginTop:12},children:o.map((r,d)=>e.jsxs("div",{children:[e.jsxs("div",{className:"box-h",style:{marginBottom:8},children:[r.title," ",e.jsxs("span",{style:{fontWeight:600,color:"var(--ink-soft)",fontSize:12},children:["(",r.lang,")"]})]}),e.jsx(x,{code:r.code,lang:r.lang}),r.note&&e.jsxs("p",{style:{marginTop:8,fontSize:13,color:"var(--ink-soft)",lineHeight:1.7,whiteSpace:"pre-line"},children:["💡 ",e.jsx(p,{text:r.note})]})]},d))})]}),e.jsx(O,{concepts:l,examples:c}),t.tip&&e.jsxs("div",{className:"box box-tips",style:{marginTop:20},children:[e.jsx("div",{className:"box-h",children:"🔗 담당 과목과의 연결"}),e.jsx("p",{style:{margin:0,whiteSpace:"pre-line"},children:e.jsx(p,{text:t.tip})})]}),y[n]&&e.jsxs(e.Fragment,{children:[e.jsx(S,{e:y[n]}),e.jsx("p",{style:{marginTop:8,fontSize:12.5,color:"var(--ink-soft)"},children:"※ 타 강사 진행 과목의 평가기준입니다. 평가 방향을 미리 파악하는 참고자료로 활용하세요."})]}),m.length>0&&e.jsxs(e.Fragment,{children:[e.jsxs("h3",{style:{fontSize:18,fontWeight:800,color:"var(--navy-800)",margin:"28px 0 4px"},children:["📅 분반별 진행 시기 ",e.jsx("span",{style:{fontSize:13,fontWeight:600,color:"var(--ink-soft)"},children:"(실시간 배정표 기준·변동 가능)"})]}),e.jsx("div",{className:"exam-tablewrap",style:{marginTop:12},children:e.jsxs("table",{className:"exam-table",children:[e.jsx("thead",{children:e.jsxs("tr",{children:[e.jsx("th",{children:"날짜"}),e.jsx("th",{children:"분반"}),e.jsx("th",{children:"강사"})]})}),e.jsx("tbody",{children:m.map((r,d)=>e.jsxs("tr",{children:[e.jsx("td",{children:M(r.date)}),e.jsx("td",{children:r.track}),e.jsx("td",{children:r.by||"-"})]},d))})]})})]})]})}export{G as E,O as P,H as a,U as p};
